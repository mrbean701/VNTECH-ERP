<?xml version="1.0" encoding="UTF-8"?>
<!--
  VNTECH ERP â€” Web module (Spring Boot app)
  REST gateway POST /api/system + GET /api/system (bootstrap) + /api/files, serve static SPA,
  security (cookie session mep_session, PBKDF2, RBAC), health.
  Chá»‰ module nÃ y cÃ³ @SpringBootApplication vÃ  spring-boot-maven-plugin (repackage).
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.vntech</groupId>
    <artifactId>vntech-erp</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>vntech-erp-web</artifactId>
  <name>VNTECH ERP â€” Web</name>

  <dependencies>
    <dependency>
      <groupId>com.vntech</groupId>
      <artifactId>vntech-erp-infrastructure</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- H2 cho integration test (MySQL mode); production dÃ¹ng MySQL á»Ÿ infrastructure -->
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>runtime</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <mainClass>com.vntech.erp.web.VntechErpApplication</mainClass>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>