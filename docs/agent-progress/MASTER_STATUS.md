# MASTER STATUS — VNTECH ERP V5.3.0

> Tệp này là NGUỒN SỰ THẬT về trạng thái toàn cục. Mọi phiên làm việc mới PHẢI đọc tệp này trước.
> Cấu trúc theo GOAL §12. Cập nhật lần cuối: 2026-09-17 (sau **TASK-076** + gỡ chặn **TASK-034**, commit **#123** — xem mục **TIẾN ĐỘ SO VỚI MASTER TASK** ngay dưới)

## MASTER TASK STATUS

* Master Task: **ERP/MIS SYSTEM AUDIT, REFACTOR & FEATURE UPGRADE (46 mục)** + §2.1/2.2/2.3
* Overall status: **IN PROGRESS** — PHASE 1 (hạ tầng UI dùng chung) chưa xong
* Nguyên tắc: MASTER TASK quyết định PHẢI LÀM GÌ; GOAL quyết định PHẢI LÀM NHƯ THẾ NÀO

## TIẾN ĐỘ SO VỚI MASTER TASK — đo được, không phải cảm nhận

> Cổng: **`tools/probe-roadmap-progress.mjs`** — đọc nguyên văn cột TT của `docs/25_TODO_ROADMAP.md`
> (nguồn sự thật 110 mục) và phân loại. Chạy lại bất cứ lúc nào để lấy số mới; **không tự đặt tiêu chí "xong"**.

| Phân loại (nguyên văn cột TT) | Số mục | % |
|---|---|---|
| **DONE** (`**DONE**` + `DONE` + `DONE / AP-DUNG n`) | **31** | **28,2 %** |
| ĐANG LÀM (`DANG-LAM 9/32`) | 1 | 0,9 % |
| KHUNG XONG nhưng **ÁP DỤNG 0** (`KHUNG-XONG / AP-DUNG 0`) | 2 | 1,8 % |
| **BỊ CHẶN** (`**BLOCKED**` = F-01) | 1 | 0,9 % |
| TODO | 76 | 69,1 % |

| Phase | DONE / tổng | Ghi chú |
|---|---|---|
| PHASE 0 — AUDIT | **12 / 16** | A-01…A-16; A-13…A-16 còn TODO |
| PHASE 0B — BẢO MẬT | **8 / 10** | đang tạm hoãn theo yêu cầu người dùng |
| PHASE 1 — UI/UX | 8 / 17 | 2 mới có khung |
| PHASE 2 — MUA HÀNG | 3 / 9 | `P-04` (TASK-072) · `P-05` (TASK-074) · `P-06` (TASK-075) |
| PHASE 3 — CÔNG VIỆC | 0 / 10 | |
| PHASE 4 — DỰ ÁN | 0 / 6 | |
| PHASE 5 — KHO | 0 / 4 | |
| PHASE 6 — ĐỘI NHÓM | 0 / 6 | |
| PHASE 7 — QUẢN TRỊ | 0 / 16 | |
| PHASE 8 — WORKFLOW | 0 / 6 | |
| PHASE 9 — BÁO CÁO | 0 / 5 | |
| PHASE 10 — TƯƠNG LAI | 0 / 5 | 1 mục bị chặn (F-01) |

**Phần KHÔNG cộng vào 110 mục — nhánh đang chạy thực tế:** TASK-048 → TASK-071 là **vá lỗi
phát hiện được khi audit** (đường ĐỌC bootstrap JS ↔ Java, phân quyền, audit trail). Nhánh này **không có
ánh xạ 1-1** với mục roadmap nào, nên cổng trên **cố tình không cộng** — nếu cộng vào sẽ là thổi phồng.
Nói cách khác: *audit đã xong về khảo sát (A-01…A-12 DONE) nhưng việc SỬA các lỗi audit tìm ra vẫn đang chạy*.

**Từ TASK-072 trở đi THÌ ĐÃ cộng vào 110 mục** (vì có ánh xạ 1-1 với một dòng roadmap): TASK-072 làm đúng mục **`P-04`** ⇒ chuyển **DONE**; mục thứ hai là **`U-06`** — từ *có khung, áp dụng 0* thành *đã áp dụng thật 1 lần* ⇒ tiến độ **23 → 25/110 = 22,7 %**. **Quy ước:** việc nào có ánh xạ 1-1 thì cập nhật thẳng **cột TT của `docs/25_TODO_ROADMAP.md`** rồi chạy `probe-roadmap-progress.mjs` — **nguồn sự thật là roadmap, không phải tệp này**.

**Cổng đã xanh** (số nào ghi kèm task/ngày thì đúng mốc đó; **KHÔNG chép lại như thể vừa đo**): **`npm run build` EXIT 0** + `verify-vntech-fingerprint.mjs` **ĐẠT** (dấu vân tay **`3e28c42d…`** · **242 tệp** · fixed point stable) — **TASK-034 ĐÃ GỠ CHẶN**, bundle đang phục vụ ĐÚNG bằng nguồn; bootstrap `100/100` · work-items `18/18` ·
audit-requests `18/18` · owner-checks `10/10` · all-roles `20/20` · schema-drift **0 lệch** ·
đối chiếu **TẬP CỘT** **73 khoá · 0 khoá thiếu cột** (độ phủ: so được 73 ≥ sàn 65, đủ 20 khoá bắt buộc ⇒ cổng KHÔNG mất độ phủ; **đối chứng dương 5/5 khớp metadata THẬT của MySQL**; đối chiếu chéo hai bản đọc nguồn cho kết quả giống hệt) · đối chiếu **MỆNH ĐỀ** `ORDER BY`/`LIMIT`/kiểu `JOIN` **73 khoá · 0 lệch** (**đối chứng dựng sẵn 6/6**, gồm 1 cặp phải IM và 5 cặp phải BẮT) · `probe-roadmap-progress.mjs` đọc đủ **110 mục** · `probe-ui-adoption.mjs` chạy được · hồi quy `npm run test:regression` **61 test · 59 PASS · 2 FAIL** — **đúng 2 ca ĐÃ BIẾT** (`TASK-031` sentinel `__site_command_tree_disabled__` · `TASK-032` `defaultOrganizationCode`), **không phát sinh ca mới**.

**Lớp lỗi lệch `ORDER BY` / `LIMIT` / kiểu `JOIN` — ĐÃ VÁ (TASK-062/063) và nay CÓ CỔNG RIÊNG** (`tools/probe-clause-parity.mjs`):
`JOIN`. Đã thấy 3 ca khi đọc JS: `boqImportBatches` (JS sắp `project_id,contract_id,version_no DESC`;
Java sắp `created_at DESC`) · `boqChangeHistory` (JS có `LIMIT 1000` + tiebreaker `h.id DESC`) ·
`workflowAssignments` (JS **chỉ admin** mới trả, Java trả cho mọi vai trò). **Cả 3 ca ĐÃ VÁ HẾT.**

## Mốc trạng thái

| Mục | Giá trị |
|---|---|
| CURRENT PHASE | PHASE 1 — hạ tầng UI dùng chung. Song song: hoàn thiện tầng phân quyền Java |
| CURRENT TASK | **TASK-083 — `U-15` ĐỢT 2: chuyển bảng phẳng sang `DataTable`** — **đang làm, 16 bảng đã chuyển** (mới nhất: **danh mục vật tư** `material-list-table` — giữ lớp định dạng riêng qua `tableClassName`, cột **alias ĐỘNG** bằng `hidden: !showAlias`) ⇒ `DataTable` dùng thật **13 → 29**, bảng tự viết **87 → 71**, trạng thái rỗng tự viết **88 → 74**; `app/page.tsx` **4019 dòng**. ✅ **KIỂM CHỨNG TRÙNG BYTE:** cổng ảnh **32 ảnh TRÙNG NHAU TỪNG BYTE** với lượt trước (`FF18686D…967C`) — màn `05-material` (**chính màn vừa chuyển**) **không đổi một điểm ảnh**. 🔎 **VÌ SAO `rowClassName` phải là LỚP CSS (bảng 15):** `globals.css:1217` `.selected-row td { background:#eef6ff!important }` nằm trên `<td>` **và có `!important`** ⇒ style nội tuyến trên `<tr>` (thứ `rowStyle` sinh ra) **thua** ⇒ chỉ dùng `rowStyle` là **mất dấu dòng đang chọn**. ✅ **PHƯƠNG PHÁP ĐO TRƯỚC/SAU cho màn chưa có ảnh chuẩn (đã chạy thật ở bảng 15):** thêm màn `08-requests` vào `tools/probe-visual-regression.mjs` → `git stash` bản mới → build → **đồng bộ hàng định danh SQLite** (`tools/set-local-identity.mjs` — công cụ MỚI) → `--update` chụp ảnh chuẩn **bản CŨ** → `git stash pop` → build → so ⇒ **✅ 0 px cả 4 kích thước**. 🔴 **LỖI ĐÃ GẶP (ghi lại):** lần `--update` đầu sinh **4 ảnh chuẩn RÁC (12 KB)** vì `local-runtime.mjs:177` **chặn khởi động** khi hàng định danh SQLite khác SSOT ⇒ UI chết ⇒ proxy `:9000` trả **502**; **dấu hiệu: `nav=NO_GROUP` + ảnh vài KB**. 🔎 **Còn phát hiện:** cột `source_fingerprint_short` trong **SQLite không được head cập nhật** ⇒ **đừng đọc cột short của SQLite để kết luận định danh**. 🔧 **Đã mở rộng `DataTable` 4 lần:** `cellClassName` · `rowStyle` · `tableClassName` · **`rowClassName`**. 📌 **Bài học trong `TASK-083.md`:** mục 2b (so **hash báo cáo ảnh**), 2c (`rowClassName` vs `rowStyle`), 2d (hover **có chủ ý** ở bảng 15), 2e (quy trình TRƯỚC/SAU + lỗi ảnh rác), 3b (lớp CSS riêng), 3c (khoảng trắng sau `=>`), 4b (head định danh). ⚠️ **CÒN 1 ứng viên:** **`WorkCenter` bảng nhiệm vụ 10 cột** (có chọn dòng, **không** dùng `baseline-table` ⇒ phải so ảnh trước/sau); `Purchasing` (**`<Fragment>` nhóm dòng ⇒ KHÔNG chuyển được**). Chi tiết: `TASK-083.md`. ⚠️ *Bối cảnh việc trước:* **TASK-085 — cổng chống NHÂN DÒNG** (bất biến *payload ≤ CSDL* trên **17 khoá danh sách**, **15/15 ĐẠT**, 12 khoá có dữ liệu) và **TASK-084 — cổng đối chiếu SỐ TIỀN UI↔CSDL** (5 nguồn tiền, **14/14 ĐẠT**) | 🔎 **Bộ quét phân loại 89 khối `<table>` cho thấy con số "42 bảng" trong kế hoạch trước là ĐẾM THÔ:** theo tiêu chí an toàn chỉ **1 bảng đủ điều kiện chuyển ngay** (lại là bảng **tổng hợp tĩnh 3 dòng** ⇒ không nên chuyển), **12 bảng cần cân nhắc** (`colSpan` thường chỉ ở dòng trạng thái rỗng ⇒ chuyển được nếu truyền `emptyText` **nguyên văn**), **~76 bảng KHÔNG chuyển được** (nhóm dòng theo `rowRole` · không dùng `baseline-table` · là **chuỗi HTML in** như `printTabularReport`). ⚠️ **3 việc phải làm TRƯỚC khi chuyển tiếp** (nếu không sẽ MẤT tính năng): thêm **`rowClassName`/`rowStyle`** cho `DataTable` (bảng **ma trận quyền phòng ban** tô nền vàng dòng *chưa lưu*) · dựng **tiêu đề cột động** (`PERM_CAPS.map`) · chứng minh **CSS tương đương** cho các bảng dùng `<table>` trần. Chi tiết + thứ tự công việc: `TASK-083.md`. ⚠️ *Bối cảnh việc trước:* **TASK-085 — cổng chống NHÂN DÒNG** (bất biến *payload ≤ CSDL* trên **17 khoá danh sách**, **15/15 ĐẠT**, 12 khoá có dữ liệu) và **TASK-084 — cổng đối chiếu SỐ TIỀN UI↔CSDL** (**14/14 ĐẠT**, 5 nguồn tiền) — hai cổng này ra đời từ **phát hiện lớn: dữ liệu BOQ bị ĐẾM TRÙNG khiến màn Thanh toán hiện giá trị hợp đồng GẤP ĐÔI** (đã vá: 1.346.500.000 → **673.250.000 = số ĐÚNG**). Trước đó: **TASK-080 đợt 1 + 2A…2E** ("chỉ dùng dữ liệu thật"): **4/4 bảng từng rỗng nay có dữ liệu THẬT** · nối 5 cột thiếu JS↔Java · bỏ 3 hằng số giả · port `queueTaskNotice` · vá 3 lỗi chặn mô-đun công việc · điền quyền/đơn vị/email thật. |
| LAST COMPLETED | **TASK-085 — CỔNG CHỐNG NHÂN DÒNG (payload ≤ CSDL, 13 khoá danh sách)** · **14/14 ĐẠT** — biến phát hiện lỗi đếm trùng BOQ thành **bất biến tổng quát**; 11/13 khoá có dữ liệu thật; tự kiểm soát "≥ 5 khoá có dữ liệu" + "SQL lỗi thì ghi KHÔNG kết luận". *Trước đó:* **TASK-084 — cổng đối chiếu SỐ TIỀN UI↔CSDL** (5 nguồn tiền, **14/14 ĐẠT**) + **KP #83: đã thu 1,5 tỷ > hợp đồng 673,25 triệu (223 %)**. *Trước đó:* **TASK-080 đợt 2E — 2 bảng tài chính cuối cùng có dữ liệu THẬT** (`production_reports` 0→**2**, `capital_recovery_records` 0→**1**, tạo bằng chính action `save_production_report` → `approve_production_report` → `save_capital_recovery`, số liệu suy từ khối lượng đã nhận × **đơn giá hợp đồng**) + 🔴 **phát hiện & vá DỮ LIỆU BOQ ĐẾM TRÙNG** khiến giá trị hợp đồng trên màn Thanh toán **gấp đôi** (1.346.500.000 → **673.250.000** = số ĐÚNG); **19/19 ĐẠT**. *Trước đó:* **TASK-080 đợt 2D — port nốt `queueTaskNotice`**: `task_notifications` 0→**2** (người nhận tự thấy trong phiên của họ) · `email_outbox` 0→**1** · `work_item_events` 0→**6**; phát sinh & xử lý: **mọi tài khoản đều để email NULL** nên nhánh thư luôn bị bỏ qua ⇒ điền email theo khuôn mẫu thật (12/12 tài khoản); **28/28 ĐẠT**. *Trước đó:* **TASK-080 đợt 2C — `work_items` 0 → 3 việc THẬT** bằng chính action `create_work_item` (KHÔNG nhét tay vì sản phẩm chặn) + vá **3 lỗi chặn**: luật "Trưởng phòng" và "người nhận việc" của Java **so sai nguồn dữ liệu** (so `u.department` với MÃ phòng, mà cột đó chứa TÊN tiếng Việt ⇒ chặn cả 3 action phòng ban của MỌI người dùng) · thiếu hẳn `defaultDepartmentAssignee` · **quyền GHI theo dự án: mọi tài khoản ngoài admin chỉ có `read`** ⇒ chặn mọi thao tác ghi theo dự án; **24/24 ĐẠT**. *Trước đó:* **TASK-080 đợt 2B — phiếu `confirmed` thiếu người xác nhận 10→0**, tham chiếu người dùng treo 2→0, `role_catalog.default_organization_unit_id` 0/16→**15/16**, và **truy nguyên trọn vẹn test đỏ TASK-032** (kỳ vọng test theo tên NGẮN của 0045, nhưng 0079 đã đổi ngược về tên DÀI = MySQL hiện tại). *Trước đó:* **TASK-082 — "CHỈ DÙNG DỮ LIỆU THẬT": nối 5 CỘT THIẾU giữa JS↔Java + bỏ 3 HẰNG SỐ GIẢ** (đợt 2A của TASK-080 theo chỉ thị người dùng 17/09 *"từ giờ không hardcode nữa chỉ sử dụng dữ liệu thật"*). **(A) Phát hiện gốc:** `BootstrapDataAdapter` (Java — đường phục vụ thật qua cổng 9000) **thiếu 5 cột** mà JS SSOT đã có ở bảng `receipts` — `bchConfirmedByName` · `deliveryNoteNo` · `bchConfirmedAt` · `bchComment` · `attachmentCount` ⇒ dù `attachments` có **11 tệp thật**, màn *Đã giao* vẫn in **ẢNH = 0** và cột *BCH XÁC NHẬN* luôn hiện chữ dự phòng `"BCH công trường"`. **(B) Đã nối:** 5 cột trên + `certificateCount` cho `receipts`, và thêm `certificateCount`/`attachmentCount` (2 cột dẫn xuất từ `goods_receipts.certificate_status='complete'` và `attachments.entity_type='goods_receipt'`) cho `purchaseOrders` — **ở CẢ HAI đường** (Java + `scripts/system-route.mjs`) để giữ parity. **(C) Bỏ 3 hằng số giả:** `Delivered.CHỨNG CHỈ` `?2:0` (số 2 **không có căn cứ** — schema **không có bảng chứng chỉ**) → `row.certificateCount\|\|0`; `Delivered.TÌNH TRẠNG` cố định `"Đã nhập kho"` → theo `postingStatus` thật; `Payments.Quá hạn` `moneyBillion(0)` → `Σ max(0, planned−paid)` trên `payment_plans.status='overdue'` (đúng nghiệp vụ vì `SlaComplianceWorker` tự đánh dấu) ⇒ hiện **1,50 tỷ**. **(D) Vá thêm:** chuẩn hoá `paymentPlans` trong `normalizedData` (chống `TypeError` ở màn Kế hoạch thanh toán khi server không gửi khoá). **Kiểm chứng:** `tsc` **EXIT 0** · eslint **0 error** · `master-baseline-gate` **ĐẠT** · `probe-column-parity` **0 khoá thiếu cột** · `npm run build` **EXIT 0** · **cổng MỚI `tools/probe-task082-realdata.mjs` 21/21 ĐẠT** qua cổng 9000 · `test:regression` **59/61** (2 đỏ đã biết). Chi tiết: `TASK-082.md`. *Trước đó:* **TASK-081 — Vá 2 ô MẤT MÀU khi chuyển `DataTable` + DỰNG LẠI dữ liệu cha mồ côi** — (A) so **markup bảng GỐC** với bản đã chuyển phát hiện `WorkCenter` mất `className={r.late?"red-text":""}` và `ProjectProgress` mất `className={row.delta<0?"red-text":"green-text"}` (**hồi quy ẨN**: dữ liệu hiện chưa có dòng quá hạn/âm nên ảnh giống nhau) ⇒ thêm **`DataTable.Column.cellClassName`** và gắn lại đúng biểu thức gốc; **lỗi off-by-one của tôi khi vá** (tìm dấu nháy đóng bằng `indexOf` từ chính dấu nháy mở ⇒ chèn vào giữa chuỗi, `tsc` báo 4 lỗi) ⇒ hoàn tác rồi chèn lại đúng + hậu kiểm. (B) **DỰNG LẠI thay vì xoá** theo chỉ thị người dùng: tạo **PO còn thiếu** `PO-PRJ-DEMO-01-2026-0011` ⇒ `purchase_orders` **6 → 7**; tạo **10 phiếu nhập** mang **đúng ID** mà đường dẫn tệp trỏ tới (gắn PO thật, kho `KHO-DA-MAU-01`, người nhận `tkhodemo`) + **10 dòng `attachments`** ⇒ `goods_receipts` **6 → 16**, `attachments` **1 → 11**; **chứng từ mồ côi 1 → 0** · **tệp mồ côi 1 → 0**. **Bằng chứng quyết định:** ảnh `ATT_38f82d5c…` **từng 403** nay tải được **HTTP 200 · `image/png` · 70 byte · chữ ký `89 50 4e 47`**. **Bẫy đã gặp:** tên tệp `…-985135.png` ⇒ trích "giờ" = **98** ⇒ MySQL `Incorrect datetime value` ⇒ đã thêm **kiểm hợp lệ giờ/phút/giây**. Chi tiết: `TASK-081.md`. *Trước đó:* **TASK-078 — `U-09` HOÀN TẤT: mọi toolbar DANH SÁCH đã dùng `ListToolbar`** — đợt này chuyển **5 khối**: `FormFieldConfigPanel` · `UserProfilePanel` ×2 · `WorkflowModal` ×2 (bộ lọc "Chỉ hiện người có quyền duyệt" đưa vào **`extra`**) + **`Admin` → "DANH SÁCH DỰ ÁN"**. **Đo được:** `<ListToolbar>` **19 → 25 lần**; toolbar tự viết **10 → 4 chỗ**, và **4 chỗ còn lại KHÔNG phải toolbar danh sách**: `TeamManagement` · `Requests` · `AuditLogManager` là **header của KHỐI CHI TIẾT**, còn `BoqPurchaseComparison` dùng lớp CSS riêng (`purchase-comparison-head`, `globals.css` đóng băng). ⇒ **U-09 DONE theo định nghĩa đo được: 0 toolbar DANH SÁCH tự viết.** **Kiểm chứng:** `tsc` **exit 0** · eslint **0 error/73 warning** · **cổng ảnh 8/28 không tăng**, riêng **07-admin ĐẠT ✅**. 🔴 **BỘ CHUYỂN ĐỔI ĐÃ HỌC ĐƯỢC CÁCH TỰ BẢO VỆ:** lượt chạy thử **bắt 4 lỗi** trước khi ghi — nhãn TRỘN chữ + biểu thức (`BƯỚC {index + 1}`, note của `UserProfilePanel`) bị biến thành **chuỗi văn bản** (mất giá trị); quy tắc đúng: `{biểu thức}` ⇒ truyền nguyên, **trộn** ⇒ `<>…</>`; và `tsc` bắt tiếp rằng `ListToolbar` khai `title: string` ⇒ **đã mở rộng sang `ReactNode`** (tương thích ngược). Chi tiết: `TASK-078.md`. *Trước đó:* **TASK-077 — `U-09` đợt mới: 6 MÀN SANG `ListToolbar` CHUẨN §5** (`Receiving` · `Delivered` · `BusinessRoleGroupManager` · `UiDisplaySettingsManager` · `TrustLockAdmin` · `Payments`) — **đo trước:** 16 chỗ toolbar tự viết ở 14 hàm, **10 hàm chưa chuẩn hoá**; **sau:** toolbar tự viết **16 → 10 chỗ**, `<ListToolbar>` **13 → 19 lần** (17 hàm). Tiêu đề lấy từ **nhãn menu có sẵn** trong cấu hình nav (`receiving` = *Kế hoạch giao hàng*, `delivered` = *Đơn hàng đã giao*) hoặc **chữ cũ nguyên văn** giữ qua `count`/`unit`/`note` — **không tự đặt chữ mới**. **Kiểm chứng:** `tsc` **exit 0** · eslint **0 error/73 warning** · **cổng ảnh 8/28 KHÔNG TĂNG**, kiểm riêng **07-admin ĐẠT ✅** (desktop 2 px dưới ngưỡng, 3 mức còn lại 0 px) · **đường phục vụ:** UI header `VNTECH-FP-18C1157899707604`, proxy `:9000` 200, Java health 200, bundle chứa `list-toolbar`. 🔴 **SỰ CỐ SUÝT HỎNG (ghi lại):** bộ chuyển đổi tự động của tôi phát `title=` + ngoặc nhọn chứa **văn bản thô** (thiếu nháy kép) ⇒ **cú pháp JSX sai mà script ĐÃ ghi tệp**; phát hiện khi **đọc lại chính đầu ra**, vá **7 chỗ**, lấy **`tsc` làm trọng tài**. **Bài học:** hậu kiểm tự viết cũng phải được kiểm — bản đầu quét mẫu quá rộng cho **116 dương tính giả** vì `title={biến}` là HỢP LỆ. **Còn 4 hàm** (`FormFieldConfigPanel` · `UserProfilePanel` ×2 · `WorkflowModal` ×2) + `BoqPurchaseComparison` **để lại có lý do** (lớp CSS riêng, `globals.css` đóng băng). Chi tiết: `TASK-077.md`. *Trước đó:* **TASK-076 — `U-17` + `U-07`: HAI DẢI TỰ VIẾT CUỐI CÙNG ĐÃ CHUYỂN SANG `ActivityTimeline`** — "Lịch sử giao nhận" và "Tiến trình mua và giao hàng" trước đây tự dựng (`.delivery-timeline` / `.supply-timeline`); nay dùng **component dùng chung**. **Chữ hiển thị giữ nguyên** và **mã hoá MÀU giữ được** nhờ `tone` (`StatusBadge` với `label=""` render **chấm màu**); bước mua/giao nay có **số bước** ở đầu tên bước và **tô đỏ khi quá hạn** trong `detail`; **không tự đặt chữ mới**. **Kiểm chứng:** `tsc` **exit 0** · cổng áp dụng **`ActivityTimeline` 0 → 2 lần DANG DUNG** + **0 chỗ dải tự viết** · eslint **0 error/73 warning** · hậu kiểm **6/6**. *Trước đó:* **TASK-034 — GỠ CHẶN DỰNG BUNDLE UI (ĐƯỜNG GĂNG ĐÃ THÔNG)** — **ĐÍNH CHÍNH:** sổ cũ ghi *không có script GHI dấu vân tay* là **SAI**, có **`tools/refresh-phase-identity.mjs`** (fixed point); tạo head `drizzle/0109_phase1_ui_identity.sql` ⇒ `0c5e9c0a…` → **`3e28c42d…`** (**242 tệp**), cổng xác minh **ĐẠT**, đồng bộ SSOT + dòng định danh MySQL, **`npm run build` EXIT 0**; dừng **đúng 2 PID** người dùng cho phép (không đụng java) rồi chạy lại UI + proxy; **bằng chứng bản mới được phục vụ:** header vân tay mới + bundle `/assets/page-hpg07hY4.js` chứa cả 5 thay đổi giao diện + đăng nhập/`/api/files` qua `:9000` **200** + hồi quy **59/61**. *Trước đó:* **TASK-075 — MASTER TASK §8.3 (`P-06`): ảnh/hồ sơ vật tư đặc thù XEM ĐƯỢC và KHÔNG TRÀN KHUNG** — trước đây `AttachmentPanel` chỉ có **chip CHỮ** `ẢNH`/`TỆP` + link tải (**không hề thấy ảnh**) và tên tệp dài **đẩy tràn cột**. Nay: **dải ảnh xem trước** (`auto-fill/minmax` tự co) + **ô ảnh thu nhỏ 42px** thay chip chữ cho `image/*` (tệp khác **giữ nguyên chữ `TỆP`**), ảnh **chặn cứng** `height:118px` + `object-fit:cover`, tên tệp `overflow-wrap:anywhere` ⇒ hết tràn khung; **không cắt nội dung** (danh sách/tên/người tải/ngày/nút Xóa giữ nguyên). **Cổng mới `tools/probe-task075-attachments.mjs` 22/22 ĐẠT** — đo trên **Java :18081** vì `tools/cutover-proxy.mjs:39` xếp `/api/files` vào nhóm API (proxy `:9000` **không** đưa đường này cho Node; đăng nhập ở `:8787` trả 401 do runtime Node dùng SQLite riêng) — gồm: hợp đồng tên trường UI↔API (`mimeType`), **byte ảnh PNG thật** (chữ ký tệp + trùng khớp từng byte), **401 khi không cookie**, **4 ĐỐI CHỨNG**, kiểm tĩnh CSS/JSX, **fixture dọn về đúng mức nền**. **🔴 BÀI HỌC: `app/globals.css` BỊ ĐÓNG BĂNG** — lượt đầu tôi nối CSS vào đó và **`master-baseline-gate.mjs` CHẶN NGAY** (luật *cấm append sau mốc canonical R1.1.1 END* + hạn mức **400.653 byte**; khối +1600 B làm tệp thành 402.239 B) ⇒ **đã rút nguyên khối** (tệp về đúng **400.639 B**, `git diff` sạch cho tệp này) và chuyển sang **`app/styles/canonical.css`** ⇒ cổng nền **ĐẠT** (`css=400643B` · `!important=4950` **đúng sát trần**). **Phát hiện dữ liệu:** dòng `attachments` duy nhất trỏ tới chứng từ có **PO mồ côi** ⇒ **403, ảnh thật không tải được** (xem Known Problem #71). Chi tiết: `TASK-075.md`. *Trước đó:* **TASK-074 — MASTER TASK §8.2 (`P-05`): đưa "Tổng hợp giao nhận" ra MODAL RIÊNG** — bảng **13 cột** (`data-contract="VNTECH_REQUEST_DETAIL_ALL_LINES_V1"`, **1433 ký tự**) trước đây **bị nhồi vào layout chi tiết phiếu**; nay **chuyển NGUYÊN VĂN** vào `EntityDetailModal` (dùng chung, `U-01`) mở bằng nút `◉ Tổng hợp giao nhận (n dòng)`, **còn phần CẢNH BÁO CHẶN DUYỆT thì GIỮ LẠI trong drawer** (điều kiện chặn nút Duyệt **không được** giấu vào modal). **Đặt modal cùng cấp với `<aside>`, KHÔNG nhét trong `drawer-body`:** `.overlay` của drawer có `backdrop-filter` ⇒ **tạo containing block cho `position:fixed`** ⇒ đặt ngoài để chắc chắn không vượt viewport (§5/U-10). **Kiểm chứng:** `tsc --noEmit` **exit 0** · cổng áp dụng `EntityDetailModal` **0 → 1 lần DANG DUNG** · eslint **0 error/72 warning** (≤ mốc nền 74) · hậu kiểm script **3/3** (bảng còn nguyên · modal gắn 1 lần · không còn bảng ép trong drawer). **Tiến độ 25 → 27/110 = 24,5 %** (`P-05` + `U-01`). ⚠️ **Chưa kiểm được lúc chạy** — xem Known Problem #68. Chi tiết: `TASK-074.md`. *Trước đó:* **TASK-073 — CỔNG cho `teamMembers`: khoá Java-only CHƯA TỪNG ĐƯỢC ĐO** — 21/21 ĐẠT **kèm 4 phép ĐỐI CHỨNG** chứng minh cổng **báo được HỎNG**. Phát hiện kèm: **JS tham chiếu KHÔNG hề đọc `team_members`** (`grep` = 0) ⇒ khoá **Java-only** trong `if (admin)` ⇒ **không có bản JS để so parity**. Đo bằng **fixture 2 dòng** (1 người dùng thật + 1 **user_id KHÔNG tồn tại**) và đối chiếu bằng **câu viết KHÁC DẠNG** (subquery thay vì JOIN): 8 trường khớp MySQL · `leftAt` giữ `null` · **`active` là BOOLEAN** (lớp lỗi `tinyint(1)` TASK-052) · **`LEFT JOIN` giữ dòng MỒ CÔI** (nếu sai thì mất dòng) · `ORDER BY` đúng · **không rò rỉ** sang `thukydemo` · **dọn về đúng mức nền 0**. **Lượt đầu 17/17 nhưng chưa tin** ⇒ theo bài học #25 đã thêm 4 ĐỐI CHỨNG (gồm bẫy `null` vs chuỗi `"NULL"` và một phép **cố ý so sai cặp cột**) ⇒ **21/21**. **Giới hạn:** dữ liệu vẫn 0 dòng (màn Tổ đội trống **vì dữ liệu**, không phải mã). Chi tiết: `TASK-073.md`. *Trước đó:* **TASK-072 — MASTER TASK §2.1 (`P-04`): ÁP DỤNG THẬT `ApprovalTimeline` VÀO CHI TIẾT PHIẾU (§8.1)** — thay khối tự viết **1467 ký tự** bằng component dùng chung và ánh xạ **đủ 6 thông tin §8.1**: số bước · tên bước · người duyệt · **phòng ban lấy từ CỘT THẬT `approvals.department`** (thay vì dò `staffDirectory`) · thời gian + hạn · trạng thái · ý kiến. **4 cổng xanh:** cổng áp dụng UI dùng chung **0 → 1 lần DANG DUNG** · `tsc --noEmit` **exit 0** · UI `:8787` + proxy `:9000` **HTTP 200** không có marker lỗi biên dịch · cổng ảnh **8/28 lệch — ĐÃ CHỨNG MINH LÀ CÓ SẴN BẰNG ĐỐI CHỨNG THỰC NGHIỆM** (`git stash push app/page.tsx` → chạy lại → **vẫn 8/28 y hệt** → `git stash pop`), và 8 ảnh đó đều ở màn **05-material KHÔNG chứa drawer vừa sửa** ⇒ thay đổi này **không thêm ảnh lệch nào**. **Ledger cập nhật bằng SỐ ĐO:** `P-04` → DONE · `U-06` → `DONE / AP-DUNG 1` · `U-05` AP-DUNG **2 → 90** (số cũ đã bị TASK-020 vượt qua; **xác nhận chéo** bằng `grep` đếm **lần xuất hiện** chứ không đếm dòng) · `U-17` còn **2** chỗ tự viết dải ⇒ **23 → 25/110 = 22,7 %**. Chi tiết: `TASK-072.md`. *Trước đó:* **TASK-071 — Đo 7 TRƯỜNG ĐẶT ALIAS KHÁC TÊN CỘT NGUỒN: 9/9 ĐẠT · 1 phép đo KHÔNG thực hiện được.** Cổng mới `tools/probe-task071-aliased-fields.mjs` xây bản đồ `alias → bảng` từ `FROM`/`JOIN`, tìm mục SELECT theo alias, lấy **cột nguồn** trong biểu thức rồi so *"số dòng có dữ liệu ở cột nguồn (MySQL)"* với *"số dòng API KHÔNG rỗng"*. **Bộ giải mã bị CHÍNH ĐỐI CHỨNG dựng sẵn (4 ca) bắt lỗi ngay lượt chạy đầu:** ca alias có **BACKTICK** (`` level_rank AS `rank` ``) ra sai cột vì bản đầu **để nguyên phần `AS <alias>`** khi trích cột ⇒ alias bị hiểu thành tên cột; **đã sửa: bỏ `AS <alias>` TRƯỚC khi trích cột**. **Nếu không có đối chứng, cổng đã cho kết quả SAI.** Kết quả: `fullName`/`employeeCode`/`role` ← `users` (12/12, 11/12, 12/12 · API 5/5) · `organizationCode`/`organizationName` ← `organization_units` (8/8 · API **51/51**) · `rank` ← `system_level_catalog.level_rank` (5/5 · 5/5) · `staffDirectory.roleName` ← `role_catalog.name` (16/16 · 12/12) · `organizationCode` (8/8 · **11/12** — 1 tài khoản chưa gán đơn vị, **hợp lý**) · `organizationName` (12/12 qua `COALESCE` dự phòng). **Không tìm thấy lỗi trường alias luôn rỗng.** **CHƯA KIỂM ĐƯỢC:** `teamMembers` (bảng 0 dòng) và nhánh DỰ PHÒNG của `COALESCE`. Trước đó cùng phiên: **TASK-070 (#113)** · **TASK-069 (#112)** · **TASK-068 (#111)** · **TASK-067 (#110)** · **TASK-066 (#109)** · **TASK-065 (#108)** · **TASK-064 (#107)** · **TASK-063 (#106)** · **TASK-062 (#104/#105)** · **TASK-061 (#101)** · **TASK-060 (#100)** · **TASK-059 (#98)** · **TASK-058 (#96)** · **TASK-056/057 (#94)** · **TASK-049 (#92)** |
| NEXT TASK | **(1)** Tiếp tục thực thi MASTER TASK §2.1: **`P-05`** (§8.2 — modal **Tổng hợp giao nhận** về phiếu đề nghị gốc) rồi **`P-06`** (§8.3 — ảnh/hồ sơ vật tư đặc thù xem được, **không tràn khung**); cùng nhóm Phiếu, phụ thuộc đã xong. **(2)** `U-14` — chuyển **4 chỗ modal/drawer tự viết** sang `EntityDetailModal` (nay **đã có tiền lệ dùng thật**: `P-05`/TASK-074). **(3)** **TASK-034 — ĐƯỜNG GĂNG BẮT BUỘC ngay**: gỡ chặn dựng **bundle UI**. Đo cuối phiên TASK-072: `dist` mới nhất **17/09 09:35:52** vs nguồn **17/09 22:49:00** ⇒ **bundle CŨ HƠN NGUỒN ~13 giờ**, và `scripts/local-server.mjs:20` nạp `dist/server/index.js` **lúc khởi động** ⇒ **mọi thay đổi giao diện chưa có hiệu lực lúc chạy** ⇒ §8.1/§8.2/§8.3 **không thể kiểm chứng bằng mắt hay bằng probe lúc chạy** cho tới khi gỡ chặn (dựng lại `dist` **chưa đủ**, còn phải khởi động lại tiến trình UI — mà quy tắc của anh là **KHÔNG `Stop-Process node`**). **(4)** `U-15`→`U-17` — áp dụng nốt thư viện dùng chung: còn **~100** bảng tự viết · **~100** trạng thái rỗng · **~50** điều kiện quyền · **2** dải timeline (số đo `probe-ui-adoption.mjs`) |
| BLOCKED ITEMS | ~~**TASK-034**~~ **✅ ĐÃ GỠ CHẶN 17/09/2026** (bundle nay dựng lại được và đang phục vụ bản MỚI) · **TASK-040 nhóm 6** (lệch cấu trúc: `vntech_license_*` cần port cả hệ license + xác minh chữ ký số — thuộc phần **bảo mật** đã yêu cầu tạm hoãn) · **TASK-034** (`npm run build` không dựng lại được UI: dấu vân tay nguồn lệch + bảng identity có **trigger chặn UPDATE** ⇒ phải viết migration) · **TASK-035 mục 7** · **TASK-036 mục 7** · **TASK-037 mục 5** · **TASK-031** · **TASK-032** · **TASK-029** · **TASK-024** · **dữ liệu `user_module_permissions`** · **số SLA thật (24h/8h)** |
| USER CONFIRMATION REQUIRED | **YES** — **11 câu hỏi**, ghi ở mục riêng bên dưới |
| CURRENT BRANCH | `unity` |
| LATEST COMMIT | `3e16b9d` (#169 · TASK-083 — **bảng 16: danh mục vật tư**, `VNTECH-FP-6341C996591D3DD3`) · `d80116e` (#167 · bảng 15) · `b095e45` (#165 · bảng 14) · `488660d` (#164 · docs) · `70325ab` (#163 · docs) · `2c149e1` (#162 · bảng 13) · `ef9ff30` (#157 · 4 bảng tiếp, tổng **9 bảng**) · `5e8f3a9` (#155 · `rowStyle` + ma trận quyền) · `61d65d7` (#153 · 3 bảng) · `dce3ded` (#151 · bảng đầu tiên) · `c84bd88` (#149 · TASK-085 nốt + báo cáo ảnh) · `7a2bd1e` (#148) · `4473ee8` (#147) · `7df9fcb` (#146 · TASK-085) · `d11b679` (#145 · TASK-084) · `9cb4b45` (#144 · TASK-080 đợt 2E) · `78f2671` (#143 · 2D) · `5d5a98b` (#142 · 2C) · `f0b4c64` (#141 · 2B) · `f161a37` (#140 · TASK-082) · **163 commit CHƯA PUSH** (đo bằng `git rev-list --count origin/unity..HEAD`) · **KHÔNG PUSH** theo quyết định của người dùng (chờ anh test thủ công xong) |

## System State

* **Frontend**: Next 16.2.6 + React 19 + TS 5.9 + Tailwind 4; SSR; gần toàn bộ giao diện trong `app/page.tsx` (~4.140 dòng, 221 hàm — U-11 chưa tách)
* **Backend**: Java Spring Boot 3.5 (target 21, chạy JDK 26), Maven đa module Clean Architecture; `SystemController.java` ~1.481 dòng, 224 nhánh `case` = **186 action thật + 38 tên chỉ mục SQL** (`*_uidx`, chỉ để ánh xạ lỗi unique — **không phải action**)
* **Database**: MySQL 8.0.46; Flyway V1–V16 + drizzle tới `0115`; 121 bảng
* **API**: 2 route (`app/api/system`, `app/api/files`); Java phục vụ **186 action**; JS tham chiếu 174 · Java **không thiếu action nào** · Java có **thêm 12**
* **Tầng Java chỉ phục vụ action GHI** — action ĐỌC do SSR/RSC đảm nhiệm. Đây là lý do phép kiểm quyền sống phải dùng payload rỗng.
* **Infrastructure**: MySQL **3306** · Java API **18081** (PID **27412**, background job `pwsh-39`) · Node SSR **8787** (job `pwsh-41`) · cutover proxy **9000** (job `pwsh-34`; người dùng mở `:9000`)
* **JAR**: đã đóng gói lại **thành công** — `vntech-erp-web-0.1.0-SNAPSHOT.jar` **90.912.296 bytes** (18/09 01:39); **mọi sửa đổi backend ĐÃ có hiệu lực lúc chạy**
* **Kiểm chứng cuối lượt (bản `VNTECH-FP-6341C996591D3DD3`, 18/09 · TASK-083 bảng 16)**: UI `:8787` **200** + proxy `:9000` **200** · 🔬 **cổng ảnh 32 ảnh TRÙNG NHAU TỪNG BYTE với lượt trước** (`FF18686D…967C`) — màn `05-material` (**chính màn vừa chuyển**) **không đổi một điểm ảnh** · `probe-ui-adoption`: `DataTable` **29** · bảng tự viết **71** · rỗng tự viết **74** · `app/page.tsx` **4019 dòng** · **cổng `probe-money-consistency` 14/14** · **cổng `probe-row-duplication` 15/15** (17 khoá) · `probe-column-parity` **0 khoá thiếu cột** (đối chứng dương **5/5**) · `master-baseline-gate` **ĐẠT** (`!important=4950` · `css=400643B`) · `npm run build` **EXIT 0** + **BUILT ARTIFACT VALIDATION: ĐẠT** · `tsc` **EXIT 0** · eslint **0 error** (72 warning, đều có trước) · `test:regression` **59/61** (2 đỏ đã biết: TASK-031 nhánh `site_command`, TASK-032 kỳ vọng tên vai trò đã cũ)
  ↳ *Lượt bảng 15 (bản `VNTECH-FP-10606747FB52F138`):* 🔬 **cổng ảnh TRƯỚC/SAU cho màn Phiếu đề nghị — màn CHƯA TỪNG có ảnh chuẩn: ✅ 0 px cả 4 kích thước** (đã thêm màn `08-requests` vào cổng ảnh + công cụ `tools/set-local-identity.mjs`) · cổng ảnh đầy đủ **19/32** = 19 ảnh lệch **y nguyên như trước** (thuần do đổi dữ liệu) + **4 ảnh màn mới đều ĐẠT**
  ↳ *Lượt bảng 14 (bản `VNTECH-FP-ADA2358D8EFD995C`):* **cổng ảnh 28 ảnh TRÙNG NHAU TỪNG BYTE trước/sau** (`867D58DD3C1EEAC5E43A943BBBB72806109CF3F3E4AF1FE10B602AF132D4D3B4`) ⇒ **không màn nào lệch 1 điểm ảnh**, `02-project` **0 px** ×4
  ↳ *Lượt trước nữa (bản `VNTECH-FP-30EE365D549AD7D4`):* `/actuator/health` **200** · **cổng `probe-task082-realdata` 21/21** · **cổng `probe-task080c-work-items` 28/28** · **cổng `probe-task080e-production` 19/19** · `mvn -DskipTests package` **BUILD SUCCESS** · `mvn test` (web) **26/29** — 2 lỗi + 1 error **có sẵn theo nguyên nhân** (`password_reset_at` thiếu trong schema H2 của test · kỳ vọng 400 nhưng nhận 401 · 409 ở chuỗi admin catalog), **không thuộc mô-đun vừa sửa**; việc cô lập bằng `git stash` **thất bại** (lượt chạy đã tạm cất bị chết ở mô-đun `vntech-erp-domain`) nên chỉ dám kết luận theo nguyên nhân, **không dám nói đã chứng minh**
* **TODO hiện tại**: xem mục CURRENT TODO cuối tệp

## Authentication

* `login` · `setup` · `logout` · `change_password` · `update_profile_avatar` (PUBLIC_ACTIONS)
* `me` và `bootstrap`: **`me` KHÔNG được Java triển khai** (Strangler Fig) — proxy `:9000` định tuyến mọi action sang Java nên `me` trả 400 với admin / 403 với tài khoản thường. **`bootstrap` (GET) chạy tốt** và là đường UI thật sự dùng để nạp toàn bộ dữ liệu.

## Authorization — 4 lớp

1. `requireActionModule` — quyền module cho MỌI action không công khai (PHASE 0B), **mặc định TỪ CHỐI** nếu action chưa khai module
2. `requireRole` / `requireRequireAdmin` — theo vai trò, so bằng **mã ENGINE**; `requireRole` nhận **cả** `role()` và `roleBase()`
3. **PHẠM VI dự án/kho** — `AccessScopeService`; **đã phủ 64/64 action** (cổng `probe-action-scope-parity` 64/64, exit 0)
4. Quy tắc P5.3 (không cấp quyền vượt phòng ban)

### Ngữ nghĩa vai trò (TASK-021, CONFIRMED)

Giá trị phân quyền = `COALESCE(role_catalog.base_role, users.role)` = **mã ENGINE**
(`commander`/`project`/`engineer`/`warehouse`/`procurement`/`accountant`/`team`/`director`), KHÔNG phải mã chuẩn.
Ánh xạ **nhiều-về-một**: `cht→commander` · `da_nv`,`da_truong→project` · `ksda→engineer` ·
`kh_nv`,`kh_truong→procurement` · `thu_kho`,`kho_tong→warehouse` · `thuky`,`hcpc_truong→director`.
Nguồn: `drizzle/0029_v530_erp_permissions_workflow.sql:71-80`.

### Đường ống `roleBase` (TASK-021b + TASK-027)

Use-case dựng `CurrentUser` từ `Principal` **PHẢI** lấy `roleBase` thật qua `Principal.roleBase()`.
Mặc định của interface là `return role()` = **mã chuẩn** ⇒ thiếu override sẽ 403 oan.
**TASK-027 phát hiện 2 nhánh `case` tự dựng lớp vô danh `Principal` tại chỗ (không dùng helper) nên thiếu override** — xem Known Problems #16.

## Workflow

* `WF-MUAHANG` 5 bước tuần tự, mỗi bước cần 1 người duyệt; điều kiện hợp lệ = được gán **HOẶC** role nằm trong `allowed_role_codes` (so **cả** `role` và `baseRole`)
* Dữ liệu: `approval_stage_catalog` 5 bước · `workflow_assignments` 5 dòng · `team_members` **0 dòng** · `approval_stage_decisions` **0 dòng** (mã chết)
* **ĐÃ XÁC ĐỊNH (TASK-035, #40)**: **KHÔNG có version pinning.** Hồ sơ đang pending chỉ lưu `approvalStage` (một con số) — **không** lưu `workflow_id`/`workflow_version`; mọi thuộc tính bước (người duyệt · `approval_mode` · `allowed_role_codes`) được tra **LIVE** tại thời điểm quyết định. ⇒ Admin đổi cấu hình ⇒ **hồ sơ đang chờ đổi theo NGAY**, không giữ bản cũ.
* `workflow_definitions.version` **tồn tại nhưng KHÔNG hoạt động như versioning**: cả `drizzle/0080:46` lẫn MySQL `V8__workflow_multi.sql` đều ràng buộc `UNIQUE (workflow_id, step_no)` — **không có `version`** trong khoá ⇒ không thể tồn tại song song hai phiên bản của cùng một bước. `LIKELY`: tắt/xoá bước đang có hồ sơ chờ ⇒ `allowed` rỗng ⇒ **không ai duyệt được, không có đường thoát**.
* Ba bảng P4 (`workflow_definitions`, `workflow_steps`, `workflow_step_approvers`) là **metadata + màn quản trị**, **không** điều khiển nghiệp vụ phía JS (**0** tham chiếu). Java thì **có** dùng để cấp quyền duyệt (rộng hơn JS) — xếp cùng nhóm quyết định TASK-029.

## Thay đổi trong phiên gần nhất

**TASK-072 (17/09 — mới nhất):** `DATABASE` **không đổi** · `API` **không đổi** (không thêm/bớt action, không đổi bootstrap) · `WORKFLOW` **không đổi** · `PERMISSION` **không đổi** · **UI: ÁP DỤNG THẬT `ApprovalTimeline`** vào chi tiết phiếu (§8.1) — thay khối tự viết 1467 ký tự bằng component dùng chung; cổng áp dụng **0 → 1 lần**. *(Bảng dưới ghi phiên TASK-040/041 trước đó.)*

| Loại | Nội dung |
|---|---|
| DATABASE CHANGES | **Không thêm migration / không đổi lược đồ.** NHƯNG **có sửa DỮ LIỆU CẤU HÌNH bị hỏng**: `email_settings.smtp_port` **1 → 587** · `company_settings.po_sla_hours` **1 → 24** · `bch_confirmation_sla_hours` **1 → 8**. Ba giá trị `1` này do **lỗi port của `save_email_settings` ghi đè** (seed ghi 587; DDL default 24/8; chỉ action đó ghi được 2 cột SLA) ⇒ probe đã đưa về đúng thiết kế. **Cần người dùng xác nhận 24h/8h có đúng nghiệp vụ** (câu hỏi #10) |
| API CHANGES | **Không thêm/bớt action.** Bootstrap Java **thêm 2 khoá** `emailSettings` (chỉ admin, không trả `password`) và `emailRecipients` (chỉ admin) — trước đây **thiếu hẳn** nên UI đọc ra `undefined` |
| WORKFLOW CHANGES | **Không có** |
| PERMISSION CHANGES | **Không đổi quy tắc.** Kiểm chứng §7: `nvdademo` (engine role `project`) gọi `save_email_settings` ⇒ **HTTP 403**; hai khoá bootstrap mới trả `null`/`[]` cho người không phải admin ⇒ không rò rỉ cấu hình SMTP |

## Known Problems

1. **TASK-017 — CÒN MỞ**: cổng ảnh bất định giữa các phiên (0 px · 20 px · 0 px trên cùng một màn). Đã khoanh vùng tới ô tìm kiếm topbar và loại trừ, **chưa ra nguyên nhân gốc**.
2. **Tiến độ áp dụng UI dùng chung**: `StatusBadge` **90** · `ListToolbar` **13** · `DataTable` **0** · `PermissionGuard` **0** · **`ApprovalTimeline` 1 — ĐÃ DÙNG THẬT (TASK-072)** · **`ActivityTimeline` 2 — ĐÃ DÙNG THẬT (TASK-076)** · **`EntityDetailModal` 1 — ĐÃ DÙNG THẬT (TASK-074)**. Còn lại: **~100** bảng tự viết · **~100** trạng thái rỗng · **~50** điều kiện quyền · **4** modal · **0** dải timeline (đã hết — TASK-076).
3. **`/api/files` chưa được bảo vệ** (S-05) — người dùng yêu cầu tạm bỏ qua phần bảo mật.
4. ~~JAR không đóng gói lại được~~ **SAI NAY ĐÃ SỬA** — TASK-B03 DONE. JAR **90.885.239 bytes** (17/09 14:04) đã build và chạy; **mọi sửa đổi backend TỪ TASK-019 trở đi ĐÃ có hiệu lực lúc chạy**.
5. ~~Cổng ảnh + probe UI không chạy được~~ **SAI NAY ĐÃ SỬA** — TASK-B02 DONE. Cổng ảnh **28/28 ĐẠT, 0 px**; riêng probe cần `spawn mysql`/Edge headless vẫn cần mở rộng sandbox (đã xin và chạy được nhiều lần).
6. ~~Lỗ hổng P0 phạm vi: Java mới kiểm 2/64~~ **ĐÃ SỬA Ở TASK-023** — nay **64/64**, cổng `probe-action-scope-parity` 64/64 exit 0.
7. ~~**`team_members` = 0 dòng**~~ **✅ ĐÃ NẠP DỮ LIỆU THẬT (TASK-080, 17/09):** `team_members` **0 → 4 thành viên thật**, `teams.leader_user_id` từ **NULL → `cha.ht` (Chỉ huy trưởng A)**; `role_in_team` lấy từ `role_catalog.name` (không tự đặt chữ). Còn: `approval_stage_decisions` = 0 dòng (mã chết). **Cập nhật 17/09 (TASK-073): ĐÃ ĐO LẦN ĐẦU đường ĐỌC của khoá này — 21/21 ĐẠT** (fixture 2 dòng + 4 đối chứng): 8 trường khớp MySQL · **`LEFT JOIN` giữ dòng mồ côi** · `active` là **boolean** · `ORDER BY` đúng · **không rò rỉ** sang tài khoản thường · dọn về mức nền 0. ⇒ **màn Tổ đội trống vì DỮ LIỆU, KHÔNG phải vì mã**; nạp thành viên là **thao tác dữ liệu của quản trị viên** (chưa làm, chờ quyết định). **Khoá này là JAVA-ONLY**: JS tham chiếu `scripts/system-route.mjs` **không hề đọc `team_members`** (`grep` = 0).
8. **TASK-029 — Java CHẶT HƠN JS trên 30 action** (chi tiết + tác động ở mục BLOCKED).
9. ~~**Dữ liệu `user_module_permissions` thiếu**~~ **✅ ĐÃ NẠP ĐỦ (TASK-080, 17/09):** ma trận phòng ban **51 → 480 dòng** (mọi đơn vị nghiệp vụ phủ **60/60** module, chỉ `admin` để trống theo thiết kế), rồi sinh lại quyền người dùng bằng **chính action `rebuild_department_permissions`** ⇒ `user_module_permissions` **484 → 926 dòng**, phủ **39 → 60/60** module; **22 module từng có 0 dòng quyền nay đã có**. **Đối chiếu ngược với bản sao lưu: TĂNG 11 tài khoản · GIẢM 0** ⇒ không ai mất quyền. Nhánh kho-central của `canAccessWarehouse` **nay đã đạt tới được**.
10. **Known-problem #15 CŨ ĐÃ ĐÓNG**: tên capability `canView`/`canUse`/… ánh xạ đúng sang cột thật `can_view`/`can_use`/… (`ModulePermissionStoreAdapter:22-24`), mặc định `can_use` khớp JS.
11. **TASK-024 — `isCompanyLeadership` lệch**: JS = tập 7 mã **HOẶC** `base_role='director'`; Java = `{director, accountant}`. Java **cấp thừa** cho `accountant`, **cấp thiếu** cho `thuky`/`hcpc_truong`. Chờ người dùng.
12. ~~TASK-022b — `ACTION_CATALOG` chưa ghi 12 action chỉ có ở Java~~ **ĐÃ ĐÓNG ở #41 — KHÔNG cần sửa**: danh mục có hợp đồng "sinh từ nguồn JS" và đang **đúng** hợp đồng (174 mục, khớp JS 0 lệch); 12 action Java-only đã khai đầy đủ trong bản thi hành `ActionRbacRegistry`. Thêm vào sẽ **tạo sai lệch giả** và phá `probe-action-parity`. Cổng mới: `tools/probe-catalog-drift.mjs` (exit 0).
13. **Sai lệch bản đồ MODULE**: MODULE khớp **156/186**, CAPABILITY khớp **185/186** (`system_level_impact`: JS `canUse` vs Java `canView`).
14. ~~`EntityDetailModal` gây lỗi lint chặn CẢ chuỗi `npm test`~~ **ĐÃ VÁ ở TASK-008 phần 2** — `setState` trong `useEffect` (`react-hooks/set-state-in-effect`) khiến `npm test` dừng ngay ở `lint`, nên `typecheck` + 8 tệp `test:regression` + `test:workflow` **chưa từng chạy**. Nay suy ra tab khi render ⇒ `eslint .` **0 error**, bộ kiểm đã tiến qua `typecheck`.
15. **TASK-031 — cây dự án trong menu bị TẮT ở CẢ HAI nav** (`__site_command_tree_disabled__`, `app/page.tsx:664` và `:678`) ⇒ nhánh `activeSiteProjects.map(...)` là **mã chết**. Sentinel vào ở commit `1c01f39` (16/09) **âm thầm trong một commit không liên quan**, test không được cập nhật. `CONFLICT` — **cần người dùng quyết định**, không tự sửa và **không sửa test cho khớp mã**.
16. **TASK-032 — LỖ HỔNG LIÊN KẾT DỮ LIỆU: 0/16 vai trò trỏ đơn vị mặc định.** `organization_units` có 8 đơn vị đang hoạt động **gồm `BGD` = "Ban giám đốc"**, nhưng `role_catalog.default_organization_unit_id` **null ở mọi vai trò** ⇒ cơ chế "đơn vị mặc định theo vai trò" âm thầm không thể chạy. Lưu ý: **không tệp SQL/Java nào nhắc `default_organization_code`** — đó là tên hiển thị sinh từ JOIN trên `default_organization_unit_id`.
17. ~~Cổng ảnh + `test:regression` không chạy được~~ **ĐÃ RÕ NGUYÊN NHÂN GỐC** — Edge sập khi khởi động: `FATAL:mojo\...\platform_channel.cc:183 Check failed: Access is denied (0x5)` (mojo channel = **named pipe**, sandbox chặn). Node test runner cũng lỗi `spawn EPERM` vì spawn con qua pipe. ⇒ **ràng buộc MÔI TRƯỜNG, không phải lỗi mã**; chạy được khi cấp `danger-full-access`. Chẩn đoán mẫu: `tools/diag-edge-cdp.mjs`. **ĐÃ XÁC NHẬN LẠI ở #47**: chạy `test:regression` có full access ⇒ **59/61 pass** (trước chỉ 58/61 khi bị chặn) — đúng test từng đỏ nay xanh, chứng minh kết luận "dương tính giả do môi trường" là đúng.
18. **TASK-040 nhóm 1 ĐÃ ĐÓNG (#47)** — nhưng khi sửa nhóm này phát hiện **lớp lỗi thứ hai đi kèm lớp SQL**: *port không nguyên trạng* làm **mặc định bị sai âm thầm**. Cụ thể Java kẹp sàn `max(1, …)` **trước** rồi mới so `== 0` ⇒ nhánh mặc định của JS (`|| 587` / `|| 24` / `|| 8`) trở thành **mã chết** ⇒ lưu 1 thay vì 587/24/8. ⇒ **Khi port từ JS, phải đối chiếu cả THỨ TỰ TOÁN HẠNG, không chỉ tên cột.**
19. **Lỗi port đã GHI HỎNG dữ liệu thật** (`smtp_port=1`, `po_sla_hours=1`, `bch_confirmation_sla_hours=1`) — phát hiện vì probe đọc lại DB; đã khôi phục về 587/24/8. Không phải do seed (seed ghi 587, DDL default 24/8). ⇒ **Bài học: "biên dịch sạch" và "HTTP 200" đều KHÔNG chứng minh dữ liệu đúng — phải đối chiếu tầng DB.**
20. **TASK-040 — 3 nhóm còn lại**: nhóm 3b (`import_material_catalog`: UI gửi `categoryCode`, Java đọc `categoryId` ⇒ vật tư nhập vào **mất nhóm**; JS còn tự tạo category/subcategory) và nhóm 6 (`vntech_license_*` — **lệch cấu trúc**, xem mục BLOCKED).
21. **DỮ LIỆU MỒ CÔI (mới, TASK-040 vòng 4)**: `stock_issue_items` **3/5 dòng** trỏ tới `team_id` **không còn tồn tại** (`teams` chỉ có 1 dòng). `findIssueItem` dùng `JOIN teams` (giống y JS) nên các dòng đó bị loại ⇒ `confirm_installation` trả *"Dòng xác nhận lắp đặt không hợp lệ."* **Mã đúng, dữ liệu sai.** Bảng **không có khoá ngoại** nên không gì chặn tham chiếu mồ côi. **KHÔNG tự sửa dữ liệu.**
22. **KHÔNG SUY TÊN ACTION TỪ TÊN PHƯƠNG THỨC** (lỗi của chính tôi ở vòng 4): tôi gọi thử `settle_subcontract` — tên **không tồn tại** — rồi suýt kết luận nhóm 5 là **mã chết**. Tên đúng là **`settle_team_subcontract`** (`SystemController:637`, JS `system-route.mjs:1249`). Bắt được nhờ cổng mới `probe-action-coverage-controller.mjs` báo **0 action thiếu `case`** — mâu thuẫn với kết luận "chưa triển khai".
23. **Cổng đối chiếu với chính các nhánh `case`**: `tools/probe-action-coverage-controller.mjs` → Java phục vụ **224** · JS có mã **174** · UI gọi **119** · **UI gọi mà Java thiếu: 0** · **JS có mã mà Java thiếu: 0** · Java-only **50** (38 tên chỉ mục SQL hợp lệ + 12 action Java-only đã biết). Các cổng cũ chỉ so với danh mục/thanh ghi RBAC, **không** so với nhánh `case` đang phục vụ request.
24. **LỚP LỖI THỨ HAI ĐÃ ĐƯỢC PHỦ CỔNG**: `tools/probe-increment-drift.mjs` săn trường hợp JS **cộng dồn** (`col=col+?`) nhưng Java **ghi đè** (`col=?`) — lớp lỗi mà cổng lược đồ **không thể** bắt. Kết quả: 10 cột JS cộng dồn · **0 ứng viên** trên toàn kho Java, **có đối chứng dương** (`tools/_old-adapter-positive-control.java.txt`).
25. ~~`canonicalMeCode` Java lệch JS~~ **ĐÃ VÁ ở #52**: Java chỉ so **đúng bằng** trong khi JS khớp **tiền tố** ⇒ đo được **7/28 đầu vào lệch** (`Điện lực`, `DIEN123`, `ELV-1`, `PCCC-01`, `DIEN.TU`… bị xếp `KHAC` thay vì đúng hệ M&E) ⇒ **vật tư nhập vào mang `system` sai** (cả nhập BOQ lẫn danh mục vật tư). Nay có `domain/service/MaterialSystemCodes.java` làm **một nguồn sự thật**, `BoqManagementUseCase` uỷ quyền cho nó, kèm **unit test 5 ca** ghi lại đúng 7 ca từng lệch. Cổng đo: `tools/probe-canonical-me-code-drift.mjs`.
26. **Bẫy `(?i)` của Java (lỗi của chính tôi, unit test bắt được)**: `replaceAll("(?i)đ","d")` **KHÔNG** khớp `Đ` — cờ `(?i)` của Java chỉ case-fold ASCII trừ khi bật `UNICODE_CASE` ⇒ mọi đầu vào tiếng Việt rơi về `KHAC`. Phải thay **tường minh** `đ`→`d` và `Đ`→`D` (như `MaterialMatcherV2` đang làm).
27. ~~TASK-041 — `save_approval_stage` lệch cả HỢP ĐỒNG PAYLOAD lẫn QUY TẮC~~ **ĐÃ VÁ ở #57**: Java **bắt buộc trường `code` mà UI không bao giờ gửi** ⇒ action **luôn 400**; cộng thêm **5 trường bị bỏ im lặng** (`description`, `sla_hours`, `approval_mode`, `auto_approve_on_submit`, `sort_order`) ⇒ **admin sửa SLA, hệ thống báo thành công nhưng SLA KHÔNG đổi**; và **3 quy tắc JS không được thi hành**. Nay đã port đủ + **gỡ 2 phương thức tự phát minh** (`findApprovalStageCatalog`, `stageCodeExists` — chúng coi `code` như `stage_no`). Bằng chứng: probe **21/21** (SLA 8→33 đọc lại đúng) + chốt bước cuối **7/7**; cấu hình duyệt **nguyên trạng**. Chi tiết: `TASK-041.md` mục 6.
28. **LỖI LẶP LẠI CỦA CHÍNH TÔI (3 lần)**: (1)+(2) chạy `mvn` từ **thư mục workspace** thay vì `java-backend` ⇒ `no POM in this directory`; (3) dùng `Set-Content` của PowerShell để sửa tệp có tiếng Việt ⇒ tệp thành **UTF-8 không hợp lệ** (PowerShell mặc định ghi ANSI). **Quy tắc: `mvn` phải đặt `workdir = java-backend`; mọi tệp có tiếng Việt chỉ ghi bằng công cụ file (UTF-8), KHÔNG dùng `Set-Content`.**
29. **MySQL CLI mặc định dùng charset `cp850`** ⇒ chuỗi **tiếng Việt** trong `WHERE` bị hỏng và **tra không ra dòng** (đã gây tạo 3 dòng rác trong probe TASK-041 phần 3). Phải thêm `--default-character-set=utf8mb4` khi kết xuất/đối chiếu dữ liệu có tiếng Việt, và ưu tiên **tra bằng khoá ASCII** (`code`, `id`).
30. ~~`retry_email` truy vấn bảng `email_queue` KHÔNG tồn tại~~ **ĐÃ VÁ ở #61 (TASK-042)** — action trả **HTTP 500**; nay `UPDATE email_outbox SET status='queued',next_attempt_at=?,last_error=NULL,updated_at=? WHERE id=?` đúng JS. **Cổng SQL đã được mở rộng sang `FROM`/`JOIN`** để bắt cả lớp lỗi này (trước đây cổng chỉ kiểm `INSERT`/`UPDATE`).
31. **Java KHÔNG có cơ chế gửi email nào** — không `JavaMailSender`, không `jakarta.mail`, không SMTP client. `email_settings` chỉ được lưu/đọc; `email_outbox` **chưa bao giờ được INSERT** từ đường Java ⇒ **không email nào được xếp hàng hay gửi** (kể cả thông báo duyệt). Đây là **khoảng trống tính năng**, cần hạng mục riêng — **không tự bịa cơ chế gửi**.
32. **Cổng BẢN ĐỒ GHI có giới hạn đã biết: nó CHỈ thấy SQL tĩnh.** Dương tính giả đã gặp: `users.avatar_url` (Java ghi qua **JPA** — `AuthUseCase.updateProfileAvatar` → `userRepository.save`, không có SQL text). Khi cổng báo "Java không ghi cột X", **phải kiểm cả đường JPA** trước khi sửa.
33. ~~Ba bảng "Java có `case` nhưng không ghi"~~ — **`custom_field_values` ĐÃ VÁ ở #62 (TASK-043)**: Java **chưa bao giờ GHI** trường động của dòng phiếu **và ĐỌC sai chỗ** (tra bằng id PHIẾU thay vì id DÒNG ⇒ `customFields` luôn rỗng ở cả hai phía UI đọc). Còn lại: `material_code_history` (`save_material`/`merge_material_master` — **không một dòng Java nào** nhắc bảng này) · `project_archives.purge_audit_id` (`delete_project` — Java không đặt liên kết audit). Thêm `boq_versions.approved_at`, `boq_price_import_items.changed`.
34. **TASK-044 (phát hiện cùng lượt với TASK-043) — bootstrap bỏ sót 11 TRƯỜNG của dòng phiếu**: `workPackageCode` · `boqCode` · `installationArea` · `contractLineNo` · `pendingBchQty` · `closeReason` · `remainingQty` · `rejectedQty` · `linkedPoCount` · `linkedReceiptCount` · `missingDocumentCount`. UI `app/page.tsx:3609` hiển thị trực tiếp ⇒ trước khi sửa, các cột này **luôn trống/0** dù dữ liệu thật là `remainingQty=975`, `pendingBchQty=340`, `linkedPoCount=12`, `linkedReceiptCount=10`. Đây là **dạng thứ tám** của lớp lỗi *"đường ĐỌC thiếu"*. **ĐÃ VÁ ở #62.**
35. **Dòng mồ côi THẬT trong `material_request_items`**: `MRI_d1f57f9d-7b4f-4939-a854-bbc50f78c69d` trỏ tới `MR_46cee316-… **không tồn tại**` (qty 20, `line_status='issued'`, **có liên kết PO/BCH thật**). Cùng lớp với known issue #21 (`stock_issue_items` 3/5 dòng). **KHÔNG tự sửa dữ liệu.**
36. **`delete_request` để lại rác ở CẢ HAI phía** (không phải lỗi port): JS `system-route.mjs:1031-1037` và Java `deleteRequestCascade` xoá **đúng cùng 5 bảng**, nhưng **cả hai** đều bỏ quên `procurement_allocations` + `custom_field_values` của phiếu bị xoá ⇒ rác tăng dần. Cần người dùng quyết định trước khi sửa (sửa JS trước).
37. **Trường động của `request_header`: CẢ JS LẪN JAVA đều KHÔNG lưu** — JS chỉ dùng cấu hình `request_header` để **kiểm bắt buộc** (`:904-905`), không ghi `custom_field_values` và không đọc lại. Không phải lỗi port, nhưng là **lỗ hổng của bản JS**: admin bật trường động phần đầu phiếu ⇒ dữ liệu **mất**. Tương tự: `custom_field_values` với `form_key='boq'` — JS **đọc + xoá** nhưng **không có câu `INSERT` nào** ⇒ tính năng **chết ở chính bản JS**.
38. **Java thiếu `audit(...)` ở luồng Phiếu đề nghị**: JS có `audit(user.id,"CREATE","material_request",…)` (`:980`), Java **không gọi audit ở nhánh nào** của `RequestManagementUseCase`. Đo được: `SELECT COUNT(*) FROM audit_logs WHERE entity_type='material_request'` = **0 dòng**. Lỗ hổng **xuyên suốt** (không riêng `create_request`) ⇒ hạng mục riêng.
39. **Java YẾU HƠN JS 3 phép kiểm khi chọn Owner của bước duyệt** (CONFIRMED ở mức mã): JS `requireWorkflowAssignment` (`:442-452`) kiểm *có phân công · Owner hoạt động · **vai trò Owner thuộc `allowedRoleCodes`** · **Owner có `user_project_scopes`** (trừ admin)*; Java (`RequestManagementUseCase:213-222`) chỉ kiểm **2 điều đầu** ⇒ Owner **sai vai trò** hoặc **không được phân quyền dự án** vẫn được gán duyệt. **Chưa kiểm chứng lúc chạy** (phải sửa phân công thật) — đăng ký sửa ở lượt sau.
40. **Lỗi của chính tôi ở lượt này (2 lỗi phép đo)**: (1) chạy `SELECT … FROM approval_project_assignments` với cột **`stage_no`** (cột thật là `stage`) **và nuốt `stderr` bằng `2>$null`** ⇒ thấy rỗng rồi **suýt kết luận "MySQL chưa cấu hình phân công nào"**; sự thật bảng có **5 dòng thật**. **Nuốt stderr là tự che mắt** — khi kết quả rỗng phải kiểm lại xem lệnh có lỗi không. (2) So `customFields` bằng `JSON.stringify(a)===JSON.stringify(b)` ⇒ **phụ thuộc thứ tự khoá** ⇒ báo HỎNG oan; phải sắp xếp khoá trước khi so. (3) **Ở TASK-046 tôi dùng `Set-Content` để sửa `MASTER_STATUS.md` và đã XOÁ MẤT một mục** (khoảng `IndexOf` đầu–cuối nuốt luôn mục 44) — vi phạm chính quy tắc #28 của dự án; đã khôi phục bằng công cụ file. **KHÔNG dùng `Set-Content` để sửa tệp tài liệu, kể cả khi có `-Encoding utf8`.**
40b. **TASK-046 (vòng rà cuối 3 cột) — ĐÃ VÁ ở #67 (2/3 cột)**: `project_archives.purge_audit_id` — JS ghi 1 dòng `audit_logs` (`PURGE_AFTER_OFFLINE_ARCHIVE`) rồi `UPDATE project_archives SET purge_audit_id=<auditId>`, Java **thiếu cả hai câu** ⇒ đã thêm `AuditLogPort.logReturningId(...)` (giữ interface là FUNCTIONAL INTERFACE) + `setArchivePurgeAuditId(...)`; `boq_price_import_items.changed` — Java ghi thiếu cột ⇒ thêm tham số `changed` vào `BoqStore.insertPriceItem` + adapter. **Lỗi P0 bị CHÍNH PROBE bắt:** `delete_project` **luôn HTTP 500** (`MySQL: Incorrect DATETIME value: '0000'` — JS truyền mốc `"0000"`, SQLite so chuỗi nên vô hại) ⇒ **không admin nào purge được dự án**; nay dùng `ARCHIVE_FLOOR="1970-01-01 00:00:00"`. Probe **17/17 exit 0** · jar **90.896.045 B** · log **0 ERROR** · regression **59/61**. Còn lại ~~`boq_versions.approved_at` = **LIKELY dương tính giả**~~ → **ĐÃ XÁC NHẬN: DƯƠNG TÍNH GIẢ** (17/09): quét toàn bộ `scripts/` cho thấy **không có câu nào ghi giá trị thật** vào cột này — `:816` (`INSERT INTO boq_versions(…effective_at,created_by,…)`) **bỏ hẳn** cột khỏi danh sách, `:2805` **bind thẳng `null`**; không tồn tại `UPDATE boq_versions SET … approved_at=…`. Công cụ bản đồ GHI đếm giá trị `null` là "có ghi" ⇒ **Java không ghi cột này là ĐÚNG**, không cần sửa. Chi tiết: `TASK-046.md`.
40c. **Cổng BẢN ĐỒ GHI nay chỉ còn 5 bảng lệch** (đo lại sau #67): `boq_versions` (nghi dương tính giả) · `email_outbox` (Java không có cơ chế gửi email) · `users.avatar_url` (dương tính giả: ghi qua JPA) · `vntech_license_installations` + `vntech_license_transfer_requests` (nhóm 6 — chờ quyết định #11).
41. ~~`save_material`: Java **XOÁ HỆ M&E** (`system='KHAC'`) mỗi lần admin lưu mã vật tư~~ **ĐÃ VÁ ở #63 (TASK-045)**: Java đọc `payload.system` nhưng UI (`app/page.tsx:3769`) **KHÔNG BAO GIỜ gửi khoá này** — JS tính `system = canonicalMeCode(category.code)`. Cùng lượt: **thêm ghi `material_code_history`** (trước đây Java không nhắc bảng này), **buộc lý do khi đổi mã gốc** (nguyên văn JS), ghi `min_stock` (trước bị bỏ im lặng), kiểm nhóm tồn tại + nhóm con thuộc nhóm. Probe **6/14 → 14/14**.
42. **DỮ LIỆU `materials.system` LỆCH 5/14 mã — KHÔNG tự sửa**: `DIEN-DAY-CAD-001` (`KHAC`, nhóm DIEN) và `CTN-ONG-NHUA-001` (`KHAC`, nhóm CTN) khớp cơ chế lỗi #41 (nguồn ghi cụ thể **không xác định được** — `save_material` không có audit); `CTN-ONG-NHUA-002`→`DIEN`, `CTN-VAN-001`→`HVAC`, `DIEN-ONG-LUON-001`→`CTN` (16/09 12:01) nghi là **dữ liệu mẫu hoán vị có chủ ý**. **Chờ người dùng xác nhận.** Sau khi vá, admin **mở và LƯU lại** mã trong màn Danh mục là `system` tự suy đúng từ nhóm.
43. **`save_material` — phần 2 CHƯA port** (đăng ký): rebuild alias theo `aliasText` (JS xoá hết rồi tạo lại), **luật trùng tên gốc/alias** (3 thông điệp), Java còn **tự tạo alias bằng chính tên gốc** khi tạo mã (JS không), **thiếu `audit(...)`**, và **văn bản thông điệp thành công khác JS**. Thêm: `merge_material_master` JavaScript **xoá** mã nguồn còn Java chỉ `active=0` + đổi mã thành `<code>_X`.
44. ~~TASK-046: hai cột cuối của vòng rà bản đồ GHI~~ **ĐÃ VÁ ở #67** — chi tiết đầy đủ ở mục **40b/40c** ngay trên (mục này giữ chỗ để không đứt số thứ tự sau khi tôi sửa nhầm bằng `Set-Content`). Xem `TASK-046.md`.
45. **Cổng điều tra nay chỉ còn 5 bảng lệch BẢN ĐỒ GHI** (đo lại bằng `probe-write-map-triage.mjs` sau #67): `boq_versions` (nghi dương tính giả — JS `:2805` bind `null`) · `email_outbox` (Java không có cơ chế gửi email) · `users.avatar_url` (dương tính giả: ghi qua JPA) · `vntech_license_installations` + `vntech_license_transfer_requests` (nhóm 6 — chờ quyết định #11).
46. **TASK-048 — KHE HỞ `audit(...)` LÀ TOÀN HỆ THỐNG (đo 17/09, ✅ SỐ ĐÃ SỬA ở #87):** cổng `tools/probe-audit-coverage.mjs` đối chiếu nhật ký kiểm toán hai phía. **⚠️ CON SỐ "152" TRƯỚC ĐÂY LÀ HIỆN VẬT CỦA CHÍNH CỔNG** (cổng tra tên BIẾN camelCase vào bảng khoá theo tên LỚP PascalCase ⇒ không bao giờ khớp ⇒ "cả hai đều ghi" luôn 0 và "khe hở" luôn = đúng số action JS có audit). Đã sửa + **thêm đối chứng dương (186/186 nhánh case tra được lớp)**:
   * **JS: 174 action, trong đó 152 action CÓ gọi `audit(...)`**
   * **Java: 186 nhánh `case`, 3/21 lớp use-case có gọi `auditLog.log(...)`** (sau #87: thêm `RequestManagementUseCase`; trước là `MaterialCatalogManagementUseCase`, `ProjectManagementUseCase`; cộng `AuthUseCase` dùng cổng này ở 4 chỗ)
   * ⇒ **KHE HỞ Ở TẦNG USE-CASE = 128 action** (không phải 152) · **"cả hai đều ghi" = 24 action**
   * **✅ ĐÃ ĐO LẠI BẰNG DỮ LIỆU (TASK-067, 17/09) — CON SỐ NÀY TỪNG BỊ ĐỌC SAI THÀNH "128 action KHÔNG được ghi nhật ký".** `audit_logs` có **825 dòng / 39 action khác nhau**; **427 dòng** mang `module_key` + `ip_address` — hai cột **CHỈ `AuditTrailFilter` điền** (use-case `auditLog.log` không điền). Đối chiếu từng action trong danh sách "khe hở": `decide_approval` **173/173** · `create_self_work_item` **28/28** · `update_boq_contract_prices` **9/9** · `save_email_settings` **6/6** · `set_material_norm_status` **6/6** — **TẤT CẢ đều có dòng nhật ký, 100% do lớp FILTER ghi** (`usecase_written = 0`). ⇒ **Nhật ký KHÔNG thiếu.** Cổng `probe-audit-coverage.mjs` đã được vá để **tự đối chiếu `audit_logs`** và in rõ con số 128 là **khe hở về TÊN/CHI TIẾT ở tầng use-case**, không phải thiếu dòng.
   * **CÒN LẠI THẬT — GHI TRÙNG + LỆCH TỪ VỰNG (mới, TASK-067):** cùng một thao tác sinh **2 dòng** với **mã hành động khác nhau**: `create_request` **80 dòng** (filter, `action = tên action thô`) + `CREATE` **58 dòng** (use-case, mã nghiệp vụ như JS). JS chỉ ghi **1 dòng/thao tác** với mã nghiệp vụ + `before/after` đã lọc. Ngoài ra `entity_type`/`entity_id`/`after_json` của dòng filter là **suy đoán** (moduleKey + id đầu tiên trong payload + payload thô) trong khi JS **khai tường minh**. ⇒ **Sửa được CHỈ sau khi kiểm ở mức METHOD** từng action (cổng đo theo LỚP): bỏ dòng filter của một action mà use-case không thực sự ghi = **MẤT dấu vết pháp lý**. **KHÔNG đánh cược** ⇒ hạng mục riêng.
   **GIỚI HẠN của phép đo (phải nói rõ):** Java đo theo **LỚP use-case** (không theo từng method) và chỉ thấy lời gọi **tĩnh** `auditLog.log(...)`; `AuditTrailFilter` ở tầng web **ghi thêm 1 dòng cho mỗi request** nên `audit_logs` **không trống** — nhưng bản ghi đó là *"POST action X"*, **không phải** nhật ký nghiệp vụ có `before/after` như JS.
   **Lát cắt đã làm (#87 — TASK-048):** **5/6** action luồng Phiếu đề nghị (`create_request` · `update_returned_request` · `resubmit_request` · `delete_request` · `cancel_request`) — kiểm chứng lúc chạy **18/18**; mốc thứ 6 `decide_approval`/`APPROVE_PARTIAL` ⇒ **TASK-054** vì phải port **cả hành vi** nhánh `all_roles` (xem #53). Sau đó: mua hàng/PO · kho · tổ đội · admin.
47. **QUÉT MỒ CÔI TOÀN HỆ THỐNG (17/09, read-only — bổ sung cho #21 và #35):** rà 10 cặp khoá ngoại bằng `LEFT JOIN … WHERE cha IS NULL`:

| Quan hệ con → cha | Dòng mồ côi |
|---|---:|
| `procurement_allocations.request_item_id` → `material_request_items` | **32** |
| `approvals.request_id` → `material_requests` | **15** |
| `stock_issues.team_id` → `teams` | **3** |
| `project_boq_items.material_id` → `materials` | **3** |
| `material_request_items.request_id` → `material_requests` | **1** |
| `purchase_order_items.purchase_order_id` → `purchase_orders` | **1** |
| `material_requests.project_id` → `projects` · `goods_receipt_items.receipt_id` → `goods_receipts` · `project_boq_items.boq_version_id` → `boq_versions` · `custom_field_values.entity_id` → `material_request_items` | 0 |

   **Ý nghĩa:** (a) **32 allocations + 15 approvals mồ côi** là **bằng chứng thực tế cho known issue #36** — xoá phiếu để lại rác ở **cả JS lẫn Java**; (b) `stock_issues.team_id` (không phải `stock_issue_items` — cột `team_id` **không tồn tại** ở bảng đó, đã kiểm bằng `information_schema`) đúng **3 dòng**, khớp số ghi ở #21; (c) 3 dòng BOQ trỏ vật tư không còn tồn tại. **KHÔNG tự sửa dữ liệu** — cần anh quyết định: xoá rác, hay giữ để đối chiếu lịch sử? (Bản ghi mồ côi **không hiển thị** trên giao diện vì mọi truy vấn đều `JOIN` bảng cha.)
48. **TRUY NGUỒN 55 DÒNG MỒ CÔI (đo tiếp 17/09, read-only) — đã đủ dữ kiện để anh quyết:**
   * **15 approvals mồ côi = ĐÚNG 3 phiếu đã bị xoá × 5 bước**, tất cả `status='approved'`, tạo **14/09** lúc 12:00:13 · 12:23:26 · 15:06:02 — tức **cả 3 phiếu đã duyệt xong rồi mới bị xoá**: `MR_77c22ec4-…` · `MR_00b80951-…` · **`MR_46cee316-…`**.
   * **32 allocations mồ côi** thuộc **3 dự án**: `PRJ_fdbfab20` (PRJ-DEMO-01) **26 dòng / 12 dòng phiếu đã xoá**, `PRJ_62845d6e` 3 · `PRJ_daedb25b` 3; `stage='MR'`, tạo 14/09 (12:00 · 12:23 · 15:49–15:56).
   * **Cùng một gốc:** `MR_46cee316-…` chính là phiếu mà dòng `material_request_items` mồ côi (`MRI_d1f57f9d…`, known issue #35) trỏ tới ⇒ **3 phiếu bị xoá ở giai đoạn seed/demo 14/09 đã để lại: 15 approvals + 26–32 allocations + 1 dòng phiếu**.
   * **Đề xuất để anh chọn:** (1) **Xoá rác** — an toàn vì **cha không tồn tại**, không có màn hình nào đọc được các dòng này (mọi truy vấn đều `JOIN` cha); (2) **Giữ nguyên** nếu anh muốn đối chiếu lịch sử duyệt trước khi dọn; (3) **Vá gốc trước rồi mới dọn** — sửa `delete_request` ở **cả JS lẫn Java** để xoá thêm `procurement_allocations` + `custom_field_values` (known issue #36) kẻo rác tiếp tục sinh. **Tôi không tự chạy bất kỳ phương án nào.**
49. **✅ ĐÃ VÁ (#85, TASK-052) — LỚP LỖI #4: cột `tinyint(1)` của MySQL trả về `Boolean`, không phải `Number`.** `BootstrapDataAdapter` kiểm `activeVal instanceof Number` cho `module_catalog.active` ⇒ **luôn sai** ⇒ `data.modulePermissions` của **admin luôn rỗng** (JS trả **61** dòng — đã đo `MODULE_KEYS` JS = 61 = `module_catalog WHERE active=1`, 0 lệch hai chiều). Bị tầng UI che (`page.tsx:548` trả toàn quyền cho admin trước khi đọc danh sách). **Nhánh Ban giám đốc của TASK-050 dùng cùng đoạn mã nên nếu tách ra sẽ thành lỗi mới nghiêm trọng hơn** (xoá trắng toàn bộ dữ liệu của `thukydemo`). **Việc còn lại:** rà xem còn chỗ nào so `Number` với cột cờ hay không — lần rà đầu **chỉ thấy 2 chỗ và đã vá cả hai** (5 use-case khác trong kho đã dùng biến thể đúng `… : Boolean.TRUE.equals(o)`), nhưng **chưa tự động hoá** ⇒ cần một cổng riêng (mục NEXT TASK).
50. **✅ ĐÃ VÁ (TASK-066) — `data.engineRoleProfiles` = `null` cho MỌI tài khoản, kể cả admin: LỖI THỨ TỰ GẶP.** `BootstrapDataAdapter` đọc `data.get("businessRoleEngineProfiles")` ở **dòng 726** nhưng chỉ `put` khoá đó trong khối `if (admin)` ở **dòng ~826** ⇒ đọc trước khi ghi. Đo trên API: `engineRoleProfiles=null` với `admin`, `nvdademo`, `tkhodemo`, trong khi `businessRoleEngineProfiles` = **9 dòng**. JS trả **9 dòng cho mọi người**. UI **không vỡ** (phòng vệ 2 lớp: `page.tsx:376` ép về `[]`, `:2380` rơi về nhãn mặc định) nhưng **mất nhãn nghiệp vụ của công ty**. Đồng thời `businessRoleEngineProfiles`/`businessRoleGroups`/`businessScopes` nằm trong `if (admin)` còn JS trả cho **mọi người** ⇒ **lớp lỗi mới: "khoá CÓ khai nhưng giá trị null/rỗng theo vai trò" — cổng quét theo TÊN khoá không bắt được**, phải có probe HTTP theo từng vai trò. **⇒ TASK-066 đã dựng đúng cổng đó (`probe-task066-role-shape.mjs`, hợp đồng 38 khoá × 6 tài khoản) và vá cả 3 vế:** 3 khoá chuyển ra khối TOÀN CỤC kèm lọc `active=1` khi không phải admin; `engineRoleProfiles` gán nguồn TRƯỚC rồi dùng lại cùng danh sách; **port nốt phép LÀM GIÀU `scopeIds`/`scopes`** (JS `:675`) mà trước đó **0/11** dòng có. Kết quả: **1/17 → 11/11 ĐẠT**.
51. **MỚI (17/09, đo được) — 5 DÒNG `user_project_scopes` MỒ CÔI** (project_id không tồn tại trong `projects`): `thukydemo` → `PRJ_fdbfab20-bf1f-0000-0000-000000000000` · `admin` → 3 id (`PRJ_370df722…`, `PRJ_daedb25b…`, `PRJ_62845d6e…`) · **1 dòng của người dùng KHÔNG còn tồn tại** (`USR_250387b4-234c-4f0b-9767-d46640af91ed`). **Triệu chứng thật:** tài khoản Ban giám đốc `thukydemo` nhận `data.projects = []` ⇒ **không thấy dự án nào** để làm việc (đúng cả ở JS vì hai bên cùng logic lọc theo dự án `active`). ⇒ mục **D5**. **KHÔNG tự sửa dữ liệu.**
52. **MỚI (17/09, đọc mã hai phía) — `transferOrders` của Java thiếu 7 trường và thiếu bộ lọc theo kho.** JS `:713` trả `sourceProjectId` · `destinationProjectId` · `approvedAt` · `shippedAt` · `receivedAt` · `shippedQty` · `receivedQty`; Java chỉ 14 trường. JS còn **lọc theo kho của vai trò kho** ở `:714` (`warehouseScopes`), Java **không**. UI đọc `row.sourceProjectId`/`row.destinationProjectId` (`page.tsx:2238`). **Chưa sửa** — vào hàng đợi rà đường ĐỌC mức CỘT.
53. **✅ ĐÃ SỬA CỔNG ĐO SAI (#87) — `probe-audit-coverage.mjs` tra tên BIẾN thay vì tên LỚP.** Hệ quả: mọi báo cáo dùng con số **"152 action"** đều là **hằng số giả** (bằng đúng số action JS có `audit()`), và dòng *"CẢ HAI ĐỀU GHI: 0"* là **sai**. Nay có **đối chứng dương** (`186/186 nhánh case tra được lớp`): khe hở thật = **128**, cả-hai-đều-ghi = **24**. **Bài học #21.** *(Liên quan: phép kiểm audit trong `probe-task043-custom-fields.mjs` gọi hàm `q1()` không tồn tại ⇒ **chưa bao giờ chạy**, nhưng dòng "19/20 ĐẠT" khiến tôi tưởng nó chạy và hỏng.)*
54. **✅ ĐÃ PORT ĐỦ Ở JAVA (#89 — TASK-054) — NHƯNG KẾT LUẬN BAN ĐẦU Ở #88 LÀ SAI.** #88 ghi *"Java chuyển bước sớm, JS thì không"* như một lệch Java-vs-JS. **Sự thật đo được:** JS `:1080` dùng **đúng câu truy vấn** ưu tiên `approval_mode_snapshot` như Java, và JS `:976` ghi snapshot `assignedOwner ? "single" : …` ⇒ **hễ bước có Owner là snapshot = `single`** ⇒ **21/21 dòng bước `all_roles` bị ĐÓNG BĂNG thành `single`** ⇒ nhánh `all_roles` là **mã chết ở CẢ HAI LÕI**. Việc Java thiếu mã nhánh đó là **thật** (nay đã port đủ: `required`/`approved`/nhánh ADMIN điền vai trò thiếu/`missing`/comment tiến độ/**RETURN SỚM**/audit `APPROVE_PARTIAL` + nhánh từ chối) và đã kiểm chứng **20/20** bằng fixture — nhưng nó **không** sửa được hành vi nghiệp vụ. **⇒ TASK-055** (cần người dùng chốt: giữ nguyên cho khớp JS, hay sửa để cấu hình 2 vai trò có hiệu lực thật; **7 phiếu đang chờ ở bước 5** sẽ bị ảnh hưởng).
55. **MỚI (17/09, đo được) — `approval_mode_snapshot` ĐÓNG BĂNG chế độ duyệt ⇒ quy tắc 2 vai trò ở bước cuối KHÔNG có hiệu lực.** Bước 5 cấu hình `all_roles` (`da_truong,kh_truong`, *"Trưởng phòng Dự án + Kế hoạch xác nhận cuối"*) nhưng **21/21** dòng `approvals` của bước này có snapshot `single` ⇒ **chỉ cần MỘT vai trò xác nhận**. Sửa SLA/cấu hình bước **không** giúp (snapshot cũ vẫn thắng). Đây là **hành vi dùng chung cả JS lẫn Java** ⇒ sửa sẽ **lệch khỏi JS** ⇒ cần quyết định nghiệp vụ: **D6 / TASK-055** (`docs/agent-progress/TASK-055.md`).
56. **✅ ĐÃ PORT (#92 — TASK-049) + ⚠️ HỆ QUẢ DỮ LIỆU: LUỒNG LẬP PHIẾU CỦA PRJ-DEMO-01 ĐANG BỊ CHẶN.** Java trước đây **bỏ qua 2 phép kiểm Owner** mà JS `requireWorkflowAssignment` (`system-route.mjs:442-452`) có — và JS `:966` **gọi hàm này cho MỌI bước** trong `create_request` ⇒ bản Java đang **âm thầm cho phép lập phiếu với cấu hình phân công sai**. Nay đã kiểm đúng (vai trò Owner ∈ `allowedRoleCodes`; Owner phải có `user_project_scopes`, trừ admin) và kiểm chứng **10/10**. **Hệ quả đo được:** Owner **bước 2** (`thukydemo`) **không có phạm vi** cho PRJ-DEMO-01 (dòng phạm vi của họ trỏ tới project **KHÔNG TỒN TẠI** `PRJ_fdbfab20-bf1f-0000-0000-000000000000` — cùng gốc **known issue #51 / D5**) ⇒ `create_request` trả **400** *"Owner Thư ký TGĐ D chưa được phân quyền dự án này."* và **không lập được phiếu đề nghị mua** cho dự án này. **Bản JS cũng chặn y hệt** (không phải lỗi port). **Cần người dùng: cấp 1 dòng `user_project_scopes` cho Owner bước 2** (SQL sẵn ở `TASK-049.md` mục 3, hoặc dùng giao diện *Quản trị → Phân quyền người dùng*) + dọn 5 dòng mồ côi.
57. **MỚI (17/09, cổng TẬP CỘT) — lớp lỗi "THIẾU CỘT": 13 khoá, đã vá 4, còn 9.** Cổng mới `tools/probe-column-parity.mjs` (TASK-056, **có đối chứng dương với metadata MySQL**) so tập tên cột SQL hai phía cho **30 khoá trùng tên**. **Đã vá (#94/TASK-057):** `issues` (+`receivedByName`, +`installedQty`) · `returns` (+`returnedByName`) · `transferOrders` (14 → **21 cột** theo JS `:713` **+ bộ lọc theo kho của `:714`**) · `productIdentity` (`id AS productId`). **Còn 9 khoá:** `workItems` · `taskNotifications` · `businessRoleGroupScopes` · `roleCatalog` · `organizationUnits` · `accountingVouchers` · `hrRecords` · `officialCorrespondence` · `legalDocuments`.
58. **✅ ĐÃ VÁ (#96 — TASK-058) — `workItems`: Java KHÔNG có mệnh đề lọc ⇒ trả TOÀN BỘ công việc cho mọi tài khoản; thiếu 14 cột.** JS `:715-717` dựng 4 nhánh lọc theo vai trò (admin `1=1`; KH `department_code='KH' AND (assigned_to=? OR EXISTS(role kh_truong…))`; DA tương tự với `da_truong`; BCH `department_code='BCH' AND (assigned_to=? OR project_id IS NULL OR project_id IN (<phạm vi>))`; còn lại `assigned_to=?`). **Đo được TRƯỚC khi vá (jar 19:21, probe dựng 7 dòng tạm + 6 tài khoản thật): 4/18 ĐẠT — cả 5 tài khoản khác vai trò đều thấy ĐỦ `T1..T7`.** Sau khi port **nguyên văn** (kèm `departmentForRole`, `departmentCodeForUser` + `stripDiacritics` theo đúng cách JS, và `Context.department` mới): **18/18 ĐẠT** — admin 7/7 · `nvkhdemo` T1,T5 · `trinhtrench` (kh_truong) T1,T5,T6 · `nvdademo` T2 · `tkhodemo` T3,T4 · `thukydemo` T7; `workItemEvents` nay **chỉ** sự kiện của công việc trong phạm vi (JS `:718`) và đủ cột. Cổng tập cột **9 → 8 khoá**. Chi tiết: `TASK-058.md`.
59. **✅ ĐÃ VÁ (#98 — TASK-059) — 8 khoá thiếu CỘT còn lại; cổng tập cột nay về 0.** Đã bổ sung: `taskNotifications` (5 cột + **sắp "chưa đọc TRƯỚC"** + LIMIT 100 như JS; giữ thêm `taskId`/`message` Java-only vì `grep` toàn `app/` chỉ đọc `n.readAt`) · `hrRecords` (`identityDate`/`identityPlace` — form **nhập** mà đọc lại **không có trường**) · `accountingVouchers`+`officialCorrespondence`+`legalDocuments` (`createdBy`/`createdByName`) · `roleCatalog` (`businessGroupId` + `businessGroupName` ⇒ cột "Nhóm nghiệp vụ" ở màn Chức danh/vai trò **trước luôn "—"**) · `businessRoleGroupScopes` (đúng JS: `businessScopeId`/`scopeCode`/`scopeName` + JOIN `business_scope_catalog` + thứ tự + lọc `bs.active=1` khi không phải admin) · `organizationUnits` (`projectName`/`effectiveFrom`/`effectiveTo`). **Cổng `probe-column-parity.mjs` = 0 khoá thiếu cột** (13 → 9 → 8 → **0**; đối chứng dương 4/4) · **probe bootstrap 100/100** (mục 7 đối chiếu giá trị **theo từng dòng** với MySQL; bảng rỗng được in rõ là "(bỏ qua)").
60. **✅ ĐÃ VÁ (#100 — TASK-060) — cổng tập cột từng BỎ QUA 49/78 khoá.** Nay ánh xạ được **khoá kết quả → biến JS** (parse `const result = { key: value }`) + gom **mọi** khối SQL trong một lệnh khai báo (dạng `cond ? await all(A) : []` và 2 nhánh đều bị bỏ sót trước đây) ⇒ so được **65 khoá** (bỏ qua 13), **lộ ra 18 khoá thiếu cột**. Kèm lớp lỗi *"khoá có khai nhưng TRƯỜNG DẪN XUẤT thiếu"* (`engineRoleProfiles` = null cho mọi tài khoản — #50; `businessRoleGroups[].scopeIds/scopes` do JS enrich `:675`) mà cổng theo **tập cột** **không** bắt được ⇒ vẫn cần probe HTTP theo vai trò.
61. **MỚI (phát hiện khi rà TASK-061) — cổng tập cột KHÔNG bắt được lớp lệch `ORDER BY` / `LIMIT` / kiểu `JOIN`.** Ba ca đã xác nhận bằng cách đọc thẳng JS (chưa vá):
    * `boqImportBatches` — JS `:647` sắp `ORDER BY project_id,contract_id,version_no DESC`; Java sắp `created_at DESC` ⇒ **thứ tự lô BOQ hiển thị sai**.
    * `boqChangeHistory` — JS `:648` có `ORDER BY h.created_at DESC, h.id DESC LIMIT 1000`; Java **thiếu cả tiebreaker lẫn LIMIT** ⇒ bảng lịch sử BOQ có thể trả về **toàn bộ** lịch sử của mọi dự án trong phạm vi.
    * `workflowAssignments` — JS `:723` là `isAdmin ? await all(…) : []` và **không lọc theo dự án**; Java trả cho **mọi vai trò** và lọc `project_id IN (…)` ⇒ **rò rỉ thông tin** "ai duyệt bước nào của dự án nào" cho tài khoản không phải admin.
    * Bài học: mỗi khi port một khoá, phải đọc **cả câu SQL** (JOIN · WHERE · ORDER BY · LIMIT), không chỉ danh sách cột. Cần **cổng riêng** cho lớp này.
62. **✅ ĐÃ ĐO HẾT (TASK-063) — cổng `tools/probe-clause-parity.mjs`** so `ORDER BY`/`LIMIT`/kiểu `JOIN` cho **71 khoá**, có **đối chứng của chính bộ so sánh** (6 cặp SQL dựng sẵn: 1 cặp chỉ khác định dạng ⇒ phải IM; 5 cặp khác LIMIT/chiều sắp/cột sắp/tiebreaker/kiểu JOIN ⇒ phải BẮT) + **phần ĐỘ PHỦ** như cổng tập cột. Kết quả: **2 lệch THẬT đã vá** — `adminMaterialCategories` (Java sắp `sort_order,code`; JS `:694` sắp `CASE WHEN active=1 THEN 0 ELSE 1 END,sort_order,name` ⇒ nhóm chưa dùng không bị đẩy xuống cuối) và `audits` (**Java `LIMIT 500`, JS `:698` `LIMIT 100`** ⇒ nhật ký kiểm toán dài gấp 5 lần giao diện tham chiếu). Cùng lượt, cổng loại **2 DƯƠNG TÍNH GIẢ**: `productIdentity` (JS dùng `first()` = 1 dòng, Java ghi `LIMIT 1` tường minh) và khoảng trắng sau dấu phẩy trong `ORDER BY`.
63. **✅ ĐÃ ĐÓNG HOÀN TOÀN (TASK-064 + TASK-065) — LỚP MÙ THỨ HAI: khoá có NHÁNH THEO VAI TRÒ mà nhánh dự phòng là BIẾN KHÁC.** Ví dụ JS `:694` `const adminMaterialCategories = canEditCentral ? await all(A) : materialCategories;` — cổng mệnh đề chỉ so **câu ĐẦU** nên **không thấy** rằng với tài khoản thường JS trả **chính `materialCategories`** còn Java (`if (admin)`) để khoá **trống**. **(1) TASK-064 vá NHÁNH DỰ PHÒNG:** đo lúc chạy admin ↔ `thukydemo` — `adminMaterialCategories` **0 → 6 dòng**, `adminMaterialSubcategories` **0 → 8 dòng**; `adminMaterials` **giữ `[]`** vì JS `:696` trả **mảng rỗng** (Java vốn đã đúng). Cổng mở rộng nay **kiểm** 4 phép ⇒ **7/7 ĐẠT**. **(2) TASK-065 vá ĐIỀU KIỆN:** JS `:693` `canEditCentral = isAdmin(user) || canUseModule(user,"central_warehouse","canEdit")` — phép kiểm **MODULE**, không phải vai trò. Đặt ở **use-case** (`BootstrapUseCase` nhận `ModulePermissionStore`), truyền xuống adapter qua `Context.canEditCentral`; **kèm sửa chỗ dễ sai:** 3 khoá `adminMaterial*` trước nằm trong danh sách **xoá-trắng vô điều kiện** của nhánh không-phải-admin, nếu chỉ đổi `if (admin)` thì dữ liệu vừa dựng sẽ bị xoá ngay sau đó ⇒ đã tách danh sách và bọc `if (!canEditCentral)`. Cổng mới `tools/probe-task065-caneditcentral.mjs` **10/10 ĐẠT** với **ĐỐI CHỨNG ÂM trên cùng một tài khoản**: CẤP quyền ⇒ `adminMaterials` **14 dòng** (14/14 có `aliases`); THU HỒI ⇒ **0 dòng**; **14 ≠ 0 ⇒ phép kiểm CÓ tác dụng**; fixture dọn **0→1→0**.

64. **MỚI (TASK-067, 17/09) — NHẬT KÝ KIỂM TOÁN: báo động giả đã bác bỏ + GHI TRÙNG thật đã đo được, CHƯA vá.**
    * **Bác bỏ báo động giả:** con số *"KHE HỞ 128 action"* của `probe-audit-coverage.mjs` từng bị đọc thành *"128 action không được ghi nhật ký"*. **Dữ liệu thật:** `audit_logs` **825 dòng / 39 action**; **427 dòng** mang `module_key` + `ip_address` — hai cột **chỉ `AuditTrailFilter` điền**; đối chiếu từng action "khe hở": `decide_approval` **173/173** · `create_self_work_item` **28/28** · `update_boq_contract_prices` **9/9** · `save_email_settings` **6/6** · `set_material_norm_status` **6/6** — **100% do FILTER ghi, use-case 0**. `AuditTrailFilter` (P6) phủ **MỌI** POST `/api/system` thành công (trừ 7 action `SKIP_ACTIONS`). ⇒ **Nhật ký KHÔNG thiếu.** Cổng đã được vá để **tự đối chiếu `audit_logs`** và đổi nhãn thành *"KHE HỞ Ở TẦNG USE-CASE"*.
    * **CÒN LẠI THẬT — GHI TRÙNG + LỆCH TỪ VỰNG:** cùng một thao tác sinh **2 dòng** với **mã khác nhau** — `create_request` **80 dòng** (FILTER ghi **tên action thô**) + `CREATE` **58 dòng** (use-case ghi **mã nghiệp vụ** như JS). JS ghi **1 dòng/thao tác**; `entity_type`/`entity_id`/`after_json` của dòng FILTER là **suy đoán** (moduleKey + id đầu tiên trong payload + payload thô) trong khi JS khai tường minh.
    * **VÌ SAO CHƯA VÁ:** cách sửa hiển nhiên (cho FILTER bỏ qua các action đã có audit ở use-case) chỉ an toàn nếu biết **từng METHOD** có ghi hay không — cổng hiện đo theo **LỚP use-case**. Bỏ dòng FILTER của một action mà use-case **không thực sự ghi** ⇒ **MẤT dấu vết pháp lý**. **Không đánh cược vào tính năng an toàn** ⇒ phải kiểm mức METHOD trước (TASK-068).
    * **✅ ĐÃ RA QUYẾT ĐỊNH (TASK-068) — GIỮ CẢ HAI DÒNG, KHÔNG ĐỔI MÃ.** Đo bằng dữ liệu: dòng FILTER có **IP + module** (`create_request` **80/80** · `decide_approval` **173/173** · `cancel_request` 15/15 · `save_email_settings` 6/6) còn dòng use-case **KHÔNG có IP** (`CREATE` **0/58** · `APPROVE_PARTIAL` **0/13** · `CANCEL` **0/15** · `UPDATE` **0/8**) — vì `AuditLogPort.log(...)` **không nhận ngữ cảnh request** (chỉ FILTER có `HttpServletRequest`; JS thì truyền `request` vào `audit(...)`). ⇒ Hai dòng **BỔ SUNG** cho nhau: *"ai/khi nào/từ đâu"* + *"đã làm gì với đối tượng nào"*. **KHÔNG** thêm 24 action vào `SKIP_ACTIONS` (sẽ **mất IP**). **Hướng cải thiện ĐÚNG (ghi lại, chưa làm):** cho `AuditLogPort.log` nhận ngữ cảnh request ⇒ dòng use-case tự đủ ⇒ **khi đó mới** cho FILTER bỏ qua, và phải **kiểm mức METHOD** trước. Chi tiết: `TASK-068.md`.
65. **ĐÃ SỬA DÒNG TODO CŨ (TASK-067) — `SlaComplianceWorker` thực ra ĐÃ XONG từ commit #34.** Dòng TODO trong tệp này ghi *"hỏng âm thầm mỗi giờ (cột `overdue_at` không tồn tại)"* nhưng câu lệnh đã được sửa từ #34 (bỏ hẳn `overdue_at`) và `TASK-025.md` ghi DONE. **Đo lại hôm nay:** log `INFO SlaComplianceWorker : SLA worker: 0 supply steps quá hạn; 0 payment plans quá hạn; 4 BCH chờ xác nhận.` (chạy 60s sau khởi động, **không còn WARN**); DB `supply_workflow_steps` **29 `overdue`** / 27 `completed` / **0 `pending` quá hạn**; `payment_plans` 2 `overdue`; `company_settings` `po_sla_hours 24` / `bch_confirmation_sla_hours 8`. **Bài học: TODO phải được ĐO LẠI trước khi truyền tiếp — hai hạng mục của tôi trong lượt đó đều đã cũ/sai.**
66. **✅ ĐÃ VÁ (TASK-069) — LỖI THỨ 10 CỦA LỚP "ĐƯỜNG ĐỌC THIẾU KHOÁ": `emailOutbox`.** Khoá này là **BẮT BUỘC** trong hợp đồng `AppData` của UI (`app/page.tsx:31 emailOutbox: Row[]`) nhưng **KHÔNG một adapter Java nào** khai nó (`grep` toàn `java-backend` = 0 kết quả) ⇒ khối chuẩn hoá của UI **âm thầm** thay `[]` ⇒ màn **Hộp thư gửi** luôn rỗng dù `email_outbox` có dữ liệu (email phê duyệt / giao việc / thư thử đều ghi vào bảng này). JS `:724` trả `isAdmin(user) ? <11 cột + requestNo qua LEFT JOIN material_requests, ORDER BY queued_at DESC LIMIT 100> : []`; JS `:737` xác nhận non-admin nhận `[]`. **Đã port nguyên văn cả hai vế.** **Phát hiện bằng cổng MỚI `tools/probe-task069-appdata-contract.mjs`** — cổng này đọc **khai báo KIỂU `AppData`** (nguồn sự thật mạnh hơn dạng viết `result.data?.<khoá>` của cổng TASK-066): **95 khoá** (88 bắt buộc + 7 tuỳ chọn) × 6 tài khoản thật. **Lượt chạy đầu 0/6 ĐẠT (thiếu đúng `emailOutbox` cho cả 6 tài khoản) ⇒ cổng tự chứng minh CÓ TÁC DỤNG bằng cách bắt một lỗi THẬT; sau khi vá 6/6 ĐẠT.** Cùng lượt, rà 11 khoá Java-only: **10/11 khớp số dòng với bảng nguồn**; `boqMappingCandidates` là **payload chết** (UI không đọc, không có trong `AppData`, JS không trả, bảng rỗng) — ghi lại, không xoá.
67. **CÒN LẠI của lớp "đường ĐỌC thiếu khoá":** các cổng đã phủ **khoá + kiểu** (95 khoá × 6 vai trò) và **tập cột** (73 khoá) và **mệnh đề** (73 khoá), nhưng **CHƯA** kiểm **trường bên trong từng dòng** cho 11 khoá Java-only (mới kiểm khoá + số dòng) và **13 khoá trùng tên còn bỏ qua** của cổng tĩnh. Ngoài ra cổng `AppData` chỉ kiểm khoá **BẮT BUỘC/tuỳ chọn** theo khai báo — nếu UI đọc một khoá KHÔNG khai trong `AppData` (truy cập động) thì cổng không thấy.
68. **MỚI (TASK-072) — CỔNG ẢNH 28 ẢNH KHÔNG PHỦ MÀN CHI TIẾT PHIẾU, nên nó KHÔNG xác nhận được §8.1 hiển thị đúng.** 28 ảnh chuẩn chỉ gồm **7 màn danh sách** (dashboard · project · work · team · material · warehouse · admin) — **không** có màn **chi tiết phiếu**, đúng nơi vừa áp dụng `ApprovalTimeline`. Cổng ảnh vì vậy chứng minh được *không hỏng màn khác*, **không** chứng minh được *timeline mới hiển thị đúng*; hiện chỉ có phép kiểm **TĨNH** (cổng áp dụng đếm 1 lần dùng + `tsc` + HTTP 200). ⇒ Cần probe **mở drawer bằng headless** rồi đọc DOM/chụp ảnh. Ghi kèm: cổng ảnh đang có **8/28 ảnh lệch CÓ SẴN** do **DỮ LIỆU đổi** (đã chứng minh bằng đối chứng `git stash`), **chưa cập nhật ảnh chuẩn**. **Bổ sung (đo cuối phiên TASK-072):** nguồn `app/page.tsx` **17/09 22:49:00** vs `dist` mới nhất **17/09 09:35:52** ⇒ **bundle đang chạy cũ hơn nguồn ~13 giờ**; `scripts/local-server.mjs:20` nạp `dist/server/index.js` **một lần lúc khởi động** ⇒ dựng lại `dist` **không đủ**, còn phải **khởi động lại tiến trình UI** ⇒ đây chính là **TASK-034**. **✅ ĐÃ GỠ CHẶN 17/09/2026:** dựng lại `dist` được (`npm run build` EXIT 0), UI `:8787` + proxy `:9000` đã chạy **bản MỚI** (header `x-vntech-source-fingerprint` = `VNTECH-FP-3E28C42DB2755DAF`, bundle chứa cả 5 thay đổi §8.1/§8.2/§8.3) ⇒ **phần "không kiểm được lúc chạy" nay CÓ THỂ kiểm bằng mắt**. Mục này **vẫn giữ** vì **cổng ảnh 28 ảnh KHÔNG phủ màn chi tiết phiếu** (cần probe mở drawer bằng headless). **Bổ sung (TASK-074, §8.2):** thay đổi *modal Tổng hợp giao nhận* cũng **chưa kiểm được lúc chạy** vì cùng lý do; phần đã kiểm chỉ là **cổng kiểu + cổng áp dụng + hậu kiểm cấu trúc**, nên **việc modal hiển thị đúng vẫn là ô CHƯA BIẾT** cho tới khi gỡ chặn. **Bổ sung (TASK-075, §8.3):** dải ảnh xem trước cũng **chưa kiểm được bằng mắt**; đã kiểm được **đường ống byte ảnh thật + biên CSS trong nguồn** (22/22) nhưng **bố cục hiển thị vẫn CHƯA BIẾT**.
69. **MỚI (TASK-072) — `ApprovalStep` THIẾU 2 MỐC THÔNG TIN so với khối cũ.** Khối tự viết trước đây hiển thị thêm *Nhận hồ sơ (`queuedAt`)* và *Đã gửi email (`notifiedAt`)*; component dùng chung **chưa có chỗ** cho 2 trường này ⇒ khi thay đã **mất 2 mốc**. **Không nhét vào trường khác** (là hiển thị sai nhãn); hướng đúng là **mở rộng `ApprovalStep`** thêm 2 trường rồi truyền từ `approvals` — dữ liệu Java **đã có sẵn** (`approvals.queuedAt`/`notifiedAt`).
70. **MỚI (TASK-075) — `app/globals.css` LÀ TỆP BỊ ĐÓNG BĂNG; CSS mới PHẢI vào `app/styles/canonical.css`.** `scripts/master-baseline-gate.mjs` chặn cứng hai luật: (dòng 44) **không được append CSS sau mốc `VNTECH_MASTER_BASELINE_CSS_R1_1_1_END`**; (dòng 51) **kích thước ≤ 400.653 byte**. Lượt đầu của TASK-075 nối +1600 byte vào `globals.css` (thành **402.239 B**) ⇒ **cổng CHẶN**; đã rút ra và chuyển sang `canonical.css`. ⚠️ **Con số cần nhớ:** `!important` của `globals.css` = **4950 = ĐÚNG BẰNG TRẦN** ⇒ tệp này **không nhận thêm được một `!important` nào**; mọi việc giao diện sau phải tính tới ràng buộc này (liên quan `U-12`).
71. **MỚI (TASK-075) — DỮ LIỆU TỆP ĐÍNH KÈM MỒ CÔI ⇒ ẢNH THẬT KHÔNG TẢI ĐƯỢC.** Dòng `attachments` **duy nhất** trỏ tới `GRN_b1cbfe5f-…`: chứng từ **có tồn tại** nhưng `purchase_order_id` của nó **trỏ tới PO không tồn tại** (đo: `LEFT JOIN purchase_orders` ⇒ NULL; `goods_receipts` 6 dòng / `purchase_orders` 6 dòng). Java tra chủ dự án bằng `JOIN purchase_orders` (`FileStoreAdapter:77-79`) ⇒ rỗng ⇒ **403** *"Chứng từ không tồn tại hoặc loại hồ sơ không được hỗ trợ."* ⇒ **bản ghi mồ côi, KHÔNG phải lỗi mã**. Kèm: kho tệp `java-backend/data/files` có **11 tệp nhưng DB chỉ 1 dòng**, cả 11 tệp **đều 70 byte** và trỏ tới chứng từ **không còn trong DB** ⇒ dấu vết các lượt thử trước. **KHÔNG tự xoá** — cần người dùng quyết định (cùng nhóm D1/D5).
72. **MỚI (TASK-082) — ĐƯỜNG ĐỌC JAVA THIẾU 5 CỘT so với JS ⇒ UI hiển thị 0/chữ dự phòng dù DỮ LIỆU THẬT ĐÃ CÓ.** Bảng `receipts`: `scripts/system-route.mjs:603` (SSOT) có `bchConfirmedByName` · `deliveryNoteNo` · `bchConfirmedAt` · `bchComment` · `attachmentCount`, còn `BootstrapDataAdapter` (Java — **đường phục vụ thật** qua cổng 9000) **thiếu cả 5**. Hệ quả đo được: `attachments` có **11 tệp thật** nhưng màn **Đã giao** luôn in **ẢNH = 0**; cột **BCH XÁC NHẬN** luôn rơi vào nhánh dự phòng `"BCH công trường"`. **Vì sao cổng `probe-column-parity` không bắt:** cổng chỉ so các khoá **trùng tên biến hai phía** (13 khoá bị bỏ qua) và **CHỈ so tập cột** — không kiểm `ORDER BY`/`LIMIT`/`WHERE` (chính "GIỚI HẠN" in ra của cổng, xem #61, #67). **✅ ĐÃ VÁ 18/09/2026 (TASK-082):** nối 5 cột + `certificateCount` ở Java; thêm `certificateCount`/`attachmentCount` + 2 `LEFT JOIN` ở **cả hai** đường (giữ parity); cổng mới `tools/probe-task082-realdata.mjs` **18/18 ĐẠT** (10/15 dòng có ảnh thật · 7/7 PO có chứng chỉ + ảnh thật).
73. **MỚI (TASK-082) — 3 HẰNG SỐ GIẢ HIỂN THỊ DỮ LIỆU.** (a) màn *Đã giao* cột `CHỨNG CHỈ` in `certificateStatus==="complete"?2:0` — **số 2 không có căn cứ**: rà toàn bộ schema MySQL chỉ có `goods_receipts.certificate_status`, **KHÔNG có bảng chứng chỉ nào** (`attachments` · `document_sequences` · `legal_documents` là các bảng gần nghĩa duy nhất) ⇒ phân loại **UNKNOWN** ⇒ thay bằng **số đếm thật** `certificateCount`. (b) màn *Đã giao* cột `TÌNH TRẠNG` **cố định** `"Đã nhập kho"` cho mọi dòng ⇒ nay theo `posting_status` thật (`posted`=12 · `pending_confirmation`=4). (c) màn *Thanh toán* ô `Quá hạn` = `moneyBillion(0)` ⇒ nay `Σ max(0, planned−paid)` trên `payment_plans.status='overdue'` — **đúng nghiệp vụ hệ thống** vì chính `SlaComplianceWorker` (Java) đánh dấu `overdue` khi `planned_date` quá hạn (`SlaStoreAdapter:52`); số thật đo được **1,50 tỷ / 2 kế hoạch**. ⚠️ **Còn treo:** cột `TÌNH TRẠNG` nên theo `posting_status` hay `delivery_document_status` — **cần người dùng chốt** (chọn `posting_status` vì đã có tiền lệ `page.tsx:1119`).
74. **MỚI (TASK-080 đợt 2B) — DỮ LIỆU "ĐÃ XÁC NHẬN" NHƯNG THIẾU NGƯỜI XÁC NHẬN + ÁNH XẠ VAI TRÒ→ĐƠN VỊ CHƯA PORT SANG MySQL.** (a) `goods_receipts`: **12** phiếu `bch_confirmation_status='confirmed'`, trong đó **10** phiếu `bch_confirmed_by IS NULL` (10 phiếu TASK-081 dựng lại) và **2** phiếu trỏ tới `USR_2f435847-…` — **ID KHÔNG tồn tại trong `users`** (tham chiếu treo do các lượt smoke trước) ⇒ Java `LEFT JOIN users` ra NULL ⇒ UI in chữ dự phòng `"BCH công trường"` dù trạng thái là "đã xác nhận". **✅ ĐÃ VÁ:** gán người xác nhận THẬT `cha.ht` (*Chỉ huy trưởng A*, vai trò `cht`, Ban chỉ huy công trường — đúng cột "BCH XÁC NHẬN") + mốc `bch_confirmed_at = received_at` (khớp cách sản phẩm ghi: bản ghi thật `GRN-…-0005` lệch 0,237 giây) ⇒ **thiếu người 10 → 0 · treo 2 → 0**. (b) `role_catalog.default_organization_unit_id` NULL **16/16 trên MySQL** dù quy tắc ánh xạ **đã có sẵn** ở `drizzle/0045:80-90` — nhưng **chỉ chạy trên SQLite**, chưa bao giờ port sang MySQL ⇒ Java `LEFT JOIN organization_units` ra NULL. ⚠️ **Bẫy:** KHÔNG được chép literal `'ORG-KH'`/`'ORG-TCKT'` của 0045 vì MySQL dùng **ID HỖN HỢP** (`ORG-BGD`/`ORG-DA`/`ORG-HCPC` là chuỗi mã; `BCH`/`KH`/`TCKT`/`VNTECH`/`VNTECH-01` là `ORG_<uuid>` sau `V13__merge_duplicate_org_units`) ⇒ phải JOIN theo `organization_units.code`, nếu không **5/7 giá trị thành tham chiếu treo**. **✅ ĐÃ VÁ:** **0/16 → 15/16** (chỉ giữ NULL `team`, xem câu hỏi cho người dùng). **Tệp:** `tools/task080b-seed-real-data.sql` + sao lưu `tools/_backup-bch-roleorg-truoc-TASK080B.txt`; cổng `probe-task082-realdata` mở rộng **18 → 21** phép kiểm, **21/21 ĐẠT**.
75. **MỚI (TASK-080 đợt 2B) — TRUY NGUYÊN TRỌN VẸN TEST ĐỎ ĐÃ BIẾT `TASK-032` (chỉ báo cáo, KHÔNG tự sửa).** Test `runtime-admin-boq-regression.test.mjs` đòi `roleCatalog` có `code='thuky' && name='Thư ký Tổng giám đốc' && defaultOrganizationCode='BGD'`. Chuỗi thật: `drizzle/0029:76` tạo `thuky` với **tên DÀI** → `drizzle/0045:90` đổi thành **tên NGẮN** + gán `ORG-BGD` (**test viết theo mốc này**) → `drizzle/0079` (sau đó) **đổi ngược về tên DÀI**, và tên dài đó **đúng bằng MySQL hiện tại**. ⇒ Phần **ánh xạ đơn vị ĐÚNG** (`thuky → BGD`, có ở cả SQLite lẫn MySQL sau đợt 2B); **phần đỏ còn lại là KỲ VỌNG TEST ĐÃ CŨ**. **Không tự sửa test, không tự đổi tên vai trò** (quyết định đặt tên nghiệp vụ) ⇒ đưa vào câu hỏi cho người dùng. Chạy riêng tệp này: **26/27**.
76. **MỚI (TASK-080 đợt 2C) — LUẬT "TRƯỞNG PHÒNG" VÀ "NGƯỜI NHẬN VIỆC" CỦA JAVA SO SAI NGUỒN DỮ LIỆU (vi phạm quy tắc #9) ⇒ MỌI ACTION PHÒNG BAN BỊ CHẶN.** JS SSOT `scripts/system-route.mjs:247` so **`user.role`** (`kh_truong`/`da_truong`); Java `OpsTaskStoreAdapter:30` so **`u.department='KH'/'DA'`** — nhưng `users.department` chứa **TÊN tiếng Việt** (`Phòng Dự án`/`Phòng Kế hoạch`) ⇒ **không bao giờ khớp**. Đo thật qua cổng 9000 (Trưởng phòng Dự án `trdademo`): `{"ok":false,"error":"Chỉ Trưởng phòng hoặc Quản trị viên được giao việc thủ công."}` ⇒ chặn cả `create_work_item` · `update_work_item_status` (xác nhận Hoàn thành) · `reassign_work_item`. Cùng lớp lỗi ở `userCanReceiveDepartmentTask` (`u.department` so với mã phòng, trong khi JS `:248-255` dùng `base_role` + `user_project_scopes`), và Java **thiếu hẳn** `defaultDepartmentAssignee` (JS `:256-259`) nên khi payload không có người nhận thì ghi `""` ⇒ công việc không có người nhận. **✅ ĐÃ VÁ:** port lại đúng nguồn dữ liệu ở cả 3 chỗ + thêm `defaultDepartmentAssignee` vào cổng `OpsTaskStore`; `createWorkItem` nay chọn/kiểm người nhận theo đúng JS và **báo lỗi** khi không có người hợp lệ (thay vì ghi rỗng). **Bằng chứng:** cổng mới `tools/probe-task080c-work-items.mjs` **24/24 ĐẠT**, trong đó có phép **kiểm ngược** "giao cho người KHÁC phòng (`ksda.demo`, `base_role=engineer`) phải bị từ chối" ⇒ chứng minh luật `base_role` đã port đúng.
77. **MỚI (TASK-080 đợt 2C) — QUYỀN GHI THEO DỰ ÁN: MỌI TÀI KHOẢN NGOÀI ADMIN CHỈ CÓ `read` ⇒ CHẶN MỌI THAO TÁC GHI THEO DỰ ÁN.** Đo: `user_project_scopes.permission` = `read` cho **tất cả** tài khoản (kể cả hai Trưởng phòng), trong khi luật ghi ở **cả hai đường** đòi `{write, approve, admin}` (JS `:221` ↔ Java `AccessScopeService:40/70`). Biểu hiện thật: `create_work_item` trả `{"error":"Không có quyền tại dự án."}` **dù** người gọi có dòng phạm vi dự án. **✅ ĐÃ VÁ (dữ liệu):** cấp **`write`** — mức **tối thiểu đủ dùng** vì đã rà toàn bộ mã và **không nơi nào phân biệt `approve` với `write`** cho phạm vi dự án (mọi phép kiểm đều hỏi đúng tập 3 giá trị; quyền PHÊ DUYỆT thật do `approval_stage_catalog`/workflow quyết định) ⇒ **không** cấp `approve`/`admin` để tránh leo thang quyền. Ánh xạ: `write` cho da_truong · kh_truong · da_nv · kh_nv · ksda/engineer · cht · thu_kho/warehouse; **giữ `read`** cho accountant và director. Tệp: `tools/task080c-seed-project-write-scope.sql` + sao lưu `tools/_backup-project-scope-truoc-TASK080C.txt`. ⚠️ **CẦN NGƯỜI DÙNG XÁC NHẬN** ánh xạ này (hoàn tác được từ sao lưu).
78. **MỚI (TASK-080 đợt 2C) — ⚠️ CHƯA PORT: GIAO VIỆC XONG KHÔNG SINH THÔNG BÁO / EMAIL / SỰ KIỆN `ASSIGNED`.** JS `createDepartmentTask` (`system-route.mjs:273-277`) ghi một lượt **3 việc**: `work_items` + `work_item_events` (event **`ASSIGNED`**) rồi `queueTaskNotice` (`:261-267`) ⇒ `task_notifications` + (nếu người nhận có email) `email_outbox` với **subject/body đã có mẫu sẵn trong JS**. Java `OpsTaskStoreAdapter.insertWorkItem` **chỉ ghi `work_items`**. **Đo sau khi giao 3 việc thật:** `work_items=4` · `work_item_events=2` (chỉ `STATUS` + `REASSIGNED`, **0 event `ASSIGNED`**) · `task_notifications=0` · `email_outbox=0` — **trong khi thông điệp trả về của chính action vẫn nói "…và đã tạo thông báo cho nhân viên"** ⇒ câu thông báo đang **SAI sự thật**. Đây là **nguyên nhân trực tiếp** khiến 2 bảng `task_notifications` và `email_outbox` rỗng (2 trong 4 bảng rỗng của TASK-080 đợt 2). **Đây là việc kế tiếp, đã có chỉ dẫn port cụ thể:** thêm 2 phương thức vào `OpsTaskStore` (`insertTaskNotification`, `insertEmailOutbox`) + gọi `insertWorkItemEvent(...,"ASSIGNED",...)` và 2 lệnh insert trong `insertWorkItem` (hoặc trong use case, đúng thứ tự JS), nội dung lấy nguyên mẫu JS `:262-266`; cổng `probe-task080c` sẽ tự chuyển 2 dòng "GHI NHẬN" thành phép kiểm ĐẠT. **✅ ĐÃ VÁ 18/09/2026 (TASK-080 đợt 2D):** thêm `findUserContact` · `emailBaseUrl` · `insertTaskNotification` · `insertEmailOutbox` vào cổng `OpsTaskStore` + cài đặt trong adapter, và trong `createWorkItem` gọi `insertWorkItemEvent("ASSIGNED")` rồi helper `queueTaskNotice` (**port nguyên mẫu câu của JS**: tiêu đề `Công việc mới: …`, thân `mã · mã−tên dự án · Hạn: …`, thư `[VNTECH ERP] <mã> - <tên việc>`; khác JS **một điểm có chủ ý**: khi `email_settings.base_url` trống thì liên kết ở dạng tương đối `/?task=<id>` vì tầng use-case không có origin của request — **không bịa origin**). **Bằng chứng đo được:** `task_notifications` **0 → 2** (người nhận `nvdademo` TỰ THẤY trong phiên của chính họ, `status=SENT`) · `email_outbox` **0 → 1** (`status=queued`, đúng tiêu đề) · `work_item_events` **0 → 6** (có `ASSIGNED`) · cổng `probe-task080c` **28/28 ĐẠT**. **Phát sinh khi đo (đã xử lý):** `email_outbox` vẫn 0 vì **mọi tài khoản nhận việc đều để `email = NULL`** mà JS chỉ xếp thư khi `assignee.email` có giá trị ⇒ **đúng luật nhưng không kiểm chứng được**; đã điền email công ty theo khuôn mẫu dữ liệu thật (`<username>@vntech.vn`, xem KP #79).
79. **MỚI (TASK-080 đợt 2D) — TÀI KHOẢN THẬT THIẾU EMAIL ⇒ LUỒNG GỬI THƯ KHÔNG THỂ KIỂM CHỨNG.** Đo: **mọi** tài khoản hoạt động đều `email = NULL` (chỉ 3 tài khoản kỹ sư có email) ⇒ bước `queueTaskNotice` chạy đúng nhưng **luôn bỏ qua nhánh email** (JS `:266` `if(assignee.email)`). **✅ ĐÃ VÁ (dữ liệu):** điền `<username>@vntech.vn` theo **đúng khuôn mẫu đã có trong hệ thống** (`ksda.demo@vntech.vn`), **không ghi đè** email đã có ⇒ **12/12 tài khoản hoạt động có email, 0 NULL, 12 email khác nhau**; tệp `tools/task080d-seed-user-email.sql` + sao lưu `tools/_backup-user-email-truoc-TASK080D.txt`. ⚠️ Đây là **dữ liệu tài khoản thử** — cần người dùng xác nhận (hoàn tác được từ sao lưu).
80. **MỚI (TASK-080 đợt 2E) — ⚠️ CẦN NGƯỜI DÙNG QUYẾT ĐỊNH: `department_module_permissions.can_create = 0` Ở MỌI PHÒNG cho các module nghiệp vụ.** Đo: **8/8 đơn vị** có `can_use=1` nhưng `can_create=0` ở `production` · `capital_recovery` · `payments` · `inventory` · `boq` · `approvals` · `construction` · `delivered` … trong khi `ActionRbacRegistry` đòi **`canCreate`** cho `save_production_report` · `save_capital_recovery` · `save_contract_payment` ⇒ **không người dùng phòng ban nào tạo được** các chứng từ đó (Trưởng phòng Dự án bị **403**: *"Tài khoản chưa được quản trị viên cấp đúng quyền cho thao tác này."*). Đây là **một nguyên nhân gốc** khiến nhiều bảng nghiệp vụ trống. **KHÔNG tự cấp thêm quyền** (quyết định nghiệp vụ) — đợt 2E dùng **phiên admin** cho hợp lệ. Ma trận CHUẨN của sản phẩm (`V10:73-82`) chỉ định nghĩa cho `dept_*` và phần đó **đã đúng** (DA 13 · KH 12 · HCPC 6 · TCKT 6 module có `can_create=1`). **Câu hỏi:** có cấp `can_create`/`can_edit` cho phòng ban ở các module nghiệp vụ không, và nếu có thì phòng nào được tạo gì?
81. **MỚI (TASK-080 đợt 2E) — 🔴 DỮ LIỆU BOQ BỊ ĐẾM TRÙNG ⇒ GIÁ TRỊ HỢP ĐỒNG TRÊN MÀN THANH TOÁN GẤP ĐÔI.** Payload `boqItems` trả **16 dòng** trong khi `project_boq_items` chỉ có **8**; màn Thanh toán tính Σ(`contract_qty`×`unit_price`) nên hiện **1.346.500.000 đ** thay vì **673.250.000 đ** thật. **Nguyên nhân:** `BootstrapDataAdapter:554` nối theo **liên kết NGƯỢC** `boq_source_items.project_boq_item_id`, đồng thời một khối khác gộp các dòng nguồn **CHƯA ÁNH XẠ** vào cùng mảng (JS `:653`); dữ liệu thật có **liên kết xuôi** (`project_boq_items.source_item_id`) nhưng **cột ngược NULL cả 8 dòng** ⇒ bị coi là chưa ánh xạ ⇒ gộp lần thứ hai. Sản phẩm **có ghi** cột ngược khi ánh xạ thật (`BoqManagementUseCase:186`) ⇒ **dữ liệu vi phạm bất biến của chính sản phẩm**, không phải lỗi mã. **✅ ĐÃ VÁ (dữ liệu):** `tools/task080e-fix-boq-source-link.sql` khôi phục liên kết ngược từ liên kết xuôi (không tạo/xoá bản ghi) ⇒ payload còn **8 dòng**, giá trị hợp đồng **673.250.000 đ** đúng bằng CSDL; 2 báo cáo sản lượng đã tạo trước đó cũng được **sửa lại bằng chính action thật**. **Cổng `probe-task080e` có phép kiểm chống tái phát** ("payload boqItems KHÔNG có dòng trùng/rỗng vật tư"). ⚠️ **Hệ quả cần biết:** con số tiền hợp đồng/thanh toán trên UI **giảm một nửa** so với trước — nay mới là số ĐÚNG.
82. **MỚI (TASK-080 đợt 2E) — ⚠️ GỐC DỮ LIỆU TIỀN: `purchase_order_items.unit_price = 0` cho 13/13 dòng ⇒ `purchase_orders.total_value = 0` cho 7/7 PO.** Hệ quả: mọi số tiền suy ra từ PO đều bằng 0 (giá trị đặt hàng, giá trị nhập kho theo giá, giá trị tồn theo giá, công nợ NCC…). Vì vậy báo cáo sản lượng đợt 2E **không thể** lấy đơn giá từ PO mà dùng **đơn giá HỢP ĐỒNG trong BOQ** (dữ liệu thật) — công thức đã ghi rõ trong cổng. **Câu hỏi:** có điền đơn giá PO theo đơn giá hợp đồng của cùng mã vật tư không (đây là **quyết định tài chính**, KHÔNG tự làm)?
83. **MỚI (TASK-084) — ⚠️ BẤT THƯỜNG TIỀN: `PRJ-DEMO-01` ĐÃ THU/THANH TOÁN 1.500.000.000 đ NHƯNG GIÁ TRỊ HỢP ĐỒNG CHỈ 673.250.000 đ (223 %).** Phát hiện bởi cổng MỚI `tools/probe-money-consistency.mjs` (đối chiếu **con số UI ↔ con số CSDL** cho 5 nguồn tiền). Hai bên **khớp nhau** (`contract_payments` = 1,5 tỷ ở cả hai phía) nên **không phải lỗi đọc dữ liệu**, mà là **bất thường dữ liệu cần đối chiếu**: hoặc BOQ thiếu dòng/thiếu giá, hoặc các dòng thanh toán thuộc phạm vi khác. **Cổng CHỈ BÁO CÁO, KHÔNG tự sửa** (quyết định tài chính). **Câu hỏi:** BOQ của dự án có đầy đủ không, hay 6 dòng `contract_payments` đang ghi thừa?
84. **MỚI (TASK-084) — LỖI ĐẾM TRÙNG BOQ (KP #81) LỌT QUA MỌI CỔNG CŨ; NAY ĐÃ CÓ CỔNG ĐỐI CHIẾU SỐ TIỀN.** Lỗi đếm trùng BOQ (KP #81) lọt qua **tất cả** các cổng hiện có: `tsc`/eslint (mã hợp lệ) · cổng khoá bootstrap (khoá vẫn đủ) · `probe-column-parity` (so **TẬP CỘT** — số cột đúng, chỉ số **DÒNG** sai) · cổng ảnh 28 ảnh (không phủ màn Thanh toán). ⇒ Nay có **`tools/probe-money-consistency.mjs`: 14/14 ĐẠT** so **5 nguồn tiền** (BOQ · đơn hàng · thanh toán hợp đồng · báo cáo sản lượng · thu hồi vốn) giữa UI và CSDL, kèm **3 phép tự kiểm soát** (có dự án thật để so · không dòng BOQ trùng/rỗng vật tư · tiền thực thu không vượt giá trị duyệt). Chi tiết: `TASK-084.md`.
85. **MỚI (TASK-085) — CỔNG CHỐNG NHÂN DÒNG: bất biến "payload ≤ CSDL" phủ 13 khoá danh sách.** Lỗi đếm trùng BOQ (KP #81) là **một biểu hiện** của lớp lỗi *đường ĐỌC nhân dòng*; nay bất biến được tổng quát hoá: với mỗi khoá danh sách theo dự án, **số DÒNG payload ≤ số DÒNG CSDL** (payload được phép ÍT hơn do lọc quyền/phạm vi, **không được NHIỀU hơn**). Cổng `tools/probe-row-duplication.mjs` **14/14 ĐẠT · 0 HỎNG** với **11/13 khoá có dữ liệu** (`boqItems` 8≤8 · `purchaseOrders` 7≤7 · `receipts` 15≤16 · `issues` 1≤1 · `returns` 1≤1 · `constructionDailyLogs` 1≤1 · `productionReports` 2≤2 · `capitalRecoveryRecords` 1≤1 · `contractPayments` 2≤2 · `workItems` 7≤7 · `teams` 1≤1; bỏ qua **có ghi rõ** `stockCounts` · `stockReconciliations` vì chưa có dữ liệu). **Tự kiểm soát:** ≥ 5 khoá phải có dữ liệu thật (lượt này 11/13) và **câu SQL nào lỗi thì ghi nhận KHÔNG kết luận** chứ không tính ĐẠT. Chi tiết: `TASK-084.md` mục 5.
86. **MỚI (18/09) — CỔNG ẢNH TĂNG TỪ 10/28 LÊN 19/28 ẢNH LỆCH SAU KHI ĐỔI DỮ LIỆU; ẢNH CHUẨN VẪN CŨ.** `node tools/probe-visual-regression.mjs` → **KHÔNG ĐẠT · 19/28 ảnh lệch** (trước lượt này là **10/28**). Nguyên nhân **dữ liệu**, đúng như KP #68 đã ghi (ảnh chuẩn chưa cập nhật): lượt này đổi **giá trị BOQ (1.346.500.000 → 673.250.000)** · thêm **công việc/thông báo/hàng đợi email** · thêm **báo cáo sản lượng + hồ sơ thu hồi vốn** · điền **email cho 12 tài khoản** · đổi cột **CHỨNG CHỈ/TÌNH TRẠNG** (TASK-082). ⚠️ **Chưa cập nhật ảnh chuẩn** — theo quy tắc của dự án phải **chờ người dùng xác nhận dữ liệu mới là đúng** rồi mới chạy `probe-visual-regression.mjs --update` (cập nhật trước sẽ "hợp thức hoá" cả lỗi). Cần người dùng chốt **4 quyết định dữ liệu**: KP #79 (email tài khoản thử) · #80 (cấp `can_create`) · #82 (đơn giá PO) · #83 (chênh lệch 223 %). **Phân tích TỪNG MÀN (báo cáo đầy đủ: `tools/_bao-cao-anh-sau-doi-du-lieu-18092026.txt`) — mọi lệch đều khớp với thay đổi CÓ CHỦ ĐÍCH:**
   * `01-dashboard` **4/4** kích thước — số liệu tổng hợp đổi (công việc/thông báo mới · giá trị BOQ 673.250.000).
   * `02-project` **0/4 ✅** — không bị ảnh hưởng (đối chứng âm: cổng không báo bừa).
   * `03-work` **4/4** — lệch **rất nhỏ** (vùng 30×9 px) = nhãn đếm/badge đổi vì có công việc mới.
   * `04-team` **2/4** (desktop + laptop) — có thành viên/email thật; tablet/phone 0.
   * `05-material` **4/4** — trong đó có **8/28 ảnh lệch CÓ SẴN từ trước** (KP #68, đã chứng minh bằng `git stash`).
   * `06-warehouse` **4/4** — màn này **trước đây 0 px**, nay lệch vì phiếu nhập **đã hiển thị giá trị THẬT** ở các cột `ẢNH` · `BCH XÁC NHẬN` · `CHỨNG CHỈ` · `TÌNH TRẠNG` (chính là TASK-082 nối 5 cột thiếu) ⇒ **đây là bằng chứng cổng ảnh NHÌN THẤY thay đổi của TASK-082**.
   * `07-admin` **1/4** (desktop, rất nhỏ 0,06 %) — số liệu phân quyền/email đổi.

1. **Không tự suy đoán nghiệp vụ.** Chuỗi audit Code → DB → API → UI → Permission → Workflow → Dữ liệu; phân loại CONFIRMED/LIKELY/UNKNOWN/CONFLICT; gặp UNKNOWN ảnh hưởng DB/logic thì DỪNG và báo.
2. **Không push git** cho tới khi TOÀN BỘ công việc xong VÀ người dùng đã test thủ công (chốt 17/09/2026).
3. **Giữ ảnh chuẩn** `tools/baseline/` (28 PNG) và `tools/_tools/_diff` trong kho mã.
4. **Không viết lại mã đang chạy** khi không cần thiết; ưu tiên tái sử dụng.
5. **Mọi lớp CSS của thư viện dùng chung phải có tiền tố** `vt-`.
6. **Cổng ảnh dùng cơ chế chống lỗi giả**: chụp lại khi lệch, chỉ kết luận LỆCH khi cả hai lần đều vượt ngưỡng.
7. **Không tự đặt chữ mới** trong giao diện — mọi chữ phải lấy nguyên văn từ markup cũ.
8. **Phân quyền so bằng mã ENGINE** (`role_catalog.base_role`), không so bằng mã vai trò chuẩn.
9. **Khi port từ JS sang Java phải port cả NGUỒN DỮ LIỆU**, không chỉ chuỗi so sánh.
10. **Không dùng bảng ánh xạ mã-chuẩn→mã-engine trong Java.** Truyền `roleBase` thật từ `role_catalog` xuống. *(Ngoại lệ có kiểm soát: `requireRole` và `isWarehouseRole` chấp nhận CẢ HAI mã để chống 403 oan — đây là chấp nhận, không phải ánh xạ.)*
11. **Mọi thay đổi tầng vai trò phải chạy `tools/probe-action-role-parity.mjs`.**
12. **Mọi thay đổi tầng phạm vi phải chạy `tools/probe-action-scope-parity.mjs`.**
13. **Nối phạm vi theo LÔ, không ồ ạt** — mỗi lô phải biên dịch sạch và cổng phải chứng minh tiến độ.
14. **Giá trị truyền vào hàm kiểm phạm vi phải là giá trị JS dùng** (thường tra từ DB), không phải tham số thô của payload.
15. **Payload rỗng là cách đo quyền AN TOÀN** trên backend chỉ có action ghi (chốt quyền chạy trước validate) — nhưng phải **loại trừ tường minh** các action nguy hiểm (`factory_reset_*`, `rebuild_department_permissions`, `bulk_import_*`, `install_license_foundation`, `retry_email`, `reorder_*`).
16. **Thông điệp lỗi là dữ liệu chẩn đoán**: phải phân biệt **T1-module** ("chưa được quản trị viên cấp đúng quyền") · **T2-vai trò** ("không có quyền thực hiện nghiệp vụ") · **T3-phạm vi** ("không được … dự án/kho") trước khi buộc tội mã nguồn.
17. **Luôn chuẩn hoá KIỂU trước khi kết luận** — `active` trong payload là **boolean `true`**, không phải số `1`; so `=== 1` từng gây báo động giả 9/9.
18. **Port từ JS sang Java phải đối chiếu cả THỨ TỰ TOÁN HẠNG của biểu thức mặc định**, không chỉ tên cột/bảng. Bằng chứng TASK-040: `Math.max(1, x || 587)` ≠ `Math.max(1, Math.round(x))` rồi mới `if (==0)` — bản Java biến nhánh mặc định thành **mã chết**.
19. **Khi sửa một cặp GHI/ĐỌC, phải kiểm CẢ HAI CHIỀU.** TASK-039 và TASK-040 nhóm 1 đều cho thấy lỗi nằm ở **đường đọc thiếu** (khoá bootstrap không tồn tại) chứ không chỉ ở SQL ghi.
20. **Probe gọi action GHI phải tự chứng minh tính không phá hoại**: sao lưu 2 bảng ra `tools/_backup-*.sql` (đã gitignore), gửi LẠI đúng dữ liệu đang có, chỉ tạo bản ghi tạm rồi xoá, và in ra mọi thay đổi thật.
21. **Probe phải tự nói rõ giới hạn của nó.** `probe-task040-nhom1.mjs` in thẳng dòng *"script KHÔNG tự kiểm DB"* — vì "13/13 ĐẠT" mới chỉ chứng minh tầng HTTP; thiếu bước đối chiếu MySQL là chưa đủ kết luận.
22. **KHÔNG suy tên action từ tên phương thức.** Tên action là **dữ liệu giao kèo** UI ↔ API: phải tra `case "…"` trong `SystemController` hoặc `action === "…"` trong JS. (Bài học vòng 4: `settle_subcontract` không tồn tại ⇒ suýt kết luận sai rằng nhóm 5 là mã chết.)
23. **Với action GHI dữ liệu thật, chứng minh tầng SQL bằng TRANSACTION + ROLLBACK** thay vì tạo/huỷ dữ liệu thật: chạy đúng câu lệnh, đọc kết quả, `ROLLBACK`, rồi khẳng định dữ liệu **nguyên trạng** (`tools/probe-task040-nhom45.sql`). Vẫn phải nói rõ đây **không** phải phép kiểm end-to-end.
24. **Cổng đối chiếu với CHÍNH các nhánh `case` đang phục vụ request** (`probe-action-coverage-controller.mjs`), không chỉ so với danh mục/thanh ghi RBAC — vì một action có thể có SQL + có trong thanh ghi mà **thiếu `case`** ⇒ không bao giờ chạy.
25. **Mọi cổng mới phải có ĐỐI CHỨNG DƯƠNG trên một lỗi đã biết.** Công cụ luôn báo "sạch" thì vô dụng: `probe-increment-drift.mjs` được kiểm bằng tệp chứa nguyên văn câu lệnh cũ (`--extra-file`).

## USER CONFIRMATION REQUIRED (11 câu hỏi)

1. **TASK-029** — 6 action (`save_team_subcontract`, `save_team_production`, `approve_team_production`, `save_team_payment`, `settle_team_subcontract`, `create_project_team`): JS cho phép theo vai trò, Java bắt buộc module `teams` (**0 dòng quyền**) nên **chỉ admin làm được**. Chọn **(A)** khôi phục đúng JS, hay **(B)** giữ Java + nạp đủ dữ liệu quyền module?
2. **Dữ liệu `user_module_permissions`** (484/732; 5 module 0 dòng: `boq`, `stocktake`, `inventory`, `teams`, `warehouse_issue`) — (a) chạy lại cơ chế cấp mặc định theo phòng ban, (b) cấu hình thủ công, hay (c) giữ nguyên?
3. **TASK-024** — `isCompanyLeadership`: giữ `{director, accountant}` hay theo JS (7 mã HOẶC `base_role='director'`)?
4. **TASK-031** — cây dự án trong nhóm **QUẢN LÝ DỰ ÁN** hiện **không hiển thị** danh sách dự án + cây workspace ở **cả desktop và mobile**. Chủ ý (giữ nguyên + cập nhật test) hay tắt nhầm (bật lại)? Hay chỉ muốn một nav có cây?
5. **TASK-032** — (a) có khôi phục `role_catalog.default_organization_unit_id` cho 16 vai trò không (xin xác nhận bảng ánh xạ)? (b) tên vai trò `thuky` chuẩn là **"Thư ký Tổng giám đốc"** (theo test) hay **"Thư ký Tổng giám đốc / Trưởng phòng Hành chính Pháp chế"** (theo dữ liệu đang chạy)?
6. **TASK-034** — cho phép **tái lập dấu vân tay nguồn** để `npm run build` chạy được? Việc này còn phải cập nhật **dòng `vntech_product_identity` trong DB** (vì `local-runtime.mjs:177` ném lỗi nếu lệch) ⇒ **có tác động dữ liệu**. Nếu không làm: bạn test thủ công trên **bundle cũ**.
7. **TASK-035 mục 7** — khi admin đổi cấu hình bước/phân công giữa chừng, hành vi mong muốn: **(A)** giữ bản cũ cho hồ sơ đang chạy (cần version pinning — **thay đổi kiến trúc**) hay **(B)** áp dụng ngay (**đúng hiện tại**)? Và có cần **chặn tắt/xoá** bước đang có hồ sơ chờ (tránh kẹt hồ sơ)?
8. **TASK-036 mục 7** — `required_permission` (quyền cần để làm người duyệt) và `allow_skip_level` (vượt cấp) hiện **không được thi hành ở đâu**: có cần **thi hành** không, hay **gỡ** khỏi lược đồ/payload để tránh bẫy cấu hình về sau?
9. **TASK-037 mục 5** — 3 action quản trị workflow chỉ cần **đăng nhập** + quyền module `admin` (hiện **0 dòng**): **(A)** thêm chặn **vai trò admin** cho nhất quán, hay **(B)** giữ nguyên (coi quản trị workflow là một quyền module — đúng thiết kế)?
10. **Số SLA thật của nghiệp vụ** — lỗi port TASK-040 đã ghi đè `po_sla_hours=1` và `bch_confirmation_sla_hours=1` vào DB thật; probe đã khôi phục về **mặc định do mã JS quy định là 24 giờ (PO)** và **8 giờ (BCH)**. Đây là *mặc định của mã*, **không phải con số nghiệp vụ do bạn công bố**. Hỏi: **24h/8h có đúng không**, hay SLA thật khác để tôi ghi lại đúng? (Nếu khác, sửa qua giao diện Quản trị → Cấu hình email.)
11. **TASK-040 nhóm 6 — hệ license lệch CẤU TRÚC** giữa Java và JS. UI gửi `{licenseEnvelope}` (chuỗi JSON) và JS **xác minh chữ ký số** (`verifyLicenseEnvelope` với public key) rồi ghi theo `claims`; Java lại nhận `licenseKey/companyName/edition` và ghi **6 cột không tồn tại** trong `vntech_license_installations`. Không có cột nào để ánh xạ `edition`/`license_key` ⇒ ánh xạ sẽ là **bịa nghiệp vụ**. Hỏi: **(A)** port đầy đủ hệ license + xác minh chữ ký (thuộc phần bảo mật bạn đã yêu cầu tạm hoãn), hay **(B)** giữ nguyên trạng thái hỏng (HTTP 500) và ghi vào roadmap, hay **(C)** tạm **gỡ** 2 action này khỏi danh mục để người dùng không bấm vào chỗ hỏng?

## QUYẾT ĐỊNH DỮ LIỆU CẦN NGƯỜI DÙNG CHỐT (bổ sung 17/09 — gom về MỘT chỗ để trả lời 1 lần)

| # | Việc | Bằng chứng | Đề xuất của tôi | Nếu anh chọn |
|---|---|---|---|---|
| D1 | **55 dòng mồ côi**: 15 `approvals` + 32 `procurement_allocations` + 3 `stock_issues.team_id` + 3 `project_boq_items.material_id` + 1 `material_request_items` + 1 `purchase_order_items` | `MR_46cee316` là gốc chung; 3 phiếu bị xoá 14/09 (seed/demo) | **(3) Vá gốc trước rồi dọn** — sửa `delete_request` ở **cả JS lẫn Java** để xoá thêm `procurement_allocations` + `custom_field_values`, sau đó mới dọn rác | (1) dọn ngay · (2) giữ nguyên · (3) vá gốc trước *(khuyến nghị)* |
| D2 | **`materials.system` lệch 5/14 mã** | `DIEN-DAY-CAD-001`/`CTN-ONG-NHUA-001` = `KHAC` (khớp cơ chế lỗi #41) · 3 mã còn lại nghi dữ liệu mẫu hoán vị | **Anh mở Danh mục bấm Lưu** cho 2 mã đầu (tự suy lại đúng từ nhóm — đã vá ở #63); 3 mã kia **giữ nguyên** chờ anh xác nhận là dữ liệu mẫu | (1) tự bấm Lưu · (2) cho tôi sửa SQL · (3) giữ nguyên |
| D3 | **11 câu hỏi cũ** (mục trên): số SLA thật 24h/8h · hệ license `vntech_license_*` (nhóm 6) · nhóm quyền nghiệp vụ có tham gia kiểm quyền · backup/PITR MySQL · spec nghiệp vụ MEP · mã vai trò cũ ở mảng sản lượng … | xem mục USER CONFIRMATION | trả lời theo thứ tự ưu tiên: **license (#11)** và **SLA thật (#10)** đang chặn 2 hạng mục | — |
| D4 | **Git**: 76 commit chưa push (trước lượt này) | theo yêu cầu 17/09 "không push tới khi tôi test thủ công xong" | giữ nguyên, **không push** | (1) tiếp tục giữ · (2) cho phép push |
| D5 | ⚠️ **MỤC NÀY NAY LÀ MỤC CHẶN** — **5 dòng `user_project_scopes` MỒ CÔI** (known issue #51) + **Owner bước 2 của PRJ-DEMO-01 (`thukydemo`) KHÔNG có phạm vi dự án** ⇒ sau #92 **không lập được phiếu đề nghị mua cho PRJ-DEMO-01** (400 đúng như JS) | `LEFT JOIN projects` → `p.id IS NULL` cho **5** dòng; probe TASK-049 đo trực tiếp: `create_request` → **400** *"Owner Thư ký TGĐ D chưa được phân quyền dự án này."* | **(1) Dọn 5 dòng rác + cấp 1 dòng phạm vi `read` cho `thukydemo` ở PRJ-DEMO-01** (SQL sẵn trong `TASK-049.md` mục 3) — hoặc anh tự làm qua giao diện *Quản trị → Phân quyền người dùng* | (1) cho tôi chạy SQL · (2) anh tự gán qua giao diện · (3) xoá assignment bước 2 để dùng Owner khác |
| D6 | **`approval_mode_snapshot` ĐÓNG BĂNG chế độ duyệt** (known issue #55 / TASK-055) ⇒ bước 5 `all_roles` (`da_truong,kh_truong`) **chỉ cần 1 vai trò**; sửa sẽ **lệch khỏi JS** | **21/21** dòng bước `all_roles` có snapshot `single`; JS `:976` ghi `assignedOwner ? "single" : …`; JS `:1080` ưu tiên snapshot | **(C) Sửa cho phiếu MỚI** (ghi snapshot đúng luật) — giữ phiếu cũ theo luật cũ, **7 phiếu đang chờ ở bước 5** không bị ảnh hưởng; rồi chuyển (B) sau | (A) giữ nguyên cho khớp JS · (B) sửa cả hai phía ngay (bước 5 cần đủ 2 vai trò) · (C) chỉ sửa cho phiếu mới *(khuyến nghị)* |
| D7 | **`merge_material_master`**: JS **XOÁ** mã nguồn (`:2670`), Java chỉ `active=0` + đổi mã `<code>_X` | `MaterialCatalogStoreAdapter:420-422` vs JS `:2670` | **Giữ hành vi LƯU TRỮ của Java** (an toàn hơn: còn dấu vết để đối chiếu) và **ghi rõ đây là khác JS có chủ ý**; nếu anh muốn khớp JS thì port theo JS | (1) giữ lưu trữ *(khuyến nghị)* · (2) port theo JS (xoá hẳn) |

## BOOTSTRAP — CỔNG QUÉT KHOÁ ĐỌC (mới 17/09, `tools/probe-bootstrap-keys.mjs`)

Lớp lỗi *"đường ĐỌC thiếu khoá"* đã bắt được **8 lần thủ công**; nay có cổng quét một lượt:
đối chiếu **92 khoá `data.*` mà UI đọc** với **127 khoá Java khai trong 26 tệp adapter** (trước lượt vá: 123).

| Kết quả | Nội dung |
|---|---|
| **TRẠNG THÁI HIỆN TẠI (sau #85)** | Cổng tĩnh còn **ĐÚNG 1 khoá**: `user` = **DƯƠNG TÍNH GIẢ đã kiểm** (`SystemController.java:167` có `data.put("user", userMap)` — lớp web, ngoài phạm vi cổng quét). **4 khoá dữ liệu đã được port xong và kiểm chứng lúc chạy 87/87** — xem `TASK-050.md`. |
| **UI đọc mà không thấy Java trả (trước #85: 5 khoá)** | `centralInventory` · `centralReturns` · `companyAvailability` · `constructionDailyLogItems` · `user` |
| `user` | **DƯƠNG TÍNH GIẢ — đã kiểm:** `SystemController.java:167` có `data.put("user", userMap)` (lớp web, ngoài phạm vi cổng quét) ✓ |
| **4 khoá dữ liệu còn lại — CẦN RÀ TỪNG KHOÁ** | **`centralReturns` — ĐÃ XÁC NHẬN THIẾU (17/09):** quét toàn bộ tầng persistence: mọi câu liên quan `central_returns` chỉ là **đếm cho close-check** (`ProjectAdminStoreAdapter:90` → `out.put("open_central_return", COUNT…)`), **tra 1 phiếu theo id** (`WarehouseStockStoreAdapter:501`), **đường xoá/purge** và **archive/export** — **KHÔNG có truy vấn DANH SÁCH nào được đưa vào bootstrap** ⇒ màn **"Trả hàng về Kho Tổng"** sẽ **rỗng danh sách sau cutover** dù action ghi đã port. **`constructionDailyLogItems` — ĐÃ XÁC NHẬN THIẾU (17/09):** Java **CÓ** trả `constructionDailyLogs` (`BootstrapDataAdapter:1018`, `FROM construction_daily_logs`) nhưng **KHÔNG trả** `constructionDailyLogItems` — không tồn tại câu `FROM construction_daily_log_items` nào trong tầng persistence; UI khai cả hai khoá (`app/page.tsx:30`) và dùng `itemsByLog()` (`:2530`) để render **các dòng công việc của nhật ký thi công** ⇒ sau cutover nhật ký **hiện dòng đầu nhưng mất toàn bộ dòng công việc**. **Cần kiểm thêm phía JS** để phân biệt *lỗi port* hay *lỗ hổng chung của cả hai bản*. **`centralInventory` · `companyAvailability` — CHƯA xác nhận** |
| **Giới hạn của cổng (ghi rõ)** | so khớp **tên chuỗi**, không phân tích kiểu; không thấy khoá do lớp khác (web/JPA) trả; không kiểm khoá lồng trong object con. **Mỗi khoá phải mở mã xác nhận trước khi kết luận.** |
| **KẾT LUẬN CUỐI — CẢ 4 KHOÁ ĐỀU LÀ LỖI PORT CỦA JAVA (17/09)** | **JS trả ĐỦ CẢ 4, Java KHÔNG trả khoá nào** (`grep` toàn bộ `*.java`: **0 lần** xuất hiện `centralInventory`/`companyAvailability`): `centralInventory` ← JS `:621` (CTE trên `stock_movements` của kho `WH-CENTRAL`) · `centralReturns` ← JS `:727` · `constructionDailyLogItems` ← JS `:659` (JOIN `construction_daily_log_items` → `construction_daily_logs` theo dự án) · `companyAvailability` ← JS `:712` (CTE tồn − giữ chỗ theo từng kho). **Hệ quả sau cutover (đã kiểm chứng hai phía):** màn **Tồn Kho Tổng**, **Trả hàng về Kho Tổng**, **Khả dụng toàn công ty** và **dòng công việc của Nhật ký thi công** sẽ **RỖNG** — dù các action GHI đã port. **Lưu ý khi port:** JS còn **xoá trắng các khoá này theo vai trò/module** (`:622`, `:732`, `:733`) ⇒ phải port **cả truy vấn ĐỌC lẫn bộ lọc theo quyền**, nếu chỉ port truy vấn là rò rỉ dữ liệu kho cho người không có quyền. |
| Việc kế tiếp | ✅ **XONG ở #85.** Bước tiếp của lớp lỗi này: cổng đối chiếu **tập CỘT SQL JS ↔ Java** theo từng khoá bootstrap, vì 2 ca liên tiếp đã lộ ở **mức CỘT** (TASK-053: `constructionDailyLogs` thiếu 2 cột; known issue #52: `transferOrders` thiếu 7 trường + thiếu lọc theo kho) — và **lớp lỗi mới "khoá CÓ khai nhưng null/rỗng theo vai trò"** (known issue #50) mà cổng theo TÊN khoá **không** bắt được. |

## CURRENT TODO

* [x] **TASK-081 DONE (chờ commit)** — **vá 2 ô mất màu**
  ↳ **Kiểm chứng cuối qua proxy `:9000`:** chứng từ **từng mồ côi** và chứng từ **vừa dựng lại** đều tải ảnh **HTTP 200 · `image/png` · chữ ký `89 50 4e 47`**. **Lỗi thứ hai của tôi (tự phát hiện):** tách tên tệp ở **dấu `-` đầu tiên** ⇒ `attId` cụt + tên lẫn uuid; đã sửa **10 dòng** theo **mẫu `ATT_<uuid>`** và **vá cả script nguồn**. (`cellClassName` cho `DataTable`) + **DỰNG LẠI dữ liệu cha mồ côi** (PO **6 → 7** · phiếu nhập **6 → 16** · `attachments` **1 → 11** · mồ côi **0**) ⇒ ảnh từng 403 nay tải được **200/PNG**. Chi tiết: `TASK-081.md`
* [~] **TASK-080 IN PROGRESS — chỉ thị "không hardcode, chỉ dữ liệu thật"**: **đợt 1 xong** — ma trận quyền phòng ban **51 → 480** (mọi đơn vị nghiệp vụ phủ **60/60** module; trước đó **22 module có 0 dòng quyền** ⇒ ngoài admin không ai mở được), sinh lại quyền bằng **chính action `rebuild_department_permissions`** ⇒ `user_module_permissions` **484 → 926**, **ĐỐI CHIẾU NGƯỢC: TĂNG 11 · GIẢM 0**; tổ đội **tổ trưởng + 4 thành viên thật**. Đã **sao lưu** `tools/_backup-permissions-truoc-TASK080.txt` + lưu toàn bộ SQL ở `tools/task080-seed-real-data.sql`. **Tự phát hiện & tự vá:** đồng bộ lần đầu làm tài khoản lãnh đạo (đơn vị `VNTECH`) tụt còn 15 quyền ⇒ bổ sung ma trận cho `VNTECH`/`VNTECH-01` rồi chạy lại; đối chiếu bản sao lưu cho thấy **nghi ngờ "giảm quyền" là SAI** và nay tài khoản đó có 60. Chi tiết: `TASK-080.md`
* [x] **TASK-079 DONE (chờ commit)** — **`U-15` đợt 1 + `U-02`: `DataTable` dùng thật 0 → 13 bảng** (2 lô: `WarehouseReceipt` · `Delivered` · `Dashboard` ×2 · `ProjectManagement` ×2 · `ProjectProgress` · `WorkCenter` ×2 · `TaskTable` · `TeamManagement` · `MaterialCatalogManager` · `Inventory`). **Đã mở rộng `DataTable.render` nhận thêm `index`** (cần cho cột STT — tương thích ngược). **Kiểm chứng:** `tsc` **exit 0** · eslint **0 error/74 warning** (= mốc nền). 🔴 **HAI LỖI CỦA CHÍNH EM trong đợt này (ghi lại):** (1) trích `key={…}` bằng `indexOf("}")` nên **cắt sai template literal** `` `${a}-${b}` `` ⇒ sinh mã hỏng, đã sửa bằng **cân ngoặc**; (2) phép kiểm dùng `\bindex\b` trong khi biến chỉ số thật tên `i` ⇒ lại hỏng, đã sửa thành **kiểm theo đúng tên biến**; và (3) **vi phạm quy tắc đã ghi trong sổ: dùng `Set-Content` của PowerShell ⇒ tệp script thành MOJIBAKE** (`"Chưa có dữ liệu."` hỏng) — đã viết lại bằng công cụ file. Cả 3 lần tệp `page.tsx` đều được **hoàn tác về commit sạch** rồi chạy lại, không để lại mã hỏng. Chi tiết: `TASK-079.md`
* [x] **TASK-078 DONE (chờ commit)** — **`U-09` HOÀN TẤT**: chuyển nốt **5 khối** (`FormFieldConfigPanel` · `UserProfilePanel` ×2 · `WorkflowModal` ×2 với bộ lọc vào `extra` · `Admin` "DANH SÁCH DỰ ÁN") ⇒ `<ListToolbar>` **19 → 25 lần**, toolbar tự viết **10 → 4 chỗ** và **4 chỗ đó là header KHỐI CHI TIẾT (không phải danh sách) + 1 BOQ có CSS riêng** ⇒ **0 toolbar DANH SÁCH tự viết**. Đã **mở rộng `ListToolbar.title/note` sang `ReactNode`** (cần cho nhãn động, tương thích ngược). **Kiểm chứng:** `tsc` exit 0 · eslint 0 error/73 warning · cổng ảnh 8/28 không tăng (07-admin ĐẠT). **Tiến độ 30 → 31/110 = 28,2 %**. Chi tiết: `TASK-078.md`
* [x] **TASK-077 DONE (chờ commit)** — **`U-09` đợt mới: 6 màn sang `ListToolbar` chuẩn §5** (`Receiving` · `Delivered` · `BusinessRoleGroupManager` · `UiDisplaySettingsManager` · `TrustLockAdmin` · `Payments`). Tiêu đề dùng **nhãn menu có sẵn** (`Kế hoạch giao hàng` · `Đơn hàng đã giao`) hoặc **chữ cũ nguyên văn**; câu đếm cũ giữ qua `count`/`unit`/`note`. **Đo:** toolbar tự viết **16 → 10 chỗ**, `<ListToolbar>` **13 → 19 lần**, `tsc` **exit 0**, eslint **0 error/73 warning**. 🔴 **Suýt hỏng (ghi lại):** bộ chuyển đổi đầu tiên phát `title=` + ngoặc nhọn chứa VĂN BẢN THÔ (thiếu dấu nháy kép) ⇒ **cú pháp JSX sai mà script ĐÃ ghi tệp**; phát hiện khi **đọc lại đầu ra của chính mình**, đã vá **7 chỗ** và lấy **`tsc` làm trọng tài**. Hậu kiểm thô đầu tiên của tôi còn cho **116 dương tính giả** vì `title={biến}` là HỢP LỆ. Chi tiết: `TASK-077.md`
* [x] **TASK-076 DONE (chờ commit)** — **`U-17` + `U-07`: hai dải tự viết cuối cùng → `ActivityTimeline`** ("Lịch sử giao nhận" + "Tiến trình mua và giao hàng"). Chữ cũ giữ nguyên, **màu giữ được** bằng `tone` (chấm màu), số bước + **tô đỏ khi quá hạn** giữ trong `detail`. **Kiểm chứng:** `tsc` exit 0 · cổng áp dụng **`ActivityTimeline` 0 → 2** và **0 chỗ dải tự viết** · eslint 0 error/73 warning · hậu kiểm 6/6. **Tiến độ 28 → 30/110 = 27,3 %**. Chi tiết: `TASK-076.md`
* [x] **TASK-075 DONE (chờ commit)** — **MASTER TASK §8.3 (`P-06`): ảnh/hồ sơ vật tư đặc thù xem được, không tràn khung**. `AttachmentPanel` trước đây chỉ có **chip chữ** `ẢNH`/`TỆP` + link tải (không thấy ảnh) ⇒ nay có **dải ảnh xem trước** + **ô ảnh thu nhỏ 42px**, ảnh **chặn cứng** `height:118px`/`object-fit:cover`, tên tệp `overflow-wrap:anywhere`. **Cổng mới 22/22 ĐẠT** (`probe-task075-attachments.mjs`, đo trên Java :18081 + **4 đối chứng** + fixture dọn về mức nền). **Bài học:** `app/globals.css` **BỊ ĐÓNG BĂNG** (master-baseline-gate: cấm append sau mốc END + hạn mức 400.653 B; `!important` đang **sát trần 4950**) ⇒ CSS mới **phải** vào `app/styles/canonical.css`. **Tiến độ 27 → 28/110 = 25,5 %**. Chi tiết: `TASK-075.md`
* [x] **TASK-074 DONE (chờ commit)** — **MASTER TASK §8.2 (`P-05`): "Tổng hợp giao nhận" ra MODAL RIÊNG**. Bảng **13 cột / 1433 ký tự** trước đây **ép trong layout chi tiết** nay **chuyển NGUYÊN VĂN** vào `EntityDetailModal` (`U-01`), mở bằng nút `◉ Tổng hợp giao nhận (n dòng)`; **cảnh báo CHẶN DUYỆT giữ nguyên trong drawer** (không giấu vào modal). Đặt modal **ngoài `drawer-body`** vì `.overlay` có `backdrop-filter` **tạo containing block cho `position:fixed`** ⇒ tránh lỗi vượt viewport (§5/U-10). **Kiểm chứng:** `tsc` **exit 0** · cổng áp dụng `EntityDetailModal` **0 → 1 lần** · eslint **0 error/72 warning** (≤ nền 74) · hậu kiểm script **3/3**. **Tiến độ 25 → 27/110 = 24,5 %** (`P-05` + `U-01`). **Chưa kiểm được lúc chạy** (Known Problem #68). Chi tiết: `TASK-074.md`
* [x] **TASK-073 DONE (chờ commit)** — **cổng cho `teamMembers`** (khoá **Java-only chưa từng được đo**): cắm **fixture 2 dòng** (1 người dùng thật + 1 **user_id KHÔNG tồn tại**), đối chiếu API ↔ MySQL bằng **câu viết KHÁC DẠNG** ⇒ **21/21 ĐẠT**. Kèm **phát hiện**: JS tham chiếu **không hề đọc `team_members`** (`grep` = 0) ⇒ **Java-only trong `if (admin)`**, không có bản JS để so parity. **Lượt đầu 17/17 nhưng CHƯA TIN** ⇒ theo bài học #25 đã thêm **4 ĐỐI CHỨNG** chứng minh cổng **báo được HỎNG** (giống⇒KHỚP · khác⇒LỆCH · `null` vs chuỗi `"NULL"`⇒LỆCH · **cố ý so SAI CẶP CỘT**⇒LỆCH). **Giới hạn:** dữ liệu vẫn 0 dòng ⇒ màn Tổ đội trống **vì DỮ LIỆU**, không phải mã; chưa đo đường GHI. Chi tiết: `TASK-073.md`
* [x] **TASK-072 DONE (chờ commit #115) — MASTER TASK §2.1 (`P-04`): ÁP DỤNG THẬT `ApprovalTimeline` VÀO CHI TIẾT PHIẾU (§8.1)**. Trước khi sửa: component **đã tồn tại và ĐÃ ĐƯỢC IMPORT nhưng CHƯA HỀ render** (cổng áp dụng ghi **0 lần**) và màn chi tiết phiếu tự dựng `<div className="timeline">` trong **1 dòng 1467 ký tự**, phòng ban phải **dò `staffDirectory`** dù cột `approvals.department` đã có sẵn. Nay ánh xạ **đủ 6 thông tin §8.1**; **4 cổng xanh** (áp dụng **0 → 1** · `tsc` exit 0 · UI `:8787` + proxy `:9000` HTTP 200 · cổng ảnh **8/28 lệch ĐÃ CHỨNG MINH CÓ SẴN** bằng đối chứng `git stash`). Ledger: `P-04` → DONE · `U-06` → DONE/AP-DUNG 1 · `U-05` AP-DUNG 2 → **90** · `U-17` còn **2** dải tự viết ⇒ **23 → 25/110 = 22,7 %**. Còn lại: xem **Known Problems #68/#69**. Chi tiết: `TASK-072.md`
* [x] **TASK-071 DONE (#114)** — đo **7 TRƯỜNG ĐẶT ALIAS KHÁC TÊN CỘT NGUỒN** mà cổng TASK-070 không đối chiếu được: **9/9 ĐẠT**. **Đối chứng dựng sẵn (4 ca) bắt lỗi bộ giải mã ngay lượt chạy đầu** — ca alias có **BACKTICK** (`` level_rank AS `rank` ``) cho sai cột vì bản đầu để nguyên phần `AS <alias>` khi trích cột ⇒ **nếu không có đối chứng thì cổng đã cho kết quả SAI**. Không tìm thấy lỗi *trường alias luôn rỗng* (**kết quả ÂM có kiểm chứng**). Vẫn **chưa đo được**: `teamMembers` (bảng 0 dòng) và nhánh **DỰ PHÒNG** của `COALESCE`. Chi tiết: `TASK-071.md`
* [x] **TASK-070 DONE (#113)** — **cổng ĐỘ PHỦ TRƯỜNG** cho 10 khoá Java-only: **8/8 ĐẠT · 2 phép đo KHÔNG thực hiện được** (nói rõ, **không** tính ĐẠT). **Không tìm thấy lỗi trường-luôn-rỗng.** Bẫy đã gặp và ghi lại: vị từ `cột <> ''` làm MySQL 8 báo `ERROR 1525 (HY000): Incorrect DATETIME value: ''` với cột `DATETIME` ⇒ phải đọc `DATA_TYPE` từ `information_schema` rồi mới thêm điều kiện chuỗi. Chi tiết: `TASK-070.md`
* [x] **TASK-069 DONE (#112)** — **lỗi thứ 10 của lớp "đường ĐỌC thiếu khoá": `emailOutbox`**. Cổng mới `tools/probe-task069-appdata-contract.mjs` đọc **khai báo KIỂU `AppData`** (nguồn sự thật mạnh hơn dạng viết `result.data?.<khoá>`) ⇒ **95 khoá × 6 tài khoản THẬT**; lượt chạy đầu **0/6 ĐẠT** (thiếu `emailOutbox` cho **cả 6**, kể cả admin — khoá BẮT BUỘC của UI mà **không adapter Java nào** khai ⇒ màn Hộp thư gửi luôn rỗng). **Đã port nguyên văn JS `:724`** (admin: 11 cột + `requestNo`, `LIMIT 100`; non-admin `[]`), cổng SQL chạy trước build, sau vá **6/6 ĐẠT**. Rà 11 khoá Java-only: **10/11 khớp số dòng** với bảng nguồn; `boqMappingCandidates` = **payload chết**, ghi lại không xoá. Chi tiết: `TASK-069.md`
* [x] **TASK-068 DONE (#111)** — **quyết định** nhật ký ghi trùng: đo được dòng FILTER **có IP + module** còn dòng use-case **KHÔNG có IP** ⇒ hai dòng **BỔ SUNG** cho nhau ⇒ **GIỮ CẢ HAI, không đổi mã**; hướng cải thiện đúng (cho `AuditLogPort.log` nhận ngữ cảnh request) đã ghi lại. Chi tiết: `TASK-068.md`
* [x] **TASK-067 DONE (#110)** — cổng **BẪY THỨ TỰ** `probe-put-order.mjs` (0 vi phạm · đối chứng **3/3**) · **BÁC BỎ báo động giả "128 action thiếu audit"** bằng dữ liệu (`audit_logs` 825 dòng/39 action; `decide_approval` 173/173, `save_email_settings` 6/6 — 100% do lớp FILTER ghi) · xác minh lại **TASK-025 ĐÃ XONG từ #34** ⇒ **HAI HẠNG MỤC TRONG TODO CỦA TÔI ĐÃ CŨ/SAI**. Chi tiết: `TASK-067.md`
* [x] **TASK-066 DONE (#109)** — cổng **HỢP ĐỒNG KHOÁ theo vai trò** (nguồn: khối chuẩn hoá của chính UI) + vá 3 lỗi thật, **đóng Known Problems #50**: `engineRoleProfiles` = **null cho MỌI tài khoản** · `businessScopes`/`businessRoleGroups` **mất hẳn với 5/5 vai trò thường** · `businessRoleGroups[].scopeIds/scopes` **0/11 dòng** ⇒ **1/17 → 11/11 ĐẠT**. Chi tiết: `TASK-066.md`
* [x] **TASK-065 DONE (#108)** — `canEditCentral ≠ admin`: phép kiểm **module** đặt ở **use-case** (`BootstrapUseCase` nhận `ModulePermissionStore`) truyền xuống qua `Context.canEditCentral`; **kèm sửa chỗ xoá-trắng vô điều kiện** sẽ xoá oan dữ liệu. Cổng `probe-task065-caneditcentral.mjs` **10/10** có **ĐỐI CHỨNG ÂM** (cấp quyền 14 dòng ↔ thu hồi 0 dòng). Chi tiết: `TASK-065.md`
* [x] **TASK-050 DONE (#85)** — **4 khoá bootstrap thiếu** (`centralInventory` · `centralReturns` · `constructionDailyLogItems` · `companyAvailability`) **+ PHÁT HIỆN LỚN HƠN ĐẶC TẢ: Java thiếu HẲN khối xoá-trắng-theo-module** (`system-route.mjs:728-737`) **và bộ lọc theo vai trò kho-site** (`:622`) ⇒ đã port cả hai, **kèm nhánh thứ ba `:684`** (`isCompanyLeadership`) để không xoá trắng oan tài khoản Ban giám đốc. **Cổng mới `tools/probe-task050-bootstrap.mjs`: 87/87 ĐẠT** (số dòng ↔ truy vấn MySQL **độc lập**, shape cột ↔ alias chép từ JS, 3 tài khoản thật, **đối chứng dương**; 10 phép đo không thực hiện được vì dữ liệu rỗng — **nói rõ, không tính ĐẠT**). Cổng tĩnh nay còn **1 khoá** (`user` = dương tính giả). Chi tiết: `TASK-050.md`
* [x] **TASK-051 DONE (#85)** — `data.user` thiếu `mustChangePassword` + `warehouseScopeKind` ⇒ **modal bắt buộc đổi mật khẩu không bao giờ mở** (đó là **cửa DUY NHẤT** UI đọc cờ này). **Đo trên dữ liệu hiện tại: 0 tài khoản bị ảnh hưởng** ⇒ lỗi **tiềm ẩn**, ghi trung thực. `TASK-051.md`
* [x] **TASK-052 DONE (#85)** — **LỚP LỖI #4**: `tinyint(1)` → JDBC trả **Boolean** ⇒ `modulePermissions` của admin **luôn rỗng** (JS: 61 dòng) — lỗi **có từ trước**, bị UI che (`page.tsx:548`). Vá bằng helper theo đúng quy ước 5 use-case khác trong kho. `TASK-052.md`
* [x] **TASK-053 DONE (#85)** — `constructionDailyLogs` **thiếu 2 CỘT** (`itemCount`/`completedQty`) ⇒ cột "Khối lượng" của Nhật ký thi công **luôn 0**; probe đối chiếu **theo từng dòng**. `TASK-053.md`
* [x] **TASK-048 DONE (#87)** — **5/6 mốc nhật ký kiểm toán** luồng Phiếu đề nghị (`CREATE` · `EDIT_RETURNED` · `RESUBMIT` · `DELETE_RETURNED` · `CANCEL`) port **nguyên văn** hợp đồng JS; cổng mới `probe-task048-audit-requests.mjs` **18/18 ĐẠT** (lái đúng luồng nghiệp vụ + dọn sạch có kiểm chứng). **Sửa 2 cổng đo sai:** `probe-task043` (gọi `q1()` không tồn tại ⇒ phép kiểm audit **chưa bao giờ chạy**; nay **27/27**) và `probe-audit-coverage` (tra tên BIẾN thay vì tên LỚP ⇒ con số **152 là hiện vật**, số đúng **128**; đã thêm **đối chứng dương**). Chi tiết: `TASK-048.md`
* [x] **TASK-054 DONE (#89)** — **nhánh duyệt song song `all_roles`** port đủ **9 hành vi** JS `:1087-1108` (+ `stageDecisionRoles` & `updateApprovalComment` ở tầng store); cổng `probe-task054-all-roles.mjs` **20/20 ĐẠT** (láI thật tới bước 5, xác nhận lần 1 ⇒ hồ sơ **vẫn pending**, lần 2 ⇒ mới hoàn tất; audit `APPROVE_PARTIAL`; dọn sạch có kiểm chứng). **Kèm ĐÍNH CHÍNH** kết luận sai ở #88. Xem `TASK-054.md`
* [!] **TASK-055 · CHỜ NGƯỜI DÙNG CHỐT (D6)** — `approval_mode_snapshot` **đóng băng `single`** (21/21 dòng) ⇒ bước 5 `all_roles` **chỉ cần 1 vai trò**; sửa sẽ **lệch khỏi JS**, **7 phiếu đang chờ ở bước 5** bị ảnh hưởng. Đặc tả + 3 hướng: `TASK-055.md`
* [x] **TASK-049 DONE (#92)** — port **2 phép kiểm Owner** của bước duyệt (`JS :445-450`), cổng mới `probe-task049-owner-checks.mjs` **10/10 ĐẠT** + **ĐỐI CHỨNG DƯƠNG** (trên jar trước đó cùng lời gọi trả 200). **Hệ quả cần anh xử lý: D5 nay là mục CHẶN** — luồng lập phiếu của PRJ-DEMO-01 trả 400 cho tới khi Owner bước 2 được cấp phạm vi (`TASK-049.md` mục 3 có SQL sẵn). Xem `TASK-049.md`
* [!] **D7 (mới)** — `merge_material_master`: Java **lưu trữ** (`active=0` + mã `_X`), JS **xoá hẳn** — cần anh chốt hành vi đúng (không đưa vào lượt này vì là quy tắc nghiệp vụ)
* [x] **TASK-056 + TASK-057 DONE (#94)** — **cổng mới `tools/probe-column-parity.mjs`** đối chiếu **TẬP CỘT** SQL hai phía (30 khoá trùng tên; **bộ tách cột được ĐỐI CHỨNG DƯƠNG bằng metadata THẬT của MySQL 4/4**) ⇒ **13 khoá thiếu cột**; **đã vá 4**: `issues` (+`receivedByName`, +`installedQty`) · `returns` (+`returnedByName`) · `transferOrders` (**14 → 21 cột** + **bộ lọc theo kho `:714`**) · `productIdentity` (`id AS productId`). Probe bootstrap **93/93** (đối chiếu **theo từng dòng** với MySQL). Chi tiết: `TASK-056.md` · `TASK-057.md`
* [x] **TASK-062 · ĐÃ VÁ + KIỂM CHỨNG (chờ commit)** — `boqItems` **thiếu HẲN một nửa danh sách**: JS `:653-655` gộp `unmappedSourceRows` vào cùng mảng còn Java thì không ⇒ đo được **8/16 dòng** (đã ánh xạ 8 ⊕ chưa ánh xạ 8) trước khi vá. Nay port đủ **4 phần**: 13 cột vô hướng · 7 cột TỔNG HỢP (subquery) · 3 trường DẪN XUẤT · `customFields` + dòng nguồn chưa ánh xạ, kèm `boqSourceItems` **+9 cột**. Cổng mới `tools/probe-task062-boq.mjs`: **19/19 ĐẠT** (so giá trị 7 cột tổng hợp với **SQL viết KHÁC DẠNG** — 56 phép so khớp; kiểm bất biến 3 trường dẫn xuất; `boqSourceItems` 64 phép so theo từng dòng). Cổng tập cột: **71 khoá · 0 thiếu · 0 mất độ phủ**.
* [!] **TASK-062 vá kèm 3 ca lệch KHÔNG phải cột** (Known Problems #61): `boqImportBatches` sắp lại theo `project_id,contract_id,version_no DESC` · `boqChangeHistory` +`h.id DESC` +`LIMIT 1000` · **`workflowAssignments` chỉ admin** (trước trả cho MỌI vai trò ⇒ **rò rỉ** "ai duyệt bước nào của dự án nào"; probe xác nhận `thukydemo` nay nhận **0 dòng**).
* [!] **MỚI — LỚP LỖI #63: cổng tập cột KHÔNG bắt được `ORDER BY`/`LIMIT`/kiểu `JOIN`/`WHERE`.** Đã xác nhận 3 ca (boqImportBatches sắp sai; boqChangeHistory thiếu tiebreaker+LIMIT; workflowAssignments trả cho cả vai trò thường) và **đã vá cả 3** ở TASK-062. **Cần viết cổng `probe-clause-parity.mjs`** để biết còn bao nhiêu ca — chưa đo được hết.
* [!] **MỚI — BÀI HỌC #103: cổng mất độ phủ mà KHÔNG báo = cổng nói dối.** Khi `boqItems` chuyển sang mảng ghép, cổng **âm thầm bỏ khoá đó** nhưng vẫn in "0 thiếu cột". Nay cổng có **phần ĐỘ PHỦ** (danh sách khoá BẮT BUỘC + sàn số khoá so được) và **tự báo HỎNG** nếu mất. Cùng lượt: vá 2 DƯƠNG TÍNH GIẢ của chính cổng — backtick MySQL (`m.` `system` `)` bị coi là literal) và **trùng tên biến giữa các hàm** (5 khai báo `materials` ở 5 hàm bị hợp nhất ⇒ báo oan `active`/`materialId`/`aliasName`).
* [x] **TASK-063 · ĐÃ VÁ (chờ commit)** — **cổng `tools/probe-clause-parity.mjs`** cho lớp lệch `ORDER BY`/`LIMIT`/kiểu `JOIN` (Known Problems #61/#62). Đối chứng của chính bộ so sánh **6/6 ĐẠT**; so **71 khoá** ⇒ **2 lệch thật đã vá**: `adminMaterialCategories` (sắp lại `CASE WHEN active=1 THEN 0 ELSE 1 END,sort_order,name`) · `audits` (`LIMIT 500 → 100`). Loại 2 dương tính giả (JS `first()` = `LIMIT 1`; khoảng trắng sau dấu phẩy). Mô-đun dùng chung `tools/lib/sql-parity-extract.mjs` + **đối chiếu chéo hai bản đọc nguồn** trong cổng tập cột (tự báo HỎNG nếu trôi khác nhau).
* [x] **TASK-064 · ĐÃ VÁ (chờ commit)** — **nhánh dự phòng là BIẾN KHÁC** (JS `:694`/`:695`): Java xoá-trắng **cả ba** khoá `adminMaterial*` cho người không phải admin, nhưng JS chỉ trả `[]` cho **một** khoá. Nay `adminMaterialCategories` = `materialCategories` và `adminMaterialSubcategories` = `materialSubcategories` (đọc giá trị CUỐI sau bước xoá-trắng theo module, đúng như JS); `adminMaterials` giữ `[]`. Cổng `probe-task063-clauses.mjs` **7/7 ĐẠT** (thêm 4 phép kiểm: 2 nhánh dự phòng · `adminMaterials` = `[]` · **không rò rỉ** 6 khoá admin-gated).
* [!] **CÒN LẠI của #63** — `canEditCentral = isAdmin || canUseModule(user,"central_warehouse","canEdit")`: Java chỉ xét `admin` ⇒ vai trò có quyền `central_warehouse.canEdit` vẫn nhận danh sách **rút gọn**. Cần đưa phép kiểm module vào `BootstrapDataAdapter`.
* [!] **MỚI — LỚP MÙ #63: khoá có NHÁNH THEO VAI TRÒ, nhánh dự phòng là BIẾN KHÁC** (`canEditCentral ? A : materialCategories` · `isAdmin ? … : []`). Cổng mệnh đề chỉ so **câu ĐẦU** ⇒ không thấy.
* [!] **CÒN 13 khoá cổng KHÔNG so được** (JS không có SQL tương ứng/khoá dựng động) — rà tay, **không coi là "đã khớp"**.
* [!] **VIỆC KẾ TIẾP — TASK-058 (known issue #58): ĐÃ VÁ ở #96** ⇒ `workItems` nay có đủ 4 nhánh lọc theo phòng ban/BCH + 14 cột; probe `probe-task058-work-items.mjs` **18/18**.
* [!] **8 khoá thiếu cột của TASK-056 · ĐÃ VÁ HẾT ở #98/#99** (`taskNotifications` · `businessRoleGroupScopes` · `roleCatalog` · `organizationUnits` · `accountingVouchers` · `hrRecords` · `officialCorrespondence` · `legalDocuments`) ⇒ cổng tập cột **về 0**.
* [!] **CỔNG MỚI** — ghi vào danh sách cổng: `probe-column-parity.mjs` (tập cột, **có đối chứng dương + phần ĐỘ PHỦ**) · `probe-roadmap-progress.mjs` (tiến độ so với MASTER TASK)
* [x] **TASK-025 · ĐÃ XONG Ở #34 (dòng TODO này bị CŨ — đã sửa 17/09 bởi TASK-067).** `SlaComplianceWorker` từng lỗi `bad SQL grammar` mỗi giờ vì câu lệnh dùng cột `overdue_at` **không tồn tại** (không có ở MySQL `V1__baseline.sql:1795-1810`, không có ở SQLite `drizzle/0005:31-49`, và **JS không hề dùng**). Đã bỏ cột khỏi câu lệnh. **Kiểm chứng LẠI bằng log thật hôm nay (17/09, TASK-067):** `INFO c.v.e.i.worker.SlaComplianceWorker : SLA worker: 0 supply steps quá hạn; 0 payment plans quá hạn; 4 BCH chờ xác nhận.` — chạy 60 giây sau khi khởi động, **không còn dòng WARN nào**; đối chiếu DB: `supply_workflow_steps` = 29 `overdue` / 27 `completed` / **0 pending quá hạn** ⇒ worker đã và đang dọn đúng. Chi tiết: `TASK-025.md`.
* [x] **TASK-040 nhóm 1+1b · `save_email_settings`** — #47: SQL sai 3 cột ⇒ 500; bootstrap **thiếu 2 khoá đọc**; **4 mặc định JS bị port sai**; khôi phục dữ liệu `smtp_port=587`/`po_sla=24`/`bch_sla=8`. Probe **13/13** · authz **9/9** · regression **59/61**
* [x] **TASK-040 nhóm 2 · `level_rank`** — **ĐÓNG: DƯƠNG TÍNH GIẢ.** Công cụ của tôi bỏ sót `CHANGE COLUMN` nên tố oan `UserAdminStoreAdapter`; DB đang chạy **đúng là `level_rank`**. Đã vá công cụ + thêm cổng đối chiếu lược đồ đang chạy.
* [x] **TASK-040 nhóm 3 · `material_norms` + `materials`** — #49: SQL 6 cột + hợp đồng payload + nghiệp vụ + **đường ĐỌC thiếu `source_type`**; probe **25/25**
* [x] **TASK-040 nhóm 4 · `confirm_installation`** — #50: bỏ cột `status` không tồn tại + **sửa lỗi NGỮ NGHĨA ghi đè → cộng dồn**; SQL chứng minh bằng transaction + ROLLBACK; HTTP **9/9**
* [x] **TASK-040 nhóm 5 · `settle_team_subcontract`** — #50: bỏ `settlement_id`/`settled_at`; probe HTTP **9/9**
* [x] **TASK-040 nhóm 3b phần 2** · port thân `import_material_catalog` — #54: nhóm tự tạo từ mã, `system=canonicalMeCode`, thông điệp JS; **vá đường ĐỌC thứ NĂM** (`adminMaterials` thiếu `specification`/`brand`/`minStock`/`requiresCocq`/mã-tên nhóm); probe **17/17**
* [x] **`canonicalMeCode` gom về một nguồn** — #52: vá **7/28 đầu vào lệch** với JS; unit test **5/5** · domain **19/19**
* [x] **TASK-041 · DONE (#57)** · `save_approval_stage` + `set_approval_stage_status` — bỏ yêu cầu `code` UI không gửi, ghi đủ **5 trường** (**SLA nay lưu thật**), port **3 quy tắc chặn**; probe **21/21** + chốt bước cuối **7/7**. Chi tiết: `docs/agent-progress/TASK-041.md` mục 6
* [x] **`material_subcategories` DONE (#59)** — phần 3 của TASK-041: Java đòi `code` mà UI không gửi (⇒ **luôn 400**), **bỏ 3 cột** (`scope_examples`/`review_status`/`adjustment_note`), **không đồng bộ vật tư con** khi đổi nhóm cha (⇒ `materials.category_id` mâu thuẫn), và **đường ĐỌC thứ SÁU** (`adminMaterialSubcategories` thiếu 5 trường). Probe **19/19**; dữ liệu về nguyên trạng `14/8/6`
* [x] **`retry_email` DONE (#61 — TASK-042)** — gọi bảng **không tồn tại** `email_queue` ⇒ HTTP 500 + hiểu sai nghiệp vụ; **cổng SQL mở rộng sang `FROM`/`JOIN`** để bắt cả lớp lỗi; probe **9/9**
* [x] **`custom_field_values` DONE (#62 — TASK-043)** — trường động của DÒNG phiếu: Java **chưa bao giờ GHI** (dù chú thích `RequestStore.insertRequest` đã hứa "…+ custom fields…") **và ĐỌC sai chỗ** (tra bằng id PHIẾU thay vì id DÒNG ⇒ `customFields` rỗng ở cả hai phía UI đọc). Sửa **cả hai chiều** + port `routeTag` (trước luôn NULL). Probe **26/26** (có **đối chứng dương**: truy vấn CŨ trả 0 dòng)
* [x] **TASK-044 DONE (#62)** — bootstrap **bỏ sót 11 trường** của dòng phiếu ⇒ UI `page.tsx:3609` luôn hiện trống/0 (thật: `remainingQty=975` · `pendingBchQty=340` · `linkedPoCount=12` · `linkedReceiptCount=10`). Đối chiếu số liệu bootstrap ↔ SQL **khớp tuyệt đối**
* [x] **TASK-045 DONE (#63)** — `save_material`: Java **xoá hệ M&E** (`system='KHAC'`) mỗi lần admin lưu (UI **không gửi** `system`) + **không ghi** `material_code_history` + bỏ `minStock` + không kiểm nhóm + không chặn đổi mã thiếu lý do. Đã port JS; probe **6/14 → 14/14** (chạy probe TRƯỚC khi vá để làm đối chứng dương)
* [ ] **Rà tiếp:** `project_archives.purge_audit_id` · `boq_versions.approved_at` · `boq_price_import_items.changed`
* [!] **MỚI — DỮ LIỆU `materials.system` lệch 5/14 mã** (2 mã `KHAC` khớp cơ chế lỗi #41; 3 mã nghi dữ liệu mẫu hoán vị) — **chờ người dùng xác nhận**, KHÔNG tự sửa
* [!] **MỚI — Java THIẾU `audit(...)` toàn luồng Phiếu đề nghị** (đo: `audit_logs.entity_type='material_request'` = **0**) — hạng mục riêng, cần rà phạm vi toàn hệ thống
* [!] **MỚI — Java yếu hơn JS 3 phép kiểm Owner** của bước duyệt (vai trò thuộc `allowedRoleCodes` + `user_project_scopes`) — CONFIRMED ở mức mã
* [!] **MỚI — `delete_request` (CẢ JS LẪN JAVA) để rác** `procurement_allocations` + `custom_field_values`; **1 dòng mồ côi THẬT** `MRI_d1f57f9d…→MR_46cee316…` (KHÔNG tự sửa dữ liệu)
* [!] **MỚI — trường động `request_header` không được lưu ở CẢ HAI phía** (JS chỉ dùng để kiểm bắt buộc); `form_key='boq'` thì JS đọc+xoá nhưng **không bao giờ ghi** ⇒ tính năng chết ở bản JS. Cần người dùng quyết định
* [!] **Java KHÔNG có cơ chế gửi email** (`email_outbox` chưa bao giờ được INSERT) — hạng mục riêng, cần quyết định
* [x] **`delete_approval_stage` DONE (#58)** — phần 2 của TASK-041: Java cũ cho **xoá bước đã có 20 bản ghi lịch sử** và **xoá bước hoạt động cuối cùng**; nay port đủ **2 chốt** + thông điệp nguyên văn, probe **12/12**; cấu hình duyệt **nguyên trạng** (5 dòng khớp bản sao lưu ở mọi trường nghiệp vụ)
* [!] **TASK-040 nhóm 6 · CHỜ QUYẾT ĐỊNH** — `vntech_license_*` lệch **cấu trúc** (câu hỏi #11)
* [x] **Cổng mới**: `probe-java-sql-live.mjs` (lược đồ đang chạy) · `probe-schema-drift.mjs` (tệp migration ↔ DB: **0 lệch**) · `probe-increment-drift.mjs` (SET vs cộng dồn, **có đối chứng dương**) · `probe-action-coverage-controller.mjs` (UI ↔ nhánh `case`: **0 thiếu**) · `show-js-lines.mjs`
* [!] **DỮ LIỆU MỒ CÔI** — `stock_issue_items` 3/5 dòng trỏ tới tổ đội không tồn tại (known issue #21) — **KHÔNG tự sửa dữ liệu**
* [!] **SỐ SLA THẬT · CHỜ XÁC NHẬN** — 24h (PO) / 8h (BCH) là mặc định của mã JS, không phải con số nghiệp vụ do người dùng công bố (câu hỏi #10)
* [x] TASK-027 · Kiểm chứng SỐNG bằng tài khoản thật; vá **2 lỗi P0** — #35
* [x] TASK-030 · 43 ca chặn tầng module là **ĐÚNG DỮ LIỆU** — #36
* [!] **TASK-008 phần 2 · Quét hồi quy rộng** — **DONE** (#38): vá lỗi lint chặn cả chuỗi `npm test`; cổng ảnh ĐẠT 28/28 · 0 px; `test:regression` 61 test / 58 PASS; 3 test đỏ **đã phân loại**; tìm ra nguyên nhân gốc rào cản cổng ảnh
* [!] **TASK-031 · CHỜ QUYẾT ĐỊNH** — cây dự án trong menu bị tắt ở cả hai nav
* [!] **TASK-032 · CHỜ QUYẾT ĐỊNH** — 0/16 vai trò trỏ đơn vị mặc định (lỗ hổng liên kết dữ liệu)
* [ ] ~~TASK-022b · Bổ sung 12 action chỉ có ở Java vào `ACTION_CATALOG`~~ **ĐÓNG — KHÔNG cần sửa** (#41): danh mục có hợp đồng "sinh từ nguồn JS" và đang đúng hợp đồng (174 mục, khớp JS 0 lệch); 12 action Java-only đã khai đầy đủ trong bản thi hành. Thêm vào sẽ phá `probe-action-parity`. Cổng mới: `tools/probe-catalog-drift.mjs`
* [ ] TASK-009 · U-09 đợt 6 — 13 màn còn lại
* [ ] TASK-010/011/012/013 · Áp dụng `EntityDetailModal` / `DataTable` / `PermissionGuard` / `ApprovalTimeline`
* [ ] TASK-014 · U-11 — tách `page.tsx`
* [ ] TASK-015 · U-12 — loại `!important` + gộp selector trùng
* [ ] TASK-017 · nguyên nhân gốc cổng ảnh bất định
* [ ] **MASTER TASK §2.1** · timeline luồng duyệt phiếu đề nghị mua hàng
* [ ] **MASTER TASK §2.2** · modal "Tổng hợp giao nhận về phiếu đề nghị gốc"
* [ ] **MASTER TASK §2.3** · responsive ảnh/hồ sơ vật tư đặc thù
* [ ] Đóng gói lại UI bundle trước khi người dùng test thủ công
* [!] TASK-024 · CHỜ QUYẾT ĐỊNH `isCompanyLeadership`
* [!] TASK-029 · CHỜ QUYẾT ĐỊNH mức chặt module

## CONTINUATION NOTES (cho phiên sau)

1. **Đọc theo thứ tự**: tệp này → `TASK_INDEX.md` → **`docs/28_DANH_SACH_110_MUC_MASTER_TASK.md`** (110 mục MASTER TASK, checklist + bảng theo phase, sinh tự động từ `docs/25_TODO_ROADMAP.md`) → **`TASK-050.md`** (4 khoá bootstrap + bộ lọc quyền) → `TASK-052.md` (lớp lỗi #4) → `TASK-051.md`/`TASK-053.md` (cùng lượt #85) → `TASK-048.md` (việc KẾ TIẾP) → `TASK-043.md`/`TASK-045.md` (2 vòng vá trước) → `TASK-040.md` (mục 13 = nhóm 1+1b).
2. **KHÔNG chạy lại** các việc đã DONE: TASK-B01/B02/B03, TASK-025, TASK-027, TASK-030, **TASK-040 nhóm 1+1b**.
3. **Chốt quyền ở tầng Java nằm ở ĐẦU mỗi nhánh `case`, TRƯỚC validate** — nhờ vậy payload rỗng đo được CHO/CHẶN mà không ghi dữ liệu.
4. **18 khối `Principal` vô danh** trong `SystemController`; **đã rà toàn bộ**: chỉ 2 khối của `RequestManagement` từng nguy hiểm và đã vá. Nếu thêm nhánh `case` mới có tạo lớp vô danh thì **phải override `roleBase()`** — hoặc tốt hơn là dùng helper `asXxxPrincipal(cu)`.
5. **Trước khi buộc tội mã nguồn**, hãy phân loại 403 theo 3 tầng (quyết định #16) và đối chiếu dữ liệu (`tools/probe-module-permission-data.mjs`).
6. **Java API phải chạy bằng background job**; **TUYỆT ĐỐI KHÔNG** `Start-Process` (tiến trình chết theo cửa sổ) và **KHÔNG** `Stop-Process node`. Chỉ dừng đúng PID đang giữ cổng 18081 trước khi `mvn package`.
7. **Đóng gói**: `JAVA_HOME=C:\Users\PC\.jdks\openjdk-26.0.2.1`; maven wrapper `apache-maven-3.9.16`; `java-backend/.mvn/maven.config` chứa `-Dmaven.repo.local=<workspace>\_m2-repo` (PowerShell làm hỏng `-D` vì đường dẫn có dấu cách). Fat jar phải ≈ **90 MB**; 68 KB nghĩa là `repackage` thất bại.
8. **Thứ tự chẩn đoán**: 4 cổng → `/actuator/health` → `tools/probe-live-stack.mjs` → cổng ảnh.
9. **Vá TASK-040 theo nhóm — quy trình 5 bước bắt buộc** (đúc từ nhóm 1): (1) đọc SQL tương ứng của JS; (2) sửa Java theo JS **kể cả thứ tự toán hạng của mặc định**; (3) **kiểm cả đường ĐỌC** (khoá bootstrap có tồn tại không, tên khoá UI dùng là gì); (4) build lại jar + khởi động lại + gọi thật + **đối chiếu MySQL** (không chỉ HTTP 200); (5) chạy `probe-java-sql-schema.mjs` lại để chắc không còn cột sai.
10. **Jar đang chạy**: nếu sửa mã Java mà KHÔNG build lại + khởi động lại thì mọi probe sẽ đo **bản cũ** — PID hiện tại **25608** (job `pwsh-96`) đang chạy jar có **TASK-050/051/052/053** (90.903.412 bytes). Trước khi `mvn package` phải dừng **đúng PID đang giữ cổng 18081**.
11. **Bốn cổng phải chạy trước khi kết luận về SQL/ngữ nghĩa** (đúc từ TASK-040): `probe-java-sql-live.mjs` (cột có tồn tại?) → `probe-increment-drift.mjs` (ghi đúng cách?) → đọc lại qua bootstrap (**đường ĐỌC có không?**) → `probe-action-coverage-controller.mjs` (action có `case` không?). Thiếu một cổng là còn một lớp lỗi không nhìn thấy.
12. **Khi chứng minh action GHI**: dùng `transaction + ROLLBACK` cho tầng SQL và chỉ gọi HTTP vào các **nhánh chặn**; luôn ghi rõ giới hạn "chưa test end-to-end".
13. **BÀI HỌC LỚN NHẤT CỦA PHIÊN NÀY (TASK-043/045): lỗi "port sai NGUỒN DỮ LIỆU" có thể nằm trong CÙNG một action với lỗi GHI/ĐỌC và chỉ lộ ra khi "ghi xong ĐỌC LẠI ĐÚNG TRƯỜNG".** Hai dạng đã gặp: (a) Java đọc một khoá payload mà **UI KHÔNG BAO GIỜ gửi** (`payload.system` ở TASK-045, `categoryId` ở TASK-040 nhóm 3b) ⇒ giá trị mặc định **ghi đè dữ liệu thật**, API vẫn trả 200; (b) Java tra bảng bằng **sai loại khoá** (id PHIẾU thay vì id DÒNG ở TASK-043) ⇒ đường ĐỌC **không bao giờ khớp**. **Quy trình bắt buộc: đối chiếu payload với FORM UI, và với mỗi cặp GHI/ĐỌC phải có một phép kiểm "ghi rồi đọc lại" — 7 lần dự án đã dính lớp lỗi này.**
14. **Probe cho action sửa DANH MỤC nên TỰ DỰNG bản ghi TẠM bằng SQL rồi tự xoá** (TASK-045) — rẻ, không đụng dữ liệu thật, mà vẫn chứng minh được toàn bộ hợp đồng; và **phải chạy probe TRƯỚC khi vá** để làm **đối chứng dương** (TASK-045: 6/14 → 14/14).
15. **Cảnh báo thao tác đo**: **KHÔNG nuốt `stderr`** (`2>$null`) khi chạy `mysql` — một câu SQL sai cột sẽ trả về "rỗng" và rất dễ bị đọc thành "bảng không có dữ liệu" (suýt xảy ra ở TASK-043 với `stage_no`/`stage`).
16. **LỚP LỖI #4 (mới, TASK-052): cột `tinyint(1)` của MySQL được JDBC trả về `Boolean`, KHÔNG phải `Number`.** `x instanceof Number` **luôn sai** với cột cờ ⇒ nhánh mã "có vẻ đúng" lại **luôn cho 0 dòng**. Đã gặp ở `modulePermissions` (admin **luôn rỗng**, bị UI che). **Quy ước bắt buộc trong kho: dùng `isActiveOne/isOne` với `Boolean.TRUE.equals(o)`** — 5 use-case đã làm đúng, adapter bootstrap thì chưa. Khi đọc JSON để chẩn đoán: `"active": true` trong JSON **chính là** dấu hiệu kiểu Boolean.
17. **ĐỪNG port một BỘ LỌC QUYỀN dựa trên dữ liệu mà chính bản port đang thiếu** (TASK-050): bộ lọc xoá-trắng-theo-module lấy `modulePermissions` làm đầu vào; nếu nhánh dựng `modulePermissions` sai (TASK-052) thì bộ lọc **xoá trắng toàn bộ dữ liệu** của một nhóm tài khoản. Bài học: **hai lỗi ở hai lớp khác nhau có thể cộng dồn thành lỗi nặng hơn từng lỗi** ⇒ khi thêm một bước phụ thuộc dữ liệu, phải kiểm **cả dữ liệu đầu vào** của bước đó.
18. **Khi JS là "bản chuẩn" cần port, ĐỪNG tự "cải tiến" hành vi của nó** — nhưng **phải ghi lại chỗ JS sai thiết kế** (TASK-050 mục 5.3: tài khoản 0 dòng quyền ⇒ JS xoá trắng mọi dữ liệu nhưng UI vẫn dựng menu theo vai trò ⇒ màn hình rỗng). Port khớp JS là **an toàn để cutover**, còn "sửa giúp JS" là **lệch không kiểm chứng được**.
19. **Cổng theo TÊN khoá là chưa đủ** (TASK-050 → #50/#52/#53): liên tiếp lộ 3 dạng cổng tĩnh **không bắt được** — (a) **thiếu CỘT** trong câu SQL (`constructionDailyLogs` thiếu `itemCount`/`completedQty`; `transferOrders` thiếu 7 trường); (b) **khoá có khai nhưng giá trị `null`/rỗng theo vai trò** (`engineRoleProfiles` = null cho **mọi** tài khoản vì **put sau khi get** — dòng 726 ↔ 826); (c) **bộ lọc quyền thiếu** (dữ liệu nhiều hơn JS trả). ⇒ Việc kế tiếp của lớp lỗi này: **cổng đối chiếu tập CỘT** + **probe HTTP theo từng vai trò**.
20. **Một probe tốt phải tự buộc mình "không thể kết luận":** mục nào dữ liệu rỗng thì in **"(bỏ qua) — KHÔNG kiểm được"** và **không** tính là ĐẠT (TASK-050: 10 phép đo như vậy), và **kỳ vọng phải tính từ NGUỒN ĐỘC LẬP** (MySQL) chứ **không** lấy lại dữ liệu trong chính response đang kiểm (tránh lập luận vòng tròn). **Lỗi của chính tôi ở lượt này:** bản probe đầu dùng "sentinel" `*LEADERSHIP*` cho tài khoản Ban giám đốc ⇒ **kỳ vọng sai** và các phép kiểm **ĐẠT vì lý do sai**; chỉ lộ ra khi tôi **đọc số thật** (`perms=0`) thay vì tin dòng "ĐẠT".
21. **MỌI CỔNG ĐO PHẢI CÓ ĐỐI CHỨNG DƯƠNG — nếu không, nó có thể in ra một HẰNG SỐ mà không ai biết (#87).** `probe-audit-coverage.mjs` tra tên **biến** camelCase vào bảng khoá theo tên **lớp** PascalCase ⇒ không bao giờ khớp ⇒ "khe hở" luôn **= đúng số action JS có audit (152)** và "cả hai đều ghi: 0" — **hai con số vô nghĩa được ghi vào tài liệu như sự thật**. Cách phát hiện: thấy *"số khe hở + số JS có audit = tổng JS"* là dấu hiệu **phép trừ luôn bằng 0**. Cổng nay có dòng `ĐỐI CHỨNG: 186/186 nhánh case tra được LỚP` (nếu = 0 là cổng hỏng). **Hệ quả thực tế: khe hở audit đúng là 128, không phải 152.**
22. **"Probe báo ĐẠT" ≠ "phép kiểm đã chạy".** Phép kiểm audit của TASK-048 gọi hàm `q1()` **không tồn tại** ⇒ probe **ném lỗi**, nhưng dòng tổng kết vẫn in "19/20 ĐẠT" và tôi ghi vào sổ là *"phép kiểm audit ĐỎ"* suốt một phiên. Từ nay: (a) mọi probe phải có **phép kiểm "probe chạy trọn vẹn"**; (b) khi đọc kết quả phải xem **dòng tổng kết và MỤC HỎNG**, không chỉ đếm số ĐẠT; (c) chạy `2>&1` khi chẩn đoán — **lần thứ HAI trong cùng một lượt** tôi tự bịt mắt bằng `2>$null` (bài học #15).
23. **Dọn dẹp trong `finally` phải chạy TỪNG CÂU và GOM LỖI.** Một `finally` gộp 7 câu `DELETE` vào một lần gọi mysql: câu thứ 5 sai tên cột (`procurement_allocations.request_id` **không tồn tại** — cột thật là `request_item_id`) ⇒ mysql dừng ngay đó, **6 câu sau không chạy**, dữ liệu probe còn nằm lại trong DB thật (phiếu `cancelled` + 1 dòng vật tư + 1 dòng phân bổ). Đã dọn tay **có kiểm chứng** và sửa probe. Kèm theo: **phải LƯU id dòng TRƯỚC khi gọi API xoá**, vì sau khi API xoá thì không còn khoá nào để dọn rác con.
24. **⚠️ KHI MỘT PHÉP ĐO CHO RA HÀNH VI "SAI", HÃY KIỂM BẢN ĐỐI CHỨNG (JS) TRƯỚC KHI BUỘC TỘI BẢN PORT (#89 — lỗi của chính tôi).** Tôi ghi vào sổ (#88) rằng *"Java chuyển bước sớm ở bước duyệt song song, JS thì không"* — **sai**: JS `:1080` dùng **đúng câu truy vấn** ưu tiên `approval_mode_snapshot` như Java, và `:976` ghi snapshot `single` cho mọi bước **có Owner** ⇒ **21/21 dòng bị đóng băng** ⇒ nhánh `all_roles` là **mã chết ở cả hai lõi**. Dấu hiệu tôi đã bỏ qua: bản port chỉ **thiếu mã**, còn **câu SQL đọc dữ liệu thì giống hệt** — nghĩa là hành vi quan sát được khó có thể chỉ do bản port. **Quy tắc:** trước khi kết luận "Java lệch JS", phải **đọc câu truy vấn tương ứng của JS** (không chỉ đọc chú thích/khối mã Java). Bài học này cũng cho thấy **giá trị của probe**: nó biến một kết luận suy luận thành một phép đo và **lật ngược** kết luận đó.
25. **`mysql --batch --raw` in `NULL` thành CHUỖI `"NULL"` — phép kiểm `=== ""` sẽ BỎ QUA OAN (#92, lỗi của chính tôi).** Probe TASK-049 lượt đầu báo **5/5 ĐẠT** nhưng thực ra **2 nhánh quan trọng nhất bị bỏ qua** *(đúng lúc dữ liệu thật đang vi phạm phép kiểm mới)*, vì tôi so `(row[6] ?? "") === ""` trong khi giá trị là `"NULL"`. **Quy tắc:** khi đọc kết quả CLI dạng bảng, `NULL` = **chuỗi `"NULL"`**; và **số ĐẠT không nói lên điều gì nếu có nhánh bị bỏ qua** — phải in rõ số phép đo THỰC SỰ chạy.
26. **Một phép kiểm mới có thể LÀM LỘ ra dữ liệu sai đang bị che (#92).** Sau khi port 2 phép kiểm Owner của JS, luồng lập phiếu của PRJ-DEMO-01 **bị chặn** vì Owner bước 2 không có phạm vi dự án — **bản JS cũng chặn y hệt**, tức bản Java đang *âm thầm bỏ qua* một phép kiểm và cho phép chạy với cấu hình sai. **Không được "nới" phép kiểm để mọi thứ chạy trôi** — phải port đúng rồi **báo dữ liệu cần sửa** (D5). Đồng thời **mọi probe tạo phiếu thật phải tự cấp TẠM phạm vi còn thiếu và xoá lại** (đã áp cho probe 048/054, số dòng `user_project_scopes` đưa vào bảng đối chiếu).
27. **CỔNG SO "TẬP CỘT" bắt được cả một LỚP LỖI mà cổng theo TÊN KHOÁ không thấy (#94 — TASK-056/057).** Sau **3 ca liên tiếp ở mức CỘT** (TASK-053, `transferOrders`, và lớp "khoá null theo vai trò"), cổng `probe-column-parity.mjs` so tập tên cột SQL hai phía ⇒ **13 khoá thiếu cột trong MỘT lần quét** (đã vá 4). **Bộ tách cột là SUY LUẬN VĂN BẢN nên bắt buộc phải có ĐỐI CHỨNG DƯƠNG**: cổng tự dựng **bảng tạm** từ chính câu SQL rồi `SHOW COLUMNS` để lấy metadata THẬT của MySQL — nếu lệch thì cổng tự báo HỎNG và **không cho kết luận**. *(Ghi chú kỹ thuật: `SELECT * FROM (<sql>) x LIMIT 0` **không** in dòng tiêu đề ở chế độ `--batch`; phải dùng `CREATE TEMPORARY TABLE … AS <sql>; SHOW COLUMNS …`.)*
