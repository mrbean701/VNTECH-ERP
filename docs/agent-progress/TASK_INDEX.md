# TASK INDEX — VNTECH ERP V5.3.0 MASTER TASK

Trạng thái hợp lệ: `PENDING` · `IN PROGRESS` · `DONE` · `BLOCKED` · `CANCELLED`
**Không bao giờ xoá hồ sơ việc đã xong.**

| Task | Tiêu đề | Trạng thái | Commit | Ngày | Ghi chú |
|---|---|---|---|---|---|
| TASK-001 | PHASE 0 — SYSTEM AUDIT + TODO/ROADMAP + probe bảo mật RBAC | DONE | `8b4a5da` (#9) | 16/09/2026 | docs/24 (20 mục + Phụ lục B), docs/25 |
| TASK-002 | PHASE 0B — Bật kiểm quyền RBAC ở tầng action, bịt lỗ hổng 15/15 action lọt qua | DONE | `3c948cb` (#10) | 16/09/2026 | Lỗ hổng 15 → 0; đúng 20/20 |
| TASK-003 | PHASE 1 — Thư viện UI dùng chung + sửa xung đột lớp CSS + xác minh cổng ảnh 28/28 (U-01…U-08) | DONE | `7fa59ba` (#11) | 16/09/2026 | docs/26; đính chính kết luận sai về nguyên nhân 28/28 |
| TASK-004 | U-09 đợt 1 — Chuẩn hoá toolbar 3 màn (Requests · WarehouseReceipt · Inventory) | DONE | `8b29344` (#12) | 16/09/2026 | docs/27 mục 1–6 |
| TASK-005 | U-09 đợt 2 — Màn Phân quyền người dùng + nâng ngưỡng cổng ảnh có bằng chứng | DONE | `426763c` (#13) | 16/09/2026 | docs/27 mục 7–9 |
| TASK-006 | U-09 đợt 3 — WorkCenter · TeamManagement · ProjectManagement | DONE | `44c0a5e` (#14) | 17/09/2026 | docs/27 mục 10–11 |
| TASK-007 | U-09 đợt 4 — Danh sách nhân sự + Danh bạ nội bộ + probe chặt hơn + cổng chống lỗi giả + phát hiện roadmap báo quá | DONE | `9148212` (#15) | 17/09/2026 | docs/26 mục 10; docs/27 mục 12–13 |
| TASK-008 | U-09 đợt 5 — 3 màn tab Quản trị (UserPermissionMatrix · SystemLevelManager · AuditLogManager) | IN PROGRESS | `96c3aa8` (#16, PARTIAL) | 17/09/2026 | Code + probe riêng 11/11 ĐẠT; **còn thiếu lượt quét hồi quy rộng** vì cổng ảnh/probe cần mở rộng sandbox |
| TASK-009 | U-09 đợt 6 — 13 màn còn lại | PENDING | — | — | Xem docs/25 |
| TASK-010 | U-14 — ÁP DỤNG EntityDetailModal | PENDING | — | — | Dùng thật 0 lần |
| TASK-011 | U-15 — ÁP DỤNG DataTable + StatusBadge | PENDING | — | — | 100 bảng + 100 rỗng; StatusBadge 2/88 |
| TASK-012 | U-16 — ÁP DỤNG PermissionGuard | PENDING | — | — | 50 chỗ điều kiện quyền |
| TASK-013 | U-17 — ÁP DỤNG Approval/ActivityTimeline | PENDING | — | — | 3 dải tự viết |
| TASK-014 | U-11 — Tách `page.tsx` thành module | PENDING | — | — | 4.140 dòng / 221 hàm |
| TASK-015 | U-12 — Loại `!important` + gộp selector trùng | PENDING | — | — | Phụ thuộc U-11 |
| TASK-016 | U-13 — Tách `TaskTable` khỏi thân render WorkCenter | DONE | #17 (cùng lượt) | 17/09/2026 | eslint 3 lỗi → **0** lỗi; làm trước TASK-009…015 vì kiểm được bằng kiểm tra tĩnh |
| TASK-017 | Điều tra còn mở — nguyên nhân gốc bất định của cổng ảnh | PENDING | — | — | Đã khoanh vùng ô tìm kiếm topbar |
| TASK-018 | Đối chiếu action giữa bản JS tham chiếu và bản Java đã port (đính chính audit) | DONE | #18 (cùng lượt) | 17/09/2026 | Cáo buộc "catalog lệch 50 action" là **SAI**; Java **không thiếu action nào** |
| TASK-019 | Sửa 5 lỗi mã vai trò còn sót trong `ProductionManagementUseCase` (commander→cht, project→da_nv) | DONE | #19 (cùng lượt) | 17/09/2026 | Người dùng `cht`/`da_nv` hết bị 403 oan; javac **exit 0**; chưa đóng gói lại JAR |
| TASK-020 | U-15 (phần StatusBadge) — thay 88 chỗ `<Pill>` bằng `<StatusBadge>`, chứng minh tương đương bằng công cụ | DONE | #21 (cùng lượt) | 17/09/2026 | StatusBadge **2 → 90** lần dùng thật; `<Pill>` 88 → 0; tsc ĐẠT · eslint 0 lỗi; **chưa kiểm bằng mắt** (TASK-B02) |
| TASK-021 | **Sửa GỐC ngữ nghĩa vai trò**: `roleBase` nạp từ `role_catalog` + `requireRole` nhận mã engine + 13 điểm gọi về mã engine | DONE | #22 (cùng lượt) | 18/09/2026 | docs/28; công cụ `probe-role-code-scan` · `patch-role-engine-codes` · `verify-java-compile`; **0 lỗi mức CAO** · 13/13 phép thay thế khớp · biên dịch 99 tệp **0 lỗi**; **chưa đóng gói lại JAR** |
| TASK-021b | **Sửa HỒI QUY do TASK-021**: `principalAsCurrent` dựng `CurrentUser` với `roleBase` = mã chuẩn ⇒ 16 action sẽ 403 với mọi tài khoản không phải admin. Vá bằng `default roleBase()` trên `Principal` + controller truyền `cu.roleBase()` thật | DONE | #23 (cùng lượt) | 18/09/2026 | Cổng `probe-action-role-parity` nay có mục **ĐƯỜNG ỐNG roleBase** để bắt đúng lớp lỗi này |
| TASK-022 | Rà soát action Java **thiếu** `requireRole` so với JS + thắt 5 action còn hở | DONE | #23 (cùng lượt) | 18/09/2026 | TASK-022.md; cổng `probe-action-role-parity` **exit 0**: 0 hở · 0 chặt hơn · 21 khớp; `OpsTaskManagementUseCase` được thêm hạ tầng RBAC |
| TASK-023 | **Kiểm PHẠM VI dự án/kho** (lỗ hổng P0: JS kiểm 64 action, Java kiểm 0) — `AccessScopeService` + port `AccessScopeStore` dùng chung | **DONE** | #24…#29 · #31 · #32 (lô 8a) · #33 (lô 8b) | 18/09/2026 | **64/64 · cổng `probe-action-scope-parity` exit 0**; hồi quy **cổng ảnh 28/28 ĐẠT**; `probe-live-stack` ALL PASS; jar béo BUILD SUCCESS + health UP |
| TASK-023b | 3 action `ProjectContractUseCase` — kiểm phạm vi ở **tầng web** (đúng chú thích lớp) + đưa `Principal` vào 2 phương thức cần tra bản ghi | **DONE** | #33 (cùng lượt) | 18/09/2026 | `save_project_contract` kiểm ngay ở controller; `set/delete` nhận `Principal` để kiểm `row.project_id` |
| TASK-028 | Cổng ảnh báo **28/28 lệch ~96%** — điều tra và xác định **KHÔNG phải hồi quy mã nguồn** | DONE | — | 18/09/2026 | Nguyên nhân: tiến trình Java khởi động bằng `Start-Process` **đã chết** ⇒ proxy :9000 trả **502**. Đã ghi bài học vào `docs/29` mục 6.6–6.7: **không dùng `Start-Process` cho Java API**; trước khi kết luận hồi quy giao diện phải kiểm `probe-live-stack` |
| TASK-026 | ~~Dọn khối `rbac.requireRole` bị nhân đôi~~ **MỞ RỘNG**: còn phát hiện thêm **import bị lặp** và **dòng `rbac.requireRole` lặp thêm 5 chỗ** do cùng lỗi idempotency | DONE | #31 · #32 | 18/09/2026 | `tools/cleanup-duplicate-requirerole.mjs` nay gỡ cả import lặp; **đã sửa THỨ TỰ KIỂM trong công cụ vá** (`to` trước `from`) để không tái diễn |
| TASK-025 | `SlaComplianceWorker` lỗi `bad SQL grammar` **lặp mỗi giờ** ⇒ worker SLA chưa bao giờ chạy được | **DONE** | #34 (cùng lượt) | 18/09/2026 | **Nguyên nhân gốc:** câu lệnh dùng cột `overdue_at` **không tồn tại** — không có ở lược đồ MySQL (`V1__baseline.sql:1795-1810`), không có ở SQLite (`drizzle/0005:31-49`), và **JS không hề dùng**. Đã bỏ cột khỏi câu lệnh. **Bằng chứng chạy thật:** log đổi từ `WARN ... bad SQL grammar` (mỗi giờ) sang `INFO SLA worker: 29 supply steps quá hạn; 0 payment plans quá hạn; 4 BCH chờ xác nhận.` |
| TASK-026 | Sửa lỗi **idempotency của công cụ vá** (đã gỡ 5 khối `rbac.requireRole` bị nhân đôi) + ghi bài học | DONE | #31 (cùng lượt) | 18/09/2026 | Nguyên nhân: kiểm "đã áp dụng chưa" bằng `includes(from)` mà `from` là dòng chữ ký phương thức (vẫn còn sau khi chèn) ⇒ chèn lần hai. `tools/cleanup-duplicate-requirerole.mjs` đã gỡ và **kiểm chứng không còn khối lặp** |
| TASK-024 | Lệch `isCompanyLeadership`: JS = 7 mã + `base_role='director'`; Java = `{director, accountant}` | BLOCKED | — | 18/09/2026 | **Cần người dùng quyết định** — thuộc nhóm bảo mật đang tạm hoãn; Java cấp thừa cho `accountant`, cấp thiếu cho `thuky` |
| TASK-B01 | ~~Xác nhận tên màn Receiving từ menu~~ **ĐÃ GIẢI QUYẾT** bằng dữ liệu, không cần hỏi người dùng | DONE | — | 18/09/2026 | Nhãn cuối cùng của module `receiving` = **"Kế hoạch giao hàng"** (nhóm `MUA HÀNG`). Chuỗi tiến hoá: `Giao nhận công trường` (0010) → `Chờ giao hàng` (0011) → **`Kế hoạch giao hàng`** (0029). Khớp với `app/page.tsx:116` |
| TASK-B02 | ~~Cổng ảnh + bộ probe KHÔNG chạy được~~ **ĐÃ GỠ CHẶN** — cổng ảnh cần Edge headless (named pipe), chạy được khi mở rộng sandbox | DONE | — | 18/09/2026 | **Cổng ảnh: 28/28 ảnh ĐẠT, 0 px lệch** trên 7 màn × 4 kích thước. Lưu ý: các probe cần `spawn mysql` vẫn cần mở rộng sandbox |
| TASK-B03 | ~~Không đóng gói lại được JAR~~ **ĐÃ GỠ CHẶN** — nguyên nhân gốc gồm 3 lớp, đã xử lý cả 3 | DONE | — | 18/09/2026 | (1) `JAVA_HOME` chưa đặt → mvn báo sai về `.m2`; (2) sandbox chặn ghi `.m2` → dùng local repo trong workspace qua `.mvn/maven.config` (PowerShell làm hỏng `-D` vì đường dẫn có dấu cách); (3) tiến trình Java đang giữ JAR → dừng đúng PID rồi repackage. **`BUILD SUCCESS`, jar béo 90.883.789 bytes, server khởi động lại, `/actuator/health` = UP** |
| TASK-B01 | Xác nhận tên màn Receiving từ menu | BLOCKED | — | 17/09/2026 | Cần người dùng; KHÔNG chặn tiến độ |
| TASK-027 | Kiểm chứng **SỐNG** bằng tài khoản thật (`da_truong`/`kho_tong`/`cht`/`ksda`…) cho các sửa đổi vai trò + phạm vi | **DONE** | #35 (cùng lượt) | 18/09/2026 | **Phương pháp:** gọi action với **payload rỗng** — chốt quyền chạy TRƯỚC validate nên 403 = bị chặn, 400 = được phép, **không ghi dữ liệu nào**. **Kết quả:** vai trò 10/10 đúng mã engine; chặn đúng **351/351, lọt 0**; **403 tầng vai trò = 0**. **Tìm + vá 2 LỖI P0:** (1) `create_request`/`decide_approval` dựng lớp vô danh `Principal` **thiếu `roleBase()`** ⇒ trả mã chuẩn `ksda` ⇒ chỉ admin lập được phiếu đề nghị mua (đã xác nhận lại sau vá: 403 chuyển từ tầng vai trò sang tầng phạm vi); (2) `AccessScopeService:77` so `"warehouse".equals(role)` với mã ENGINE nhưng nhận mã CHUẨN ⇒ **nhánh phạm vi kho là mã chết** (chặn oan kho site + lọt kho central). Xem `docs/agent-progress/TASK-027.md` |
| TASK-029 | Bản đồ **MODULE** của Java **CHẶT HƠN JS trên 30 action** (JS không kiểm module, Java kiểm) | **BLOCKED** | — | 18/09/2026 | Đo bằng `tools/probe-action-module-parity.mjs`: MODULE khớp **156/186**, **30 lệch — tất cả cùng chiều** `JS=[] → Java=[module]`; CAPABILITY khớp **185/186** (lệch 1: `system_level_impact` JS `canUse` vs Java `canView`). **Tác động đã định lượng:** 6/30 action (`save_team_subcontract`, `save_team_production`, `approve_team_production`, `save_team_payment`, `settle_team_subcontract`, `create_project_team`) bắt buộc module `teams`/`site_command` — mà `teams` có **0 dòng** quyền ⇒ **chỉ admin làm được**, trong khi **JS cho phép theo vai trò**. **Cần người dùng quyết định** giữ (an toàn hơn nhưng lệch §6) hay khôi phục đúng JS — **KHÔNG tự ý đổi** |
| TASK-030 | 43 ca bị chặn ở **tầng MODULE** — xác định là **thiếu dữ liệu** hay hành vi đúng | **DONE** | #36 · #37 | 18/09/2026 | **Kết luận: ĐÚNG DỮ LIỆU, không phải lỗi mã.** `tools/probe-module-permission-data.mjs` đọc chính payload `bootstrap`: `user_module_permissions` chỉ có **484 dòng** cho 61 module × 12 người dùng (đầy đủ phải 732), và **5/9 module JS yêu cầu có 0 dòng toàn hệ thống** (`stocktake`, `inventory`, `teams`, `warehouse_issue`, `boq`). **Đã LOẠI TRỪ nguyên nhân sâu hơn:** 0 module không tồn tại · 0 module bị tắt · 0 nhóm menu bị tắt ⇒ **nguyên nhân duy nhất là thiếu dòng quyền**, chỉ cần cấp quyền. JS đọc **chỉ** bảng này nên **chặn y hệt** ⇒ không phải sai lệch mã. **Đóng known-problem #15:** tên capability ánh xạ đúng cột thật (`ModulePermissionStoreAdapter:22-24`); `central_warehouse` hoạt động nhưng **0 dòng quyền** ⇒ nhánh kho-central chỉ đạt mức ĐỌC cho số ít tài khoản, **không bao giờ đạt mức GHI** — khoảng trống dữ liệu, không phải lỗi mã. **Bài học:** lần chạy đầu phép kiểm báo "nhóm menu TẮT" cho cả 9/9 module vì tôi so `active === 1` trong khi payload trả **boolean `true`** ⇒ kết quả đồng loạt bất thường phải nghi **phép kiểm trước khi nghi hệ thống**. Việc nạp đủ quyền là **thao tác dữ liệu của quản trị viên**, cần người dùng quyết định (a) chạy lại cấp mặc định theo phòng ban, (b) cấu hình thủ công, (c) giữ nguyên |
| TASK-008 | U-09 đợt 5 — chuẩn hoá toolbar 3 màn tab Quản trị (UserPermissionMatrix, SystemLevelManager, AuditLogManager) | **DONE** | #38 | 18/09/2026 | Code đợt 5 xong trước đó; **lượt quét hồi quy rộng là bước còn lại đã hoàn thành ở #38**: vá **F1** — `EntityDetailModal` gọi `setState` trong `useEffect` (`react-hooks/set-state-in-effect`) làm `npm test` **dừng ngay ở `lint`** nên `typecheck` + 8 tệp `test:regression` + `test:workflow` **chưa từng chạy**; nay suy ra tab khi render ⇒ `eslint .` **0 error**, bộ kiểm tiến qua `typecheck`. **Kết quả cổng:** vai trò exit 0 · phạm vi exit 0 · `probe-live-stack` ALL PASS · **cổng ảnh 28/28 ĐẠT 0 px** · `test:regression` **61 test / 58 PASS / 3 FAIL** (đã phân loại) |
| TASK-031 | Cây dự án trong menu **bị TẮT ở CẢ HAI nav** — chủ ý sản phẩm hay tắt nhầm? | **BLOCKED** | — | 18/09/2026 | Sentinel `__site_command_tree_disabled__` ở `app/page.tsx:664` (desktop) **và** `:678` (mobile) ⇒ nhánh `activeSiteProjects.map(...)` là **MÃ CHẾT**; `activeSiteProjects` (dòng 671) vẫn tính mà không còn nơi dùng. Sentinel vào ở commit **`1c01f39`** (16/09) — **TRƯỚC** công việc RBAC, và **mô tả commit KHÔNG nhắc việc tắt cây dự án** ⇒ đưa vào âm thầm, test không cập nhật. `CONFLICT` ⇒ **cần người dùng quyết định**; **KHÔNG tự sửa** và **KHÔNG sửa test cho khớp mã** (làm vậy là đóng băng một trạng thái có thể là lỗi) |
| TASK-033 | MASTER TASK **§8.1** — dải phê duyệt phiếu thiếu **PHÒNG BAN** của người duyệt | **IN PROGRESS** | (chờ dựng bundle) | 18/09/2026 | Đối chiếu §8.1 (6 thông tin/bước): **5/6 đã có**, thiếu đúng **PHÒNG BAN** (thời gian ✓, trạng thái ✓, người duyệt ✓, số bước ✓, **ý kiến ĐÃ có sẵn**). Bản vá: suy `approverDept` từ `approval.approverUserId → data.staffDirectory[].department` (tên đọc được, không phải mã) + hiển thị nhãn "Phòng ban:" **nguyên văn** từ component dùng chung. **Kiểm chứng:** tsc **exit 0** · eslint **0 error/74 warning** (đúng mốc nền) · cổng ảnh **28/28 · 0 px** (dự đoán nêu trước: ĐÚNG) · bản vá idempotent + 5/5 hậu kiểm. **Thêm phép kiểm mới** vào `probe-request-page.mjs` (kiểm theo TỪNG bước) — và nó **bắt đúng sự thật**: bundle đang chạy cũ (`dist` 09:35 vs nguồn 12:41). **Gỡ được chặn dựng bundle tầng 1**: `preflight-source.mjs:205` + `trust-lock-foundation.test.mjs:19` thiếu `.local-data` (thư mục runtime đã gitignore) ⇒ `npm run build` bị chặn cứng; nay in `FULL W2 SOURCE PREFLIGHT: ĐẠT`. **CÒN CHẶN tầng 2:** dấu vân tay nguồn lệch, **không có script ghi lại** — xem TASK-034 |
| TASK-034 | **`npm run build` không dựng lại được UI** — dấu vân tay nguồn lệch và không có công cụ tái lập | **BLOCKED** | — | 18/09/2026 | `verify-vntech-fingerprint.mjs:29`: expected `0c5e9c0a…` vs actual `d8e90726…`. Phạm vi dấu vân tay (`lib/trust/source-fingerprint.mjs`): tệp gốc + `app db deploy drizzle lib public scripts tests worker` (**không** phủ `docs/`, `tools/`); loại trừ duy nhất `lib/vntech-identity-data.mjs`. Giá trị kỳ vọng ở `lib/vntech-identity-data.mjs` + `VNTECH_FINGERPRINT.json`. **KHÔNG có script GHI** (chỉ có `verify:fingerprint` để đọc). Hệ quả: **30 commit chưa lần nào dựng lại** ⇒ bundle đang phục vụ cũ hơn nguồn ~3 giờ ⇒ **bạn sẽ test thủ công trên bundle cũ**. Cần người dùng xác nhận tái lập định danh (ghi `sourceFingerprint`/`sourceFingerprintShort` mới + đồng bộ `VNTECH_FINGERPRINT.json`) rồi mới build |
| TASK-032 | **LỖ HỔNG LIÊN KẾT DỮ LIỆU**: 0/16 vai trò trỏ tới đơn vị mặc định | **BLOCKED** | — | 18/09/2026 | `organization_units` có **8 đơn vị đang hoạt động, gồm `BGD` = "Ban giám đốc"**, nhưng `role_catalog.default_organization_unit_id` **null ở MỌI vai trò (0/16)** ⇒ "đơn vị mặc định theo vai trò" âm thầm không thể chạy. Đo bằng `tools/show-role-catalog.mjs`. **Bẫy kỹ thuật:** không tệp SQL/Java nào nhắc `default_organization_code` — đó là tên hiển thị sinh từ JOIN trên `default_organization_unit_id`, tìm theo tên đó sẽ không thấy gì. Kèm 1 điểm lệch test: tên `thuky` thật dài hơn ("…/ Trưởng phòng Hành chính Pháp chế") so với test. **KHÔNG tự sửa dữ liệu nghiệp vụ** |
| TASK-035 | **GOAL §8** — admin đổi workflow khi hồ sơ đang pending: theo bản CŨ hay bản MỚI? | **DONE** (điều tra) | #40 (cùng lượt) | 18/09/2026 | **KẾT LUẬN: KHÔNG có version pinning — hồ sơ đang chờ đổi theo cấu hình MỚI ngay lập tức.** Bằng chứng: (1) `requests[]` **không có** `workflowId`/`workflowVersion` — chỉ `approvalStage` là một con số (đo dữ liệu sống) và `approvals[]` chỉ có `stage`; (2) mọi thuộc tính bước tra **LIVE** khi quyết định (`RequestStoreAdapter:270,283 LEFT JOIN approval_stage_catalog ON stage_no`; `canApproveRequestStage` tra pool/owner/roles live); (3) `workflow_definitions.version` **không hoạt động như versioning** — cả `drizzle/0080:46` lẫn **MySQL đang chạy** `V8__workflow_multi.sql` đều là `UNIQUE (workflow_id, step_no)`, **không có `version`** ⇒ không thể tồn tại song song bước 1 của v1 và của v2. **RỦI RO (`LIKELY`):** tắt/xoá một bước đang có hồ sơ chờ ⇒ `allowed` rỗng ⇒ không ai duyệt được và không có đường thoát. **PHÁT HIỆN THÊM (`CONFIRMED`):** **Java RỘNG HƠN JS** — JS có **0** tham chiếu tới cả 3 bảng P4 (chỉ legacy), Java còn cấp quyền qua `workflow_step_approvers`; bảng này **seed một lần** rồi có thể **trôi lệch** ⇒ xếp cùng nhóm TASK-029. **KHÔNG tự sửa kiến trúc** (GOAL §8 + nguy cơ phá 20+ bản ghi phê duyệt đang chạy) — 3 câu hỏi ở `TASK-035.md` mục 7 |
| TASK-022b | `ACTION_CATALOG` "chưa ghi 12 action chỉ có ở Java" | **DONE — KHÔNG cần sửa** (tiền đề SAI) | #41 (cùng lượt) | 18/09/2026 | **Đo được:** danh mục **174** mục, bản thi hành `ActionRbacRegistry.java` **186**; lệch 12 một chiều, 0 chiều ngược. **Danh mục CÓ HỢP ĐỒNG là "sinh từ nguồn JS"** và ĐANG đúng: `probe-action-parity` exit 0 — *"Catalog KHỚP HOÀN TOÀN với nguồn JS"*, cả hai chiều 0, và *"Action của JS mà Java THIẾU: 0"*. **Thêm 12 mục vào sẽ TẠO SAI LỆCH GIẢ** (inCatNotJs 0 → 12) ⇒ **phá một cổng đang xanh**. 12 action đó **đã khai đầy đủ** module+capability trong chính bản thi hành mà `requireActionModule` dùng ⇒ **không bỏ sót gì**. **Giả thuyết rủi ro đã kiểm và BÁC BỎ:** không có tệp nào GHI `ACTION_CATALOG.json`, và `patch-rbac-registry.mjs` chỉ `replace` chứ không xoá ⇒ không có đường tự động làm mất mục. **Củng cố TASK-035:** 3 trong 12 mục là `save_workflow`/`set_workflow_status`/`delete_workflow` ⇒ khớp việc JS có **0** tham chiếu bảng P4 (quản trị workflow là Java-only). **Để lại cổng** `tools/probe-catalog-drift.mjs` (exit 0) — báo HỎNG nếu có tên lạ ngoài tập 12 Java-only đã biết |
| TASK-036 | **GOAL §8/§7** — hai cột cấu hình của bước workflow (`required_permission`, `allow_skip_level`) **không được thi hành ở đâu** | **DONE** (điều tra) | #42 (cùng lượt) | 18/09/2026 | **CONFIRMED:** cả hai cột tự khai trong DDL MySQL là *"Quyền cần có để được chọn làm người duyệt"* / *"Cho phép cấp bậc cao hơn duyệt vượt cấp"*, nhưng quét **toàn kho** 4 tên (camel+snake) trên `*.tsx *.ts *.mjs *.js *.sql` ra **9 chỗ — TẤT CẢ là SQL** ⇒ **UI không dùng**; Java chỉ **ghi/đọc/hiển thị** (5 chỗ), **không** nhánh quyết định nào đọc; form quản trị bước (`page.tsx:3834`) **chỉ có** `slaHours` + `approvalMode` ⇒ **không bao giờ gửi**. **Mức độ: THẤP (tiềm ẩn)** — hôm nay không gây hại (không ai đặt được qua UI), nhưng là **bẫy cấu hình**: thêm ô nhập sau này sẽ tạo "hạn chế im lặng không tác dụng". **Trả lời GOAL §8 chiều *permission*:** quyền của bước đến từ `allowed_role_codes` + pool (`workflow_step_approvers` ∪ owner) + quyền module — **KHÔNG** từ `required_permission`. **Củng cố lớp P4 là tính năng dở dang** — 4 phép đo độc lập hội tụ (JS 0 tham chiếu · version không hoạt động · 2 cột không thi hành · action quản trị Java-only) |
| TASK-037 | **GOAL §7** — rà cổng quyền của **12 action Java-only** (8 action có `module=[]` ⇒ mặc định TỪ CHỐI) | **DONE** (điều tra) | #43 (cùng lượt) | 18/09/2026 | **PHẦN 1 — CHÍNH ĐÁNG, 0 rủi ro:** cả **8/8** action `module=[]` đều đã được controller chặn bằng `requireRequireAdmin` ⇒ nhánh mặc-định-từ-chối **không chặn oan** chức năng nào (`tools/audit-java-only-actions.mjs`, exit 0). **PHẦN 2 — 1 cổng MONG MANH:** `save_workflow`/`set_workflow_status`/`delete_workflow` **KHÔNG** chặn theo vai trò, chỉ cần **đăng nhập** + quyền **module `admin`**; đo dữ liệu thật: **0 dòng quyền module `admin`** ⇒ hôm nay đúng là chỉ admin vào được, nhưng **nếu sau này ai được cấp module `admin`** thì quản trị được workflow mà không phải admin. **CHỜ BẠN QUYẾT:** (A) thêm chặn vai trò admin cho nhất quán, hay (B) giữ nguyên (coi đây là quyền module — đúng thiết kế) |
| TASK-038 | **GOAL §6** — đối chiếu action **UI GỌI** với backend Java (chiều chưa cổng nào kiểm) | **DONE** (điều tra) | #44 (cùng lượt) | 18/09/2026 | **0 điểm lệch:** `tools/probe-ui-action-coverage.mjs` (exit 0) — **155/155** action UI gọi đều có ở Java; **0** action UI gọi mà không backend nào có (nếu có ⇒ bấm nút sẽ lỗi); **0** action chỉ-JS. Các cổng cũ chỉ so JS↔Java↔danh mục, **chưa cổng nào so với mã giao diện**. **TỰ PHÁT HIỆN 2 LỖI TRONG CÔNG CỤ (loại làm phép đo YẾU đi mà vẫn báo ĐẠT):** (1) bản đầu trộn "GỌI" với "chỉ NHẮC TỚI" ⇒ báo 157 nhưng thực chỉ 15 lời gọi `requestApi(`; đã tách hai tập, kết luận chỉ dựa tập có bằng chứng; (2) regex cũ đòi chuỗi ngay sau dấu ngoặc nên **bỏ sót cả `create_project` lẫn `update_project`** trong `submit(editing ? "update_project" : "create_project", …)` — phát hiện vì `create_project` nằm trong danh sách "không thấy gọi" dù UI chắc chắn tạo được dự án; đã quét trọn **đối số thứ nhất** ⇒ 153→**155**. **GIỚI HẠN (ghi rõ):** còn **31 action Java** chưa thấy UI gọi, một số rõ ràng CÓ dùng (`cancel_request`, `ship_transfer_order`…) ⇒ đây là **CẬN DƯỚI**, **không** khẳng định đã kiểm hết, **không** kết tội 31 action kia là chết |
| TASK-039 | **3 chỗ lệch** làm 2 action admin lỗi **HTTP 500** + 1 cấu hình ghi được mà không đọc ra | **DONE** | #45 (cùng lượt) | 18/09/2026 | **Nguồn:** đọc **log Java đang chạy** (cách đã tìm ra TASK-025). **Lỗi 1:** `ui_display_settings` — câu lệnh dùng `id='UI'` và **thiếu `scope_key` + `created_at`** (đều NOT NULL không mặc định) ⇒ MySQL error 1364 ⇒ 500. **Lỗi 2:** `vntech_trust_settings` — INSERT cột **`settings_json` KHÔNG tồn tại** + dùng `id='TRUST'` trong khi hàng thật là `'TRUST-ROOT'` ⇒ 500. **Lỗi 3 (do CHÍNH PROBE của tôi bắt):** `BootstrapDataAdapter:594` đọc `scope_key='GLOBAL'` trong khi đường ghi + JS dùng `'company_default'` ⇒ **ghi được mà không bao giờ đọc ra**; quét toàn kho xác nhận `'GLOBAL'` chỉ xuất hiện ĐÚNG một chỗ này. **Không chỉ sai cú pháp SQL — Java port sai cả NGUỒN DỮ LIỆU lẫn NGHIỆP VỤ** (payload `{developerMode,…}` thay vì `licenseServerUrl`; thiếu chặn bật enforcement + thiếu bắt buộc HTTPS). **Đã port lại đúng JS.** **Kiểm chứng:** hậu kiểm bản vá **11/11** · compile **102 tệp/0 lỗi** · jar **90.884.756 bytes** · probe lúc chạy **10/10 exit 0** (200 + 400 đúng nghiệp vụ, và **đọc lại được** `{"id":"UI-company","scopeKey":"company_default",…}`). **An toàn dữ liệu:** probe **đọc giá trị hiện tại rồi ghi lại đúng giá trị đó** ⇒ chứng minh đường ghi mà không đổi cấu hình. **Bài học:** nếu chỉ kiểm "hết 500" thì đã **bỏ sót** lỗi 3 — phép kiểm **"ghi xong ĐỌC LẠI"** mới đủ |
| TASK-040 | **LỚP LỖI**: 26 câu lệnh SQL trong Java ghi **CỘT KHÔNG TỒN TẠI** ⇒ HTTP 500 lúc chạy | **CONFIRMED — đăng ký khuyết điểm** | #46 (cùng lượt) | 18/09/2026 | Phát hiện bằng công cụ mới `tools/probe-java-sql-schema.mjs` (đối chiếu SQL Java với **lược đồ MySQL thật** từ 16 migration Flyway = **120 bảng/1543 cột**, và **thêm** lược đồ drizzle/SQLite = 119 bảng, để phân biệt *lỗi mã* với *lệch lược đồ*). Quét **97 tệp Java**. **Phân loại: 26 lỗi mã · 0 lệch lược đồ · 0 bảng thiếu** ⇒ cột không có ở **CẢ HAI** lược đồ. **Đã kiểm chứng bộ phân tích bằng DDL thật của 5 bảng (khớp 5/5):** `approval_email_recipients` (thiếu `user_email/cc_emails/updated_by`) · `stock_issue_items` (thiếu `status`) · `vntech_license_transfer_requests` (thiếu `to_company_name/created_at`) · `material_norms` (thiếu `name/unit_rate/scope_project_id/description/approved_by/approved_at`; cột thật là `item_name/quantity_per_unit/project_id/notes`) · `system_level_catalog` (`level_rank` → cột thật **`rank`**). **7 tệp:** `AdminOpsStoreAdapter` · `MaterialCatalogStoreAdapter` · `ProductionStoreAdapter` · `SystemSettingsStoreAdapter` · `UserAdminStoreAdapter` · `WarehouseStockStoreAdapter`. **Bản sửa đã xác định từ nguồn sự thật JS** cho `approval_email_recipients` (JS dùng **MỘT** cột `emails`, Java tách đôi sai) · `material_norms` · `stock_issue_items` (JS cộng dồn `installed_qty`, Java lại `SET status` — **sai cả cột lẫn nghiệp vụ**). **Mức độ P1 (một số P0 với admin)**; **KHÔNG** sửa bằng cách thêm cột vào MySQL. **Chưa sửa mã ở vòng này** — sửa theo nhóm tệp, mỗi nhóm phải kiểm chứng lúc chạy |
| TASK-040 nhóm 1+1b | `save_email_settings` — SQL sai 3 cột ⇒ **HTTP 500**; bootstrap **thiếu 2 khoá ĐỌC**; **4 mặc định JS bị port sai** | **DONE** | #47 | 17/09/2026 | **Ba lỗi trong MỘT action (đều do port không nguyên trạng):** (1) `INSERT approval_email_recipients` ghi `user_email`/`cc_emails`/`updated_by` — cả ba **không có ở bất kỳ lược đồ nào** ⇒ `Unknown column` ⇒ 500; sửa về **MỘT** cột `emails` như JS `system-route.mjs:1615`, id `AREC` → **`MAILTO`**. (2) **Đường ĐỌC thiếu**: UI đọc `data.emailSettings` + `data.emailRecipients` (`app/page.tsx:31,3739`) nhưng `BootstrapDataAdapter` **không có hai khoá đó** ⇒ lưu được mà **mở lại màn hình thì trống** — dạng lỗi *ghi được mà không bao giờ đọc lại* **lần thứ ba** trong cùng tính năng. (3) **4 mặc định sai**: Java kẹp sàn `max(1,…)` **TRƯỚC** rồi mới so `== 0` ⇒ nhánh `|| 587`/`|| 24`/`|| 8` của JS thành **MÃ CHẾT** ⇒ lưu **1** thay vì **587/24/8**; `baseUrl` không bỏ `/` cuối; `senderEmail` chỉ đòi khác rỗng thay vì đòi **email hợp lệ**. **HƯ HẠI DỮ LIỆU THẬT (đã khôi phục):** DB đang có `smtp_port=1`, `po_sla_hours=1`, `bch_confirmation_sla_hours=1` — **không phải do seed** (seed ghi 587; DDL default 24/8) và **chỉ action này** ghi được 2 cột SLA ⇒ lỗi port **âm thầm sửa hỏng cấu hình thật** (PO quá hạn sau 1 giờ thay vì 24). **Kiểm chứng:** jar **90.885.239 bytes** · API PID **13800** · Flyway 16 migration · log **0 ERROR** · `probe-task040-nhom1.mjs` **13/13 exit 0** (gọi thiếu 3 trường ⇒ lưu 587 + `baseUrl` bỏ `/`; ghi người nhận tạm rồi **ĐỌC LẠI qua bootstrap**; 5 dòng phân công nguyên vẹn) · `probe-task040-nhom1-authz.mjs` **9/9 exit 0** (`nvdademo` ⇒ **403**; `emailSettings=null`, `emailRecipients=[]` cho non-admin; không rò rỉ trường mật khẩu) · `test:regression` **59/61** (baseline 58/61 ⇒ **một test đỏ nay xanh**, xác nhận kết luận "dương tính giả do môi trường") · MySQL sau vá **587/24/8**. **An toàn dữ liệu:** sao lưu `tools/_backup-task040-nhom1.sql` (đã gitignore) + gửi lại đúng 5 dòng phân công + chỉ tạo 1 bản ghi tạm rồi xoá. **Cần người dùng xác nhận** 24h/8h có đúng nghiệp vụ (câu hỏi #10) |
| TASK-040 nhóm 2 | `system_level_catalog.level_rank` — **DƯƠNG TÍNH GIẢ của công cụ** | **ĐÓNG — không cần sửa mã** | #49 | 17/09/2026 | Trước khi sửa, tôi đối chiếu `information_schema` của MySQL **đang chạy** và thấy `system_level_catalog` **có `level_rank`**, không có `rank` ⇒ mã Java **ĐÚNG**; nếu tin báo cáo công cụ thì đã **phá mã đang đúng**. **Nguyên nhân gốc:** `V10:42` tạo cột `` `rank` `` rồi **`V11__rename_level_rank.sql:24`** đổi tên bằng `ALTER TABLE … CHANGE COLUMN` — công cụ cũ **chỉ** áp dụng `ADD [COLUMN]`, **không** áp dụng `CHANGE/RENAME/DROP` nên bản đồ lược đồ giữ tên cũ. **Đã làm 3 việc:** (1) vá công cụ cũ + ghi rõ giới hạn; (2) thêm `tools/probe-java-sql-live.mjs` đối chiếu **lược đồ đang chạy** (kết xuất `tools/_live-schema.tsv` từ `information_schema`, **bắt buộc `--raw`** vì `--batch` escape tab); (3) thêm `tools/probe-schema-drift.mjs`. **Hai phương pháp ĐỘC LẬP hội tụ:** 26 → **22**. **Chứng thực chéo 6 bảng** bằng DDL thật (khớp 6/6). **`probe-schema-drift` = 0 lệch:** 120 bảng nghiệp vụ · 1543 cột ở **cả hai** phía ⇒ không có `ALTER` tay ngoài Flyway, và 22 phát hiện là **lỗi mã Java** |
| TASK-040 nhóm 3 | `material_norms` + `materials` — **ba lớp lỗi** trong một tính năng | **DONE** | #49 | 17/09/2026 | **Lớp SQL:** ghi `name`/`unit_rate`/`scope_project_id`/`description`/`approved_by`/`approved_at` — **6 cột không tồn tại** ⇒ 500. **Lớp HỢP ĐỒNG PAYLOAD:** Java đọc `name`/`unitRate`/`scopeProjectId` và **bắt buộc `normCode`**, trong khi form UI (`app/page.tsx:2519`) gửi `itemName`/`quantityPerUnit`/`projectId`/`baseUom`/`subcategoryId` và **không bao giờ** gửi `normCode` (JS tự sinh `DM-%04d`) ⇒ hỏng **ngay ở validate**, chưa tới SQL. **Lớp NGHIỆP VỤ:** `set_material_norm_status` — UI gửi `{normId, active:0\|1}` nhưng Java đọc `status`; JS ghi `active` + `status='active'\|'inactive'`. Thêm: `status='pending'` (JS: `'active'`), **bỏ sót** `subcategory_id`/`base_uom`/`source_component_id`/`source_type`, `updateNorm` **đổi cả `norm_code`** (JS không đổi). **Lớp ĐỌC:** bootstrap thiếu `source_type`/`source_component_id`/`created_by`/`createdByName` so với JS:697 ⇒ UI **luôn hiện "Thủ công"** — **chính probe của tôi bắt được** (sau khi vá SQL vẫn còn 2 mục HỎNG). **Kiểm chứng:** probe **25/25 exit 0** (payload đúng như `FormData` — mọi giá trị là chuỗi); cổng **22 → 11**. **Sửa cả một lỗi trong phép kiểm của tôi:** bản đầu dùng `Number(active)===0` mà `Number(false)===0` **luôn đúng** ⇒ có thể ĐẠT OAN; đã siết thành so sánh nghiêm |
| TASK-040 nhóm 4 | `confirm_installation` — cột không tồn tại **+ MỘT LỚP LỖI MỚI: ngữ nghĩa** | **DONE** | #50 | 17/09/2026 | **Lỗi 1:** `updateIssueItemStatusInstalled` ghi `stock_issue_items.status` — **không tồn tại** ⇒ 500, và **chỉ nổ ở lần xác nhận lắp CUỐI CÙNG** (nhánh `installedQty+quantity>=issueQty`) nên rất dễ lọt kiểm thử. JS `confirm_installation` (system-route.mjs:1520) **không đánh dấu trạng thái ở đâu cả** ⇒ đây là **hành vi tự thêm** ⇒ đã **XOÁ**, KHÔNG thêm cột vào MySQL. **Lỗi 2 (lớp NGỮ NGHĨA — công cụ lược đồ KHÔNG THỂ bắt vì cột có thật):** JS `SET installed_qty=installed_qty+?` (cộng dồn) vs Java `SET installed_qty=?` (ghi đè) ⇒ xác nhận 3 rồi 4: JS ra **7**, Java ra **4** — sai số liệu âm thầm, không lỗi HTTP, không log. **Công cụ mới** `tools/probe-increment-drift.mjs`: JS cộng dồn **10 cột** · Java ghi đè 485 cột · **giao = 0 ứng viên**, **có ĐỐI CHỨNG DƯƠNG** (`tools/_old-adapter-positive-control.java.txt` chứa nguyên văn câu lệnh cũ ⇒ công cụ BÁO; cây hiện tại ⇒ SẠCH). **Kiểm chứng SQL bằng TRANSACTION + ROLLBACK** (`tools/probe-task040-nhom45.sql`): câu CŨ ném `ERROR 1054 Unknown column 'status'`; câu MỚI cho **7.0000**; mô phỏng ghi đè cho **4.0000**; sau ROLLBACK **nguyên trạng** (5 dòng / installed 0). HTTP **9/9 exit 0** (3 nhánh chặn đúng nguyên văn JS + `nvdademo` 403). **GIỚI HẠN nói rõ: chưa test end-to-end — cần người dùng bấm nút.** |
| TASK-040 nhóm 5 | `settle_team_subcontract` — bỏ 2 cột không tồn tại | **DONE** | #50 | 17/09/2026 | Câu cũ ghi `settlement_id=?` + `settled_at=?` — **cả hai không tồn tại** trong `team_subcontracts` (15 cột thật) ⇒ 500. Cột `settled_at` thật ra thuộc **`team_settlements`** (Java **đã ghi đúng** ở `insertTeamSettlement`, cùng transaction); liên kết nằm ở phía `team_settlements.subcontract_id`. JS (system-route.mjs:1250) chỉ đặt `status='settled'` + `updated_at` ⇒ bỏ cột + bỏ tham số `settlementId` khỏi port. **LỖI CỦA CHÍNH TÔI ở vòng này:** probe đầu gọi `settle_subcontract` — **tên do tôi tự suy từ tên phương thức, KHÔNG tồn tại** — Java trả *"chưa được triển khai trên backend Java"* và tôi **suýt kết luận nhóm 5 là MÃ CHẾT**. Cổng mới `probe-action-coverage-controller.mjs` báo **0 action thiếu `case`** ⇒ mâu thuẫn ⇒ lộ ra tên đúng là **`settle_team_subcontract`** (`SystemController:637`). Đã sửa chú thích + probe; **không sửa lịch sử** (commit #50 đã ghi tên sai) mà ghi đính chính ở commit sau. **Cổng mới:** Java `case` **224** · JS action **174** · UI gọi **119** · **UI gọi mà Java thiếu: 0** · **JS có mã mà Java thiếu: 0** |
| TASK-040 nhóm 3b (phần 1) | `canonicalMeCode` — gom về **một nguồn sự thật**, vá lệch **7/28 đầu vào** với JS | **DONE** | #52 | 17/09/2026 | Khi chuẩn bị port `import_material_catalog` (nhóm 3b) tôi tra `canonicalMeCode` và thấy nó **đã có** ở Java (`BoqManagementUseCase:943`) nhưng **KHÁC JS**: JS khớp theo **TIỀN TỐ** (`raw.startsWith("DIEN")`, `"CTN"`, `"HVAC"`, `"PCCC"`, `"DNHE"`, `"ELV"`), Java chỉ so **ĐÚNG BẰNG** (`List.contains`). **Đo bằng công cụ mới** `tools/probe-canonical-me-code-drift.mjs` (mô phỏng **cả hai** thuật toán đọc từ mã nguồn): **7/28 đầu vào LỆCH** — `DIEN LUC`, `Điện lực`, `DIEN123`, `DIEN.TU`, `ELV-1`, `DNHE_2`, `PCCC-01` đều bị Java xếp `KHAC` thay vì đúng hệ M&E ⇒ **vật tư nhập vào mang `system` SAI** (ảnh hưởng cả nhập BOQ lẫn danh mục vật tư). **Đã làm:** tệp mới `domain/service/MaterialSystemCodes.java` (`canonicalMeCode` + `normalizeMaterialName` + `internalGroupCode`, port nguyên trạng JS kể cả nhánh `startsWith`), `BoqManagementUseCase` **uỷ quyền** cho helper chung (bỏ bản yếu), **unit test 5 ca** ghi lại **đúng 7 ca từng lệch** để không ai bỏ mất nhánh `startsWith`. **Kiểm chứng:** `mvn -pl domain test -Dtest=MaterialSystemCodesTest` → **5/5**; toàn module domain → **19/19**; `mvn -q -DskipTests compile` → exit 0; jar **90.887.539 bytes** (15:07:51) · API PID **19532** · cổng lược đồ vẫn **8** · `probe-task040-nhom3` vẫn **25/25**. **LỖI CỦA CHÍNH TÔI bị unit test bắt:** tôi viết `replaceAll("(?i)đ","d")` — cờ `(?i)` của Java **chỉ case-fold ASCII** nên **không khớp `Đ`** ⇒ mọi đầu vào tiếng Việt rơi về `KHAC`; đã sửa thành thay tường minh `đ`/`Đ`. Và một **kỳ vọng sai nữa của tôi** trong test: `"Ø 20"` → tôi tưởng `"d20"`, nhưng `\b` của **JS cũng** chỉ tính `[A-Za-z0-9_]` là ký tự chữ nên JS **cũng** cho `"20"` (việc đổi `ø` thành `" d "` nằm ở hàm khác) — đã sửa test theo đúng hành vi JS |
| TASK-040 nhóm 3b (phần 2) | `import_material_catalog` — UI gửi `categoryCode`, Java đọc `categoryId` ⇒ **vật tư nhập vào mất nhóm**; + **đường ĐỌC thứ NĂM** | **DONE** | #54 | 17/09/2026 | UI (`app/page.tsx:1909-1921`) gửi `categoryCode/categoryName/subcategoryCode/subcategoryName/code/name/unit/specification/brand/minStock`; bản Java cũ đọc `row.get("categoryId")`/`subcategoryId` — **khoá UI không bao giờ gửi** ⇒ mọi vật tư **MẤT NHÓM**, và `system` lấy từ khoá `"system"` cũng không được gửi ⇒ **luôn `KHAC`**. **Đã port đủ JS (system-route.mjs:2550-2606):** yêu cầu đủ **Mã + Tên + ĐVT**; `system = canonicalMeCode(category.code)` dùng helper chung #52; nhóm mới `sort_order=999` + mô tả *"Tạo từ file danh mục vật tư V5.0.0"*; nhóm con `999`, riêng `CHUA_PHAN_NHOM` là `9999` + *"Nhóm mặc định"*; **2 port method mới** `renameCategoryActive`/`renameSubcategoryActive` (vì `updateCategory`/`updateSubcategory` ghi đè `code`/`description`/`sort_order` và **không bật `active=1`**); thông điệp `Đã nhập/cập nhật N mã vật tư[; tạo mới X hệ M&E và Y nhóm con].` **LỖI ĐƯỜNG ĐỌC THỨ NĂM:** probe báo `brand`/`minStock` = `undefined` **dù DB đã ghi đúng** (`brand='brand probe'`, `min_stock=7`) ⇒ truy vấn `adminMaterials` chỉ trả 10 trường, thiếu 5 trường so với JS `system-route.mjs:696` ⇒ màn Quản trị **hiển thị thiếu hãng/tồn tối thiểu/quy cách**; đã sửa khớp JS (LEFT JOIN nhóm + `aliases`). **Kiểm chứng:** `probe-task040-nhom3b.mjs` **17/17 exit 0**; MySQL xác nhận `brand`/`min_stock=7`/`standard_price=0`/`requires_cocq=0`/`requires_mar=0` + `category_id`/`subcategory_id` đã gán; nhóm `sort_order=999 active=1`; nhập lại cùng mã ⇒ **1 dòng** (upsert đúng); 2 nhánh chặn đúng **nguyên văn**; dọn về **14/6/8**; regression **59/61** (2 lỗi cũ); log **0 ERROR**. **LỖI CỦA TÔI:** đặt `categoryCode="ZZPROBE040 DIEN LUC"` rồi kỳ vọng `system='DIEN'` — sai, vì `canonicalMeCode` bỏ ký tự không phải chữ-số nên mã thành `ZZPROBE040DIENLUC` (không bắt đầu bằng `DIEN`) ⇒ `KHAC` là **đúng**; đổi thành `DIENLUC-ZZPROBE040`. Và một lệnh `mvn` chạy sai thư mục (`no POM in this directory`). **Còn lại:** JS có `audit(...)` còn Java chưa ghi audit; phân quyền giữ nguyên cổng module (thuộc nhóm quyết định TASK-029) |
| TASK-041 | `save_approval_stage` / `set_approval_stage_status` — **sai hợp đồng payload** + **5 trường bị bỏ im lặng** + **3 quy tắc JS không thi hành** | **CONFIRMED — chưa sửa** (đã có kế hoạch) | — | 17/09/2026 | **Nguồn phát hiện:** cổng mới `tools/probe-write-map-drift.mjs` (so **bản đồ GHI** JS ↔ Java: 108 bảng mỗi phía · 1374 vs 1371 cặp `(bảng,cột)` · **17 bảng lệch**), có **đối chứng dương** bắt đúng `vntech_license_installations` (ca lệch đã biết ở nhóm 6) ⇒ phép đo có giá trị. **Ứng viên đáng nghi nhất về nghiệp vụ:** `approval_stage_catalog` — JS ghi `approval_mode`/`auto_approve_on_submit`/`sla_hours`/`sort_order` mà Java **không ghi**. Truy nguyên thì **lệch cả hợp đồng lẫn quy tắc**: (1) **UI (`app/page.tsx:3830-3834`) KHÔNG gửi `code`**, nhưng Java `OpsTaskManagementUseCase:323` **bắt buộc** `code` ⇒ action **luôn HTTP 400** trước khi tới SQL — **cùng lớp lỗi với nhóm 3**; (2) Java **bỏ im lặng 5 trường** (`description`, `sla_hours`, `approval_mode`, `auto_approve_on_submit`, `sort_order`) ⇒ **admin sửa SLA của bước duyệt, hệ thống báo thành công nhưng SLA KHÔNG đổi** (`OpsTaskStoreAdapter:280` chỉ ghi `name`/`stage_no`/`allowed_role_codes`); (3) Java còn **tự thêm** quy tắc `stageNo < 100` không có trong JS; (4) **3 quy tắc JS không được thi hành**: chỉ **một** bước được tự duyệt (`SET auto_approve_on_submit=0 WHERE id<>?`), **chặn đổi `stage_no`** khi bước đã có lịch sử duyệt, và với `set_approval_stage_status`: **chặn tắt** bước đang có hồ sơ chờ + **không cho tắt bước cuối cùng** (`Hệ thống phải có ít nhất một bước phê duyệt đang hoạt động.`) — hai chốt này chính là **hàng rào** cho rủi ro "hồ sơ kẹt" mà TASK-035 ghi là `LIKELY`; (5) JS đồng bộ tên bước sang `approvals.department` cho hồ sơ đang chờ + ghi `audit`, Java không. **Đã đăng ký kế hoạch 5 bước ở `docs/agent-progress/TASK-041.md`; CHƯA sửa** vì phải đổi hợp đồng payload + ~7 phương thức port + probe có tạo/sửa bước duyệt thật (cần lượt riêng để kiểm chứng đầy đủ theo GOAL §11). **Còn 16 bảng lệch chưa rà** — đáng nghi tiếp: `material_subcategories` (`adjustment_note`/`review_status`/`scope_examples`), `project_archives.purge_audit_id` |
| TASK-041 (triển khai) | `save_approval_stage` + `set_approval_stage_status` — port đủ hợp đồng payload, 5 trường, 3 quy tắc | **DONE** | #57 | 17/09/2026 | **Đã sửa 3 tầng:** (1) **hợp đồng payload** — bỏ yêu cầu `code` (bảng không có cột này; UI không gửi) và **gỡ luật `stageNo < 100` tự thêm**, validate đúng theo JS (`name` + ít nhất 1 vai trò + mọi vai trò phải có trong `role_catalog`); (2) **ghi đủ trường** — `insertApprovalStage` **12 cột** (bỏ `null` cứng cho `description`), `updateApprovalStage` **8 trường**; (3) **3 quy tắc chặn** + 4 thông điệp nguyên văn JS. **GỠ 2 phương thức tự phát minh** `findApprovalStageCatalog`/`stageCodeExists` (chúng coi `code` như `stage_no`) và thêm **8 phương thức port** theo JS. **Kiểm chứng:** `probe-task041.mjs` **21/21 exit 0** — tạo bước tạm `stageNo=900` bằng **đúng payload UI** (không `code`) ⇒ 200 nguyên văn; `description`/`approvalMode`/`sortOrder`/`slaHours` đọc lại đúng; **SỬA SLA 8 → 33 RỒI ĐỌC LẠI = 33** (đây chính là lỗi cũ: báo thành công nhưng SLA không đổi); chốt tự duyệt (bước không phải đầu) · chốt đổi `stage_no` khi bước 1 **đã có 100 bản ghi lịch sử** (giữ nguyên số bước) · chốt tắt bước 1 **đang có 7 hồ sơ chờ** — cả 3 trả **400 đúng nguyên văn**; dọn về 5 bước. `probe-task041-last-stage.mjs` **7/7 exit 0** — dựng kịch bản 1 bước hoạt động rồi tắt bước cuối ⇒ 400 *"Hệ thống phải có ít nhất một bước phê phê duyệt đang hoạt động."* và bước đó **tự bật lại**; script tự khôi phục trong `finally`. **Đối chiếu dữ liệu:** 4/5 dòng `approval_stage_catalog` **giống hệt** bản sao lưu, dòng 5 **chỉ khác `updated_at`** (hệ quả của phép kiểm chốt cuối) — mọi trường nghiệp vụ nguyên vẹn. **LỖI CỦA TÔI:** probe đầu dùng `allowedRoleCodes:["admin"]` — `admin` không có trong `role_catalog` và UI **lọc bỏ** nó (`page.tsx:3833`), JS cũng từ chối; và **lặp lại lỗi chạy `mvn` sai thư mục lần thứ hai**. **Còn lại:** JS có `audit(...)` còn Java chưa; phân quyền giữ nguyên cổng module; `delete_approval_stage` **chưa rà** |
| TASK-042 | `retry_email` — SQL truy vấn **BẢNG KHÔNG TỒN TẠI** (`email_queue`) ⇒ **HTTP 500**; + **cổng mới soi bảng trong `FROM`/`JOIN`** | **DONE** | #61 | 17/09/2026 | **Nguồn phát hiện:** đọc **log Java đang chạy** khi bấm thử chức năng (cùng cách đã tìm ra TASK-025/039). `SystemSettingsStoreAdapter.retryEmailQueue` chạy `SELECT COUNT(*) FROM email_queue WHERE status IN (...)` — bảng thật là **`email_outbox`** (V-migration) ⇒ `Table 'email_queue' doesn't exist` ⇒ 500. **Sửa theo đúng JS** (`system-route.mjs`): JS **không** quét hàng loạt mà **xếp lại theo TÊN email** ⇒ đổi hợp đồng port `retryEmailQueue(int, Instant)` → **`requeueEmail(String emailId, Instant now)`** với `UPDATE email_outbox SET status='queued', next_attempt_at=?, last_error=NULL, updated_at=? WHERE id=?`; `SystemSettingsUseCase.retryEmail` trả **nguyên văn** *"Đã xếp lại email để máy chủ gửi."*. **LỚP LỖI MỚI ĐƯỢC ĐẶT TÊN:** cổng cũ chỉ soi **cột** ⇒ **không thể** bắt lỗi *bảng không tồn tại*; đã **mở rộng `tools/probe-java-sql-live.mjs`** để soi cả bảng xuất hiện trong **`FROM`/`JOIN`**. **Cổng mới báo 6 bảng không tồn tại — 5 là DƯƠNG TÍNH GIẢ:** `b`, `r`, `owned`, `ids`, `descendants` đều là **tên CTE** (`WITH`/`WITH RECURSIVE`); phải sửa công cụ **3 lần** (bản 1 chỉ bắt CTE đầu tiên; bản 2 vẫn sót `WITH RECURSIVE descendants(id) AS (`) rồi mới còn **đúng 1 phát hiện thật**. **Nếu tin bản đầu thì đã đi sửa 5 câu SQL ĐANG ĐÚNG.** **Kiểm chứng:** `tools/probe-task042-retry-email.mjs` **9/9 exit 0** — tự dựng 1 hàng tạm `EMAIL_ZZP42` trong `email_outbox`, gọi action ⇒ 200, **ĐỌC LẠI** thấy `status='queued'`/`last_error=NULL`/`next_attempt_at` đã đặt, rồi **tự dọn sạch**; jar **90.891.800 bytes** · API PID **11404** · log **0 ERROR** · regression **59/61** (2 lỗi cũ) · cổng lược đồ sống **8 phát hiện — TẤT CẢ thuộc nhóm 6** (`vntech_license_*`, chờ người dùng chốt câu hỏi #11). **Phát hiện kèm (chưa xử lý):** (a) **Java KHÔNG có cơ chế gửi email nào cả** ⇒ `email_outbox` **không bao giờ được INSERT** (xếp lại một email không tồn tại là vô nghĩa cho tới khi port bộ gửi); (b) **3 bảng khác** cần rà `custom_field_values` (Java **chỉ đọc/xoá, không bao giờ ghi** ⇒ giá trị trường động bị mất), `material_code_history`, `project_archives.purge_audit_id`; (c) **`users.avatar_url` là DƯƠNG TÍNH GIẢ** — Java ghi qua **JPA** (`AuthUseCase.updateProfileAvatar` → `userRepository.save`) nên **không có văn bản SQL** để soi ⇒ bài học: cổng chỉ soi được SQL thô. **Công cụ mới kèm theo:** `tools/probe-write-map-triage.mjs` phân loại 17 bảng lệch bản đồ GHI theo tiêu chí *action ghi có `case` ở Java hay không* ⇒ **9 bảng "THIẾU PORT THẬT"**, **0 bảng "chưa port"** |
| TASK-043 | `custom_field_values`: Java **chưa bao giờ GHI** trường động của dòng phiếu **và ĐỌC sai chỗ** (id PHIẾU thay vì id DÒNG) | **DONE** | #62 | 17/09/2026 | **Hai lỗi NGƯỢC CHIỀU trong cùng một tính năng.** (1) **ĐƯỜNG GHI:** JS `create_request` (`system-route.mjs:974`) ghi `custom_field_values` cho **từng dòng** (`form_key='request_line'`, `entity_id=id DÒNG`); Java **không có một câu lệnh nào** — dù chú thích của `RequestStore.insertRequest` đã ghi *"…+ custom fields…"* từ lâu. (2) **ĐƯỜNG ĐỌC:** JS (`:563-568`) tra bằng **id DÒNG** rồi gắn `customFields` lên **từng dòng**; Java tra bằng **id PHIẾU** rồi gắn lên **phiếu** ⇒ truy vấn **không bao giờ khớp** (entity_id thật là `MRI_…`) ⇒ `requests[].customFields` **luôn `{}`**, mỗi dòng **không có khoá** mà UI đọc (`page.tsx:3543` → `savedRequestDocument` dùng cho **xuất Excel** + màn chi tiết). **Đây là lần thứ BẢY** dự án gặp lớp lỗi *"đường ĐỌC thiếu/sai"*, và nó nằm **cùng tính năng** với lỗi GHI nên **không thể** phát hiện bằng "ghi xong không lỗi HTTP". **Đã sửa:** port `customFieldsObject` + `cleanValue` (nguyên văn JS, kể cả `String(1.0)="1"`), gom `customFieldRows` id `CFV_<uuid>`, `INSERT … ON DUPLICATE KEY UPDATE`; đổi truy vấn đọc sang **id DÒNG**, **bỏ** khoá `customFields` cấp phiếu (JS không có); **xoá câu vô nghĩa** `header.forEach((k,v) -> { })`; port thêm **`routeTag`** (trước luôn NULL vì `nl` không chứa khoá này). **Kiểm chứng `tools/probe-task043-custom-fields.mjs` 26/26 exit 0** — tạo **phiếu THẬT** bằng payload nghiệp vụ (4 dòng) rồi **tự dọn sạch** (8 bảng về đúng số dòng trước khi chạy); có **ĐỐI CHỨNG DƯƠNG**: truy vấn CŨ theo id PHIẾU trả **0** dòng; bỏ đúng trường rỗng/null, **không** lưu khoá trong chuỗi JSON/mảng; 3 dòng không khai báo vẫn có `customFields={}` (không phải `undefined`). jar **90.893.269 bytes** · API PID **2016** · log **0 ERROR** · regression **59/61** (2 lỗi cũ). **LỖI CỦA CHÍNH TÔI:** (a) chạy `SELECT … FROM approval_project_assignments` với cột `stage_no` (cột thật `stage`) **và nuốt stderr `2>$null`** ⇒ thấy rỗng rồi **suýt kết luận "MySQL chưa cấu hình phân công nào"** (thật ra có **5 dòng thật**; probe tự báo `TRƯỚC: assignments=5` nên lộ ra); (b) so `customFields` bằng `JSON.stringify` ⇒ **phụ thuộc thứ tự khoá** ⇒ báo HỎNG oan. **Còn lại (đã đăng ký):** Java thiếu `audit(...)` toàn luồng phiếu đề nghị · Java yếu hơn JS 3 phép kiểm Owner · `delete_request` để rác `procurement_allocations`+`custom_field_values` · trường động `request_header` không lưu ở **cả hai** phía |
| TASK-044 | Bootstrap Java **bỏ sót 11 trường** của dòng phiếu ⇒ UI luôn hiện trống/0 | **DONE** | #62 (cùng jar/probe) | 17/09/2026 | Phát hiện khi dò tiếp hợp đồng ĐỌC của màn Phiếu đề nghị (sau TASK-043). UI `app/page.tsx:3609` hiển thị trực tiếp: `workPackageCode` · `boqCode` · `installationArea` · `contractLineNo` · `pendingBchQty` · `closeReason` · `remainingQty` · `rejectedQty` · `linkedPoCount` · `linkedReceiptCount` · `missingDocumentCount`. Java **không trả trường nào** trong số này ⇒ các cột **luôn trống/0**; đo trên dữ liệu THẬT (34 dòng thuộc phiếu): `remainingQty=975` · `pendingBchQty=340` · `linkedPoCount=12` · `linkedReceiptCount=10` — giao diện hiển thị **0** ở tất cả. Đã port đủ 11 trường (gồm 4 truy vấn con) **nguyên văn JS**. **Kiểm chứng trong cùng probe:** 34 dòng đều có **đủ 11 khoá**; đối chiếu tổng bootstrap ↔ SQL **khớp tuyệt đối** — và chính phép đối chiếu này **phát hiện 1 dòng mồ côi THẬT** (`MRI_d1f57f9d…` trỏ tới `MR_46cee316…` **không tồn tại**, có liên kết PO/BCH thật) — đã ghi thành known issue #35, **KHÔNG tự sửa dữ liệu**. Đây là **dạng thứ tám** của lớp lỗi *"đường ĐỌC thiếu"* |
| TASK-045 | `save_material`: Java **XOÁ HỆ M&E** (`system='KHAC'`) mỗi lần admin lưu mã vật tư + **không ghi** `material_code_history` + bỏ `minStock` | **DONE (phần 1)** | #63 | 17/09/2026 | **Nguồn:** rà `material_code_history` (mục "Rà tiếp") — cổng bản đồ GHI báo *JS ghi 0/Java ghi*, `grep` toàn kho Java **không một dòng mã nào** nhắc bảng này. Đọc tiếp thì lộ **lỗi nặng hơn cả lỗi đang tìm**: Java lấy `system` từ `payload.system` trong khi UI (`app/page.tsx:3769`) **KHÔNG BAO GIỜ gửi khoá này** — JS tính `system = canonicalMeCode(category.code)` (`:2631`) ⇒ **mỗi lần admin lưu một mã vật tư, Java ghi `system='KHAC'` và XOÁ hệ M&E thật** — API trả **200**, không log, không ai biết. **BẰNG CHỨNG LÚC CHẠY trước khi vá** (probe `tools/probe-task045-save-material.mjs`, dựng mã TẠM `MAT_ZZP045` nhóm DIEN rồi gọi bằng ĐÚNG payload UI): `system` DIEN → **`KHAC`** · `min_stock` **0** dù gửi 7 · thiếu ĐVT ⇒ **409** *"Dữ liệu vi phạm ràng buộc…"* (Java ghi `unit=NULL`) thay vì 400 nguyên văn JS · `categoryId` không tồn tại ⇒ **200** (ghi tham chiếu rác) · đổi mã gốc **thiếu lý do** ⇒ **200 + mã ĐỔI THẬT** · `material_code_history` **0 dòng**. **Đã port JS `:2607-2643`:** đòi đủ **4 trường**; kiểm nhóm tồn tại; nhóm con mặc định `CHUA_PHAN_NHOM` (**tự tạo nếu thiếu**) + kiểm nhóm con thuộc nhóm; **`system = canonicalMeCode(category.code)`**; ghi `min_stock`; đổi mã ⇒ **buộc có lý do** + ghi lịch sử; adapter thêm `min_stock` vào `INSERT/UPDATE`, thêm `insertCodeHistory`, và `mergeMaterialMaster` **chuyển lịch sử sang mã đích** (JS `:2670`). **Kiểm chứng: 14/14 ĐẠT exit 0** (trước khi vá **6/14** — chính probe là **đối chứng dương**); jar **90.894.035 bytes** (17:10:59) · API PID **19364** · log **0 ERROR** · regression **59/61** · cổng lược đồ **8 phát hiện (toàn bộ nhóm 6)**. **LỖI CỦA TÔI bị probe bắt ngay:** `findSubcategory` dùng `SELECT *` nên khoá trả về là **tên cột thật `category_id`** (không phải `categoryId`) ⇒ bản đầu **chặn oan payload đúng của UI** (400 *"Nhóm con không thuộc hệ M&E đã chọn."*); đã đọc cả hai dạng khoá. **DỮ LIỆU:** `materials.system` lệch **5/14** mã — `DIEN-DAY-CAD-001`/`CTN-ONG-NHUA-001` (`KHAC`) khớp cơ chế lỗi (nguồn ghi **không xác định được**, `save_material` không có audit), 3 mã khác nghi **dữ liệu mẫu hoán vị**; **KHÔNG tự sửa** — sau khi vá, admin mở và **LƯU lại** là `system` tự suy đúng từ nhóm. **Phần 2 chưa port (đã đăng ký):** rebuild alias theo `aliasText`, luật trùng tên gốc/alias, Java tự tạo alias bằng chính tên gốc, **thiếu `audit(...)`**, văn bản thông điệp khác JS, và `merge_material_master` (JS **xoá** mã nguồn, Java chỉ `active=0` + đổi mã `<code>_X`) |
| TASK-047 | `save_material` phần 2: alias theo `aliasText` + 2 luật trùng tên + `audit(...)` + thông điệp JS; phát hiện **hai bộ chuẩn hoá tên khác nhau** | **DONE** | #68 | 17/09/2026 | Port nguyên trạng JS `system-route.mjs:2634-2643`: (1) **rebuild alias** — JS `DELETE FROM material_aliases WHERE material_id=?` rồi INSERT lại từ `aliasText` (tách `[;\n]+`, bỏ rỗng, bỏ trùng tên gốc, bỏ trùng nhau giữ bản đầu); Java **không hề đọc `aliasText`** và còn **tự tạo alias bằng chính tên gốc** khi tạo mã mới (JS không) ⇒ đã thêm `deleteAliasesForMaterial` + ghi lại theo `LinkedHashMap<normalized,alias>`, **bỏ** alias tự-tạo. (2) **luật trùng TÊN GỐC** và **luật trùng TÊN TƯƠNG ĐƯƠNG** — 400 **nguyên văn JS** (*"Tên gốc “…” đã thuộc hoặc tương đương mã …"*, *"Tên tương đương “…” đang thuộc mã …; không được ghép hai vật tư khác thông số."*), kiểm **TRƯỚC khi xoá alias** nên nhánh chặn không mất dữ liệu. (3) **`audit(...)`** cho `CREATE`/`UPDATE` (trước đây **không có** — known issue #38): `before` = JSON cả dòng vật tư, `after` = đúng 6 khoá JS; tiêm `AuditLogPort` vào use case + tệp mới `application/support/MiniJson.java` (JSON.stringify viết tay, escape đầy đủ). (4) **thông điệp nguyên văn JS** *"Đã lưu mã gốc {code} · {name} với N tên tương đương."* **LỖI PARITY DO PROBE BẮT:** Java dùng `MaterialMatcherV2.normalizeMaterialText` (**giữ dấu chấm**) còn JS `normalizeMaterialName` kết thúc bằng `replace(/[^a-z0-9]+/g," ")` ⇒ `normalized_name` lệch (`cap cu 2x2.5` vs **`cap cu 2x2 5`**) ⇒ alias nhập ở hệ này không khớp hệ kia; đã chuyển sang helper `MaterialSystemCodes.normalizeMaterialName` (port nguyên trạng JS ở #52). **Kiểm chứng: probe-task045 (nay phủ cả TASK-047) 18/18 exit 0** — thông điệp đúng, alias `[["Bê tông M300","be tong m300"],["Cáp Cu 2x2.5","cap cu 2x2 5"]]`, audit có dòng `UPDATE`/`material` với `after_json` hợp lệ, **chặn ghép trùng** trả 400 nguyên văn và **alias giữ nguyên**, 14 phép kiểm TASK-045 không hồi quy, dọn về **14/19/14**. jar **90.899.353 B** (17:53) · log **0 ERROR**. **LỖI CỦA TÔI:** lại dùng `Set-Content` sửa mã Java ⇒ PowerShell ghi **UTF-8 kèm BOM** ⇒ build đứt `illegal character: '\ufeff'`; đã sửa bằng node (không BOM) — **cấm dùng `Set-Content` cho mã/tài liệu**; và chạy `node -e` với đường dẫn đã gồm `java-backend/` trong khi `workdir` cũng là `java-backend` ⇒ ENOENT giả. **Còn lại:** `merge_material_master` (JS **xoá** mã nguồn, Java chỉ `active=0` + đổi mã `<code>_X`) |
| TASK-048 | NHẬT KÝ KIỂM TOÁN luồng Phiếu đề nghị — **5/6 mốc DONE**; khe hở audit toàn hệ thống **128** action (không phải 152 — số cũ là **hiện vật của cổng đo sai**) | **DONE (5/6)** | **#87** (đo: #69 · hồ sơ: #70) | 17/09/2026 | **Đã port nguyên văn** `CREATE` (`:980`) · `EDIT_RETURNED` (`:994`) · `RESUBMIT` (`:1019`) · `DELETE_RETURNED` (`:1030`, **audit TRƯỚC khi xoá** đúng thứ tự JS) · `CANCEL` (`:1062`) vào `RequestManagementUseCase` (+ nối `AuditLogPort` trong `ApplicationBeansConfig`). Tránh đúng **2 bẫy** của bài học #13: `EDIT_RETURNED.after.priority` dùng giá trị **THÔ** của payload (không phải `"normal"` đã mặc định hoá) và `RESUBMIT.after.comment` dùng `clean(payload.comment)` **THÔ** (không phải `"CHT GỬI LẠI: …"`). **Cổng mới `tools/probe-task048-audit-requests.mjs` 18/18 ĐẠT**: LÁI ĐÚNG luồng (tạo → Owner từ chối → sửa → gửi lại → từ chối → hủy; tạo → từ chối → xoá), kiểm **chuỗi audit đúng thứ tự** `CREATE → EDIT_RETURNED → RESUBMIT → CANCEL`, kiểm `DELETE_RETURNED` **sống sót sau khi phiếu bị xoá**, kiểm `before_json` 5 trường `mr`; **dọn sạch có kiểm chứng** (17/35/100/0/0/91/0 dòng — `audit_logs` append-only nên giữ lại làm bằng chứng); **đối chứng dương trước khi vá**: `entity_type='material_request'` = **0 dòng**. **SỬA 2 CỔNG ĐO SAI:** (1) `probe-task043-custom-fields.mjs` gọi `q1()` không tồn tại ⇒ phép kiểm audit **CHƯA BAO GIỜ CHẠY** (dòng "19/20" là do probe dừng sớm) — nay **27/27**; (2) `probe-audit-coverage.mjs` tra tên **BIẾN** camelCase vào bảng khoá theo tên **LỚP** PascalCase ⇒ không bao giờ khớp ⇒ khe hở luôn = đúng số action JS có audit (**152 — hằng số giả**) — nay thêm **đối chứng dương `186/186`** và số ĐÚNG: **khe hở 128** · **cả hai đều ghi 24** · Java **3/21** lớp có audit. **Mốc thứ 6** `APPROVE_PARTIAL` ⇒ **TASK-054** (audit nằm GIỮA 3 hành vi chưa port). **Phát hiện kèm (bằng chứng sống cho known issue #36/D1):** sau `delete_request`, còn **1 dòng `procurement_allocations` mồ côi** (truy được theo `reference_no`) — probe dọn và ghi lại rõ |
| TASK-054 | Nhánh duyệt **SONG SONG `all_roles`**: port đủ **9 hành vi** JS `:1087-1108` + audit `APPROVE_PARTIAL` | **DONE — kiểm chứng 20/20** | **#89** (hồ sơ ban đầu: #87 — **có kết luận SAI, xem cột ghi chú**) | 17/09/2026 | **Đã port:** `required` (từ `allowed_role_codes`) · `approved` (DISTINCT `role_code`, thêm `RequestStore.stageDecisionRoles`) · **nhánh ADMIN điền vai trò còn thiếu** (JS `:1091` — Java trước đây **thiếu hẳn**) · chặn trùng vai trò · ghi `approval_stage_decisions` · `missing` + cập nhật comment tiến độ (`updateApprovalComment` — **chỉ đổi comment**, đúng JS `:1100`) · **RETURN SỚM không chuyển bước** · **audit `APPROVE_PARTIAL`** (mốc thứ **6/6** của TASK-048) · nhánh `all_roles` + **từ chối** ghi 1 dòng quyết định vai trò (JS `:1105-1108`). **Cổng mới `tools/probe-task054-all-roles.mjs` 20/20 ĐẠT** (node exit 0): láI thật tới bước 5 → xác nhận lần 1 ⇒ hồ sơ **VẪN `pending_approval`**, **VẪN bước 5**, đúng 1 dòng quyết định `da_truong` (**nhánh ADMIN**), comment `Đã xác nhận 1/2; còn chờ: kh_truong`, audit với `after_json={"stage":5,"stageName":"…","roleCode":"da_truong","missing":["kh_truong"]}` → xác nhận lần 2 ⇒ đủ 2/2 mới hoàn tất + sinh bước chờ lập PO; dọn sạch có kiểm chứng (kể cả `supply_workflow_steps`/`stock_reservations`). **⚠️ ĐÍNH CHÍNH:** kết luận ở #88 ("Java chuyển bước sớm, JS thì không") là **SAI** — JS `:1080` dùng **đúng câu truy vấn** ưu tiên snapshot như Java, và JS `:976` ghi snapshot `single` cho **mọi bước có Owner** ⇒ **21/21 dòng bước `all_roles` bị ĐÓNG BĂNG** ⇒ nhánh là **mã chết ở CẢ HAI LÕI**. Việc port là **thật và cần thiết** (Java thiếu mã) nhưng **không** sửa được hành vi nghiệp vụ ⇒ chuyển thành **TASK-055 / D6**. Probe phải tự dựng **fixture** (`approval_mode_snapshot='all_roles'` trên chính dòng của phiếu probe) mới kiểm được nhánh vừa port |
| TASK-055 | `approval_mode_snapshot` **ĐÓNG BĂNG** chế độ duyệt ⇒ bước `all_roles` **KHÔNG BAO GIỜ có hiệu lực** | **CONFIRMED — CHỜ NGƯỜI DÙNG CHỐT (D6)** | #89 (phát hiện) | 17/09/2026 | **Đo:** bước 5 cấu hình `all_roles` (`da_truong,kh_truong`) nhưng **21/21** dòng `approvals` của bước này có `approval_mode_snapshot='single'` ⇒ **chỉ cần MỘT vai trò xác nhận**. **Cơ chế (GIỐNG NHAU ở cả hai lõi):** JS `:976` ghi `assignedOwner ? "single" : (clean(stage.approvalMode) || "single")` ⇒ hễ bước có Owner phân công là snapshot cứng `single`; JS `:1080` (và Java, cùng câu SQL) **ưu tiên snapshot** hơn cấu hình hiện tại ⇒ sửa cấu hình/SLA **không** giúp. **Hệ quả:** ràng buộc "2 vai trò cùng xác nhận" ở bước cuối **không có hiệu lực**; **7 phiếu đang chờ ở bước 5** (`DNMH-…-0010/0017/0018/0020/0021/0023/0024`). **Sửa sẽ LỆCH KHỎI JS ⇒ cần quyết định nghiệp vụ.** Ba hướng (A giữ nguyên / B sửa cả hai phía / **C chỉ sửa cho phiếu mới** — khuyến nghị) ở `docs/agent-progress/TASK-055.md` |
| TASK-050 | **4 KHOÁ BOOTSTRAP THIẾU** — `centralInventory` · `centralReturns` · `constructionDailyLogItems` · `companyAvailability` **+ CẢ KHỐI bộ lọc quyền** | **DONE — đã port, kiểm chứng 87/87** | **#85** | 17/09/2026 | **Đã port nguyên trạng 4 truy vấn ĐỌC của JS** (`:620`, `:621`, `:659`, `:712`) vào `BootstrapDataAdapter`, kèm `items` lồng cho `centralReturns` và `aliasText` cho `centralInventory`. **Phát hiện lớn hơn đặc tả:** Java **không có** khối xoá-trắng-theo-module `system-route.mjs:728-737` (7 nhóm module + nhóm chỉ-admin) và **không có** bộ lọc theo **vai trò kho-site** `:622` ⇒ đã port cả hai. **Kèm nhánh thứ ba của `:684`** (`isCompanyLeadership` → mọi module trừ `admin`, `permissionSource="company_leadership"`) vì bộ lọc lấy chính danh sách đó làm đầu vào (thiếu nó thì `thukydemo` bị **xoá trắng oan** — đo trước khi vá: 0/5 nhóm còn dữ liệu). **Cổng mới `tools/probe-task050-bootstrap.mjs`: 87/87 ĐẠT** (đối chiếu số dòng với **truy vấn MySQL độc lập**, shape cột với alias chép từ JS, 3 tài khoản thật + **đối chứng dương**; 10 phép đo **không thực hiện được** vì dữ liệu rỗng — nói rõ, không tính ĐẠT). **ĐÍNH CHÍNH mức ảnh hưởng:** UI chuẩn hoá 3 khoá về `[]` (`page.tsx:393-395`) và `(data.constructionDailyLogItems||[])` (`:2530`) ⇒ **không màn hình nào crash**, hậu quả là bảng rỗng/cột 0; **trên dữ liệu hiện tại** chỉ `companyAvailability` (4 dòng) và `constructionDailyLogItems` (2 dòng) bị che thật, còn `centralInventory`/`centralReturns` **rỗng sẵn trong DB**. Cổng tĩnh nay còn **1 khoá** (`user` = dương tính giả). Regression **59/61** (không đổi). **Phát hiện kèm:** 5 dòng `user_project_scopes` **mồ côi** (D5) · `transferOrders` thiếu 7 trường + thiếu lọc theo kho (`:714`) · `engineRoleProfiles` = `null` cho **mọi** tài khoản (lỗi **thứ tự gán** dòng 726 ↔ 826) |
| TASK-051 | `data.user` **thiếu 2 trường** (`warehouseScopeKind`, `mustChangePassword`) ⇒ modal **bắt buộc đổi mật khẩu KHÔNG BAO GIỜ mở** | **DONE** | **#85** | 17/09/2026 | JS `system-route.mjs:181/189` trả **10 trường**, Java (`SystemController:158-166`) chỉ **8**. UI đọc cờ tại `app/page.tsx:565` để mở `"forcePassword"` — và đó là **cửa DUY NHẤT** (`grep mustChangePassword` toàn `app/` = **1 dòng**; UI **không** đọc cờ từ phản hồi `login` dù Java có trả) ⇒ trước khi vá, cơ chế bắt buộc đổi mật khẩu **chết hoàn toàn**. **Mức ảnh hưởng đo được trên dữ liệu hiện tại: 0 tài khoản** (`COUNT(*) WHERE must_change_password=1` = 0) ⇒ **lỗi tiềm ẩn**, ghi trung thực. Vá bằng 2 dòng `userMap.put` (dữ liệu đã có sẵn trong `AuthUseCase.CurrentUser:63-65`). Chi tiết: `TASK-051.md` |
| TASK-052 | **LỚP LỖI #4**: cột `tinyint(1)` của MySQL trả về **Boolean** ⇒ `modulePermissions` của admin **LUÔN RỖNG** | **DONE** | **#85** | 17/09/2026 | `activeVal instanceof Number` **luôn sai** với `module_catalog.active` ⇒ nhánh admin trả **0 dòng** dù JS trả **61** (đo: `MODULE_KEYS` JS = 61 = `module_catalog WHERE active=1`, **0 lệch cả hai chiều**; `data.moduleCatalog[0].active` trong JSON là **`boolean`**). **Bị tầng UI che:** `app/page.tsx:548` trả toàn quyền cho admin **trước khi** đọc danh sách. **Nhánh Ban giám đốc của TASK-050 dùng cùng đoạn mã ⇒ nếu không vá cùng lượt thì `thukydemo` bị xoá trắng** (đã đo). Vá bằng helper `isActiveOne` **theo đúng quy ước 5 use-case khác** (`AdminSystemUseCase:527`, `ProjectContractUseCase:101`, `PurchaseManagementUseCase:405`, `RequestManagementUseCase:598`, `UserManagementUseCase:596`). Sau vá: admin **61 dòng**, thukydemo **60 dòng**. Chi tiết: `TASK-052.md` |
| TASK-053 | `constructionDailyLogs` **thiếu 2 CỘT** (`itemCount`, `completedQty`) ⇒ cột "Khối lượng" của Nhật ký thi công **luôn 0** | **DONE** | **#85** | 17/09/2026 | Phát hiện khi đối chiếu **nguyên văn** truy vấn hai phía trong lúc port TASK-050: JS `:658` trả **27 cột**, Java `BootstrapDataAdapter:1018` chỉ **21** (thiếu `itemCount`, `completedQty`, `cancelledBy`, `cancelledAt`, `createdBy`, `createdByName`). UI đọc **cả 2 cột** tại `app/page.tsx:2548` (`l.itemCount||items.length`, `money(l.completedQty)`) ⇒ dữ liệu thật bị che (1 nhật ký · 2 dòng · `completed_qty` = 61). 4 cột `cancelled*`/`created*` **không** được UI đọc ⇒ **không** đưa vào diff (giữ nhỏ, có kiểm chứng). Probe mục 5: **có 2 cột** + **khớp MySQL theo TỪNG DÒNG**. Chi tiết: `TASK-053.md` |
| TASK-072 | **MASTER TASK §2.1 (`P-04`) — ÁP DỤNG THẬT `ApprovalTimeline` VÀO CHI TIẾT PHIẾU (§8.1)** | **DONE — 5 cổng xanh · 0 ảnh lệch do thay đổi này** | chờ commit | 17/09/2026 | **Vì sao chọn việc này:** sau 10 task vá lỗi port liên tiếp, chuyển sang **thực thi MASTER TASK**. Chọn `P-04` vì **không phụ thuộc gì** (`U-06` đã xong — component có sẵn), có **đặc tả rõ** (6 thông tin bắt buộc mỗi bước) và có **cổng đo sẵn**. **Hiện trạng trước khi sửa:** `ApprovalTimeline` **đã tồn tại và đã được import** trong `app/page.tsx` nhưng **CHƯA HỀ được render** ⇒ cổng `probe-ui-adoption.mjs` ghi **`ApprovalTimeline` 0 lần — CHUA AP DUNG** (đúng như `MASTER_STATUS` mục "Tiến độ áp dụng UI dùng chung"); màn chi tiết phiếu tự dựng **`<div className="timeline">`** (lớp CŨ, **không** phải `vt-timeline`) trong **1 dòng 1467 ký tự**, và **phòng ban phải dò `data.staffDirectory`** dù cột `approvals.department` đã có sẵn. **Đã sửa:** thay khối tự viết bằng `<ApprovalTimeline>` dùng chung, ánh xạ **đủ 6 trường §8.1** — số bước (`stage`) · tên bước (`approvalStages[].name` → dự phòng `department` → `Bước N`) · người duyệt (`approverName`, Java đã LEFT JOIN `users`) · phòng ban (**cột thật** `approval.department`) · thời gian (`decidedAt` + hạn `dueAt`) · trạng thái (nhãn chữ qua `StatusBadge`; `cancelled`→`skipped` · bước hiện tại→`pending` · còn lại→`waiting`) · ý kiến (`comment`). **4 cổng:** (1) `probe-ui-adoption.mjs` **0 → 1 lần DANG DUNG**; (2) `npx tsc --noEmit` **exit 0**; (3) UI `:8787` và proxy `:9000` **HTTP 200**, **0** marker lỗi biên dịch; (4) `probe-visual-regression.mjs` **8/28 ảnh lệch — đã chứng minh là CÓ SẴN bằng ĐỐI CHỨNG THỰC NGHIỆM** (`git stash push app/page.tsx` → chạy lại → **vẫn 8/28 y hệt** → `git stash pop`), và 8 ảnh đó đều ở màn `05-material` **không chứa drawer vừa sửa** ⇒ thay đổi này **không thêm ảnh lệch nào**; (5) hồi quy `npm run test:regression` **61 test · 59 PASS · 2 FAIL** — **đúng 2 ca ĐÃ BIẾT** (`TASK-031` · `TASK-032`), **không ca mới**. **Giới hạn ghi rõ:** bản cũ hiển thị thêm *"Nhận hồ sơ `queuedAt`"* / *"Đã gửi email `notifiedAt`"* mà component dùng chung **chưa có chỗ** ⇒ đề xuất mở rộng `ApprovalStep` (hạng mục sau), **không** nhét vào trường khác; 28 ảnh chuẩn **chỉ có 7 màn danh sách**, **không phủ drawer** ⇒ cổng ảnh xác nhận *"không hỏng màn khác"* chứ **không** xác nhận *"drawer hiển thị đúng"*; `cancelled→skipped` là **suy luận hợp lý theo từ vựng component**, chưa có đặc tả riêng. **Cập nhật ledger bằng SỐ ĐO (không sửa tay):** `P-04` TODO→**DONE** · `U-06` `KHUNG-XONG / AP-DUNG 0`→**`DONE / AP-DUNG 1`** · `U-05` `AP-DUNG 2`→**`90`** (số cũ đã bị **TASK-020** vượt qua: *"StatusBadge 2 → 90; `<Pill>` 88 → 0"* — **xác nhận chéo** bằng `grep` đếm 90 lần xuất hiện, không phải đếm dòng) · `U-17` dùng thật 0→**1** và còn **2** chỗ tự viết dải. ⇒ tiến độ **23 → 25/110 = 22,7 %** (`probe-roadmap-progress.mjs` đọc lại được đủ 110 mục). **Ghi chú số hiệu:** số `TASK-072` trước đó **mới chỉ là nháp** cho việc `teamMembers` trong `MASTER_STATUS`; việc đó nay là **TASK-073**. Hồ sơ: `TASK-072.md` |
| TASK-071 | **Đo 7 TRƯỜNG ĐẶT ALIAS KHÁC TÊN CỘT NGUỒN** (nhóm TASK-070 không đối chiếu được) + `teamMembers` | **DONE — 9/9 ĐẠT · 1 phép đo KHÔNG thực hiện được** | chờ commit | 17/09/2026 | **Vì sao:** cổng TASK-070 đối chiếu trường bằng `camel→snake` rồi tìm **cột cùng tên**; nhóm đặt alias KHÁC (`u.full_name AS fullName` · `o.code AS organizationCode` · `` level_rank AS `rank` ``) không đối chiếu được — mà đây **đúng nhóm dễ ẩn lỗi nhất** (đều là trường dẫn xuất/JOIN; dự án từng có `roleCatalog.businessGroupName` luôn "—", `constructionDailyLogs.itemCount` luôn 0). **Cách đo:** xây bản đồ `alias → bảng` từ `FROM`/`JOIN`, tìm mục SELECT theo alias, lấy **cột nguồn** trong biểu thức, rồi so *"số dòng có dữ liệu ở cột nguồn (MySQL)"* với *"số dòng API KHÔNG rỗng"*. **BỘ GIẢI MÃ ĐÃ BỊ CHÍNH ĐỐI CHỨNG BẮT LỖI (4 ca dựng sẵn):** ca alias có **BACKTICK** (`` level_rank AS `rank` ``) **HỎNG ở lượt chạy đầu** — bộ giải mã để nguyên phần `AS <alias>` nên alias bị hiểu thành tên cột ⇒ ra `…level_catalog.rank` thay vì `…level_rank`; **đã sửa bằng cách bỏ `AS <alias>` TRƯỚC khi trích cột**; 3 ca còn lại (trường không có trong SQL ⇒ phải báo lỗi · `COALESCE(a.x,b.y)` ⇒ lấy cột ĐẦU · `u.full_name` ⇒ `users.full_name`) đều ĐẠT. **Nếu không có đối chứng dựng sẵn thì cổng đã cho kết quả SAI.** **KẾT QUẢ 9/9 ĐẠT** — `workflowStepApprovers.fullName`←`users.full_name` (MySQL 12/12 · API 5/5) · `employeeCode`←`users.employee_code` (11/12 · 5/5) · `role`←`users.role` (12/12 · 5/5) · `departmentModulePermissions.organizationCode/Name`←`organization_units` (8/8 · **51/51**) · `systemLevelCatalog.rank`←`system_level_catalog.level_rank` (5/5 · 5/5) · `staffDirectory.roleName`←`role_catalog.name` (16/16 · 12/12) · `organizationCode`←`organization_units.code` (8/8 · **11/12** — 1 tài khoản chưa gán đơn vị, **hợp lý**, không phải lỗi port) · `organizationName`←`organization_units.name` (8/8 · 12/12 qua `COALESCE` dự phòng). **Không tìm thấy lỗi "trường alias luôn rỗng".** **CHƯA kiểm được (ghi rõ):** **`teamMembers`** — bảng `team_members` **0 dòng**, **chưa cổng nào kiểm**; nhánh DỰ PHÒNG của `COALESCE` chưa đo riêng. **Trạng thái lớp "TRƯỜNG BÊN TRONG" sau TASK-070+071:** 10 khoá trường-trùng-tên ✅ (8/8) · 7 trường alias ✅ (9/9) · `workItemEvents` ✅ (đã kiểm bằng fixture `probe-task058` 18/18) · `teamMembers` ⚠️ CHƯA · 13 khoá trùng tên ✅ phủ 12/13 về mặt KHOÁ bằng cổng `AppData` (còn `boqMappingCandidates` = payload chết). Chi tiết: `docs/agent-progress/TASK-071.md` |
| TASK-070 | **Cổng ĐỘ PHỦ TRƯỜNG** cho 10 khoá Java-only — trường API trả về có DỮ LIỆU THẬT không? | **DONE — 8/8 ĐẠT · 2 phép đo KHÔNG thực hiện được** | **#113** | 17/09/2026 | **Vì sao:** TASK-069 mới kiểm được **khoá + SỐ DÒNG**; lớp lỗi còn lại là **TRƯỜNG BÊN TRONG** — khoá CÓ, số dòng ĐÚNG, nhưng một trường **luôn rỗng** vì port sai alias/không `SELECT` cột đó (đúng dạng đã gặp: `roleCatalog.businessGroupName` luôn "—" · `constructionDailyLogs.itemCount` luôn 0 · `adminMaterials` thiếu hãng/ĐVT/tồn tối thiểu). **Cách đo độc lập:** lấy câu SQL từ chính adapter (mô-đun dùng chung `sql-parity-extract.mjs`) → tập trường `AS alias` → suy cột nguồn bằng `camel→snake` → đo `COUNT(*)`/`COUNT(*) WHERE cột IS NOT NULL [AND <>'']` trên MySQL → nếu **MySQL có dữ liệu mà MỌI dòng API rỗng** ⇒ HỎNG; nếu MySQL cũng rỗng ⇒ **bỏ qua, KHÔNG tính ĐẠT**. **Bẫy khi viết cổng:** vị từ `cột <> ''` làm MySQL 8 báo `ERROR 1525: Incorrect DATETIME value: ''` với cột DATETIME ⇒ phải đọc `DATA_TYPE` từ `information_schema` và chỉ thêm điều kiện chuỗi cho họ văn bản. **KẾT QUẢ 8/8 ĐẠT:** `workflowDefinitions` (10 trường) · `workflowSteps` (9) · `workflowStepApprovers` (4) · `departmentModulePermissions` (10) · `systemLevelCatalog` (8) · `staffDirectory` (8) · `formFieldConfigs` (15) · `supplySteps` (11) — **KHÔNG tìm thấy lỗi "trường luôn rỗng"** (kết quả ÂM có kiểm chứng). **2 phép đo KHÔNG thực hiện được (nói rõ, không tính ĐẠT):** `teamMembers` (bảng 0 dòng) · `workItemEvents` (0 dòng — nhưng **đã được kiểm ở cổng khác**: `probe-task058-work-items.mjs` tự dựng 7 `work_items` + 2 sự kiện TẠM rồi **18/18 ĐẠT**). **Phần CHƯA kiểm được (ghi rõ):** **7 trường đặt alias KHÁC tên cột nguồn** — `fullName`/`employeeCode`/`role`/`organizationCode`/`organizationName`/`roleName`/`rank` — **đúng nhóm dễ ẩn lỗi nhất** (đều là trường dẫn xuất/JOIN) ⇒ cần phép đo riêng; câu JOIN nhiều bảng cổng chỉ tìm cột trên **bảng ĐẦU TIÊN trong `FROM`**. **Quan hệ với "13 khoá trùng tên":** cổng HỢP ĐỒNG `AppData` (TASK-069, 95 khoá × 6 tài khoản) nay phủ **12/13** khoá Java-only về mặt KHOÁ; phần còn lại là **TRƯỜNG BÊN TRONG**. Chi tiết: `docs/agent-progress/TASK-070.md` |
| TASK-069 | **Cổng HỢP ĐỒNG KIỂU `AppData` (95 khoá × 6 vai trò)** + vá **lỗi thứ 10** của lớp "đường ĐỌC thiếu khoá": `emailOutbox` | **DONE — 0/6 → 6/6 ĐẠT** | **#112** | 17/09/2026 | **Vì sao:** hai cổng tĩnh đã xanh nhưng còn **11 khoá Java-only** chưa từng được đo; cổng TASK-066 chỉ phủ **38 khoá** (trích theo dạng viết `result.data?.<khoá>`). **Nguồn sự thật MẠNH HƠN: khai báo KIỂU `AppData`** trong `app/page.tsx` (`type AppData = { … emailSettings: Row \| null; … teamMembers?: Row[]; formFieldConfigs?: FormFieldConfig[]; }`) ⇒ **95 khoá** (88 BẮT BUỘC + 7 tuỳ chọn) kèm **KIỂU**; thiếu khoá bắt buộc thì khối chuẩn hoá của UI **âm thầm** thay `[]`/null. **LƯỢT CHẠY ĐẦU 0/6 ĐẠT** — thiếu đúng **`emailOutbox`** cho **CẢ 6** tài khoản (kể cả admin): khoá này **BẮT BUỘC** trong hợp đồng UI nhưng **KHÔNG một adapter Java nào** nhắc tới (`grep` toàn `java-backend` = 0) ⇒ màn **Hộp thư gửi** luôn rỗng dù `email_outbox` có dữ liệu (email phê duyệt / giao việc / thư thử đều ghi vào bảng này). **Đã port nguyên văn JS `:724`** — admin: **11 cột** + `requestNo` (LEFT JOIN `material_requests`), `ORDER BY eo.queued_at DESC LIMIT 100`; non-admin `[]` theo JS `:737`; đặt ngay cạnh `emailRecipients` cùng dạng `admin ? query : []`. Cổng SQL `probe-java-sql-live` chạy **TRƯỚC** khi build (8 phát hiện cũ, **không mới**). **Sau vá 6/6 ĐẠT** ⇒ cổng tự chứng minh CÓ TÁC DỤNG bằng cách **bắt một lỗi THẬT rồi xác nhận bản vá**. **Rà 11 khoá Java-only — 10/11 KHỚP số dòng với bảng nguồn:** `workflowDefinitions` 1/1 · `workflowSteps` 5/5 · `workflowStepApprovers` 5/5 · `departmentModulePermissions` 51/51 · `systemLevelCatalog` 5/5 · `staffDirectory` 12/12 (`users`) · `formFieldConfigs` 65/65 · `teamMembers` 0/0 · `workItemEvents` 0/0 · `supplySteps` 23/56 (khác **có chủ ý**: JOIN + lọc theo phạm vi dự án). **`boqMappingCandidates`: payload CHẾT** — UI không đọc (0 lần trong `page.tsx`, không có trong `AppData`), JS không trả, bảng rỗng ⇒ ghi lại, **không xoá**. **Giới hạn:** cổng chỉ kiểm SỰ HIỆN DIỆN + KIỂU, **KHÔNG phán nội dung** (rỗng theo vai trò là hợp lệ); số dòng theo **BẢNG ĐẦU TIÊN trong `FROM`** nên câu nhiều bảng có thể lệch có chủ ý — cổng in cặp số để người đọc tự phán; **chưa kiểm TRƯỜNG bên trong từng dòng**. **Hồi quy:** bootstrap 100/100 · 058 18/18 · 048 18/18 · 049 10/10 · 054 20/20 · 062 19/19 · 063 7/7 · 065 10/10 · 066 11/11 · 069 6/6 · `probe-put-order` **0 vi phạm** · cổng cột **73 khoá**/0 thiếu · cổng mệnh đề 0 lệch. Chi tiết: `docs/agent-progress/TASK-069.md` |
| TASK-068 | **QUYẾT ĐỊNH về NHẬT KÝ GHI TRÙNG**: hai dòng **BỔ SUNG** cho nhau, **KHÔNG thừa** ⇒ **KHÔNG đổi mã** | **DONE (quyết định có bằng chứng)** | **#111** | 17/09/2026 | **Vấn đề (từ TASK-067):** Java sinh **2 dòng** `audit_logs` cho cùng thao tác với **mã khác nhau** — lớp `AuditTrailFilter` ghi **tên action thô** (`create_request` 80 dòng · `cancel_request` 15 · `decide_approval` 173 · `save_email_settings` 6), use-case ghi **mã nghiệp vụ như JS** (`CREATE` 58 · `CANCEL` 15 · `APPROVE_PARTIAL` 13 · `UPDATE` 8); JS chỉ ghi **1 dòng/thao tác**. **Câu hỏi phải trả lời TRƯỚC khi sửa: bỏ thì MẤT GÌ?** **Đo bằng dữ liệu:** dòng FILTER có **IP + module** (80/80, 173/173, 15/15, 6/6) còn dòng use-case **KHÔNG có IP** (0/58, 0/13, 0/15, 0/8) — vì `AuditLogPort.log(userId, action, entityType, entityId, before, after)` **không nhận ngữ cảnh request**, chỉ FILTER mới có `HttpServletRequest` (JS thì truyền `request` vào `audit(...)`). ⇒ **QUYẾT ĐỊNH: GIỮ CẢ HAI** — bỏ dòng FILTER là **mất IP** (mất dấu vết pháp lý), bỏ dòng use-case là mất **mã nghiệp vụ + chi tiết đã lọc**; hai dòng là *"ai/khi nào/từ đâu"* và *"đã làm gì với đối tượng nào"*. **KHÔNG** thêm 24 action vào `SKIP_ACTIONS`. **Hướng cải thiện ĐÚNG ghi lại (chưa làm):** mở rộng `AuditLogPort.log(...)` nhận ngữ cảnh request (IP/user-agent) ⇒ dòng use-case tự đủ ⇒ **khi đó mới** cho FILTER bỏ qua 24 action; điều kiện tiên quyết là **kiểm mức METHOD** từng action (cổng hiện đo theo **LỚP**). **Giới hạn:** chỉ đo được action đã từng chạy; danh sách 24 action suy từ mã tĩnh. Chi tiết: `docs/agent-progress/TASK-068.md` |
| TASK-067 | **Cổng chống BẪY THỨ TỰ** + **BÁC BỎ báo động giả "128 action thiếu audit"** + xác minh lại TASK-025 | **DONE — 3 phần, đều có bằng chứng** | **#110** | 17/09/2026 | **Bài học chung: hai hạng mục trong TODO của tôi ĐỀU SAI và đã bị bác bỏ bằng dữ liệu/log thật — không truyền tiếp con số cũ mà không đo lại.** **(1) Cổng `tools/probe-put-order.mjs` (mới):** TASK-066 vừa vá lỗi `data.put("engineRoleProfiles", data.get("businessRoleEngineProfiles"))` gán **TRƯỚC** khi khoá nguồn tồn tại ⇒ null cho **mọi** tài khoản (Known Problems #50) — cổng-theo-tên-khoá và cổng-theo-tập-cột **đều mù** với lớp này. Cổng có **đối chứng dựng sẵn 3/3 ĐẠT** (đúng thứ tự ⇒ IM · ngược ⇒ BẮT · nguồn không hề được ghi ⇒ BẮT); trên mã thật: 96 khoá `data.put` · **3 phép gán phụ thuộc thứ tự** (`approvalStages`←706, `adminMaterialCategories`←172, `adminMaterialSubcategories`←175) · **0 vi phạm**. Kèm phát hiện **10 khoá bị ghi nhiều lần** — đã kiểm 3 ca đáng nghi nhất (`workItemEvents` = nhánh `isEmpty()`; `centralInventory`/`centralReturns` = bộ lọc vai trò kho-site JS `:622`) ⇒ **đều hợp lệ**. **(2) BÁC BỎ BÁO ĐỘNG GIẢ:** con số *"KHE HỞ 128 action"* của `probe-audit-coverage.mjs` từng bị đọc thành *"128 action không được ghi nhật ký"*. **Dữ liệu thật:** `audit_logs` **825 dòng / 39 action**; **427 dòng** mang `module_key`+`ip_address` — hai cột **CHỈ `AuditTrailFilter` điền**; đối chiếu từng action "khe hở": `decide_approval` **173/173** · `create_self_work_item` **28/28** · `update_boq_contract_prices` **9/9** · `save_email_settings` **6/6** · `set_material_norm_status` **6/6** — **100% do FILTER ghi, use-case 0** ⇒ **nhật ký KHÔNG thiếu** (`AuditTrailFilter` phủ MỌI POST action thành công trừ 7 action SKIP). **Đã vá cổng** để không ai hiểu sai: đổi nhãn thành *"KHE HỞ Ở TẦNG USE-CASE"* + cảnh báo + **tự truy vấn `audit_logs`** in số action thực sự có dòng. **CÒN LẠI THẬT (mới, chưa vá):** **GHI TRÙNG + LỆCH TỪ VỰNG** — cùng thao tác sinh **2 dòng** với **mã khác nhau**: `create_request` **80 dòng** (FILTER, tên action thô) + `CREATE` **58 dòng** (use-case, mã nghiệp vụ như JS); JS chỉ ghi **1 dòng/thao tác**. **Vì sao CHƯA sửa:** cổng đo theo **LỚP** use-case — bỏ dòng FILTER của action mà use-case **không thực sự ghi** ⇒ **MẤT dấu vết pháp lý**; phải kiểm mức METHOD trước (TASK-068). **(3) TASK-025 (`SlaComplianceWorker`) ĐÃ XONG từ #34 — dòng TODO bị CŨ:** log thật hôm nay `INFO SlaComplianceWorker : SLA worker: 0 supply steps quá hạn; 0 payment plans quá hạn; 4 BCH chờ xác nhận.` (chạy 60s sau khởi động, **không còn WARN**); DB: `supply_workflow_steps` 29 `overdue` / 27 `completed` / **0 `pending` quá hạn**; `company_settings` = `po_sla 24` / `bch_sla 8`. Đã sửa dòng TODO cũ. Chi tiết: `docs/agent-progress/TASK-067.md` |
| TASK-066 | **Cổng HỢP ĐỒNG KHOÁ theo VAI TRÒ** (nguồn: khối chuẩn hoá của chính UI) + vá 3 lỗi thật — **ĐÓNG Known Problems #50** | **DONE — 1/17 → 11/11 ĐẠT** | **#109** | 17/09/2026 | **Vì sao:** hai cổng tĩnh đã xanh nhưng khi liệt kê phần KHÔNG so được thì lộ **13 khoá Java-only** (đúng những khoá từng gây lỗi lệch tên trường ⇒ UI trắng) ⇒ cần cổng ĐO LÚC CHẠY theo vai trò. **Nguồn sự thật độc lập:** khối chuẩn hoá trong `app/page.tsx` đọc `<key>: Array.isArray(result.data?.<key>) ? … : []` — **38 khoá** + KIỂU mong đợi; nếu API thiếu khoá thì UI **âm thầm** thay `[]` (màn trống mà không có lỗi nào nổi lên). Cổng tự cảnh báo nếu số khoá trích được tụt (<30). **LƯỢT ĐO ĐẦU: 1/17 ĐẠT ⇒ 3 LỖI THẬT:** (1) **`engineRoleProfiles` = `null` cho MỌI tài khoản kể cả admin** — Java gán `data.get("businessRoleEngineProfiles")` **TRƯỚC** khi khoá nguồn được đặt (nguồn nằm trong `if (admin)` ở cuối hàm) ⇒ **Known Problems #50 xác nhận bằng thực nghiệm**; (2) **`businessScopes` + `businessRoleGroups` MẤT HẲN với 5/5 vai trò thường** — cả hai bị đặt trong `if (admin)` nhưng JS `:672`/`:674` trả cho **mọi vai trò** (chỉ khác `WHERE active=1`); (3) **`businessRoleGroups[].scopeIds`/`scopes` = 0/11 dòng** — chưa port phép **LÀM GIÀU** JS `:675` từ `businessRoleGroupScopes` ⇒ 2 nhóm **CÓ** phạm vi trong DB (`BRG-engineer`, `BRG-hcpc`) hiển thị rỗng. **Đã vá:** 3 khoá chuyển ra khối **TOÀN CỤC** với bộ lọc `admin ? "" : "WHERE active=1"` · gán nguồn TRƯỚC rồi dùng lại **cùng một list** cho `engineRoleProfiles` + `businessRoleEngineProfiles` · **port phép làm giàu** `scopeIds` (mảng ID) + `scopes` (các dòng phạm vi) theo `businessGroupId`, tái dùng danh sách đã đặt cho `businessRoleGroupScopes`. **Kiểm chứng `tools/probe-task066-role-shape.mjs` 11/11 ĐẠT:** 6 tài khoản THẬT × 38 khoá đều CÓ + đúng kiểu mảng · `engineRoleProfiles` nay **mảng 9 dòng** và **bằng** `businessRoleEngineProfiles` · **11/11** dòng nhóm có `scopeIds`+`scopes`, nhóm có phạm vi trong DB thì `scopeIds` **KHÔNG rỗng**. **Phát hiện phụ CHƯA vá:** `data.put("approvalStages", data.get("approvalStageCatalog"))` cũng là **bẫy thứ tự** cùng dạng — hiện đúng (đặt trước) nhưng sẽ vỡ nếu ai di chuyển khối. **Giới hạn:** cổng đo SỰ HIỆN DIỆN + KIỂU, KHÔNG phán nội dung; **11 khoá Java-only còn chưa đối chiếu từng trường** (`workflowSteps`/`workflowDefinitions`/`workflowStepApprovers`/`staffDirectory`/`formFieldConfigs`/`departmentModulePermissions`/`teamMembers`/`systemLevelCatalog`/`supplySteps`/`boqMappingCandidates`/`workItemEvents`). **Hồi quy:** bootstrap 100/100 · work-items 18/18 · audit-requests 18/18 · owner-checks 10/10 · all-roles 20/20 · boq 19/19 · clauses 7/7 · caneditcentral 10/10 · cổng cột **72 khoá**/0 thiếu/0 mất độ phủ · cổng mệnh đề 0 lệch · java-sql-live 8 (không mới, chạy TRƯỚC khi build). Chi tiết: `docs/agent-progress/TASK-066.md` |
| TASK-065 | **`canEditCentral` ≠ `admin`** — đưa phép kiểm MODULE vào đường bootstrap (JS `:693`), đóng hoàn toàn lớp mù #63 | **DONE — 10/10 ĐẠT (có ĐỐI CHỨNG ÂM)** | **#108** | 17/09/2026 | **Lỗi:** JS `:693` `canEditCentral = isAdmin(user) \|\| await canUseModule(user,"central_warehouse","canEdit")` — phép kiểm **MODULE**, không phải phép kiểm vai trò; Java chỉ xét `admin` ⇒ vai trò Kho trung tâm **có** `central_warehouse.canEdit` vẫn nhận danh sách **RÚT GỌN** thay vì danh sách ĐẦY ĐỦ (gồm mục đã ẩn `active=0` + `aliases`/`aliasText`). **Chỗ đặt phép kiểm quan trọng hơn bản thân phép kiểm:** `BootstrapDataAdapter` chỉ có `JdbcTemplate` (không được biết cổng quyền) ⇒ tính ở **use-case**: `BootstrapUseCase` nhận thêm `ModulePermissionStore`, tính `canEditCentral`, truyền xuống qua `Context.canEditCentral`; bean `bootstrapUseCase` thêm tham số trong `ApplicationBeansConfig`. **CHỖ DỄ SAI ĐÃ SỬA:** 3 khoá `adminMaterial*` trước nằm trong danh sách **xoá-trắng VÔ ĐIỀU KIỆN** của nhánh không-phải-admin — nếu chỉ đổi `if (admin)` → `if (canEditCentral)` thì dữ liệu vừa dựng ĐẦY ĐỦ sẽ **bị xoá ngay sau đó** ⇒ đã tách danh sách xoá-trắng và bọc phần 3 khoá trong `if (!canEditCentral)`. **Cổng mới `tools/probe-task065-caneditcentral.mjs` 10/10 ĐẠT** — thiết kế **ĐỐI CHỨNG ÂM trên CÙNG một tài khoản thật** (`tkhodemo`, tiền đề `module_catalog`/`menu_group_catalog` active=1, nền **0** dòng quyền): **CẤP** dòng `central_warehouse.can_edit=1` ⇒ `adminMaterials` **14 dòng** (**14/14 có `aliases`**), categories **6**, subcategories **8**; **THU HỒI** ⇒ `adminMaterials` **0** (JS `:696` trả `[]`) và 2 khoá kia về bản RÚT GỌN (6↔6 · 8↔8); kết luận **14 ≠ 0 ⇒ phép kiểm mới CÓ tác dụng**. Fixture ngày **0→1→0** khẳng định quay về đúng nền (dọn trong `finally`). **Giới hạn:** chỉ đo 1 tài khoản/1 module; 2 khoá nhóm hiện **0** bản ghi `active=0` nên khác biệt chỉ thấy qua thứ tự — tín hiệu chắc chắn nằm ở `adminMaterials` (14 ↔ 0) và `aliases`. **Sự cố môi trường:** DSH khởi động lại giết cả 3 tiến trình nền (18081/8787/9000 DOWN) ⇒ đã dựng lại và xác minh HTTP 200. **Hồi quy:** bootstrap 100/100 · work-items 18/18 · audit-requests 18/18 · owner-checks 10/10 · all-roles 20/20 · task062-boq 19/19 · task063-clauses 7/7 · cổng cột 71/0/0 · cổng mệnh đề 0 lệch · java-sql-live 8 (không mới). Chi tiết: `docs/agent-progress/TASK-065.md` |
| TASK-064 | **Nhánh dự phòng theo VAI TRÒ là BIẾN KHÁC** (JS `:694`/`:695`) — đóng lớp mù #63 | **DONE — 7/7 ĐẠT lúc chạy** | **#107** | 17/09/2026 | **Lỗi:** JS `:693` `canEditCentral = isAdmin(user) \|\| await canUseModule(user,"central_warehouse","canEdit")` rồi `:694-696` **nhánh dự phòng là BIẾN KHÁC**, không phải `[]`: `adminMaterialCategories = canEditCentral ? A : materialCategories` · `adminMaterialSubcategories = … : materialSubcategories` · `adminMaterials = … : []`. **ĐO TRƯỚC khi vá (admin ↔ `thukydemo`):** `adminMaterialCategories` **6 → 0** · `adminMaterialSubcategories` **8 → 0** · `adminMaterials` 14 → 0 (**ĐÚNG** vì JS trả mảng rỗng) · `workflowAssignments`/`audits`/`allModulePermissions`/`users` = 0 (**ĐÚNG** — `isAdmin ? … : []`) ⇒ chỉ **2/3** khoá `adminMaterial*` là sai, **không "vá" theo cảm giác**. **Đã vá:** đặt lại 2 khoá bằng giá trị CUỐI của `materialCategories`/`materialSubcategories` **sau** bước xoá-trắng theo module (đúng như JS vì nhánh dự phòng đọc chính biến đó); `adminMaterials` giữ `[]`. **Cổng mở rộng `tools/probe-task063-clauses.mjs` từ ĐO sang KIỂM** — thêm 4 phép: 2 nhánh dự phòng (so **theo id từng dòng**) · `adminMaterials` = `[]` · **không RÒ RỈ** 6 khoá admin-gated ⇒ **7/7 ĐẠT**; `adminMaterialCategories` **0 → 6 dòng**, `adminMaterialSubcategories` **0 → 8 dòng**. **Giới hạn CÒN LẠI (ghi rõ):** Java chỉ xét `admin` còn JS xét thêm `canUseModule(user,"central_warehouse","canEdit")` ⇒ vai trò có quyền đó vẫn nhận danh sách **rút gọn** thay vì đầy đủ (có cả mục `active=0` + `aliases`) — cần đưa phép kiểm module vào đường bootstrap. **Hồi quy:** bootstrap 100/100 · work-items 18/18 · audit-requests 18/18 · owner-checks 10/10 · all-roles 20/20 · task062-boq 19/19 · cổng cột 71/0/0 · cổng mệnh đề 0 lệch · java-sql-live 8 (không mới) · schema-drift 0. Chi tiết: `docs/agent-progress/TASK-064.md` |
| TASK-063 | **Cổng so MỆNH ĐỀ** (`ORDER BY` · `LIMIT` · kiểu `JOIN`) + vá 2 lệch thật | **DONE — 3/3 ĐẠT lúc chạy · đối chứng bộ so sánh 6/6** | **#106** | 17/09/2026 | **Vì sao:** khi vá TASK-062 phát hiện **3 ca sai nặng mà cổng TẬP CỘT hoàn toàn mù** (`boqImportBatches` sắp sai · `boqChangeHistory` thiếu tiebreaker+`LIMIT` · `workflowAssignments` trả cho MỌI vai trò ⇒ **rò rỉ**) ⇒ Known Problems #61/#62 ghi *"không được báo đã khớp JS cho tới khi có cổng riêng"*. **Cổng mới `tools/probe-clause-parity.mjs`:** so `ORDER BY` (chuẩn hoá: bỏ backtick/tiền tố bảng/khoảng trắng quanh dấu phẩy) · `LIMIT` **hiệu dụng** · **kiểu `JOIN` theo từng bảng**; **đối chứng của chính bộ so sánh 6/6 ĐẠT** (6 cặp SQL dựng sẵn: 1 cặp chỉ khác định dạng ⇒ phải IM; 5 cặp khác LIMIT/chiều sắp/cột sắp/tiebreaker/kiểu JOIN ⇒ phải BẮT) + **phần ĐỘ PHỦ**. **2 lệch THẬT đã vá:** `adminMaterialCategories` (Java `ORDER BY sort_order,code`; JS `:694` `CASE WHEN active=1 THEN 0 ELSE 1 END,sort_order,name` ⇒ nhóm chưa dùng không bị đẩy xuống cuối) · `audits` (**Java `LIMIT 500`, JS `:698` `LIMIT 100`** ⇒ nhật ký kiểm toán dài gấp 5 lần). **2 DƯƠNG TÍNH GIẢ của cổng đã loại (sửa CỔNG, không sửa mã theo số sai):** `productIdentity` (JS `first()` ngầm 1 dòng ↔ Java `LIMIT 1` tường minh) · khoảng trắng sau dấu phẩy trong `ORDER BY`. **Mô-đun dùng chung `tools/lib/sql-parity-extract.mjs`** + cổng tập cột nay **TỰ ĐỐI CHIẾU CHÉO hai bản đọc nguồn** (in HỎNG nếu trôi khác nhau; hiện *"giống hệt trên toàn bộ khoá"*). **Kiểm chứng lúc chạy `tools/probe-task063-clauses.mjs` 3/3 ĐẠT:** `audits` — MySQL **745** dòng ⇒ API trả **100** dòng và **SÁCH id khớp từng dòng một**; `adminMaterialCategories` — thứ tự khớp MySQL 6/6 (⚠️ hiện có **0** nhóm `active=0` nên nhánh "đẩy nhóm chưa dùng xuống cuối" **không có dữ liệu chứng minh** — nói rõ, không tính ĐẠT). **LỚP MÙ MỚI #63 phát hiện và ĐO được:** khoá có **NHÁNH THEO VAI TRÒ, fallback là BIẾN KHÁC** (`canEditCentral ? A : materialCategories`) — cổng chỉ so câu ĐẦU nên không thấy; đo admin ↔ `thukydemo`: `adminMaterialCategories` 6→**0** · `adminMaterialSubcategories` 8→**0** · `adminMaterials` 14→**0** (còn `workflowAssignments`/`audits`/`allModulePermissions`/`users` = 0 là **ĐÚNG** JS). **Giới hạn:** bỏ tiền tố bảng trong `ORDER BY` ⇒ có thể che lệch BẢNG; chỉ so câu ĐẦU; **KHÔNG kiểm `WHERE`**; **13 khoá không so được** — KHÔNG tính là đã khớp. **Hồi quy:** bootstrap 100/100 · work-items 18/18 · audit-requests 18/18 · owner-checks 10/10 · all-roles 20/20 · task062-boq 19/19 · cổng cột 71/0/0 · java-sql-live 8 (không mới) · schema-drift 0. Chi tiết: `docs/agent-progress/TASK-063.md` |
| TASK-062 | `boqItems` **THIẾU HẲN MỘT NỬA DANH SÁCH** + `boqSourceItems` +9 cột + **3 ca lệch `ORDER BY`/`LIMIT`/vai trò** | **DONE — 19/19 ĐẠT** | **#104** | 17/09/2026 | **Lỗi (thiếu DÒNG, không phải thiếu cột):** JS `:653-655` gộp `unmappedSourceRows` (`project_boq_item_id IS NULL`, batch `active=1`) vào **cùng mảng** `boqItems`; Java **chỉ** chạy câu chính. **Đo được: 8 dòng đã ánh xạ ⊕ 8 dòng chưa ánh xạ = 16 kỳ vọng, Java trả 8** ⇒ **một nửa danh sách BOQ chưa bao giờ hiện trên UI**. **Đã port đủ 4 phần JS làm ở tầng ứng dụng:** (a) **13 cột vô hướng** (`contractLineRef`/`parentSourceOrder`/`outlineLevel`/`sourceSheet`/`sourceRow`/`contractCode`/`contractMaterialName`/`standardMaterialName`/`variationRef`/`variationApprovedAt`…) · (b) **7 cột TỔNG HỢP** bằng subquery tương quan chép nguyên văn (kể cả **chốt "vật tư duy nhất"** `1=(SELECT COUNT(*) FROM project_boq_items p2 …)`) · (c) **3 trường DẪN XUẤT** `orderedNotReceivedQty`/`varianceContract`/`varianceRemeasured` · (d) **`customFields`** (`form_key='boq'`, tra bằng **id DÒNG**) + **gộp dòng chưa ánh xạ** (khối lượng = 0, `materialCode`/`categoryName` = `null`, `customFields` chỉ 2 khoá có giá trị) + **sắp xếp lại** theo `projectCode` rồi `sourceOrder`. `boqSourceItems` **+9 cột**: `id` (không alias) · `contractLineRef` · `contractCode` · `contractMaterialName` · `specification` · `versionActive`/`versionStatus`/`versionNo`/`versionCode`. **Vá kèm 3 ca lệch KHÔNG phải cột (Known Problems #61):** `boqImportBatches` sắp lại `project_id,contract_id,version_no DESC` (JS `:647`) · `boqChangeHistory` +`h.id DESC` +`LIMIT 1000` (JS `:648`) · **`workflowAssignments` chỉ admin, không lọc dự án** (JS `:723`) — bản cũ trả cho **MỌI vai trò** ⇒ **RÒ RỈ** "ai duyệt bước nào của dự án nào". **Cổng mới `tools/probe-task062-boq.mjs` 19/19 ĐẠT**, đo **độc lập**: 7 cột tổng hợp so với **SQL viết KHÁC DẠNG** (`JOIN…GROUP BY`) **56/56 khớp**; bất biến 3 công thức dẫn xuất; `boqSourceItems` **64 phép so theo từng dòng**; xác nhận **`thukydemo` nhận `workflowAssignments` = 0 dòng**. **Hồi quy:** bootstrap **100/100** · work-items 18/18 · audit-requests 18/18 · owner-checks 10/10 · all-roles 20/20 · cổng tập cột **71 khoá / 0 thiếu / 0 mất độ phủ**. **3 LỖI CỦA CHÍNH TÔI trong lượt này:** (1) **cổng ÂM THẦM MẤT ĐỘ PHỦ** khoá `boqItems` khi nó chuyển sang mảng ghép mà vẫn in "0 thiếu cột" ⇒ nay cổng có **phần ĐỘ PHỦ** (khoá BẮT BUỘC + sàn số khoá) và **tự báo HỎNG**; (2) **2 dương tính giả của cổng** — **backtick MySQL** (`m.`system``) bị coi là literal và **trùng tên biến giữa 5 hàm** (`materials`) ⇒ đã sửa **cổng**, không sửa mã theo số sai; (3) lượt chạy đầu của probe mới báo **4 HỎNG — cả 4 là lỗi KỲ VỌNG của cổng** (thiếu lọc phạm vi dự án · `null` vs `NULL` · `tinyint(1)` ra `true/false` · đòi cột vô hướng trên cả dòng gộp). **Giới hạn:** nhánh dự phòng "vật tư duy nhất" của JS **chưa được mô phỏng** (dữ liệu hiện có **0** dòng `boq_item_id IS NULL` ⇒ nhánh không chạy); `localeCompare` vs `compareTo`; chỉ **2/16** dòng có giá trị tổng hợp khác 0. Chi tiết: `docs/agent-progress/TASK-062.md` |
| TASK-061 | Vá **16/18** khoá bootstrap **thiếu CỘT** (2 khoá BOQ còn lại ⇒ TASK-062) | **DONE** | **#101** | 17/09/2026 | **Đã vá:** `contractStockLedger`/`contractStockBalances` (+`contractName`, +`referenceItemId`) · `modulePermissions` nhánh 3 (+`permissionExpiresAt`, **+bộ lọc hết hạn** `permission_expires_at IS NULL OR > now()`, **+lọc `menu_group_catalog.active=1`** — đúng JS `:684`/`:421`) · `workflowAssignments` (+`ownerEmail`/`ownerRole`) · `teamSettlements` (+`teamName`/`contractNo`/`approvedProductionValue`/`adjustmentValue`/`note` — JS dùng **INNER JOIN** như bản Java) · `boqImportBatches` (+`active`) · `boqChangeHistory` (+`contractId`/`boqVersionId`/`sourceItemId`/`actorName`) · `stockReconciliations` (+`warehouseCode`/`materialCode`/`materialName`/`note`, sắp `checked_at DESC`, `LIMIT 500`) · `users` (+`passwordResetAt`) · `contractPayments`/`capitalRecoveryRecords`/`paymentPlans`/`advanceRequests`/`siteExpenseClaims` (+`createdBy`/`createdByName`) · `constructionDailyLogs` (+`cancelledBy`/`cancelledAt`/`createdBy`/`createdByName`) · `teamPayments` (+`productionRecordId`). **🔴 SỰ CỐ DO CHÍNH TÔI GÂY RA:** bản vá đầu viết `c.name` trong khi `project_contracts` **KHÔNG có** cột `name` (cột thật là `contract_name`) ⇒ `GET /api/system` trả **HTTP 500 cho TOÀN BỘ giao diện**. Đã sửa và ghi **quy tắc vào ngay trong mã**: *sau mọi sửa SQL phải chạy `probe-java-sql-live.mjs` TRƯỚC khi build + restart*. **Kiểm chứng sau build lại:** bootstrap 100/100 · work-items 18/18 · audit-requests 18/18 · owner-checks 10/10 · all-roles 20/20 · schema-drift **0 lệch** · cổng tập cột 18 → **2 khoá**. |
| TASK-060 | Mở rộng phép ánh xạ của **cổng đối chiếu TẬP CỘT**: 29 → **65 khoá** so được | **DONE** | **#100** | 17/09/2026 | **Vì sao:** cổng cũ chỉ so được 29/78 khoá vì JS đặt **tên biến khác tên khoá kết quả** (`rawBusinessRoleGroups` → `businessRoleGroups`…). **Đã làm:** (a) gom **mọi** khối SQL trong một lệnh khai báo — bản cũ đòi `= await all(` **ngay sau** dấu `=` nên bỏ sót dạng phổ biến `const X = <điều kiện> ? await all(`SQL`) : []` và dạng 2 nhánh; (b) **parse chính object `result = {…}`** ở JS `:727` để ánh xạ **khoá kết quả → biến**, rồi lấy định danh đầu tiên của biểu thức; (c) chế độ `--key <tên>` in **cả hai câu SQL** + danh sách cột thiếu/thừa để vá không phải đoán. **Kết quả:** so được **65 khoá** (bỏ qua 49 → 13) ⇒ **lộ ra 18 khoá thiếu cột**. Chi tiết: các commit #100/#101 và mục Known Problems #60. |
| TASK-058 | `workItems` + `workItemEvents`: port **29 cột** + **4 nhánh lọc phòng ban/BCH** (JS `:715-718`) | **DONE — 18/18 ĐẠT, có ĐỐI CHỨNG DƯƠNG** | **#96** | 17/09/2026 | **Lỗi:** Java **KHÔNG có mệnh đề WHERE** ⇒ mọi tài khoản nhận **toàn bộ** công việc của mọi phòng ban/dự án (lộ dữ liệu) + thiếu **14 cột** và **hai tên khoá sai** (`assigneeUserId`/`assigneeName` thay vì `assignedTo`/`assignedToName` ⇒ UI đọc khoá không tồn tại ⇒ trống); `workItemEvents` trả sự kiện của **mọi** công việc và thiếu 3 cột. **Đã port nguyên văn:** 4 nhánh `workItemWhere` (admin `1=1` · KH `department_code='KH' AND (assigned_to=? OR EXISTS(role kh_truong…))` · DA tương tự `da_truong` · BCH `department_code='BCH' AND (assigned_to=? OR project_id IS NULL OR project_id IN (<phạm vi>))` · còn lại `assigned_to=?`) + `departmentForRole` (JS `:246`) + `departmentCodeForUser` (JS `:388`, xét **chuỗi phòng ban** đã bỏ dấu trước) + `stripDiacritics`; bổ sung `Context.department`; `workItemEvents` chỉ lấy sự kiện của công việc trong phạm vi. **Kiểm chứng — `tools/probe-task058-work-items.mjs`:** `work_items` rỗng nên probe **tự dựng 7 dòng TẠM** (KH×3 · DA×2 · BCH×2) + 2 sự kiện rồi đăng nhập **6 tài khoản THẬT**, dọn sạch có kiểm chứng (0→0). **ĐỐI CHỨNG DƯƠNG (jar 19:21 trước khi vá): 4/18 ĐẠT — cả 5 tài khoản khác vai trò đều thấy ĐỦ `T1..T7`.** Sau khi vá: **18/18 ĐẠT** — admin 7/7 · `nvkhdemo` T1,T5 · `trinhtrench` (kh_truong) T1,T5,T6 · `nvdademo` (DA) T2 · `tkhodemo` (BCH) T3,T4 · `thukydemo` (HCPC) T7; 29 cột đủ; `projectCode`/`projectName` + `assignedToName`/`assignedByName` có giá trị; sự kiện được lọc theo phạm vi. **Không hồi quy:** `probe-column-parity` **9 → 8 khoá** · `probe-task050` 93/93 · `probe-task048` 18/18 · `probe-task049` 10/10 · `probe-task054` 20/20 · `probe-java-sql-live` không phát sinh mới. **Lỗi của chính tôi:** câu INSERT fixture liệt kê 32 cột nhưng 31 giá trị (`ERROR 1136`) — đã sửa. Chi tiết: `docs/agent-progress/TASK-058.md` |
| TASK-059 | 8 khoá bootstrap **thiếu CỘT** còn lại ⇒ **CỔNG TẬP CỘT VỀ 0** | **DONE — 100/100 ĐẠT** | **#98** | 17/09/2026 | **Đã vá (mỗi khoá đọc cặp SQL hai phía trước khi kết luận):** `taskNotifications` (+`workItemId`/`channel`/`title`/`body`/`lastError`, sắp **"chưa đọc TRƯỚC"** và LIMIT 100 như JS `:719`; **giữ thêm** `taskId`/`message` Java-only vì `grep` toàn `app/` chỉ đọc `n.readAt`) · `hrRecords` (+`identityDate`/`identityPlace` — form **nhập** mà đọc lại **không có trường**) · `accountingVouchers` + `officialCorrespondence` + `legalDocuments` (+`createdBy`/`createdByName`) · `roleCatalog` (+`businessGroupId` + `COALESCE(bg.name,rc.base_role) AS businessGroupName` ⇒ cột **"Nhóm nghiệp vụ"** ở màn Chức danh/vai trò (`page.tsx:3522`) **trước luôn "—"**) · `businessRoleGroupScopes` (đúng JS `:675`: `businessScopeId`/`scopeCode`/`scopeName` + JOIN `business_scope_catalog` + thứ tự `is_primary DESC, sort_order, name` + lọc `bs.active=1` khi không phải admin) · `organizationUnits` (+`projectName`/`effectiveFrom`/`effectiveTo`). **Kiểm chứng:** cổng `tools/probe-column-parity.mjs` **KHÔNG khoá nào thiếu cột** (13 → 9 → 8 → **0**), đối chứng dương với metadata MySQL **4/4 ĐẠT**; mở rộng `probe-task050-bootstrap.mjs` **mục 7** ⇒ **100/100 ĐẠT** (đối chiếu **giá trị theo từng dòng** với MySQL: `roleCatalog.businessGroupName` khớp **cả 16 dòng**, `organizationUnits` 8 dòng, `hrRecords` 4 dòng, `officialCorrespondence.createdByName` khớp, `businessRoleGroupScopes.scopeCode` khớp 2 dòng); bảng **rỗng** (`accountingVouchers`/`legalDocuments`/`taskNotifications`) được in rõ **"(bỏ qua)"**, không tính ĐẠT. **Không hồi quy:** `probe-task058` 18/18 · `probe-task048` 18/18 · `probe-task049` 10/10 · `probe-task054` 20/20 · `test:regression` 59/61 (không đổi) · `probe-java-sql-live` không phát sinh mới. **Lỗi của chính tôi:** mục 7 mới gọi `q(...)` nhưng tệp probe chỉ dùng `q` làm **tên tham số** ⇒ `ReferenceError` làm probe thoát ngay sau tiêu đề mục 7; đã thêm helper và chạy lại. Chi tiết: `docs/agent-progress/TASK-059.md` |
| TASK-056 | **CỔNG ĐỐI CHIẾU TẬP CỘT** SQL JS ↔ Java theo từng khoá bootstrap (`tools/probe-column-parity.mjs`) | **DONE — có đối chứng dương 4/4** | **#94** | 17/09/2026 | **Vì sao:** cổng theo **TÊN KHOÁ** không bắt được lớp *"khoá có mà THIẾU CỘT"* — đã gặp 3 lần (`constructionDailyLogs` mất 2 cột; `transferOrders` mất 7 trường; `engineRoleProfiles` null theo vai trò). **Cách làm:** đọc câu SQL hai phía (Java `data.put("KEY", … query("""SQL"""))` · JS `const|let|var KEY = await all(`SQL`)`), tách **danh sách SELECT ở mức ngoài cùng** (mask literal để không tách nhầm dấu phẩy), mỗi mục lấy `AS <alias>` hoặc định danh cuối, rồi so **JS − Java**. **🔴 ĐỐI CHỨNG DƯƠNG BẮT BUỘC (vì bộ tách là suy luận văn bản):** cổng tự dựng **bảng tạm** từ chính câu SQL rồi `SHOW COLUMNS` để lấy **metadata THẬT của MySQL**; nếu bộ tách lệch ⇒ cổng báo **HỎNG** và **không cho kết luận**. Lượt cuối: **4/4 khớp hoàn toàn** (23/23 · 12/12 · 11/11 · 13/13 cột). *(Ghi chú: `SELECT * FROM (<sql>) x LIMIT 0` KHÔNG in tiêu đề ở chế độ `--batch`.)* **Kết quả:** **30 khoá** so được (49 khoá bỏ qua vì JS đặt tên biến khác tên khoá) ⇒ **13 khoá thiếu cột**; sau khi vá 4 khoá ở TASK-057 còn **9**. Chi tiết: `docs/agent-progress/TASK-056.md` |
| TASK-057 | 4 khoá bootstrap **thiếu CỘT**: `issues` · `returns` · `transferOrders` · `productIdentity` | **DONE — probe 93/93** | **#94** | 17/09/2026 | **Đã vá (đều đọc cặp SQL hai phía trước khi kết luận):** `issues` + `si.received_by_name AS receivedByName` + `COALESCE(sia.installed_qty,0) AS installedQty` (UI `page.tsx:963` cột "Người nhận" và `:2219` cột "Đã xác nhận lắp" **trước đây luôn 0**) · `returns` + `mr.returned_by_name AS returnedByName` (`:965`) · `transferOrders` **14 → 21 cột** theo JS `:713` (UI `:1745` "Đã xuất"/"Đã nhận" **luôn 0**; `:2238` lọc theo dự án sai) **+ port bộ lọc theo kho của JS `:714`** (trước đây thủ kho thấy phiếu của **mọi** kho) · `productIdentity` `id AS productId` như JS `:666` (trước trả `id` ⇒ `productId` undefined). **Kèm:** chuyển `roleBaseClean`/`scopeKind` lên đầu `load()` vì nay dùng cho **3** bộ lọc (`:622`, `:714`, `:728-737`). **Kiểm chứng:** `probe-task050-bootstrap.mjs` mở rộng **mục 6** ⇒ **93/93 ĐẠT**, đối chiếu **GIÁ TRỊ theo TỪNG DÒNG với MySQL** (không chỉ tên cột); `data.transferOrders` **0 dòng** nên probe **nói rõ** chỉ cổng tĩnh kiểm được phần cột (**không** tính là ĐẠT). Cổng khác không hồi quy: `probe-column-parity` **13 → 9**, `probe-java-sql-live` **không phát sinh mới**, `probe-schema-drift` **0**. Chi tiết: `docs/agent-progress/TASK-057.md` |
| TASK-049 | 2 phép kiểm **OWNER** của bước duyệt (vai trò Owner thuộc `allowedRoleCodes` + Owner phải có `user_project_scopes`) | **DONE — kiểm chứng 10/10** · ⚠️ D5 nay là mục **CHẶN** | **#92** (hồ sơ: #83) | 17/09/2026 | **Đã port nguyên văn JS `requireWorkflowAssignment` (`system-route.mjs:442-452`)**: (3) vai trò Owner phải nằm trong `allowedRoleCodes` của bước — so **cả** `users.role` **và** `rc.base_role`; (4) Owner phải có `user_project_scopes` với `permission IN ('read','write','approve','admin')`, **trừ khi chính Owner là `admin`**. Xác nhận **JS `:966` GỌI hàm này cho MỌI bước** trong `create_request` ⇒ đây là phép kiểm bản Java đang **âm thầm bỏ qua**, không phải tính năng thêm. Thêm `RequestStore.ownerHasProjectScope` (SQL nguyên văn). **Cổng mới `tools/probe-task049-owner-checks.mjs` 10/10 ĐẠT**: nhánh **SAI VAI TRÒ** (fixture đổi Owner bước 1) → 400 nguyên văn *"Owner … không thuộc vai trò được phép của Bước 1 – CHT xác nhận nhu cầu."* + **không tạo phiếu**; nhánh **THIẾU PHẠM VI trên DỮ LIỆU THẬT** (Owner bước 2 `thukydemo`) → 400 nguyên văn *"Owner Thư ký TGĐ D chưa được phân quyền dự án này."* + không tạo phiếu; nhánh **ĐẠT** (fixture cấp phạm vi) → 200 + phiếu ghi thật rồi dọn sạch; khôi phục sạch mọi bảng (`user_project_scopes` 18, `approval_project_assignments` 5). **ĐỐI CHỨNG DƯƠNG:** trên jar trước #92 cùng lời gọi trả **200 và tạo phiếu thật** (probe 048/054). **⚠️ HỆ QUẢ:** luồng lập phiếu của **PRJ-DEMO-01 bị CHẶN BỞI DỮ LIỆU** cho tới khi Owner bước 2 được cấp phạm vi (dòng phạm vi của họ trỏ project **không tồn tại** — cùng gốc known issue #51/**D5**); bản JS cũng chặn y hệt. SQL sửa sẵn ở `TASK-049.md` mục 3. **Đã cập nhật probe 048/054** (tự cấp TẠM phạm vi còn thiếu rồi xoá lại; số dòng `user_project_scopes` đưa vào bảng đối chiếu) ⇒ **18/18** và **20/20**. **`merge_material_master` KHÔNG làm ở lượt này** ⇒ **D7**. Chi tiết: `docs/agent-progress/TASK-049.md` |
| TASK-046 | Vòng rà CUỐI bản đồ GHI — `project_archives.purge_audit_id` + `boq_price_import_items.changed` + vá lỗi P0 `delete_project` HTTP 500 | **DONE** | #67 | 17/09/2026 | **Hai cột:** (1) `project_archives.purge_audit_id` — JS `system-route.mjs:2451-2452` ghi 1 dòng `audit_logs` (`PURGE_AFTER_OFFLINE_ARCHIVE`) rồi `UPDATE project_archives SET purge_audit_id=<auditId>`; Java `ProjectAdminStoreAdapter:242` **dừng ở** `status='purged',purged_at=?` ⇒ **thiếu cả hai câu**. Đã thêm `AuditLogPort.logReturningId(...)` (**để `default`, giữ interface là FUNCTIONAL INTERFACE** — không làm vỡ lambda `(a..g)->{}` của test cũ) + `ProjectAdminStore.setArchivePurgeAuditId(...)`; `deleteProject` ghi audit (before = JSON cả dòng dự án bằng **bộ escape tự viết trong tầng application**, after = `{archiveId,archiveFile,archiveSha256,status:"purged",crossProjectTracePreserved,objectCleanupFailed}` như JS) rồi liên kết id. (2) `boq_price_import_items.changed` — JS `:2827` ghi `row.isChanged?1:0`, Java `BoqStoreAdapter:706` **thiếu cột** ⇒ thêm tham số `changed` vào port + adapter. **LỖI P0 BỊ CHÍNH PROBE BẮT (lần chạy đầu):** `delete_project` ⇒ **HTTP 500** — `MySQL: Incorrect DATETIME value: '0000'` tại `latestVerifiedArchive(projectId,"0000")` (JS/SQLite so chuỗi nên vô hại; MySQL 8 từ chối) ⇒ **không admin nào purge được dự án**; đã dùng `ARCHIVE_FLOOR="1970-01-01 00:00:00"`. **Kiểm chứng `tools/probe-task046.mjs` 17/17 exit 0** — dựng **dự án TẠM** + archive tạm, purge trả 200, audit đúng 1 dòng `PURGE_AFTER_OFFLINE_ARCHIVE`, `purge_audit_id` khớp **tuyệt đối** id audit, `before_json`/`after_json` **JSON hợp lệ**; phần BOQ dùng dòng thật nhưng **trả giá về đúng giá gốc** và tự xoá batch ⇒ `changed` ghi thật `1:100000→101000 · 1:101000→100000 · 0:100000→100000`. jar **90.896.045 B** · log **0 ERROR** · regression **59/61** · cổng bản đồ GHI còn **5 bảng lệch**. **LỖI CỦA TÔI Ở LƯỢT NÀY:** probe lọc `audit_logs` theo mỗi `entity_id` nên báo HỎNG oan (2 dòng cùng `entity_id`: 1 do `AuditTrailFilter` ghi mỗi request + 1 của luồng purge) — phải lọc theo `action`; và tôi **dùng `Set-Content` sửa `MASTER_STATUS.md` làm mất 1 mục** (đã khôi phục bằng công cụ file) — vi phạm chính quy tắc #28 của dự án. | **Sau TASK-045, cổng điều tra chỉ còn 7 bảng lệch**, trong đó 2 bảng là dương tính giả đã biết (`users.avatar_url` — Java ghi qua JPA; `vntech_license_*` — nhóm 6 chờ quyết định #11) và `email_outbox` thuộc hạng mục "Java không có cơ chế gửi email". **Ba cột còn lại đã rà tới nơi:** (1) **`project_archives.purge_audit_id` — CONFIRMED:** JS `system-route.mjs:2451-2452` ghi **1 dòng `audit_logs`** (`PURGE_AFTER_OFFLINE_ARCHIVE`) rồi `UPDATE project_archives SET purge_audit_id=<auditId>`; Java `ProjectAdminStoreAdapter:242` **dừng ở** `UPDATE project_archives SET status='purged',purged_at=?` ⇒ **thiếu cả hai câu** ⇒ purge dự án **không có vết audit liên kết**; Java **có** cổng `AuditLogPort.log(...)` (9 nơi gọi) nên sửa được, không phải "chưa có cơ chế". (2) **`boq_price_import_items.changed` — CONFIRMED:** JS `:2827` tính `isChanged` cho từng dòng nhập giá và ghi cột `changed`; Java `BoqStoreAdapter:706` chỉ ghi `(id,batch_id,boq_item_id,old_unit_price,new_unit_price,created_at)` ⇒ **mất cờ "dòng có đổi giá"**. (3) **`boq_versions.approved_at` — LIKELY DƯƠNG TÍNH GIẢ:** JS `:2805` **bind thẳng `null`** cho cột này ⇒ công cụ đếm giá trị NULL là "có ghi"; **phải xác nhận** không còn chỗ nào ghi giá trị thật trước khi kết luận |
