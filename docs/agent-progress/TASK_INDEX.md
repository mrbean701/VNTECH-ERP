# TASK INDEX — VNTECH ERP V5.3.0 MASTER TASK

Trạng thái hợp lệ: `PENDING` · `IN PROGRESS` · `DONE` · `BLOCKED` · `CANCELLED`
**Không bao giờ xoá hồ sơ việc đã xong.**

| Task | Tiêu đề | Trạng thái | Commit | Ngày | Ghi chú |
|---|---|---|---|---|---|
| TASK-001 | PHASE 0 — SYSTEM AUDIT + TODO/ROADMAP + probe bảo mật RBAC | DONE | `8b4a5da` (#9) | 16/09/2026 | docs/24 (20 mục + Phụ lục B), docs/25 |
| TASK-002 | PHASE 0B — Bật kiểm quyền RBAC ở tầng action, bịt lỗ hổng 15/15 action lọt qua | DONE | `3c948cb` (#10) | 16/09/2026 | Lỗ hổng 15 → 0; đúng 20/20 |
| TASK-003 | PHASE 1 — Thư viện UI dùng chung + sửa xung đột lớp CSS + xác minh cổng ảnh 28/28 (U-01…U-08) | DONE | `7fa59ba` (#11) | 16/09/2026 | docs/26; đính chính kết luận sai về nguyên nhân 28/28 |
| TASK-004 | U-09 đợt 1 — Chuẩn hoá toolbar 3 màn (Requests · WarehouseReceipt · Inventory) | DONE | `8b29344` (#12) | 16/09/2026 | docs/27 mục 1–6 |
| TASK-005 | U-09 đợt 2 — Màn Phân quyền người dùng + nâng ngưỡng cổng ảnh có bằng chứng | DONE | `426763c` (#13) | 16/09/2026 | docs/27 mục 7–9 |
| TASK-006 | U-09 đợt 3 — WorkCenter · TeamManagement · ProjectManagement | DONE | `44c0a5e` (#14) | 17/09/2026 | docs/27 mục 10–11 |
| TASK-007 | U-09 đợt 4 — Danh sách nhân sự + Danh bạ nội bộ + probe chặt hơn + cổng chống lỗi giả + phát hiện roadmap báo quá | DONE | `9148212` (#15) | 17/09/2026 | docs/26 mục 10; docs/27 mục 12–13 |
| TASK-008 | U-09 đợt 5 — 3 màn tab Quản trị (UserPermissionMatrix · SystemLevelManager · AuditLogManager) | IN PROGRESS | `96c3aa8` (#16, PARTIAL) | 17/09/2026 | Code + probe riêng 11/11 ĐẠT; **còn thiếu lượt quét hồi quy rộng** vì cổng ảnh/probe cần mở rộng sandbox |
| TASK-009 | U-09 đợt 6 — 13 màn còn lại | PENDING | — | — | Xem docs/25 |
| TASK-010 | U-14 — ÁP DỤNG EntityDetailModal | PENDING | — | — | Dùng thật 0 lần |
| TASK-011 | U-15 — ÁP DỤNG DataTable + StatusBadge | PENDING | — | — | 100 bảng + 100 rỗng; StatusBadge 2/88 |
| TASK-012 | U-16 — ÁP DỤNG PermissionGuard | PENDING | — | — | 50 chỗ điều kiện quyền |
| TASK-013 | U-17 — ÁP DỤNG Approval/ActivityTimeline | PENDING | — | — | 3 dải tự viết |
| TASK-014 | U-11 — Tách `page.tsx` thành module | PENDING | — | — | 4.140 dòng / 221 hàm |
| TASK-015 | U-12 — Loại `!important` + gộp selector trùng | PENDING | — | — | Phụ thuộc U-11 |
| TASK-016 | U-13 — Tách `TaskTable` khỏi thân render WorkCenter | DONE | #17 (cùng lượt) | 17/09/2026 | eslint 3 lỗi → **0** lỗi; làm trước TASK-009…015 vì kiểm được bằng kiểm tra tĩnh |
| TASK-017 | Điều tra còn mở — nguyên nhân gốc bất định của cổng ảnh | PENDING | — | — | Đã khoanh vùng ô tìm kiếm topbar |
| TASK-018 | Đối chiếu action giữa bản JS tham chiếu và bản Java đã port (đính chính audit) | DONE | #18 (cùng lượt) | 17/09/2026 | Cáo buộc "catalog lệch 50 action" là **SAI**; Java **không thiếu action nào** |
| TASK-019 | Sửa 5 lỗi mã vai trò còn sót trong `ProductionManagementUseCase` (commander→cht, project→da_nv) | DONE | #19 (cùng lượt) | 17/09/2026 | Người dùng `cht`/`da_nv` hết bị 403 oan; javac **exit 0**; chưa đóng gói lại JAR |
| TASK-020 | U-15 (phần StatusBadge) — thay 88 chỗ `<Pill>` bằng `<StatusBadge>`, chứng minh tương đương bằng công cụ | DONE | #21 (cùng lượt) | 17/09/2026 | StatusBadge **2 → 90** lần dùng thật; `<Pill>` 88 → 0; tsc ĐẠT · eslint 0 lỗi; **chưa kiểm bằng mắt** (TASK-B02) |
| TASK-021 | **Sửa GỐC ngữ nghĩa vai trò**: `roleBase` nạp từ `role_catalog` + `requireRole` nhận mã engine + 13 điểm gọi về mã engine | DONE | #22 (cùng lượt) | 18/09/2026 | docs/28; công cụ `probe-role-code-scan` · `patch-role-engine-codes` · `verify-java-compile`; **0 lỗi mức CAO** · 13/13 phép thay thế khớp · biên dịch 99 tệp **0 lỗi**; **chưa đóng gói lại JAR** |
| TASK-021b | **Sửa HỒI QUY do TASK-021**: `principalAsCurrent` dựng `CurrentUser` với `roleBase` = mã chuẩn ⇒ 16 action sẽ 403 với mọi tài khoản không phải admin. Vá bằng `default roleBase()` trên `Principal` + controller truyền `cu.roleBase()` thật | DONE | #23 (cùng lượt) | 18/09/2026 | Cổng `probe-action-role-parity` nay có mục **ĐƯỜNG ỐNG roleBase** để bắt đúng lớp lỗi này |
| TASK-022 | Rà soát action Java **thiếu** `requireRole` so với JS + thắt 5 action còn hở | DONE | #23 (cùng lượt) | 18/09/2026 | TASK-022.md; cổng `probe-action-role-parity` **exit 0**: 0 hở · 0 chặt hơn · 21 khớp; `OpsTaskManagementUseCase` được thêm hạ tầng RBAC |
| TASK-023 | **Kiểm PHẠM VI dự án/kho** (lỗ hổng P0: JS kiểm 64 action, Java kiểm 0) — `AccessScopeService` + port `AccessScopeStore` dùng chung | **DONE** | #24…#29 · #31 · #32 (lô 8a) · #33 (lô 8b) | 18/09/2026 | **64/64 · cổng `probe-action-scope-parity` exit 0**; hồi quy **cổng ảnh 28/28 ĐẠT**; `probe-live-stack` ALL PASS; jar béo BUILD SUCCESS + health UP |
| TASK-023b | 3 action `ProjectContractUseCase` — kiểm phạm vi ở **tầng web** (đúng chú thích lớp) + đưa `Principal` vào 2 phương thức cần tra bản ghi | **DONE** | #33 (cùng lượt) | 18/09/2026 | `save_project_contract` kiểm ngay ở controller; `set/delete` nhận `Principal` để kiểm `row.project_id` |
| TASK-028 | Cổng ảnh báo **28/28 lệch ~96%** — điều tra và xác định **KHÔNG phải hồi quy mã nguồn** | DONE | — | 18/09/2026 | Nguyên nhân: tiến trình Java khởi động bằng `Start-Process` **đã chết** ⇒ proxy :9000 trả **502**. Đã ghi bài học vào `docs/29` mục 6.6–6.7: **không dùng `Start-Process` cho Java API**; trước khi kết luận hồi quy giao diện phải kiểm `probe-live-stack` |
| TASK-026 | ~~Dọn khối `rbac.requireRole` bị nhân đôi~~ **MỞ RỘNG**: còn phát hiện thêm **import bị lặp** và **dòng `rbac.requireRole` lặp thêm 5 chỗ** do cùng lỗi idempotency | DONE | #31 · #32 | 18/09/2026 | `tools/cleanup-duplicate-requirerole.mjs` nay gỡ cả import lặp; **đã sửa THỨ TỰ KIỂM trong công cụ vá** (`to` trước `from`) để không tái diễn |
| TASK-025 | `SlaComplianceWorker` lỗi `bad SQL grammar` **lặp mỗi giờ** ⇒ worker SLA chưa bao giờ chạy được | **DONE** | #34 (cùng lượt) | 18/09/2026 | **Nguyên nhân gốc:** câu lệnh dùng cột `overdue_at` **không tồn tại** — không có ở lược đồ MySQL (`V1__baseline.sql:1795-1810`), không có ở SQLite (`drizzle/0005:31-49`), và **JS không hề dùng**. Đã bỏ cột khỏi câu lệnh. **Bằng chứng chạy thật:** log đổi từ `WARN ... bad SQL grammar` (mỗi giờ) sang `INFO SLA worker: 29 supply steps quá hạn; 0 payment plans quá hạn; 4 BCH chờ xác nhận.` |
| TASK-026 | Sửa lỗi **idempotency của công cụ vá** (đã gỡ 5 khối `rbac.requireRole` bị nhân đôi) + ghi bài học | DONE | #31 (cùng lượt) | 18/09/2026 | Nguyên nhân: kiểm "đã áp dụng chưa" bằng `includes(from)` mà `from` là dòng chữ ký phương thức (vẫn còn sau khi chèn) ⇒ chèn lần hai. `tools/cleanup-duplicate-requirerole.mjs` đã gỡ và **kiểm chứng không còn khối lặp** |
| TASK-024 | Lệch `isCompanyLeadership`: JS = 7 mã + `base_role='director'`; Java = `{director, accountant}` | BLOCKED | — | 18/09/2026 | **Cần người dùng quyết định** — thuộc nhóm bảo mật đang tạm hoãn; Java cấp thừa cho `accountant`, cấp thiếu cho `thuky` |
| TASK-B01 | ~~Xác nhận tên màn Receiving từ menu~~ **ĐÃ GIẢI QUYẾT** bằng dữ liệu, không cần hỏi người dùng | DONE | — | 18/09/2026 | Nhãn cuối cùng của module `receiving` = **"Kế hoạch giao hàng"** (nhóm `MUA HÀNG`). Chuỗi tiến hoá: `Giao nhận công trường` (0010) → `Chờ giao hàng` (0011) → **`Kế hoạch giao hàng`** (0029). Khớp với `app/page.tsx:116` |
| TASK-B02 | ~~Cổng ảnh + bộ probe KHÔNG chạy được~~ **ĐÃ GỠ CHẶN** — cổng ảnh cần Edge headless (named pipe), chạy được khi mở rộng sandbox | DONE | — | 18/09/2026 | **Cổng ảnh: 28/28 ảnh ĐẠT, 0 px lệch** trên 7 màn × 4 kích thước. Lưu ý: các probe cần `spawn mysql` vẫn cần mở rộng sandbox |
| TASK-B03 | ~~Không đóng gói lại được JAR~~ **ĐÃ GỠ CHẶN** — nguyên nhân gốc gồm 3 lớp, đã xử lý cả 3 | DONE | — | 18/09/2026 | (1) `JAVA_HOME` chưa đặt → mvn báo sai về `.m2`; (2) sandbox chặn ghi `.m2` → dùng local repo trong workspace qua `.mvn/maven.config` (PowerShell làm hỏng `-D` vì đường dẫn có dấu cách); (3) tiến trình Java đang giữ JAR → dừng đúng PID rồi repackage. **`BUILD SUCCESS`, jar béo 90.883.789 bytes, server khởi động lại, `/actuator/health` = UP** |
| TASK-B01 | Xác nhận tên màn Receiving từ menu | BLOCKED | — | 17/09/2026 | Cần người dùng; KHÔNG chặn tiến độ |
| TASK-027 | Kiểm chứng **SỐNG** bằng tài khoản thật (`da_truong`/`kho_tong`/`cht`/`ksda`…) cho các sửa đổi vai trò + phạm vi | **DONE** | #35 (cùng lượt) | 18/09/2026 | **Phương pháp:** gọi action với **payload rỗng** — chốt quyền chạy TRƯỚC validate nên 403 = bị chặn, 400 = được phép, **không ghi dữ liệu nào**. **Kết quả:** vai trò 10/10 đúng mã engine; chặn đúng **351/351, lọt 0**; **403 tầng vai trò = 0**. **Tìm + vá 2 LỖI P0:** (1) `create_request`/`decide_approval` dựng lớp vô danh `Principal` **thiếu `roleBase()`** ⇒ trả mã chuẩn `ksda` ⇒ chỉ admin lập được phiếu đề nghị mua (đã xác nhận lại sau vá: 403 chuyển từ tầng vai trò sang tầng phạm vi); (2) `AccessScopeService:77` so `"warehouse".equals(role)` với mã ENGINE nhưng nhận mã CHUẨN ⇒ **nhánh phạm vi kho là mã chết** (chặn oan kho site + lọt kho central). Xem `docs/agent-progress/TASK-027.md` |
| TASK-029 | Bản đồ **MODULE** của Java **CHẶT HƠN JS trên 30 action** (JS không kiểm module, Java kiểm) | **BLOCKED** | — | 18/09/2026 | Đo bằng `tools/probe-action-module-parity.mjs`: MODULE khớp **156/186**, **30 lệch — tất cả cùng chiều** `JS=[] → Java=[module]`; CAPABILITY khớp **185/186** (lệch 1: `system_level_impact` JS `canUse` vs Java `canView`). **Tác động đã định lượng:** 6/30 action (`save_team_subcontract`, `save_team_production`, `approve_team_production`, `save_team_payment`, `settle_team_subcontract`, `create_project_team`) bắt buộc module `teams`/`site_command` — mà `teams` có **0 dòng** quyền ⇒ **chỉ admin làm được**, trong khi **JS cho phép theo vai trò**. **Cần người dùng quyết định** giữ (an toàn hơn nhưng lệch §6) hay khôi phục đúng JS — **KHÔNG tự ý đổi** |
| TASK-030 | 43 ca bị chặn ở **tầng MODULE** — xác định là **thiếu dữ liệu** hay hành vi đúng | **DONE** | #36 · #37 | 18/09/2026 | **Kết luận: ĐÚNG DỮ LIỆU, không phải lỗi mã.** `tools/probe-module-permission-data.mjs` đọc chính payload `bootstrap`: `user_module_permissions` chỉ có **484 dòng** cho 61 module × 12 người dùng (đầy đủ phải 732), và **5/9 module JS yêu cầu có 0 dòng toàn hệ thống** (`stocktake`, `inventory`, `teams`, `warehouse_issue`, `boq`). **Đã LOẠI TRỪ nguyên nhân sâu hơn:** 0 module không tồn tại · 0 module bị tắt · 0 nhóm menu bị tắt ⇒ **nguyên nhân duy nhất là thiếu dòng quyền**, chỉ cần cấp quyền. JS đọc **chỉ** bảng này nên **chặn y hệt** ⇒ không phải sai lệch mã. **Đóng known-problem #15:** tên capability ánh xạ đúng cột thật (`ModulePermissionStoreAdapter:22-24`); `central_warehouse` hoạt động nhưng **0 dòng quyền** ⇒ nhánh kho-central chỉ đạt mức ĐỌC cho số ít tài khoản, **không bao giờ đạt mức GHI** — khoảng trống dữ liệu, không phải lỗi mã. **Bài học:** lần chạy đầu phép kiểm báo "nhóm menu TẮT" cho cả 9/9 module vì tôi so `active === 1` trong khi payload trả **boolean `true`** ⇒ kết quả đồng loạt bất thường phải nghi **phép kiểm trước khi nghi hệ thống**. Việc nạp đủ quyền là **thao tác dữ liệu của quản trị viên**, cần người dùng quyết định (a) chạy lại cấp mặc định theo phòng ban, (b) cấu hình thủ công, (c) giữ nguyên |
| TASK-008 | U-09 đợt 5 — chuẩn hoá toolbar 3 màn tab Quản trị (UserPermissionMatrix, SystemLevelManager, AuditLogManager) | **DONE** | #38 | 18/09/2026 | Code đợt 5 xong trước đó; **lượt quét hồi quy rộng là bước còn lại đã hoàn thành ở #38**: vá **F1** — `EntityDetailModal` gọi `setState` trong `useEffect` (`react-hooks/set-state-in-effect`) làm `npm test` **dừng ngay ở `lint`** nên `typecheck` + 8 tệp `test:regression` + `test:workflow` **chưa từng chạy**; nay suy ra tab khi render ⇒ `eslint .` **0 error**, bộ kiểm tiến qua `typecheck`. **Kết quả cổng:** vai trò exit 0 · phạm vi exit 0 · `probe-live-stack` ALL PASS · **cổng ảnh 28/28 ĐẠT 0 px** · `test:regression` **61 test / 58 PASS / 3 FAIL** (đã phân loại) |
| TASK-031 | Cây dự án trong menu **bị TẮT ở CẢ HAI nav** — chủ ý sản phẩm hay tắt nhầm? | **BLOCKED** | — | 18/09/2026 | Sentinel `__site_command_tree_disabled__` ở `app/page.tsx:664` (desktop) **và** `:678` (mobile) ⇒ nhánh `activeSiteProjects.map(...)` là **MÃ CHẾT**; `activeSiteProjects` (dòng 671) vẫn tính mà không còn nơi dùng. Sentinel vào ở commit **`1c01f39`** (16/09) — **TRƯỚC** công việc RBAC, và **mô tả commit KHÔNG nhắc việc tắt cây dự án** ⇒ đưa vào âm thầm, test không cập nhật. `CONFLICT` ⇒ **cần người dùng quyết định**; **KHÔNG tự sửa** và **KHÔNG sửa test cho khớp mã** (làm vậy là đóng băng một trạng thái có thể là lỗi) |
| TASK-033 | MASTER TASK **§8.1** — dải phê duyệt phiếu thiếu **PHÒNG BAN** của người duyệt | **IN PROGRESS** | (chờ dựng bundle) | 18/09/2026 | Đối chiếu §8.1 (6 thông tin/bước): **5/6 đã có**, thiếu đúng **PHÒNG BAN** (thời gian ✓, trạng thái ✓, người duyệt ✓, số bước ✓, **ý kiến ĐÃ có sẵn**). Bản vá: suy `approverDept` từ `approval.approverUserId → data.staffDirectory[].department` (tên đọc được, không phải mã) + hiển thị nhãn "Phòng ban:" **nguyên văn** từ component dùng chung. **Kiểm chứng:** tsc **exit 0** · eslint **0 error/74 warning** (đúng mốc nền) · cổng ảnh **28/28 · 0 px** (dự đoán nêu trước: ĐÚNG) · bản vá idempotent + 5/5 hậu kiểm. **Thêm phép kiểm mới** vào `probe-request-page.mjs` (kiểm theo TỪNG bước) — và nó **bắt đúng sự thật**: bundle đang chạy cũ (`dist` 09:35 vs nguồn 12:41). **Gỡ được chặn dựng bundle tầng 1**: `preflight-source.mjs:205` + `trust-lock-foundation.test.mjs:19` thiếu `.local-data` (thư mục runtime đã gitignore) ⇒ `npm run build` bị chặn cứng; nay in `FULL W2 SOURCE PREFLIGHT: ĐẠT`. **CÒN CHẶN tầng 2:** dấu vân tay nguồn lệch, **không có script ghi lại** — xem TASK-034 |
| TASK-034 | **`npm run build` không dựng lại được UI** — dấu vân tay nguồn lệch và không có công cụ tái lập | **BLOCKED** | — | 18/09/2026 | `verify-vntech-fingerprint.mjs:29`: expected `0c5e9c0a…` vs actual `d8e90726…`. Phạm vi dấu vân tay (`lib/trust/source-fingerprint.mjs`): tệp gốc + `app db deploy drizzle lib public scripts tests worker` (**không** phủ `docs/`, `tools/`); loại trừ duy nhất `lib/vntech-identity-data.mjs`. Giá trị kỳ vọng ở `lib/vntech-identity-data.mjs` + `VNTECH_FINGERPRINT.json`. **KHÔNG có script GHI** (chỉ có `verify:fingerprint` để đọc). Hệ quả: **30 commit chưa lần nào dựng lại** ⇒ bundle đang phục vụ cũ hơn nguồn ~3 giờ ⇒ **bạn sẽ test thủ công trên bundle cũ**. Cần người dùng xác nhận tái lập định danh (ghi `sourceFingerprint`/`sourceFingerprintShort` mới + đồng bộ `VNTECH_FINGERPRINT.json`) rồi mới build |
| TASK-032 | **LỖ HỔNG LIÊN KẾT DỮ LIỆU**: 0/16 vai trò trỏ tới đơn vị mặc định | **BLOCKED** | — | 18/09/2026 | `organization_units` có **8 đơn vị đang hoạt động, gồm `BGD` = "Ban giám đốc"**, nhưng `role_catalog.default_organization_unit_id` **null ở MỌI vai trò (0/16)** ⇒ "đơn vị mặc định theo vai trò" âm thầm không thể chạy. Đo bằng `tools/show-role-catalog.mjs`. **Bẫy kỹ thuật:** không tệp SQL/Java nào nhắc `default_organization_code` — đó là tên hiển thị sinh từ JOIN trên `default_organization_unit_id`, tìm theo tên đó sẽ không thấy gì. Kèm 1 điểm lệch test: tên `thuky` thật dài hơn ("…/ Trưởng phòng Hành chính Pháp chế") so với test. **KHÔNG tự sửa dữ liệu nghiệp vụ** |
| TASK-035 | **GOAL §8** — admin đổi workflow khi hồ sơ đang pending: theo bản CŨ hay bản MỚI? | **DONE** (điều tra) | #40 (cùng lượt) | 18/09/2026 | **KẾT LUẬN: KHÔNG có version pinning — hồ sơ đang chờ đổi theo cấu hình MỚI ngay lập tức.** Bằng chứng: (1) `requests[]` **không có** `workflowId`/`workflowVersion` — chỉ `approvalStage` là một con số (đo dữ liệu sống) và `approvals[]` chỉ có `stage`; (2) mọi thuộc tính bước tra **LIVE** khi quyết định (`RequestStoreAdapter:270,283 LEFT JOIN approval_stage_catalog ON stage_no`; `canApproveRequestStage` tra pool/owner/roles live); (3) `workflow_definitions.version` **không hoạt động như versioning** — cả `drizzle/0080:46` lẫn **MySQL đang chạy** `V8__workflow_multi.sql` đều là `UNIQUE (workflow_id, step_no)`, **không có `version`** ⇒ không thể tồn tại song song bước 1 của v1 và của v2. **RỦI RO (`LIKELY`):** tắt/xoá một bước đang có hồ sơ chờ ⇒ `allowed` rỗng ⇒ không ai duyệt được và không có đường thoát. **PHÁT HIỆN THÊM (`CONFIRMED`):** **Java RỘNG HƠN JS** — JS có **0** tham chiếu tới cả 3 bảng P4 (chỉ legacy), Java còn cấp quyền qua `workflow_step_approvers`; bảng này **seed một lần** rồi có thể **trôi lệch** ⇒ xếp cùng nhóm TASK-029. **KHÔNG tự sửa kiến trúc** (GOAL §8 + nguy cơ phá 20+ bản ghi phê duyệt đang chạy) — 3 câu hỏi ở `TASK-035.md` mục 7 |
| TASK-022b | `ACTION_CATALOG` "chưa ghi 12 action chỉ có ở Java" | **DONE — KHÔNG cần sửa** (tiền đề SAI) | #41 (cùng lượt) | 18/09/2026 | **Đo được:** danh mục **174** mục, bản thi hành `ActionRbacRegistry.java` **186**; lệch 12 một chiều, 0 chiều ngược. **Danh mục CÓ HỢP ĐỒNG là "sinh từ nguồn JS"** và ĐANG đúng: `probe-action-parity` exit 0 — *"Catalog KHỚP HOÀN TOÀN với nguồn JS"*, cả hai chiều 0, và *"Action của JS mà Java THIẾU: 0"*. **Thêm 12 mục vào sẽ TẠO SAI LỆCH GIẢ** (inCatNotJs 0 → 12) ⇒ **phá một cổng đang xanh**. 12 action đó **đã khai đầy đủ** module+capability trong chính bản thi hành mà `requireActionModule` dùng ⇒ **không bỏ sót gì**. **Giả thuyết rủi ro đã kiểm và BÁC BỎ:** không có tệp nào GHI `ACTION_CATALOG.json`, và `patch-rbac-registry.mjs` chỉ `replace` chứ không xoá ⇒ không có đường tự động làm mất mục. **Củng cố TASK-035:** 3 trong 12 mục là `save_workflow`/`set_workflow_status`/`delete_workflow` ⇒ khớp việc JS có **0** tham chiếu bảng P4 (quản trị workflow là Java-only). **Để lại cổng** `tools/probe-catalog-drift.mjs` (exit 0) — báo HỎNG nếu có tên lạ ngoài tập 12 Java-only đã biết |
| TASK-036 | **GOAL §8/§7** — hai cột cấu hình của bước workflow (`required_permission`, `allow_skip_level`) **không được thi hành ở đâu** | **DONE** (điều tra) | #42 (cùng lượt) | 18/09/2026 | **CONFIRMED:** cả hai cột tự khai trong DDL MySQL là *"Quyền cần có để được chọn làm người duyệt"* / *"Cho phép cấp bậc cao hơn duyệt vượt cấp"*, nhưng quét **toàn kho** 4 tên (camel+snake) trên `*.tsx *.ts *.mjs *.js *.sql` ra **9 chỗ — TẤT CẢ là SQL** ⇒ **UI không dùng**; Java chỉ **ghi/đọc/hiển thị** (5 chỗ), **không** nhánh quyết định nào đọc; form quản trị bước (`page.tsx:3834`) **chỉ có** `slaHours` + `approvalMode` ⇒ **không bao giờ gửi**. **Mức độ: THẤP (tiềm ẩn)** — hôm nay không gây hại (không ai đặt được qua UI), nhưng là **bẫy cấu hình**: thêm ô nhập sau này sẽ tạo "hạn chế im lặng không tác dụng". **Trả lời GOAL §8 chiều *permission*:** quyền của bước đến từ `allowed_role_codes` + pool (`workflow_step_approvers` ∪ owner) + quyền module — **KHÔNG** từ `required_permission`. **Củng cố lớp P4 là tính năng dở dang** — 4 phép đo độc lập hội tụ (JS 0 tham chiếu · version không hoạt động · 2 cột không thi hành · action quản trị Java-only) |
| TASK-037 | **GOAL §7** — rà cổng quyền của **12 action Java-only** (8 action có `module=[]` ⇒ mặc định TỪ CHỐI) | **DONE** (điều tra) | #43 (cùng lượt) | 18/09/2026 | **PHẦN 1 — CHÍNH ĐÁNG, 0 rủi ro:** cả **8/8** action `module=[]` đều đã được controller chặn bằng `requireRequireAdmin` ⇒ nhánh mặc-định-từ-chối **không chặn oan** chức năng nào (`tools/audit-java-only-actions.mjs`, exit 0). **PHẦN 2 — 1 cổng MONG MANH:** `save_workflow`/`set_workflow_status`/`delete_workflow` **KHÔNG** chặn theo vai trò, chỉ cần **đăng nhập** + quyền **module `admin`**; đo dữ liệu thật: **0 dòng quyền module `admin`** ⇒ hôm nay đúng là chỉ admin vào được, nhưng **nếu sau này ai được cấp module `admin`** thì quản trị được workflow mà không phải admin. **CHỜ BẠN QUYẾT:** (A) thêm chặn vai trò admin cho nhất quán, hay (B) giữ nguyên (coi đây là quyền module — đúng thiết kế) |
