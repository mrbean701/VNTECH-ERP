# MT2 — DANH SÁCH PHASE & TASK (bản điều khiển tiến độ)

> **MASTER TASK 2** · VNTECH ERP V5.3.0 (MASTER BASELINE R1.1.1)
> Cấu trúc **giống MASTER TASK 1** (110 mục / 12 phase): chia thành **PHASE → TASK** để dễ kiểm soát.
> Nguồn sự thật chức năng: `docs/dsh/MASTER_TASK_2.md` · Quy trình: `docs/dsh/GOAL_MASTER_TASK_2.md`
> Audit: `docs/dsh/AUDIT_MT2_GAP.md` · Trạng thái: `docs/dsh/MT2_EXECUTION_STATE.md`
> ⛔ **NO COMMIT · NO PUSH** · Cập nhật: **26/09/2026** (mốc MỞ ĐỢT là 23/09/2026 — ⚠️ các mục bổ sung từ §H.20 của sổ kiểm trở đi ghi ngày **26/09**; mục CẬP NHẬT ở §0 là mốc mới nhất)

---

## 0. TỔNG QUAN

```text
TỔNG SỐ PHASE : 14
TỔNG SỐ TASK  : **100**   *(P1=8 · P2=10 · P3=9 · P4=5 · P5=4 · P6=8 · P7=5 · P8=12 · P9=9 · P10=6 · P11=5 · P12=7 · P13=6 · **P14=6**)*
                       ✅ **CHỐT MẪU SỐ 23/09/2026**: dải phase ở dòng trên TRƯỚC ĐÂY ghi «98» nhưng tự nó cộng ra 99 và khớp 99 dòng thật ⇒ mẫu số = 99; **➕ `MT2-P14-03b` (23/09)** là task PHÁT SINH khi audit license ⇒ **MẪU SỐ HIỆN TẠI = 100**. Chênh theo phase: **P2 = 9 dòng (kế hoạch 10)** · **P3 = 10 dòng (kế hoạch 9, gồm `P3-06b`)** · **P14 = 6 dòng (kế hoạch 5, gồm `P14-03b`)**.
TRẠNG THÁI    : **DONE 93** · IN_PROGRESS 0 · BLOCKED 5 · SKIPPED 2 · TODO 0   ← đếm BẰNG MÁY (script phân loại 100 dòng `| MT2-…`; nhóm «KHÁC» = `P6-04` + `P11-03` — cả hai DONE)
CÔNG THỨC     : tiến độ MT2 = (DONE + 0.5×IN_PROGRESS) / **100** → **74/81 = 91,4 % (số ĐẾM ĐƯỢC — ⚠️ đang đối soát, xem đính chính)** (P4-01/P5-03/P5-04/P10-05/P14-05 BLOCKED không tính DONE)
GHI CHÚ       : ⚠️ Máy đếm tự động xếp **2 dòng** `P6-04` và `P11-03` vào nhóm «KHÁC» (ô trạng thái viết khác mẫu, ⛔ KHÔNG phải chưa xong) — **cả hai đã DONE** ⇒ cách đếm chuẩn sau đợt đính chính: **DONE 91 + KHÁC 2 = 93** · SKIPPED 2 (`P7-05` · `P11-05`) · BLOCKED 5.
                MT2-000 (audit) + MT2-001 (tài liệu) là 2 task ĐIỀU PHỐI — ĐÃ XONG, ⛔ không nằm trong 100 task chức năng.
                ✅ **ĐỐI SOÁT LẠI THEO LOG (23/09/2026)**: PHASE 1/2/3/4/6/7/8/9/12/13 đã chốt theo bằng chứng chạy được (test/probe/bundle); PHASE 5/10/11 còn đúng phần BLOCKED/SKIPPED như bảng.
                ▸ **§2 — SỔ ĐĂNG KÝ «TẠM BỎ QUA» (9 miền, MT2 §2 + §38): xem `PHỤ LỤC A`** — đã ĐO LẠI 23/09/2026 để chắc chắn ⛔ không bỏ sót việc đã làm (chi tiết bằng chứng ở PHỤ LỤC A).
CẬP NHẬT      : 23/09/2026 — 🔎 **ĐÍNH CHÍNH TRẠNG THÁI: `P8-05` = DONE** (⛔ trước ghi SKIPPED kèm lý do «§6.5 Đối tác» — **SAI PHẠM VI**: lý do đó thuộc tính năng **Đối tác**, còn dòng này là **§6.3 Nhà cung cấp** trong MUST IMPLEMENT). Đo lại đủ đường: `SupplierDetailModal.tsx` **218 dòng** có `TABS = ["Thông tin","PO","Danh sách vật tư"]` · `SupplierManager.tsx:14` import · nút **«Chi tiết»** `title="Mở MODAL chi tiết NCC (§6.3)"` gọi `setDetail(row)` · dữ liệu từ hàm THUẦN `supplierToPurchaseOrderChain()` · render `<SupplierDetailModal … openPo={open("poDetail")} loadGaps={…}/>` ⇒ **19/19 hợp đồng XANH** (p08-supplier-po-material + p2-08) + p3-05 **3/3** ⇒ 🎉 **PHASE 8 = 12/12** · **74/81 = 91,4 % (số ĐẾM ĐƯỢC — ⚠️ đang đối soát, xem đính chính)** · **HẾT TODO**. 🔓 **P14-03 (QA DB) + P14-03b (parity license) ĐỀU DONE** ⇒ **PHASE 14 = 5/6 DONE + 1 BLOCKED (P14-05)**: P14-03b thêm lớp `LicenseEnvelopeVerifier` (port 1-1 `lib/trust/*`: canonical JSON + schema + **Ed25519**) · port/adapter/use case/controller theo **hợp đồng JS hiện hành** (licenseEnvelope + 18 cột thật + `vntech_trust_audit`) · test mới **4/4** · **full Java `mvn test` = 61/61** *(số ĐO LÚC ĐÓ)* · `probe-java-sql-schema` **8 mismatch = 0** · P14-03: 3 cổng DB XANH (SQL gate · schema drift **0 lệch** · reference integrity **15/15, 0 mồ côi**). 🎓 Bài học vá được: H2 trả **NHÃN CỘT HOA** ⇒ phải tra khoá không phân biệt hoa/thường. 🎉 **PHASE 6 = 8/8** · 🎉 **PHASE 4 = 4/4** · **PHASE 5 = 2/4** (P5-03/P5-04 BLOCKED chờ user chốt ngưỡng «phó GĐ trở lên») · 🎉 **PHASE 3 = 9/9** · ✅ **PHASE 2 = 9/9** · ✅ **PHASE 1 = 8/8** · 🎉 PHASE 13 = 6/6 · 🎉 PHASE 12 = 7/7 · **PHASE 11 = 4/5** · **PHASE 10 = 5/6** (P10-05 BLOCKED) · 🎉 PHASE 7 = 4/4 + 1 SKIP · 🎉 **PHASE 8 = 12/12** · PHASE 9 9/9
                🔄 **SỐ HIỆN HÀNH (đo lại cuối ngày 23/09/2026 — ⛔ thay cho các số cũ trong cùng dòng này):**
                • **Full Java `mvn -pl web -am test` = 64/64 · 0 Failures · 0 Errors · BUILD SUCCESS** *(61/61 là số ĐO LÚC P14-03b)*
                • **Fingerprint hiện hành = `VNTECH-FP-7A4835FBC5BCBCA7`** (nguồn **513** tệp) — *(mã `FP-6D21E3F925E64E7E` ghi ở cuối dòng trên là bản CŨ, đã bị thay bởi 2 lần làm mới identity: `MT2-P14-03c`)*
                • **Frontend = 570 hợp đồng · 569 PASS · 0 FAIL · 1 skip** · **LIVE = 9/9 bộ chính + `probe-p2-live-approval` 4/4 + `probe-live-rolebase` 9/9 · `probe-request-page` ĐẠT** · **0 lỗi SẢN PHẨM đang đỏ**
                • **`MT2-P14-03c` (23/09) đã TÌM & VÁ 1 LỖI THẬT**: nút «Thu gọn khối» ở màn Phiếu đề nghị **vô tác dụng** (state `collapsed` chỉ đổi nhãn) → đã sửa `RequestDrawer.tsx` + `globals.css`, **xác minh LIVE bằng click chuột THẬT** (`bodyScroll 1605 → 375`) và **9/9 probe live không hồi quy**; đồng thời sửa **13 lỗi CÔNG CỤ/PROBE** (chi tiết: `MT2_GATE_SWEEP_23-09.md` §H).
                • 🔎 **ĐỢT RÀ PROBE ĐÃ CHỐT (kết thúc ở `MT2_GATE_SWEEP_23-09.md` §H.19, sổ kiểm ở §H.18.1, 3 ca cuối ở §H.21/§H.22/§H.23): TỔNG 109 tệp probe · 109/109 đã có kết quả · 28 lỗi CÔNG CỤ/PROBE đã sửa · 1 lỗi SẢN PHẨM thật đã vá (nút «Thu gọn khối») · 0 lỗi SẢN PHẨM đang đỏ.** Mọi kết luận đỏ đều **được ĐỌC MÃ / ĐO CSDL / ĐO LIVE** trước khi phân loại; ⛔ không có ca nào kết luận bằng suy đoán. Các nhóm nguyên nhân: CŨ‑SHAPE do chuẩn hoá UI §22 (ô tìm kiếm/nút nay ở component dùng chung `ListToolbar`) · CŨ‑SHAPE do **thêm tab** (12 → 13) và **đổi tên** (Nhân sự → Tài khoản · CÔNG VIỆC CỦA TÔI → CÔNG VIỆC) · SAI PHƯƠNG PHÁP ĐO (`data.audits` có `LIMIT 100` nên ⛔ không được so số lượng) · TIỀN ĐỀ SAI (`approve_stock_issue` giao cho **kế toán**, ⛔ không phải kho · phòng KH **đã có** `dept_legal_correspondence`) · MÔI TRƯỜNG/FIXTURE (mysql, dữ liệu tồn dư, credential `thukydemo` 401).
```
Quy ước mã task: **`MT2-P<phase>-<số>`** · Trạng thái: `TODO` · `IN_PROGRESS` · `BLOCKED` · `DONE` · `SKIPPED`.

---

# PHASE 1 — NỀN TẢNG DỮ LIỆU (DATA) · 7 task
**Mục tiêu:** đủ cấu trúc dữ liệu cho toàn bộ MT2, **chỉ ADD/CREATE an toàn** (⛔ không DROP/DELETE/TRUNCATE).
**Phụ thuộc:** không (nền tảng) · **Cổng xong phase:** Flyway đạt version mới + test H2 xanh + không hồi quy.

| ID | Task | Deliverable | Trạng thái |
|---|---|---|---|
| **MT2-P1-01** | Migration `approvals.overdue_reason` (lý do quá hạn SLA §4.4) | **V25 đã áp dụng** — log: *Successfully applied 1 migration … now at version **v25*** ✔ | **DONE** |
| **MT2-P1-02** | Migration `users.signature_url` (chữ ký 1 ảnh §13.4) | **V25 đã áp dụng** ✔ *(cùng migration)* | **DONE** |
| **MT2-P1-03** | Vá **đường test H2** cho 2 cột trên — ⚠️ **PHẢI KHAI Ở CẢ 2 NƠI** *(đã đo bằng test thật, KHÔNG suy luận)*: ① `UserJpaEntity.signatureUrl` — Hibernate **đọc cột theo entity**, thiếu ⇒ `Column "uje1_0.signature_url" not found` làm hỏng **38 test** ✗ · ② `ALTER TABLE … ADD COLUMN IF NOT EXISTS` trong `schema-h2.sql` — bảng H2 `users`/`approvals` lấy từ file này nên phải **có cột thật** ✗ | ✅ **DONE** — bằng chứng: **42 test · 3 Failures (đúng 3 ca CÓ SẴN `ProductionRoleCounterProofTest`) · 0 Errors** ✔ *(trước khi vá: 0 Failures / **38 Errors**)* | **DONE** |
| MT2-P1-03b | **Cập nhật bài học vào memory dự án** — ghi chú cũ nói “ALTER trong schema-h2 bị Hibernate xoá ⇒ chỉ cần khai ở entity” là **CHƯA ĐỦ**: thực tế **cần CẢ HAI** (entity + schema ALTER) | Memory cập nhật | **DONE** — ghi chú (d) ở `docs/18_…md` nay có khối **«CẬP NHẬT 23/09/2026 — CHƯA ĐỦ»** ngay dưới (giữ nguyên văn bản cũ): quy tắc mới **PHẢI KHAI Ở CẢ 2 NƠI** + **vì sao luật cũ hết đúng** (`application-test.yml:18` nay `ddl-auto: none` ⇒ lược đồ H2 = chính `schema-h2.sql`) + bằng chứng số (`0 Failures/38 Errors` ⇒ `42/3/0`) + 2 đường dẫn schema THẬT (bản TEST `web/src/test/resources` và bản DEMO `web/src/main/resources/db/demo` — ⛔ đính chính đường dẫn `db/demo/…` ở gốc repo là SAI) + bẫy DDL động `PREPARE/EXECUTE` (V21/V22) mà generator không bắt. Đã ghi bài học vào **memory dự án** (quy tắc, mức `critical`). Khoá kiến thức: contract MỚI [`tests/p1-03b-h2-lesson.test.mjs`](tests/p1-03b-h2-lesson.test.mjs) **3/3 XANH** — ⛔ sẽ ĐỎ nếu ai đổi `ddl-auto`, xoá khối `[H2-MANUAL-START]`, bỏ cột ở entity, hoặc sửa bài học sang đường dẫn schema sai. |
| **MT2-P1-04** | Bảng **notification config** (§45): loại Web/Email · tên · mã · nội dung · recipient targeting · giờ gửi · giờ kết thúc | ✅ **DONE** — `V26__mt2_notification_config.sql`: **3 bảng** `notification_configs` (đủ 11 trường §45) + `notification_config_targets` (**polymorphic**: user/department/project ⇒ mở rộng không cần đổi cấu trúc) + `notification_user_states` (đọc/nhắc lại **theo user**). **BẰNG CHỨNG**: Flyway *now at version **v26*** ✔ · `SHOW TABLES LIKE 'notification%'` = 3 bảng ✔ · H2 test **42/3 ĐỎ CÓ SẴN/0 Errors** ✔ · 3 dịch vụ 200/200/200 ✔ · ⛔ KHÔNG sửa `task_notifications` | **DONE** |
| **MT2-P1-05** | Bảng trạng thái đọc/nhắc lại theo `userID + notificationID` (§48/§49) | ✅ **GỘP VÀO P1-04** — đã tạo `notification_user_states` (`config_id`,`user_id` UNIQUE · `read_at` · `snooze_until` · `delivered_at`) ✔ | **DONE** |
| **MT2-P1-06** | Bảng **vật tư NCC** (§6.3/§6.4): liên kết NCC ↔ vật tư + cờ auto-detect | ✅ **DONE** — `V27__mt2_supplier_materials.sql`: `supplier_materials` (id · supplier_id · material_id · times_ordered · last_ordered_at · last_unit_price · active · timestamps) + **UNIQUE (supplier_id, material_id)** *(đúng thứ §6.4 cần để tra nhanh “vật tư này đã từng đặt từ NCC chưa”)*. **BẰNG CHỨNG**: Flyway *now at version **v27*** ✔ · `SHOW INDEX` có UNIQUE 2 cột ✔ · H2 test **42/3 ĐỎ CÓ SẴN/0 Errors** ✔ · 3 dịch vụ 200/200/200 ✔ · ⛔ bảng KHÔNG seed dữ liệu (⛔ không bịa) | **DONE** |
| **MT2-P1-07** | Audit nguồn đặt `approvals.due_at` + cột thời lượng SLA (đã biết `task_sla_policies` là SLA **công việc**, không có cột thời lượng) | ✅ **DONE — KẾT LUẬN BẰNG BẰNG CHỨNG MÃ**: ① `due_at` đặt tại `RequestManagementUseCase.java:392` = `now.plusSeconds(slaHours*3600)`, **chỉ khi bước được xếp hàng** (`isQueued`), ngược lại `null` ✔ ② **Thời lượng SLA DUYỆT = `approval_stage_catalog.sla_hours`** *(theo TỪNG BƯỚC)*, **mặc định 8 giờ** khi chưa cấu hình (`OpsTaskManagementUseCase.java:445-446`) ✔ ③ SLA chuỗi cung ứng riêng: `supplyPoSlaHours()` + `bchConfirmationSlaHours` (màn Quản trị) ✔ ④ CSDL khớp: 259 approvals · **101 `due_at` NULL** · TB **16,4 giờ** ✔ ⑤ `task_sla_policies` **KHÔNG có cột thời lượng** (chỉ cờ + **`requires_reason`**) ⇒ xác nhận là SLA **công việc** ✗ ⇒ **hệ quả cho P3-06**: validate “quá hạn duyệt phải nhập lý do” phải dựa `due_at` + MT2 §4.4, ⛔ không dùng `task_sla_policies` | **DONE** |

---

# PHASE 2 — COMPONENT DÙNG CHUNG (FRONTEND FOUNDATION) · 8 task
**Mục tiêu:** tách/dùng component dùng chung — **điều kiện tiên quyết** cho §22–24.
**⚠️ ĐIỀU CHỈNH LỚN sau audit:** **UI KIT ĐÃ CÓ SẴN** ở `app/components/ui/` *(7 tệp/661 dòng)* ⇒ **KHÔNG tạo kit mới**, chỉ **DÙNG + mở rộng** *(MT2 §15 REUSE)*. Chi tiết: `docs/dsh/MT2_UI_KIT_INVENTORY.md`.
**Phụ thuộc:** không · ⚠️ Phase này **không đổi hành vi** (trừ P2-02 sửa bố cục) ⇒ phải chạy regression.

| ID | Task | Deliverable | Trạng thái |
|---|---|---|---|
| **MT2-P2-01** | Kiểm kê component dùng chung | ✅ `docs/dsh/MT2_UI_KIT_INVENTORY.md` — `app/page.tsx` 2.780 dòng/~110 component *(~15 màn · ~30 modal)* | **DONE** |
| **MT2-P2-07** | *(mới)* Định vị kit chính thức | ✅ `app/components/ui/`: DataTable(133) · Timeline(163, **ApprovalTimeline**) · EntityDetailModal(143, +GuardedEntityModal) · ListToolbar(107) · StatusBadge(61) · PermissionGuard(41) · index.ts | **DONE** |
| **MT2-P2-02** | **Dải phê duyệt NGANG** (§4.3) — ⚠️ không phải "tạo modal" như kế hoạch cũ ✗ | ✅ **TÌM RA BUG THẬT**: `canonical.css:734 .vt-timeline{flex-direction:column}` = **DỌC** ✗ đúng lời anh phàn nàn. ĐÃ SỬA: thêm `.vt-timeline.is-horizontal` *(flex-row + đường nối NGANG `o----o` + `waiting` ẩn người duyệt/phòng ban + `@media<768px` tự về dọc)* + `Timeline.tsx` thêm prop `layout` mặc định `horizontal`. **BẰNG CHỨNG**: bundle `VNTECH-FP-2426FA5189BCEA1C` (435 file) · CSS đang phục vụ `index-Dc3_V6hl.css` có **`is-horizontal` ×8** + **`flex-direction:row`** ✔ · tsc 0 · regression **69/69** · workflow ĐẠT | **DONE** |
| **MT2-P2-03** | Dùng `ListToolbar` (đã có search+filters+sort) cho mọi toolbar ngang §22 | ✅ **DONE (AUDIT + KẾT LUẬN)** — §22 **đã áp dụng gần hết**: page.tsx **16 lần**, Inventory 2, Purchasing(§6.6/§6.8), Receiving(§7.3), Delivered(§6.13), Payments, TeamDirectory(§8), Requests, ReportView, WorkCenter, TeamManagement, WorkflowModal ≥1 ✔. **2 tệp còn thiếu ĐÃ CHUYỂN sang phase sở hữu** *(theo §43 dependency — tránh làm 2 lần)*: ① `SupplierManager.tsx` ⇒ **gộp vào MT2-P8-03/P8-04/P8-05** *(phase này sẽ dựng lại tệp: modal tạo NCC · modal chi tiết 3 tab · auto-detect vật tư)* ② `MaterialListTable.tsx` ⇒ **gộp vào MT2-P11-03/P11-04** *(task đó sẽ fix root cause + tách 3 tab)* ③ `ProjectTeams.tsx` ⇒ MT2-P7-02. ⏭️ PartnerManager = SKIP ✔ · Stocktake ngoài danh sách MT2 | **DONE** |
| MT2-P2-04 | Chuẩn hoá `DataTable`: sort/filter/empty state + ⛔ không overflow §24 | Kiểm + chỉnh | **DONE** — audit `app/components/ui/DataTable.tsx` và CSS dùng chung: sort + aria-sort + click handler, loading/error/empty thống nhất, `table-wrap`/`baseline-table`, `tableClassName`/`rowClassName`/`rowStyle`/`cellClassName`; thêm contract test **4/4 xanh**; focused tabs/DataTable **37/37**, regression **69/69**, `tsc` 0; không sửa hành vi UI vì baseline đã đáp ứng yêu cầu. |
| MT2-P2-05 | `CardHead`(124 lần)/`Kpi`(73) **đã đủ** ⇒ chỉ thêm **tabs** nếu `ProjectDetailTabs` không tái dùng được | ⛔ không tạo mới | **DONE** — audit xác nhận `ProjectDetailTabs` đã là nguồn dùng chung: 5 tab nhãn/khoá canonical, `role=tablist/tab`, `aria-selected`, 4 nhánh tab 1–4 đều render lại component, nội dung/nguồn thật cho Nhân sự/Tổ đội/Kho/Lịch sử. Thêm contract **3/3**, focused tabs/detail **31/31**, tsc 0; không tạo component tabs thứ hai. |
| MT2-P2-06 | **Mở rộng `AttachmentPanel`/`FileUpload`**: nhiều ảnh (§6.12/§26) · viewer full (§27) · **fix font chồng chéo** (§10) · chữ ký 1 ảnh (§13.4) | ✅ **DONE (phần a+c)** — `lib/ui-shared.tsx`: thêm `multiple` + `upload()` lặp N request *(báo rõ số tệp thành công/lỗi)* · `canonical.css`: `.attachment-pick{flex-direction:column}` + `.file-upload{flex-wrap:wrap}` + `@media print` ẩn ô tải ⇒ **hết chồng chéo** (§10). **BẰNG CHỨNG**: CSS phục vụ `index-D0IG0ly_.css` có `attachment-pick` ×8 + `flex-direction:column` ✔ · bundle `VNTECH-FP-F84430F4FB963D3F` (437 file) · 3 dịch vụ 200/200/200 · tsc 0 · regression 69/69 · workflow ĐẠT. ⚠️ *Phần (b) viewer full: hiện click ảnh mở TAB MỚI (§27 tạm đạt); (d) chữ ký 1 ảnh ⇒ task riêng **MT2-P12-06*** | **DONE (a+c)** |
| **MT2-P2-06b** | *(mới — phát sinh)* **Lightbox xem ảnh trong app** thay vì mở tab mới (§27) | Modal/overlay ảnh full + đóng bằng ESC | **DONE** — audit: `AttachmentPanel` đã có lightbox + ESC nhưng **dòng ảnh trong danh sách tệp vẫn mở tab mới**. Đã nối click ảnh sang lightbox (`preventDefault` + `setPreview`), giữ tệp không phải ảnh tải như cũ, thêm marker `data-vntech="attachment-lightbox"`. Contract **2/2** (đỏ trước khi sửa), focused modal/lightbox **23/23**, regression **69/69**, `tsc` 0. |
| **MT2-P2-08** | *(mới)* Rà **~30 modal tự chế** trong `page.tsx` ⇒ chuyển sang `BaseModal`/`EntityDetailModal` theo §23 | Danh sách + chuyển dần | **DONE (AUDIT + chuyển lô 1)** — inventory thật: 9 `role="dialog"` tự chế / 8 tệp; `page.tsx` đã dùng `<BaseModal>` 33 lần. Lô 1: chuyển modal **Tạo công việc/nhiệm vụ** trong `ProjectDetailTabs.tsx` sang `BaseModal` (giữ nguyên field + backend `create_work_item`, bỏ overlay/header tự chế) ⇒ inventory còn **8 dialog / 7 tệp**: `SupplierDetailModal`(2) · `Requests` · `SupplierManager` · `Receiving` · `page.tsx`(1: permission-help) · `EntityDetailModal`(chính là modal dùng chung) · `ReceiptDrawer`. Contract P2-08 **2/2**; focused modal/tab **44/44**; regression **69/69**; `tsc` 0. |

---

# PHASE 3 — BACKEND SERVICE & API · 9 task
**Mục tiêu:** mọi logic ra backend (đúng chỉ đạo “frontend chỉ render”) + **backend là enforcement layer** (§17).

| ID | Task | Deliverable | Trạng thái |
|---|---|---|---|
| MT2-P3-01 | **`NotificationService` + Rule + Recipient Resolver + Log** (kiến trúc §15.1) | Service mới + test H2 | **DONE (tách tầng §15.1)** — audit: pipeline đã nằm trong MỘT class `NotificationManagementUseCase`; đã TÁCH 2 mắt thành lớp riêng `application/notification/NotificationRule` (chọn cấu hình theo event + kênh web/email) và `NotificationRecipientResolver` (all/user/users/department/project, khử trùng, ⛔ không bịa người nhận); service vẫn là cửa DUY NHẤT điều phối Business Event → Rule → Resolver → Web/Email → Log → Read/Delivery, constructor không đổi nên `ApplicationBeansConfig` giữ nguyên. Bằng chứng: contract `p3-01-notification-architecture` **3/3** (đỏ→xanh) · `NotificationRuleTest` **EXIT 0** · `NotificationCenterTest` **EXIT 0** · full `mvn -q test` **EXIT 0**. |
| MT2-P3-02 | API **notification config CRUD** (§45) | action + RBAC + test | **DONE (AUDIT-VERIFIED, 0 dòng mã mới)** — đã có ĐỦ 4 action: `save_notification_config` (C/U) · `notification_configs` (R) · `set_notification_config_status` (bật/tắt) · `delete_notification_config` (D); RBAC `ActionRbacRegistry` gác module **admin** với capability `canCreate`/`canView`/`canEdit`; xoá cấu hình ⛔ KHÔNG xoá `notification_user_states` (lịch sử đọc). Bằng chứng: node contract §13.1/§13.2 **14/14**; `NotificationCenterTest` **EXIT 0** (CRUD + recipient scoping + active period + email queue + admin RBAC). |
| MT2-P3-03 | API **read-state theo user** (§48/§49) + “không nhắc lại hôm nay” | action + test | **DONE (AUDIT-VERIFIED, 0 dòng mã mới)** — đã có ĐỦ 3 action: `mark_notification_read` · `mark_notification_all_read` · `mark_notification_snooze`, tất cả ⛔ **không nhận `userId` từ payload** mà dùng `cu.id()` của CHÍNH người gọi (chống snooze/đánh dấu thay người khác); 3 action nằm trong `PUBLIC_ACTIONS` (self-service, ⛔ không gác module); luật snooze +24h do backend giữ; `markAllRead` INSERT nốt dòng thiếu theo ĐÚNG bộ lọc của `notificationsForUser`. Bằng chứng: node contract `p13-02` + `p13-03` **13/13**; `NotificationCenterTest` **EXIT 0**. |
| MT2-P3-04 | API **upload/đổi chữ ký** (§13.4 — thay ảnh cũ, đúng 1 ảnh) | API + `/api/files` + test | **DONE (AUDIT-VERIFIED, 0 dòng mã mới)** — chữ ký lưu **ĐÚNG 1 ảnh/user** ở cột `users.signature_url`: upload ảnh mới ⇒ **GHI ĐÈ** giá trị cũ, gửi rỗng ⇒ ghi NULL = xoá (⛔ không có danh sách chữ ký active). 2 đường ghi: self-service `update_profile_signature` (dùng `cu.id()`, `PUBLIC_ACTIONS`) + admin `create_user`/`update_user` (chỉ ghi khi payload CÓ khoá `signatureUrl`). Kiểm ảnh **dùng lại đúng luật khuôn avatar** (MIME + 2 MB), ⛔ không phát minh luật mới. ⚠️ Ghi nhận trung thực: chữ ký đi theo **data URL trong payload** (thiết kế sẵn có), ⛔ không qua `/api/files` — nhưng vẫn thoả «đúng 1 ảnh, thay ảnh cũ». Bằng chứng: contract `p12-06` **6/6**; `ProfileSignatureTest` **EXIT 0**. |
| MT2-P3-05 | API **vật tư NCC**: thêm/xoá/liệt kê + **auto-detect khi tạo PO** (§6.4) | action + test | **DONE** — §6.4 ĐÃ có đủ 3 action thật: `supplier_materials` (liệt kê kèm mã/tên/ĐVT) · `save_supplier_material` (UPSERT theo UNIQUE(`supplier_id`,`material_id`), tăng `times_ordered`, cập nhật `last_unit_price`) · `supplier_material_gaps` (truy vấn `NOT EXISTS` tìm vật tư dòng PO chưa có trong danh mục NCC); RBAC gác module `supplier_catalog` (`canView`/`canEdit`/`canUse`). **§6.4 ⛔ không tự thêm**: UI `SupplierDetailModal` chỉ ĐỌC gaps rồi hỏi ĐÚNG nguyên văn «Vật tư này chưa có trong danh mục vật tư của nhà cung cấp. Bạn có muốn thêm không?», việc ghi nằm sau thao tác **bấm của user** (`approveAdd`), ⛔ không có `useEffect` auto-add. Bằng chứng: `SupplierMaterialTest` + `RbacSupplierMaterialTest` **EXIT 0** (gồm ca user thường 200/403) · contract mới `p3-05-supplier-material-autodetect` **3/3** · `p08-supplier-po-material` **17/17**. |
| **MT2-P3-06** | API **duyệt quá hạn**: validate bắt buộc lý do (§4.4) | ✅ **DONE** — ① `RequestStoreAdapter` thêm **`a.due_at AS duedate`** *(alias một từ viết thường — né bẫy H2 hạ alias)* ② `RequestManagementUseCase.decideApproval` chèn validate: **quá hạn + `comment` rỗng ⇒ 400** *“Bước này đã QUÁ HẠN SLA. Bắt buộc nhập lý do duyệt quá hạn.”* — **backend enforcement §17** ✔ **TEST**: `RequestOverdueReasonTest` ✔ | **DONE** |
| **MT2-P3-06b** | Lưu vết `overdue_reason` + **test ĐỎ→XANH** | ✅ **DONE** — ③ port+adapter `updateApprovalOverdueReason` = `UPDATE approvals SET overdue_reason=?…` +use-case gọi khi `overdue` ✔ **TEST 3 CA** (`java-backend/web/src/test/java/…/RequestOverdueReasonTest.java`): ① **chưa quá hạn + lý do rỗng ⇒ 200** và chuyển bước *(⛔ không chặn oan)* ② **quá hạn + rỗng ⇒ 400** + `$.error` chứa “QUÁ HẠN SLA” & “Bắt buộc nhập lý do” + hồ sơ **vẫn ở bước cũ** + ⛔ KHÔNG ghi lý do ③ **quá hạn + có lý do ⇒ 200** + `approvals.overdue_reason` **ĐƯỢC GHI** ✔ **BẰNG CHỨNG**: `mvn -B -pl web -am test` ⇒ **Tests run: 43 · Failures: 3 · Errors: 0** *(43 = 42 nền + 1 test mới; 3 ca Đỏ = ĐÚNG 3 ca CÓ SẴN `ProductionRoleCounterProofTest`)* · `RequestOverdueReasonTest`: **1 run · 0 failures · 0 errors** ✔ | **DONE** |
| **MT2-P3-07** | API **tracking quá hạn**: `total overdue` + theo phòng ban/người duyệt/thời lượng | ✅ **DONE** — **AUDIT**: `SlaStore` chỉ có cho chuỗi cung ứng + kế hoạch thanh toán ⇒ ⛔ không có cho approvals ✗; `page.tsx:973` chỉ đếm `stageCount` ✗ ⇒ **CODE** `BootstrapDataAdapter` (sau `:162`) dựng **`data.approvalOverdue`** = `{total, byDepartment:[{department,total,avgOverdueMinutes,maxOverdueMinutes}]}` — **TÍNH BẰNG JAVA** từ `approvalRows` sẵn có ⇒ ⛔ **không bảng/cột mới** + ⛔ **không SQL riêng** (`TIMESTAMPDIFF` là cú pháp MySQL ⇒ H2 hỏng) · chỉ tính bước `decidedAt == null` mà `dueAt` đã qua ✔ **TEST 3 MỐC** (`RequestOverdueReasonTest.tongHopQuaHanDuyet_demDungVaBoBuocDaQuyetDinh`): ① bình thường ⇒ `total=0` ② ép `due_at` quá khứ 2 giờ ⇒ `total=1` + có `byDepartment` + `maxOverdueMinutes` ≠ 0 ③ duyệt xong ⇒ về `total=0` (⛔ không đếm bước đã có quyết định) ✔ **BẰNG CHỨNG**: `RequestOverdueReasonTest` **2 run · 0 failures · 0 errors** · tổng **44 test / 3 ĐỎ CÓ SẴN / 0 Errors** *(44 = 42 nền + 2 test mới)* ⛔ không hồi quy ✔ ⚠️ **LỖI EM TỰ GÂY & TỰ SỬA**: build lần 1 FAIL `cannot find symbol — class Instant` (tệp ⛔ không import `java.time.Instant`) ⇒ dùng tên đầy đủ ✔ | **DONE** |
| **MT2-P3-08** | Fix **PR table**: tách `status` vs `approval_stage` (§6.9) | ✅ **DONE** — sửa **5 chỗ** ở `app/screens/Purchasing.tsx`: ① `purchasingPrStatusLabel` (PR CHỈ dùng PR map, ⛔ không lùi sang nhãn PO) ② `SUPPLY_STATUS_LABEL`+`purchasingSupplyStatusLabel` (trục cung ứng RIÊNG; ⚠️ `issued` **CỐ Ý không có nhãn** ⇒ hiện nguyên văn, đúng §6.9 “⛔ không tự thêm state `Issued` cho PR”) ③ `purchasingApprovalStageLabel(row,data)` ⇒ ô “Bước duyệt” hiện **TÊN bước** từ `data.approvalStages`, thiếu cấu hình ⇒ «Bước N» (⛔ không bịa tên) ④ bộ lọc «Trạng thái» **bỏ nhánh khớp chéo `supplyStatus`** ⑤ dropdown bộ lọc dùng **`statusOptionLabel`** theo ĐÚNG loại dòng (chỉ PR ⇒ nhãn PR · chỉ PO ⇒ nhãn PO · cả hai ⇒ hiện cả hai) + ⛔ **xoá hẳn `purchasingStatusLabel`** (hàm trộn) + xoá `isPurchaseRequestRow` (dead code, §27) **BẰNG CHỨNG**: `tsc --noEmit` **0** ✔ · `npm run test:regression` **69/69** ✔ · `npm run test:workflow` **passed** ✔ · rebuild `gd-cycle` **GD_EXIT 0** · **FINGERPRINT ĐẠT `VNTECH-FP-B66D6990037DA5EB`** (439 file) · **BUILT ARTIFACT VALIDATION ĐẠT** ✔ · bundle SSR chứa đủ **3 hàm mới** và **0** hàm trộn cũ ✔ · chunk client `page-BqgxBiej.js` mới build 00:37:04 + có `purchasing-pr-table` ✔ · 3 dịch vụ 200/200/200 ✔ ⚠️ **TỰ SỬA 2 KẾT LUẬN SAI CỦA MÌNH**: (a) báo lỗi “`:208` gọi label với cả ROW” — **SAI**, `:206` truyền VALUE ⇒ rút lại (b) 3 lần dò artifact bằng “tìm chuỗi” đều **sai công cụ** (client bundle bị minify/escape Unicode) ⇒ bài học: ⛔ không dùng tìm-chuỗi-trong-artifact làm cổng nghiệm thu khi chưa kiểm chứng công cụ ⚠️ **CÒN**: ⛔ cần anh quyết dòng `supply_status='issued'` (1 dòng) | **DONE** |
| **MT2-P3-09** | API **tạo phiếu nhập từ STO/phiếu xuất** (§7.4 — tự fill kho đi/đến) | ✅ **DONE (backend)** — nguyên văn MT2 §7.4: *“tạo phiếu nhập từ **STO/phiếu xuất kho**: nếu phiếu liên quan đã có **kho đi/kho đến** ⇒ **tự động fill**”*. **AUDIT**: nguồn **phiếu xuất đã xong** (`stockIssueGrnLines`/`insertStockIssueGrn`/`markStockIssueGrnCreated` · `StockManagementUseCase.createIssueGrn` · RBAC `create_issue_grn` · `SystemController:1224`) nhưng nguồn **STO THIẾU TOÀN BỘ** ✗ ⇒ **ĐÃ LÀM 5 TẦNG**: ① port +3 hàm ② adapter +3 (số phiếu dòng **`GRN-STO`** · kho nhận = `destination_warehouse_id` **tự điền** · chốt `status='received'`) ③ use-case **`createTransferGrn`** (soi gương `createIssueGrn`, có khử trùng dòng theo `transferOrderItemId`) ④ RBAC `create_transfer_grn → receiving/canCreate` (⛔ 0 module mới) ⑤ `SystemController` case mới · **TEST**: thêm **nhánh STO** vào `SupplyChainEndToEndIntegrationTest` (tái dùng PO đã tạo qua API ⇒ ⛔ không seed thêm MR/PO): âm sai trạng thái ⇒ 400 + 0 GRN · dương ⇒ 200 + `GRN-STO-` + kho nhận tự điền + `posted` + gắn PO + đúng 1 dòng · âm gọi 2 lần ⇒ 400 + vẫn 1 GRN ✔ **BẰNG CHỨNG**: **Tests run: 44 · Failures: 3 · Errors: 0** (3 Đỏ = ĐÚNG baseline `ProductionRoleCounterProofTest`) ⛔ không hồi quy ✔ · `SupplyChainEndToEndIntegrationTest` **1/0/0** ✔ ⚠️ **TEST BẮT 3 LỖI CỦA EM**: ① subquery tương quan ⇒ H2 `BadSqlGrammar` ② `purchase_order_items.material_id` ⛔ KHÔNG tồn tại ⇒ **CẢ HAI đã được ghi sẵn trong chú thích `stockIssueGrnLines`** mà em ⛔ không đọc thân hàm ③ `assertNotNull` chưa static-import ⇒ **BÀI HỌC: soi gương hàm phải đọc CẢ THÂN HÀM + chú thích trong thân, ⛔ không chỉ chữ ký** ⚠️ **CÒN**: bước ⑦ UI nút «Tạo phiếu nhập từ lệnh điều chuyển» (gộp đợt UI màn Kho — Phase 9) | **DONE** |

---

# PHASE 4 — RBAC & PHẠM VI THEO CHỨC VỤ · 5 task
**Mục tiêu:** §3.2 + §4.1 + §16 + §52 — **backend quyết định quyền**, frontend chỉ hiển thị.
**⚠️ Chặn:** BLK-01 (`pho_giam_doc`) · BLK-02 (2 user trống cấp) — cần user quyết trước khi làm P4-01.

| ID | Task | Deliverable | Trạng thái |
|---|---|---|---|
| MT2-P4-01 | **Phạm vi xem/giao việc theo chức vụ**: trưởng phòng = phòng ban mình · phó GĐ trở lên = toàn công ty | service phạm vi + test âm/dương | ✅ **DONE (26/09/2026 — đề án ①A user chốt)** — `V30` thêm cấp `pho_giam_doc` (`level_rank` **35**, ĐO từ CSDL: nằm giữa trưởng phòng 30 và giám đốc 40); Flyway đã áp thật `success=1` ⇒ `system_level_catalog` **5 → 6 cấp**. Port `userDepartment()` + adapter; **`WorkScopeService`** 3 tầng `SELF/DEPARTMENT/COMPANY` (ngưỡng **30/35** ĐO từ CSDL, ⛔ không hard-code ở UI, thiếu cấp bậc ⇒ `SELF` ⛔ không mở quyền); `OpsTaskManagementUseCase` ctor 6 tham số + `workScope()` + `canAssignToDepartment()` (giữ luật cũ, CHỈ thêm tầng phó GĐ). **Test `WorkScopeServiceTest` 10 ca âm/dương**; đã **smoke server thật** (`scope=COMPANY · levelRank=50`). |
| MT2-P4-02 | **Ẩn card “phiếu chờ duyệt”** cho user không đủ quyền (quản trị hệ thống hoặc ≥ trưởng phòng) | RBAC backend + test | **DONE (AUDIT-VERIFIED — đã có trong baseline)** — `BootstrapDataAdapter` chặn 2 lớp: ① thiếu module duyệt ⇒ `blank(requests, supplySteps, …, approvals, approvalOverdue)` (vá lỗ rò dữ liệu duyệt) ② **admin OR `system_level_catalog.level_rank >= 30`** (`truong_phong`, ĐO từ CSDL) — nếu không đủ thì `blank(approvals, approvalOverdue)`; ⚠️ `level_rank` NULL/0 dòng ⇒ **KHÔNG đủ** (⛔ không suy diễn). Bằng chứng: `RequestOverdueReasonTest` (gồm 2 ca P4-02: nâng cấp ⇒ thấy vùng duyệt · **hạ cấp xuống `nhan_vien` ⇒ bị blank**) **EXIT 0**. |
| MT2-P4-03 | **Card “Chờ Giám đốc duyệt”** lấy dữ liệu **thực** từ approval engine (§4.1) | API + test | **DONE (AUDIT-VERIFIED — đã có trong baseline)** — action `director_pending_approvals`: use-case **tự ném 403** nếu không phải admin và `level_rank < 30`, cộng RBAC module `approvals`/`canView` (2 lớp); UI `WorkCenter.DirectorPendingCard` gọi API thật và **403 ⇒ tự ẩn card** (⛔ không vỡ màn). Bằng chứng: `DirectorPendingApprovalsTest` (**cấp ≥ trưởng phòng ⇒ 200** · **cấp thấp dù CÓ module `approvals` ⇒ 403**) **EXIT 0**. |
| MT2-P4-04 | Audit 5 bảng role/level (`role_catalog` · `business_role_*` · `user_*_scopes`) ⇒ **mở rộng đúng chỗ**, ⛔ không tạo cơ chế quyền mới | Báo cáo + bảng ánh xạ | **DONE** — báo cáo [`docs/agent-progress/MT2-P4-04-ROLE-LEVEL-AUDIT.md`](MT2-P4-04-ROLE-LEVEL-AUDIT.md) với **SỐ LIỆU THẬT từ MySQL** (`vntech_erp`): `role_catalog` 16 · `business_role_engine_catalog` 9 · `business_role_group_catalog` 11 · `business_role_group_scopes` **2** · `user_module_permissions` **1403** (61 module / 49 user) · `user_project_scopes` 19 (15 user) · `user_warehouse_scopes` 12 (4 user) · `system_level_catalog` 5 mức (10/20/30/40/50). **Bảng ánh xạ** khái niệm → bảng → điểm thi hành (§2 báo cáo): `RbacService.requireRole` (so CẢ `role()` và `roleBase()`) · `RbacService.requireActionModule` + `ActionRbacRegistry` · `AccessScopeService.canAccessProject/canAccessWarehouse` · ngưỡng `level_rank>=30` cho vùng duyệt. **Phát hiện**: F1 cầu nối «nhóm nghiệp vụ ↔ phạm vi» (`business_group_id` NULL 16/16 · `business_role_group_scopes` 2 dòng) **có bảng nhưng chưa nối vào luồng quyền** ⇒ cần user quyết, ⛔ không tự bịa luật; F2 ngưỡng `30` tham chiếu cứng ở 2 chỗ (giữ nguyên, đổi phải sửa cả 2 + test); F3 thiếu dòng scope ⇒ `false` có chủ đích (đã có test ngoại lệ PR không dự án). ⛔ **0 bảng/cột mới, 0 cơ chế quyền mới**. |
| MT2-P4-05 | Rà **mọi action MT2** có khai module/capability trong `ActionRbacRegistry` (bài học F1: module rỗng ⇒ 403 toàn bộ) | Danh sách + vá thiếu | **DONE** — công cụ MỚI `tools/probe-action-registry-coverage.mjs` phân loại **210 action dispatch** ở `SystemController` thành 4 nhóm hợp lệ: ① `PUBLIC_ACTIONS` **9** ② khai MODULE **141** ③ admin-only qua `List.of()` + admin bypass **52** ④ **admin-gated ngay tại controller** (`requireRequireAdmin`) **8** ⇒ **⛔ MÙ QUYỀN = 0**. **VÁ THIẾU 1 chỗ THẬT**: `notification_log` khai module `admin` nhưng thiếu capability ⇒ rơi về mặc định `canUse` trong khi đây là thao tác ĐỌC ⇒ thêm `Map.entry("notification_log", "canView")` (soi gương `notification_configs`). ⚠️ Công cụ phải **lọc tên RÀNG BUỘC CSDL** (`*_uidx`, `*_pkey`, `primary_key`…) vì regex `case "x" ->` khớp cả các `switch` đó (bẫy đã trả giá — nếu không lọc thì báo ~47 "mù quyền" GIẢ). Bằng chứng: `probe-action-role-parity` **EXIT 0** (22/22 nhóm vai trò khớp JS↔Java, đường ống `roleBase` đủ) · `probe-action-scope-parity` **EXIT 0** (67/67 action JS kiểm phạm vi đều được Java kiểm, 0 không ánh xạ) · `probe-action-registry-coverage` **EXIT 0** · `NotificationCenterTest` + `RbacSupplierMaterialTest` **EXIT 0**. ⚠️ `probe-security-rbac.mjs` (cần 3 dịch vụ đang chạy) ⛔ CHƯA chạy lượt này vì Java/UI/proxy đang tắt — ghi nhận trung thực, sẽ chạy lại khi bật dịch vụ. |

---

# PHASE 5 — CÔNG VIỆC (§3) · 4 task
| ID | Task | Deliverable | Trạng thái |
|---|---|---|---|
| MT2-P5-01 | Click menu **Công việc ⇒ hiển thị Dashboard ngay** (§3.1) | UI + router | **DONE (NGHIỆM THU LIVE)** — mã đã có từ 22/09 (sửa lỗi có sẵn `view:"kpi"` ⇒ `view:"dashboard"` + 5 chỗ liên quan, xem `MT2-PHASE-5-AUDIT.md`); lượt này **ĐO LẠI trên bundle ĐANG PHỤC VỤ** bằng trình duyệt headless THẬT (công cụ MỚI `tools/probe-p5-dashboard-menu.mjs`, qua proxy :9000): ① mục ĐẦU TIÊN của sidebar là lối vào Dashboard (`nav-dashboard-direct` «TỔNG QUAN ĐIỀU HÀNH», đang `active` khi vào) ✔ ② mở nhóm «Công việc» ⇒ **thứ tự DOM thật: Dashboard → Cá nhân → Phòng ban → Giao việc → Báo cáo** ✔ ③ **click «Dashboard» ⇒ tab «Dashboard» ĐANG CHỌN** (`aria-selected=true`) ✔ ④ đối chứng âm: click «Cá nhân» ⇒ tab đổi sang «Cá nhân» ✔ ⇒ **4/4 ĐẠT**. Gate: `tsc` **0** · regression **69/69** · contract `p5-01-work-menu-dashboard` **4/4**. ⚠️ Ghi trung thực: `nav-dashboard-direct` là lối vào màn dashboard của VỎ ứng dụng (⛔ không mở WorkCenter tablist) — phép đo tab vì vậy đi qua mục con «Dashboard» đúng câu chữ §3.1. |
| MT2-P5-02 | Đưa Dashboard lên **đầu menu** hoặc **bỏ menu item thừa** (§3.1) | menu config | **DONE (NGHIỆM THU LIVE)** — chọn **§3.1 ②** (đưa lên đầu, ⛔ KHÔNG xoá mục ⇒ ⛔ không thể mất lối vào): DOM thật của bundle đang phục vụ xác nhận mục «Dashboard» **ĐỨNG ĐẦU** nhóm «Công việc»; contract test kiểm cả thứ tự khai báo trong `lib/menu-helpers.ts` (deepEqual 5 khoá, Dashboard đầu) và tab index suy từ `WORK_TABS` thật (⛔ không hard-code mù). Bằng chứng: probe 4/4 (mục ② ở trên) + `tsc` 0 + regression 69/69. |
| MT2-P5-03 | UI hiển thị **quyền xem/giao theo cấp** (dùng API P4-01) | UI + test | ✅ **DONE (26/09/2026 — đề án ①A)** — user đã chốt phương án **(A) thêm cấp `pho_giam_doc`** ⇒ ngưỡng nay **XÁC ĐỊNH ĐƯỢC**. Đã có: API **`work_scope`** (`SystemController` ⇒ `OpsTaskManagementUseCase.workScope()`) trả `scope` · `levelRank` · `department` · 4 cờ `canView*`/`canAssign*` + 2 ngưỡng `levelRankTruongPhong=30` / `levelRankPhoGiamDoc=35` ⇒ UI có đủ dữ liệu để hiển thị quyền theo cấp. **Test `WorkScopeServiceTest` 10 ca** (dương: 35/40/50 ⇒ COMPANY; âm: null/10/20 ⇒ SELF; trưởng phòng không rõ phòng ban ⇒ SELF). ⚠️ **Sửa 1 lỗi THẬT tự phát hiện:** cờ `canViewDepartmentWork` từng trả `false` cho tầng `COMPANY` (so sánh nhầm `kind == DEPARTMENT`) ⇒ đã vá + khoá bằng test + **verify live = `true`**. |
| MT2-P5-04 | Giao việc **đúng phạm vi** + chặn ở backend | UI + test âm | ✅ **DONE (26/09/2026 — đề án ①A)** — `OpsTaskManagementUseCase.canAssignToDepartment()` giữ **NGUYÊN** luật cũ (`userIsDepartmentManager`) và **CHỈ THÊM** tầng §3.2:42 «phó GĐ trở lên ⇒ giao việc toàn công ty theo quyền» (⛔ không nới cho tầng thấp hơn, ⛔ không bỏ lớp RBAC ở controller). ⛔ **Backend vẫn là nơi chặn** (§17). Ca âm đã khoá bằng test: `truongPhong` (30) ⛔ **không** giao được sang phòng khác; thiếu cấp bậc ⇒ `SELF`. |

---

# PHASE 6 — TRUNG TÂM PHÊ DUYỆT (§4) · 8 task
| ID | Task | Deliverable | Trạng thái |
|---|---|---|---|
| MT2-P6-01 | Dashboard cards đúng RBAC + card **Chờ Giám đốc duyệt** | UI (dùng P4-02/03) | **DONE (AUDIT-VERIFIED + khoá regression)** — ⛔ **0 dòng mã sản phẩm mới**: card «Chờ Giám đốc duyệt» đã có sẵn (`WorkCenter.DirectorPendingCard`) gọi **action thật** `director_pending_approvals`, **403 ⇒ TỰ ẨN** (⛔ không vỡ màn); tầng chặn là BACKEND: `BootstrapDataAdapter` `blank(…, approvals, approvalOverdue)` khi thiếu module duyệt **hoặc** không phải admin và `level_rank < 30`, cộng RBAC `approvals`/`canView`. Contract MỚI `tests/p6-01-approval-dashboard-cards.test.mjs` **5/5** (khoá regression — ⚠️ ghi trung thực: mã có trước nên ⛔ không giả vờ đỏ→xanh; hành vi 403 theo cấp bậc đã được `DirectorPendingApprovalsTest` + `RequestOverdueReasonTest` chứng minh **EXIT 0** ở P4-02/P4-03). |
| MT2-P6-02 | **List → “Phiếu đang xử lý” → nút “Chi tiết” → modal** (§4.2) — ⛔ không mở detail ngay | UI | **DONE (TDD đỏ→xanh)** — sửa ĐÚNG 2 chỗ ở `app/page.tsx`: ① dòng hàng đợi `onClick` **bỏ `open("detail", row)`**, chỉ còn `setSelectedId(String(row.id))` ⇒ click phiếu CHUYỂN sang khu vực «PHIẾU ĐANG XỬ LÝ», ⛔ không mở modal ngay ② nút trong khu vực xử lý đổi nhãn «◉ XEM / TẢI PHIẾU» ⇒ **«◉ CHI TIẾT»** (vẫn `open("detail",selected)`) ⇒ có đúng nút «Chi tiết» mở modal. Bằng chứng: contract MỚI `tests/p6-02-approval-queue-detail.test.mjs` **2/2 đỏ→xanh** · `npx tsc --noEmit` **0** · `npm run test:regression` **69/69** (⚠️ phải cập nhật 1 assert CŨ ở `runtime-admin-boq-regression.test.mjs:312` vì §4.2 đổi NHÃN nút — hành vi `open("detail")` ⛔ không đổi, lý do ghi ngay tại chỗ theo GOAL §4 «MT2 ưu tiên»). ⚠️ `dist` ⛔ chưa build lại ⇒ nghiệm thu mắt cần `gd-cycle`. |
| MT2-P6-03 | **Timeline dạng `bước 1 o----o bước 2 o----o …`** (§4.3) | UI | **DONE (AUDIT-VERIFIED + khoá regression)** — ⛔ **0 dòng mã sản phẩm mới**: `ApprovalTimeline` **mặc định `layout="horizontal"`** ⇒ gắn lớp `is-horizontal`; CSS `.vt-timeline.is-horizontal` xếp `flex-direction: row` + **đường nối NGANG** `o----o` (`::before` cao 2px, `left:30px right:0 top:13px`); **§24 responsive**: `@media (max-width:768px)` tự về DÒC + bỏ cuộn ngang (⛔ không tràn layout); bước `is-waiting` ẩn meta/note. Bằng chứng: contract MỚI [`tests/p6-03-timeline-horizontal.test.mjs`](tests/p6-03-timeline-horizontal.test.mjs) **4/4** (⚠️ ghi trung thực: baseline đã có ⇒ khoá regression viết sau) · `p2-d4-approval-timeline` vẫn **3/3**. |
| MT2-P6-04 | Mỗi bước hiện **người duyệt + phòng ban + thời gian**; bước chưa tới **chỉ “đang chờ”** | UI | **DONE (TDD đỏ→xanh)** — §4.3 vi phạm ĐO ĐƯỢC: khối `approval-person` rơi vào nhánh dự phòng `approval?.approverName || <nhãn allowedRoleCodes>` cho **MỌI bước** ⇒ bước **CHƯA TỚI** vẫn lộ người/nhóm duyệt; và bước **đã xử lý** ⛔ không hiện **phòng ban**. Đã sửa 1 chỗ ở `app/page.tsx`: bước chưa tới ⇒ `«Đang chờ»` + chú thích `«Chưa tới bước này»` (⛔ không lộ vai trò người duyệt); bước đã xử lý ⇒ `«Đã xử lý · <phòng ban>»` lấy từ `approval.department` (nguồn thật, ⛔ không bịa). Bằng chứng: contract MỚI `tests/p6-04-future-step-no-approver.test.mjs` **2/2 đỏ→xanh** · bộ contract PHASE 6 **9/9** · `tsc` **0** · regression **69/69**. |
| MT2-P6-05 | **Duyệt khi quá hạn SLA + BẮT BUỘC lý do** (§4.4) | UI + API (P3-06) | **DONE (TDD đỏ→xanh — UI; backend đã xong ở P3-06)** — §4.4 vi phạm ĐO ĐƯỢC: màn duyệt gọi `decide(...)` với `approvalComment` **CÓ THỂ RỖNG dù đang quá hạn** ⇒ chỉ thấy lỗi 400 SAU khi bấm, ⛔ không nhắc và ⛔ không chặn nút. Đã sửa `app/page.tsx`: thêm cổng `const overdueNeedsReason=timing.late&&!approvalComment.trim();` + nhắc **«⚠️ Bước này đã QUÁ HẠN SLA. Bắt buộc nhập lý do duyệt quá hạn vào ô BÌNH LUẬN.»** + **`disabled={overdueNeedsReason}`** cho cả nút ✓ DUYỆT và ↩ TRẢ LẠI (khớp validate 400 của backend — ⛔ backend vẫn là tầng quyết định §17). Bằng chứng: contract MỚI [`tests/p6-05-overdue-reason-ui.test.mjs`](tests/p6-05-overdue-reason-ui.test.mjs) **3/3** (2 ca đỏ trước khi sửa) · `tsc` 0 · regression 69/69. |
| MT2-P6-06 | Card/khối **đếm đơn quá hạn** (§4.4) | UI + API (P3-07) | **DONE (AUDIT-VERIFIED + khoá regression)** — ⛔ **0 dòng mã sản phẩm mới**: `WorkCenter.overdueApprovalCount(data)` chỉ đếm bước `status="pending"` và `dueAt < now`; **thiếu nguồn ⇒ `null` ⇒ UI ghi «chưa có nguồn»** (⛔ không hiện số 0 giả); card «Đơn quá hạn SLA» nằm cạnh card Giám đốc. Backend (P3-07) dựng `data.approvalOverdue` = **`total` + `byDepartment[]`** kèm `avgOverdueMinutes` (số THẬT). Bằng chứng: contract MỚI [`tests/p6-06-overdue-count-card.test.mjs`](tests/p6-06-overdue-count-card.test.mjs) **3/3** · evidence P3-07: `RequestOverdueReasonTest` 3 mốc (bình thường 0 · ép quá hạn ⇒ 1 · duyệt xong ⇒ về 0). |
| MT2-P6-07 | Fix **font chồng chéo + ô upload** tài liệu đính kèm (§4.5) | CSS/UI | **DONE (ĐO LIVE 4 kích thước + TDD khoá CSS)** — **LỖI THẬT tìm bằng ĐO**, không phải suy luận: công cụ MỚI [`tools/probe-p6-07-attachment-layout.mjs`](tools/probe-p6-07-attachment-layout.mjs) (headless Edge + CDP) đo khối «TÀI LIỆU ĐÍNH KÈM» trong hồ sơ chi tiết phê duyệt ⇒ **TRÀN NGANG 77–86px** (`scrollWidth 343 > clientWidth 266`) và **form tải tệp bị co còn 34px** (ô chọn tệp **26px** — ⛔ không dùng được) vì khối cha là **GRID nhiều cột** (đo `gridTemplateColumns = 34px 1fr 25px`) + luật cũ `grid-template-columns: minmax(260px,1fr) auto auto !important`. **ĐÃ SỬA 4 luật CSS** (`app/styles/canonical.css`): ① khối đính kèm trong hồ sơ duyệt ⇒ `display:block` ② form ⇒ 1 cột `minmax(0,1fr)` (bỏ SÀN 260px) ③ chặn trần 100% ở mọi tầng bao ④ clamp ô chọn tệp native (`overflow:hidden` + `text-overflow:ellipsis`). **BẰNG CHỨNG**: probe **4/4** trên bundle mới nhất (`VNTECH-FP-EF1A0EB3429FD95F` · 507 file · BUILT ARTIFACT VALIDATION ĐẠT) **← mã bundle TẠI THỜI ĐIỂM ĐÓ; mã HIỆN HÀNH = `VNTECH-FP-7A4835FBC5BCBCA7`** — 1600×1000 ✅ · 1366×768 ✅ (ô chọn tệp **188×44** / **179×44**, chồng **0px²**, không tràn) · 1280×800 & 1024×768 ➖ **N/A** (khung phải bị THU GỌN theo responsive — ghi rõ, ⛔ không tính ĐẠT khống) · contract MỚI [`tests/p6-07-attachment-layout.test.mjs`](tests/p6-07-attachment-layout.test.mjs) **4/4** · `tsc` **0** · regression **69/69**. ⚠️ Probe có **CỔNG CHỐNG ĐẠT GIẢ**: ô chọn tệp phải ≥60% bề rộng khối (bản đầu báo ĐẠT dù ô chỉ 26px ⇒ đã siết lại). |
| MT2-P6-08 | **Menu 1 cấp** — bỏ lồng “Trung tâm phê duyệt → Trung tâm phê duyệt” (§4.6) | menu config | **DONE (TDD đỏ→xanh + ĐO LIVE)** — §4.6 vi phạm ĐO ĐƯỢC: `app/page.tsx` dựng nhóm `approval_center` («PHÊ DUYỆT») chứa **ĐÚNG 1 mục con** (`approvals` = «Trung tâm phê duyệt»), render dạng `section.nav-tree-group` + `button.nav-parent` (bung nhóm) + `button.nav-child` ⇒ **phải bấm 2 lần** mới vào màn = LỒNG 2 CẤP. Đã sửa: thêm nhánh render **MỤC TRỰC TIẾP** cho nhóm 1 mục (`groupKey===approvalCenterGroup.groupKey && group.children.length===1` ⇒ `button.nav-single-direct[data-nav-single-group]` gọi `activateModule(onlyChild.key)`, kế thừa kiểu `.nav-dashboard-direct`) + lớp CSS `.nav-single-direct` (sáng/tối/thu gọn); ⛔ **chỉ áp cho `approval_center`**, ⛔ không đổi cấu trúc các nhóm khác (nhóm khác có thể còn 1 mục do lọc quyền). Bằng chứng: contract MỚI [`tests/p6-08-menu-single-item.test.mjs`](tests/p6-08-menu-single-item.test.mjs) **3/3 đỏ→xanh** · `T-10` **4/4 vẫn xanh** (giữ nguyên dòng `groupTree.push`, chỉ đổi khâu RENDER) · probe MỚI [`tools/probe-p6-08-menu-single-item.mjs`](tools/probe-p6-08-menu-single-item.mjs) **3/3 ĐẠT trên bundle `VNTECH-FP-6D21E3F925E64E7E`** **← mã bundle TẠI THỜI ĐIỂM ĐÓ; mã HIỆN HÀNH = `VNTECH-FP-7A4835FBC5BCBCA7` (đã ĐO LẠI 23/09: `probe-p6-08` vẫn 3/3 ĐẠT)**: ① không còn `.nav-tree-group[data-nav-group=approval_center]` ② có mục trực tiếp nhãn «Trung tâm phê duyệt» (213×42) ③ **1 click ⇒ màn phê duyệt hiện ngay** · probe `probe-p6-07` **vẫn 4/4** sau khi đổi menu (đã nâng probe hỗ trợ CẢ 2 hình dạng menu) · `tsc` **0** · regression **69/69**. |

---

# PHASE 7 — QUẢN LÝ DỰ ÁN (§5) · 5 task
| ID | Task | Deliverable | Trạng thái |
|---|---|---|---|
| MT2-P7-01 | Toolbar **hàng ngang** trên label “DANH SÁCH DỰ ÁN” (§5.1) | UI (P2-03) | **DONE** — log §PHASE 7 «4/4 TASK THẬT ✅» (bundle marker `list-toolbar-controls`) |
| MT2-P7-02 | **5 tab hoạt động**: Tổng quan · Nhân sự · Tổ đội · Kho · Ban chỉ huy (§5.2) | UI + API | **DONE** — log §PHASE 7 · marker tab dự án (`pr01`/`project` contract test xanh) |
| MT2-P7-03 | Click dòng trong tab ⇒ **modal chi tiết tương ứng** (§5.2) | UI (P2-02) | **DONE** — log §PHASE 7 (modal chi tiết theo tab) |
| MT2-P7-04 | Tiến độ: nút **tạo công việc/nhiệm vụ** + **Excel mẫu** tự tạo để tải/điền/upload (§5.3) | template + import | **DONE** — log §PHASE 7 (`work-item-comment-participant` + BOQ import test xanh) |
| MT2-P7-05 | ⛔ **KHÔNG làm logic đánh giá tiến độ** — chỉ hiển thị; ghi rõ phụ thuộc nghiệp vụ (CHT + TP Dự án quyết) | tài liệu | SKIPPED *(theo MT2 §14)* |

---

# PHASE 8 — MUA HÀNG & CUNG ỨNG (§6) · 12 task
| ID | Task | Deliverable | Trạng thái |
|---|---|---|---|
| MT2-P8-01 | Menu **NCC xuống cuối nhóm** (§6.1) | menu config | **DONE** — mã+gate+LIVE (bundle marker) · log §P8-01 |
| MT2-P8-02 | Đổi tên **“Danh mục nhà cung cấp dùng cho PO” ⇒ “Danh mục nhà cung cấp”** | label | **DONE** — đổi nhãn + LIVE · log §P8-02 |
| MT2-P8-03 | NCC **CRUD + search/sort/filter** (§6.2) | UI + API | **DONE** — `ListToolbar` + LIVE · log §P8-03/P8-04 |
| MT2-P8-04 | **Modal tạo NCC** (⛔ không sideform) — 8 trường §6.2 | UI (P2-02) | **DONE** — 7/7 §6.2 · V28 `suppliers.email` + test `email` 3/3 (§25) · LIVE |
| MT2-P8-05 | **Modal chi tiết NCC 3 tab**: Thông tin · PO · Danh sách vật tư (§6.3) | UI + API (P3-05) | **DONE** — ⚠️ **ĐÍNH CHÍNH TRẠNG THÁI (23/09/2026)**: dòng này từng ghi **SKIPPED** kèm lý do «§6.5 Đối tác» — ⛔ **SAI PHẠM VI** (lý do tạm-bỏ-qua đó thuộc tính năng **Đối tác**, còn deliverable của dòng này là **§6.3 Nhà cung cấp** — nằm trong MUST IMPLEMENT). **ĐO LẠI ĐỦ ĐƯỜNG**: ① `app/screens/SupplierDetailModal.tsx` tồn tại **(218 dòng)** và khai `const TABS = ["Thông tin", "PO", "Danh sách vật tư"]` — đúng 3 tab §6.3 ② **đã NỐI vào màn NCC**: `SupplierManager.tsx:14` import · mỗi dòng NCC có nút **«Chi tiết»** `title="Mở MODAL chi tiết NCC (§6.3)"` gọi `setDetail(row)` ③ dữ liệu Tab 2/3 lấy từ hàm THUẦN dùng chung `supplierToPurchaseOrderChain()` (`lib/p08-nav-trace.ts`) — PO **đã lọc theo NCC** ④ render `{detail && chain && <SupplierDetailModal supplier={detail} purchaseOrders={chain.purchaseOrders} materialLines={chain.materialLines} onClose={…} openPo={(po)=>open("poDetail",po)} loadGaps={…}/>}` ⇒ click PO mở **modal chi tiết PO** · Tab 2 còn có nút «Kiểm vật tư thiếu» (§6.4). **BẰNG CHỨNG**: `tests/p08-supplier-po-material.test.mjs` + `tests/p2-08-modal-reuse-contract.test.mjs` = **19/19 XANH** · `tests/p3-05-supplier-material-autodetect.test.mjs` **3/3 XANH**. |
| MT2-P8-06 | **Auto-detect vật tư NCC** khi tạo PO + **hỏi user** (§6.4) | UI + API (P3-05) | **DONE** — banner hỏi user + LIVE · marker `supplier-material-gap`=1 · log §P8-06 |
| MT2-P8-07 | PR toolbar **hàng ngang** (§6.6) | UI (P2-03) | **DONE** — thêm `sort` THẬT (⛔ không chỉ hiện nút) · LIVE · log §P8-07 |
| MT2-P8-08 | **CARD “VẬT TƯ ĐANG THIẾU”** + modal danh sách + nút **lập PR tự fill** (§6.7) | UI + API | **DONE** — card + modal + prefill PR · marker `requests-shortage-card`=1 · log §P8-08 |
| MT2-P8-09 | Mua hàng & PO toolbar **hàng ngang** (§6.8) + **tách tab PR/PO** (§6.10) | UI | **DONE** — đã có sẵn từ trước, ⛔ 0 dòng code · LIVE · log §P8-09 |
| MT2-P8-10 | Fix **cột bước duyệt bị hiển thị trạng thái** (§6.9 — dùng P3-08) | UI | **DONE** — đã sửa sẵn, ⛔ 0 dòng code · LIVE · log §P8-10 |
| MT2-P8-11 | **Giao nhận công trường**: modal cho 4 card + CRUD/search/sort/filter + “Ghi nhận SL giao” dạng **modal + nhiều ảnh** (§6.11/§6.12) | UI + API | **DONE** — 4 card bọc `onClick` + modal · LIVE · marker `receiving-kpi-row`=1 · log §P8-11 |
| MT2-P8-12 | **Đơn hàng đã giao**: search/sort/filter + đổi nút ⇒ **“Chi tiết đơn giao hàng” modal** + ảnh bảng/viewer + **lịch sử giao nhận** + **xoá bảng ngoài danh sách** (§6.13) | UI | **DONE** — `ReceiptDrawer`⇒MODAL + §6.13 đủ · LIVE · log §P8-12 |

---

# PHASE 9 — KHO VẬT TƯ (§7) · 9 task
| ID | Task | Deliverable | Trạng thái |
|---|---|---|---|
| MT2-P9-01 | Kho hiển thị **dạng card** + dashboard **tổng hợp ↔ theo kho** (§7.1) | UI | **DONE** — card kho + dashboard tổng hợp↔theo kho · LIVE · marker `inventory-warehouse-cards`=2 · log §P9-01 |
| MT2-P9-02 | Nút **Nhập/Xuất** trong màn Kho ⇒ **modal tạo phiếu** (§7.2) | UI | **DONE** — §7.2 ĐÃ ĐẠT sẵn, ⛔ 0 dòng code (audit lần 7) · log §P9-02 |
| MT2-P9-03 | **Nhập kho**: toolbar hàng ngang + sort/filter (§7.3) | UI (P2-03) | **DONE** — `sort` 6 khoá + filter kho/mức thấp · marker `list-toolbar-sort`=1 · log §P9-03 |
| MT2-P9-04 | **Tạo phiếu nhập từ STO/phiếu xuất** + tự fill kho đi/đến + upload ảnh/chứng chỉ (§7.4) | UI + API (P3-09) | **DONE** — ①②③ mã+live · ④ USER CHỐT **OPTION A** · marker `receipt-source-picker` · log §P9-04 |
| MT2-P9-05 | **Xuất kho**: CRUD + search/sort/filter + danh sách phiếu xuất/đơn xuất (§7.5) | UI + API | **DONE** — USER CHỐT **OPTION A** · danh sách 9 cột · marker `issue-list-screen`=1 · log §P9-05 |
| MT2-P9-06 | **CẤP PHÁT — HOÀN TRẢ**: menu mới + 2 tab (§7.6) — **tái dùng** `procurement_allocations` · `central_returns` · `material_returns` | UI | **DONE** — menu `warehouse_allocate_return` + màn `AllocateReturn` 2 tab (khuôn P-07) · LIVE · markers `allocate-return-screen`/`allocate-return-tab`=1 · log §P9-06 |
| MT2-P9-07 | Cấp phát: danh sách + mã đơn/người tạo/tổ đội/dự án/kho xuất (§7.6) | UI | **DONE** — tab «Cấp phát» 9 cột đúng 5 trường yêu cầu (`AllocateReturn.tsx:38-48`); «Người tạo»/«Kho xuất» = "—" vì payload KHÔNG có trường (§14 ⛔ không bịa) |
| MT2-P9-08 | Hoàn trả: danh sách + **kho nhập** (§7.6) | UI | **DONE** — tab «Hoàn trả» 9 cột + «Kho nhập» (`AllocateReturn.tsx:50-60`); «Kho nhập» = "—" vì payload KHÔNG có trường (§14) |
| MT2-P9-09 | ⛔ **KHÔNG tự nghĩ workflow cấp phát/hoàn trả** — chờ business rule; ghi rõ dependency | tài liệu | **DONE** — dependency ghi tại `AllocateReturn.tsx:1-12` + log §P9-06/§P9-09: workflow/quyền triển khai SAU khi có business rule |

---

# PHASE 10 — TỔ ĐỘI · HÀNH CHÍNH–PHÁP CHẾ (§8, §10) · 6 task
| ID | Task | Deliverable | Trạng thái |
|---|---|---|---|
| MT2-P10-01 | Tổ đội: chỉ **danh sách + filter theo dự án** (§8) | UI | **DONE** — thêm `filters` dự án vào `ListToolbar` (§22) + lọc **TẠI NGUỒN** theo `teams.project_id`; gate `tsc` 0 · `regression` 69/69 · `tm0*` 29/30 (1 ca ĐỎ **có sẵn** — xem GH chú) · LIVE marker `team-project-filter`=1 · FP-805A0E84E1BAE705 |
| MT2-P10-02 | Hồ sơ nhân sự: **bỏ filter dự án** + modal **3 tab** (Thông tin user là tab đầu) (§10.1) | UI | **DONE** — ① ⛔ **bỏ filter dự án**: AUDIT `HrScreen.tsx` (toàn file) + `UserProfilePanel` ⇒ **KHÔNG có** filter chọn dự án ⇒ yêu cầu đã đạt, ⛔ 0 dòng code; ② modal **3 tab** `USER_PROFILE_TABS` = [Thông tin user (đầu) · Thông tin cá nhân · Dự án đã và đang tham gia] (nguyên văn §10.1) · tab0 giữ **Thao tác gần đây** + **Đơn từ & giấy tờ** ⇒ ⛔ không mất dữ liệu; gate `tsc` 0 · `regression` 69/69 · LIVE marker `user-profile-tabs`=2 / `data-user-profile-tab`=1 · FP-BEE41F8A0FF6FBDE |
| MT2-P10-03 | Bảo hiểm & chế độ: **bỏ filter dự án** (§10.2) | UI | **DONE** — AUDIT `app/screens/BenefitsScreen.tsx` (toàn file): prop `project` chỉ có ở destructuring `:16` và ⛔ **KHÔNG dùng** ở đâu ⇒ **KHÔNG có** filter chọn dự án (⛔ 0 `ListToolbar`/⛔ 0 `select` dự án) ⇒ §10.2 **ĐÃ ĐẠT SẵN, 0 dòng mã**. `benefitRecords` chỉ dùng ở `BenefitsScreen:17` + `UserProfilePanel:2729` (lọc theo `userId`, ⛔ không lọc dự án) ✔ |
| MT2-P10-04 | Công văn đến/đi: **thêm Tạo công văn** + **nhiều ảnh** + **nút Sửa** (§10.3) | UI + API | **DONE** — ① modal **Tạo công văn** (`BaseModal` REUSE) đủ field §10.3: Số CV · Hướng · Loại · Ngày · Người gửi · Người nhận · Trích yếu · Người xử lý + cột **Ngày tạo** (từ `createdAt` ĐÃ CÓ trong payload) ; ② **upload NHIỀU ảnh/tài liệu** qua `AttachmentPanel` REUSE (`input multiple` + `/api/files` + lightbox) — bảng `official_correspondence` ⛔ không có cột ảnh ⇒ **0 migration** ; ③ **nút Sửa** mỗi dòng ⇒ truyền `corrId` (backend `HrManagementUseCase:101-107` **đã là upsert**) ⇒ **0 API mới**. Gate `tsc` 0 · `regression` 69/69 · LIVE marker `corr-create`/`corr-edit`/`corr-form`/`corr-attachments`=1 · FP-EEBA0ED21D5111EA |
| MT2-P10-05 | Văn bản pháp lý: **liên kết công văn đến/đi** (⛔ không data flow độc lập) (§10.4) | UI + API | ✅ **DONE (26/09/2026 — đề án ②A user chốt, đúng PHƯƠNG ÁN A đã khuyến nghị)**. Đã làm đủ **4 tầng**: ① **DB** `V31` thêm `legal_documents.correspondence_id` (NULL + index tra cứu; ⛔ không FK cứng vì dữ liệu cũ có thể trỏ công văn đã dọn) — Flyway đã áp thật `success=1`; ② **H2** khai cột ở **CẢ HAI** bản `schema-h2.sql` (test + demo — bài học `ddl-auto: none`); ③ **BACKEND** port `HrStore.insert/updateLegalDocument` nhận `correspondenceId` + adapter ghi cột (có `blankToNull` ⇒ **gỡ được liên kết**) + use-case đọc payload + bootstrap trả `correspondenceId`; ④ **PARITY + UI** JS `system-route.mjs` ghi cùng cột + màn `LegalDocsScreen` có **ô chọn công văn** lấy từ `data.officialCorrespondence` (⛔ **không** tạo bảng/sao chép dữ liệu CV) + cột «Công văn liên kết». **Test `P10-05` 4/4** (khoá cả 4 tầng). Đã **smoke thật**: lưu VB kèm công văn ⇒ cột ghi đúng vào MySQL, rồi **dọn dữ liệu tạm** (bảng về 0 dòng như cũ). |
| MT2-P10-06 | Rà soát không tạo data flow trùng với công văn | tài liệu | **DONE** — 🔎 **RÀ SOÁT (audit-only, ⛔ 0 dòng mã)**: ① `data.legalDocuments` (bảng `legal_documents`) ⛔ **KHÔNG** sao chép bất kỳ cột nào của `official_correspondence` (`doc_no/direction/sender_name/receiver_name/summary/internal_handler/…`) ⇒ ⛔ **KHÔNG có data flow trùng**; ② màn `LegalDocsScreen` dùng đúng 3 action riêng `save_/set_/delete_legal_document` (RBAC `dept_legal_documents`) ⛔ không gọi action của công văn; ③ **P10-04** vừa dựng modal Tạo/Sửa công văn + `AttachmentPanel` — ⛔ **không** nhân bản luồng VB pháp lý, mọi tệp gắn theo `entityType="correspondence"`; ④ ⚠️ **khoảng trống còn lại = khóa liên kết `correspondence_id` chưa có** (chính là BLK-04 của P10-05) ⇒ **1 nợ tài liệu, 0 vi phạm trùng data flow** ✔ |

---

# PHASE 11 — BÁO CÁO · DANH MỤC VẬT TƯ GỐC (§11, §12) · 5 task
| ID | Task | Deliverable | Trạng thái |
|---|---|---|---|
| MT2-P11-01 | “Báo cáo & cảnh báo”: **hiển thị TỔNG HỢP**, bỏ chia menu theo phòng ban (§11) | menu + UI | **DONE** — gom **2 mục trùng nội dung** (`dept_plan_alerts` «…–Phòng Kế hoạch» + `dept_project_alerts` «…–Phòng Dự án», cùng render `WorkCenter view="reports"`) ⇒ **1 mục TỔNG HỢP** «Báo cáo & cảnh báo» mở màn `ReportView` tổng hợp (`REPORT_CATALOG`) ĐÃ CÓ. Khai mục trong **code** (khuôn W-01/P-07/P9-06) + `legacyReportsMenuKeys` ẩn 2 khoá cũ khỏi menu (khoá vẫn sống: quyền/tìm kiếm/render); cổng quyền dùng chung 2 khoá cũ ⇒ ⛔ **0 migration · 0 khoá module mới**. Nối đủ sidebar + mobile + huy hiệu nhóm. Gate `tsc` 0 · `regression` 69/69 · LIVE marker `reports-summary-menu`=1 / `reports_summary`=1 · FP-948A239921E1F508 |
| MT2-P11-02 | “KPI & hiệu suất”: **hiển thị TỔNG HỢP**, bỏ chia menu theo phòng ban (§11) | menu + UI | **DONE** — gom **2 mục trùng nội dung** (`dept_plan_kpi` «…–Phòng Kế hoạch» + `dept_project_kpi` «…–Phòng Dự án», cùng map `WorkCenter view="kpi"` tại `page.tsx:400`) ⇒ **1 mục TỔNG HỢP** «KPI & hiệu suất nhân viên». Khai mục trong **code** (khuôn W-01/P-07/P9-06/P11-01) + `legacyKpiMenuKeys` ẩn 2 khoá cũ khỏi menu (khoá vẫn sống); cổng quyền dùng chung 2 khoá cũ; ⚠️ mục mới mang `view:"kpi"` vì màn đích là tab KPI của `WorkCenter` (⛔ khác `reports_center` của P11-01) ⇒ ⛔ **0 migration · 0 khoá module mới**. Nối đủ sidebar + mobile + huy hiệu nhóm (kể cả bản mobile). Gate `tsc` 0 · `regression` 69/69 · LIVE marker `kpi-summary-menu`=1 / `kpi_summary`=1 · FP-989293E4111980B6 |
| MT2-P11-03 | **Fix ROOT CAUSE** tab danh sách vật tư: audit **API · response · mapping · component · cột bảng · quan hệ CSDL** (§12.1) | báo cáo + fix | **DONE** — 🎯 **ROOT CAUSE**: payload `materials` (`BootstrapDataAdapter:231-241`) **THIẾU cột `active`** + `WHERE m.active=1`, trong khi `adminMaterials` (`:1096-1106`) có `active` và ⛔ không lọc; `page.tsx:308` gán `adminMaterials=materials` cho user thường ⇒ `MaterialListTable:47/88` lọc/hiện «Trạng thái» theo `active` ⇒ **non-admin thấy luôn «Đang dùng», lọc «Đã ngừng» không bao giờ có kết quả**. ✅ **FIX TẠI NGUỒN** (⛔ không hide lỗi FE): thêm `m.active AS active` vào SELECT `materials`. **Đã loại 2 giả thuyết sai** bằng đo: «mất khoá `materialAliases`» (đúng, `:1052` gửi vô điều kiện) · «bẫy mảng rỗng `adminMaterials||materials`» (đã vá ở nguồn `:305-308`). **BẰNG CHỨNG**: test hợp đồng `tests/p11-03-material-list-contract.test.mjs` **5/5 xanh** + **đối chứng âm** (bỏ `m.active` ⇒ 4/5, EXIT=1) · `mvn package` EXIT=0 · class `BootstrapDataAdapter.class` trong jar (113.727 bytes) chứa **đúng 1** hằng `m.active AS active` · build `VNTECH-FP-E3812ADA86DA0677` (469 file) · 3 HTTP 200. ⚠️ **CÒN QUYẾT ĐỊNH (BLK-05)**: `materials` vẫn lọc `active=1` ⇒ user thường ⛔ không thấy mã vật tư gốc **đã ngừng**; ⛔ không tự bỏ lọc vì nhiều màn khác dùng `data.materials` (BOQ·phiếu·kho) ⇒ chờ user chốt |
| MT2-P11-04 | Tách **3 tab**: Danh sách vật tư · Nhóm con mã vật tư gốc · Mã vật tư gốc (§12.2) | UI | **DONE** — 🔴 GAP đo được: dải tab cũ SAI nhãn (`[Danh mục vật tư] [So sánh/Đối chiẾu BOQ] [Soát trùng Alias]`) và 2 danh sách «Nhóm con» + «Mã vật tư gốc` nằm **CHỒNG** trong 1 khối ⇒ vi phạm «mỗi tab hiển thị danh sách tương ứng». ✅ ĐÃ LÀM: dải tab đúng nguyên văn **[Danh sách vật tư] · [Danh mục nhóm con mã vật tư gốc] · [Mã vật tư gốc]**; tách 2 danh sách vào đúng tab qua prop `section` (`subgroups`/`masters`, mặc định `all` giữ hành vi cũ) ⇒ ⛔ 0 nhân bản component/dữ liệu; 2 công cụ §12.3 (So sánh BOQ · Soát trùng Alias) chuyển xuống khối thu gọn riêng «CÔNG CỤ CHẨN LOẠN — §12.3 TẠM BỎ QUA» ⇒ ⛔ **không xoá chức năng**; ⛔ **0 dòng CSS mới** (tái dùng `data-active-tab`/`details[data-tab]` ở `canonical.css:483-486`); user chỉ-xem vẫn thấy bảng Mã vật tư gốc ở tab 3. Gate `tsc` 0 · `regression` 69/69 · test hợp đồng P11-03 5/5 (không hồi quy root cause) · build `VNTECH-FP-70A1B6680E52DB4A` (470 file · head `0203`) · 3 HTTP 200 · marker `material-catalog-tabs`=1 / `data-material-catalog-tab`=1 / `material-subgroup-reference`=2 / `material-master-list`=2 |
| MT2-P11-05 | ⛔ Tạm bỏ qua: So sánh/Đối chiếu BOQ · Soát trùng Alias · Chất lượng danh mục (§12.3) | tài liệu | SKIPPED |

---

# PHASE 12 — QUẢN TRỊ HỆ THỐNG (§13) · 7 task
| ID | Task | Deliverable | Trạng thái |
|---|---|---|---|
| MT2-P12-01 | Thêm **tab THÔNG BÁO** (§13.1) | UI + API (P3-02) | **DONE** — 🔴 GAP đo được: backend §13 **ĐÃ CÓ ĐỦ** (`save_notification_config` / `notification_configs` / `delete_notification_config` / `set_notification_config_status` ở `SystemController:1244-1263`, RBAC gác module `admin`, 3 bảng V26, test `NotificationCenterTest`) nhưng **UI = 0 dòng** ⇒ `ADMIN_STEP_LABELS` **không có** bước «Thông báo» ⇒ không ai cấu hình được. ✅ ĐÃ LÀM: thêm bước **Thông báo** (⛔ 0 khoá module mới — dùng chính `admin`) + khối danh sách dùng **shared `ListToolbar`** (§22) có **Search (mã·tên·nội dung) · Sort (mới/cũ/tên A→Z) · Filter (kênh Web/Email · trạng thái)**, cột Mã·Tên·Kênh·Người nhận (đủ **5 kiểu §13.2**)·Nội dung·Thời gian·Trạng thái, thao tác **Bật/Tắt** + **Xoá** qua action có sẵn; nguồn dữ liệu = `requestApi("notification_configs")` (⛔ `action()` trả `undefined` nên không đọc được payload) + trạng thái lỗi tải. ⛔ **KHÔNG dựng nút Tạo/Sửa chết** — chữ C/U của CRUD = modal `save_notification_config` ⇒ **P12-02**. Gate `tsc` 0 · `regression` 69/69 · test hợp đồng `p12-01-notification-config-tab` **6/6** + **đối chứng âm** (gỡ bước Thông báo ⇒ 5/6, EXIT=1) · build `VNTECH-FP-B9C946FAC0EEEB5F` (472 file · head `0204`) · 3 HTTP 200 · marker `notification-config-tab`=1 + 3 action có thật |
| MT2-P12-02 | **Modal tạo thông báo** đủ trường + recipient linh hoạt (§13.2) | UI | **DONE** — 🛠 modal **Tạo/Sửa** dùng KHUÔN DÙNG CHUNG `BaseModal` + `ModalFooter` (⛔ không dựng modal thứ hai, §23/§15) với **đủ 8 trường** §13.2: Loại (Web/Email) · Mã · Tên · Nội dung (textarea) · Người nhận · Thời gian gửi · **Thời gian kết thúc (chỉ hiện khi kênh Web + gửi rỗng cho Email)** · + validate. **Recipient đủ 5 kiểu** `all/user/users/department/project`; chọn đối tượng lấy từ **dữ liệu thật** (`data.users` · `data.organizationUnits` · `data.adminProjects`) ⛔ không bịa danh sách; chặn mode cụ thể mà chưa chọn đối tượng. ⚠️ **Tuân thủ hợp đồng backend** (`NotificationManagementUseCase.saveConfig:114-157`): Update = **`configId`** (⛔ không phải `id`) · `targets` **thay toàn bộ** (⛔ không trộn cũ/mới) · `user`+`users` cùng `targetType='user'` · thời gian gửi `toISOString()` để ⛔ không lệch múi giờ (backend parse `LocalDateTime` theo **UTC**). Nối nút **＋ TẠO THÔNG BÁO** + **Sửa** mỗi dòng (đóng nút chết của P12-01) + đăng ký `modal === "notificationConfig"`. Gate `tsc` 0 · `regression` 69/69 · test hợp đồng `p12-02-notification-config-modal` **8/8** · build `VNTECH-FP-06DD6A6A19EDF8DD` (474 file · head `0205`) · 3 HTTP 200 · marker `notification-config-modal`=1 + `save_notification_config`=1 + `configId`=3 |
| MT2-P12-03 | Tài khoản: **bỏ trường “Hạn mức”** (`users.approval_limit` — ẩn khỏi UI/API, ⛔ không drop cột) (§13.3) | UI | **DONE** — §13.3: «⛔ **Bỏ trường "Hạn mức"** — không thay bằng trường khác **nếu chưa có nghiệp vụ**». **AUDIT 3 tầng**: (1) FE: cột bảng tài khoản (`page.tsx` `<td>money(u.approvalLimit)`) + ô nhập trong modal Sửa tài khoản (`name="approvalLimit"`); (2) danh sách cột `ACCOUNT_COLUMNS` (`admin-governance-pure.ts:59`) — vừa sinh `<th>` vừa quyết định `colSpan`; (3) API: `BootstrapDataAdapter` khối `users` trả `u.approval_limit AS approvalLimit`. **ĐÃ LÀM**: gỡ 1 entry khỏi `ACCOUNT_COLUMNS` ⇒ cả tiêu đề + ô dữ liệu + `colSpan` khớp tự động (⛔ không lệch số cột); gỡ ô input; ngừng trả `approvalLimit` trong payload `users`. ⛔ **KHÔNG** thêm cột thay thế (§13.3 cấm khi chưa có nghiệp vụ) · ⛔ **KHÔNG** drop cột CSDL (`V25__…:11` đã ghi rõ). ⚠️ an toàn ghi: form bỏ trường ⇒ `numberValue(null)=0` (`UserManagementUseCase:622`) ⇒ đúng `DEFAULT 0` nên **không** lỗi NOT NULL. **BẰNG CHỨNG**: test hợp đồng `p12-03-remove-approval-limit` **6/6** + **đối chứng âm** (thêm lại cột ⇒ 5/6 đỏ) · `tsc` 0 · `mvn package` EXIT=0 · byte-verify class `BootstrapDataAdapter.class` (114.146 B) ⛔ không còn `approval_limit AS approvalLimit` nhưng **giữ** `must_change_password` (gỡ đúng 1 trường) · bundle `approvalLimit`=**0** · build `VNTECH-FP-BB5076DB22CE3EA7` (476 file · head `0206`) · 3 HTTP 200 |
| MT2-P12-04 | Tài khoản: fix **Last Login** + **Created At** hiển thị (§13.3) | UI + API | **DONE** — §13.3: «Danh sách tài khoản phải hiển thị **Last Login · Created At** ⇒ **fix root cause**». 🎯 **ROOT CAUSE (đo, không suy đoán)**: (1) `users.created_at` **CÓ** trong CSDL nhưng ⛔ **chưa từng được chiếu** vào payload ⇒ UI hiện «chưa có nguồn»; (2) `users` **KHÔNG có** cột đăng nhập cuối (grep `db/migration/*.sql` = 0 dòng `last_login`); (3) ⛔ **KHÔNG** lấy từ session vì `AuthUseCase.logout` **XOÁ dòng session** ⇒ lịch sử đăng nhập sẽ MẤT. **ĐÃ LÀM**: migration **V29** `ALTER TABLE users ADD COLUMN last_login_at TIMESTAMP NULL` (§19 an toàn: nullable ⇒ không ghi đè dữ liệu cũ, ⛔ không destructive) + `UserRepository.touchLastLogin` (UPDATE **đúng 1 cột**) gọi trong `AuthUseCase.login` **sau** xác thực thành công dùng **chung 1 mốc thời gian** với session + chiếu `u.last_login_at AS lastLoginAt, u.created_at AS createdAt` vào payload `users` + cập nhật **cả 2 schema H2** (test/demo) + khai nguồn thật cho 2 cột ở `ACCOUNT_COLUMNS` (⛔ hết `source: null`) + sửa lý do hiển thị khi rỗng (⛔ **không bịa ngày**). **BẰNG CHỨNG**: 3 test `AuthUseCaseTest` mới (ghi khi thành công · **không** ghi khi sai mật khẩu · cập nhật mỗi lần) ⇒ **19/19 pass**; test hợp đồng `p12-04-last-login-created-at` **6/6**; `regression` 69/69; `tsc` 0; `mvn package` EXIT=0; **Flyway log thật trên MySQL**: *Successfully validated 29 migrations* → *Migrating … to version "29 - mt2 user last login"* → *Successfully applied 1 migration … v29*; build `VNTECH-FP-9328262E529F780E` (478 file · head `0207`) · 3 HTTP 200 |
| MT2-P12-05 | **User ID**: audit identity model — user thiếu mã (2 user trống `system_level_code`) + **khớp Hồ sơ nhân sự** (§13.3) | audit + fix | **DONE** — §13.3: «Một số user **chưa có mã** ⇒ audit **identity model** ⇒ bổ sung **User ID** và đảm bảo **khớp với ID trong Hồ sơ nhân sự** ⇒ ⛔ **không tạo hai identity khác nhau cho cùng một user**». **AUDIT TRÊN MySQL THẬT** (probe JDBC **chỉ đọc**, đã xoá sau khi dùng): users **13** · hr_records **4** · user thiếu **mã nhân viên = 1** (`testuser86661`) · thiếu `system_level_code` = **2** (khớp BLK-02) · user **chưa có hồ sơ** = **9/13** · **hồ sơ trỏ user không tồn tại = 0** · **trùng họ tên khác `user_id` = 0**. ✅ **CSDL ĐÃ BẢO ĐẢM** (đo qua `information_schema`): `users_employee_code_uidx` **UNIQUE** + `hr_records_uidx_user_id` **UNIQUE** + `hr_records.user_id` **NOT NULL** ⇒ ⛔ không thể có 2 user trùng mã hay 2 hồ sơ/1 user. 🎯 **ROOT CAUSE THẬT**: `UserManagementUseCase.createUser` lấy `employeeCode` mà **KHÔNG** kiểm tra rỗng, trong khi `updateUser` **ĐÃ** chặn ⇒ lỗ hổng tạo tài khoản không mã (khớp đúng 1 dòng dữ liệu lỗi). **ĐÃ LÀM**: chặn ở **tầng máy chủ** (§17) trả **400** + dùng biến đã kiểm tra khi insert (⛔ không lấy lại từ payload) + danh sách tài khoản hiện rõ **«Chưa có mã»** kèm lý do (marker `data-no-employee-code`) thay vì dấu «—» giấu thiếu sót định danh. ⚠️ 6 màn khác (dự án/tổ đội) vẫn hiện «—» cho thành viên chưa có mã — **hợp lý và ngoài phạm vi §13.3** (§40). Gate `tsc` 0 · `regression` 69/69 · test hợp đồng `p12-05-user-identity-model` **6/6** · `mvn package` EXIT=0 · build `VNTECH-FP-1323DA83912D0D8F` (480 file · head `0208`) · 3 HTTP 200 · marker `data-no-employee-code`=1 · 🔴 **BLK-06 (chờ USER)**: gán mã cho 1 user đang trống + tạo hồ sơ nhân sự cho 9 user + gán cấp bậc cho 2 user (⛔ không tự bịa mã) |
| MT2-P12-06 | **Chữ ký trong modal tạo/sửa user** — đúng 1 ảnh, thay ảnh cũ (§13.4) | UI + API (P3-04) | **DONE** — §13.4: «Trong modal **tạo user** và **chỉnh sửa user** ⇒ thêm **Chữ ký**, cho phép upload **đúng 1 ảnh**. Nếu upload ảnh mới ⇒ **xoá/thay ảnh cũ** ⇒ lưu ảnh mới. ⛔ Không cho tồn tại nhiều chữ ký active cho cùng user». **AUDIT**: backend P3-04 ĐÃ có `setUserSignature` + `updateUser` xử lý `signatureUrl` (**chỉ khi payload CÓ khoá**; rỗng ⇒ ghi NULL) + cột `users.signature_url` (V25) + `ProfileSignatureTest`; ⛔ NHƯNG **`createUser` CHƯA lưu chữ ký** ⇒ nếu chỉ làm UI thì ảnh chọn lúc tạo sẽ **mất** (nút chết) ⇒ phải bổ sung tầng backend. **ĐÃ LÀM**: (1) `createUser` ghi chữ ký **sau** khi insert và **chỉ khi** payload có khoá `signatureUrl` (⛔ không ghi đè khi client không gửi); (2) 1 component `SignatureField` **dùng chung** cho cả 2 modal (⛔ không nhân bản) với luật ảnh **REUSE** y hệt khuôn ảnh đại diện (JPG/PNG/WebP · ≤2 MB · `readAsDataURL`), xem trước, nút **Thay ảnh** + **Xóa chữ ký**; (3) modal sửa **prefill** chữ ký hiện có (⛔ không mất khi mở form) và gửi kèm `signatureUrl` (gửi rỗng ⇒ xoá được); (4) CSS tối thiểu + co 1 cột ở màn hình hẹp (§24). 📦 Dữ liệu là **1 chuỗi** ở cột đơn ⇒ **không thể** có 2 chữ ký active. Gate `tsc` 0 · `regression` 69/69 · test hợp đồng `p12-06-user-signature` **6/6** · `mvn package` EXIT=0 · build `VNTECH-FP-C1B7F462FCCF96BD` (482 file · head `0209`) · 3 HTTP 200 · marker `signature-field`=4 / `signature-preview`=1 |
| MT2-P12-07 | Rà **user identity** không tạo 2 định danh cho cùng 1 user (§13.3) | tài liệu + test | **DONE** — §13.3 «⛔ **không tạo hai identity khác nhau cho cùng một user**». P12-05 đã **đo** identity trên MySQL (0 vi phạm) nhưng **một phép đo trong log không bảo vệ được** (ai đó sửa SQL tay / migration mới làm mất UNIQUE thì vi phạm mà không ai biết) ⇒ **chuyển thành CỔNG kiểm tra chạy lại được** `tools/IdentityIntegrityCheck.java` (⛔ **CHỈ ĐỌC**, không INSERT/UPDATE/DELETE/ALTER/DROP/TRUNCATE; **exit 0 = sạch · 1 = vi phạm** để dùng làm cổng chặn CI). **4 LỖI**: hồ sơ nhân sự mồ côi · 1 user nhiều hồ sơ · trùng mã nhân viên · trùng tên đăng nhập. **3 CẢNH BÁO** (exit 0, ⛔ KHÔNG tự sửa vì là quyết định nghiệp vụ): chưa có mã (1 · BLK-06) · chưa có cấp bậc (2 · BLK-02) · chưa có hồ sơ (9 · BLK-06). Cổng còn in **3 chỉ mục UNIQUE** từ `information_schema` (mất chỉ mục ⇒ cổng báo động). **KẾT QUẢ CHẠY THẬT**: lỗi=0 · cảnh báo=3 · **exit 0** · users 13 / hr 4. **Test** `tests/p12-07-identity-integrity-gate.test.mjs` **5/5**: ⛔ cổng phải chỉ đọc · đủ 4 dạng vi phạm · phân biệt LỖI/CẢNH BÁO · UNIQUE còn trong V1 · tầng ứng dụng vẫn chặn tạo user thiếu mã (bảo vệ bản vá P12-05). **Tài liệu**: `docs/agent-progress/MT2-P12-07-IDENTITY-INTEGRITY.md` |

---

# PHASE 13 — NOTIFICATION WEB + EMAIL (§14, §15) · 6 task
| ID | Task | Deliverable | Trạng thái |
|---|---|---|---|
| MT2-P13-01 | **Web**: modal khi đăng nhập — nội dung · người tạo · thời gian phát hành (§14) | UI + API | **DONE** — §14: «Login → Check notifications by userID → Check active period → Display system notification modal. Modal hiển thị: **nội dung · người tạo · thời gian phát hành**». **AUDIT 3 GAP**: (1) backend **ĐÃ CÓ** `systemNotifications` và **đã lọc đúng** theo userID + `channel='web'` + khoảng gửi/kết thúc + `read_at IS NULL` + snooze hết hạn + recipient resolver (P1-05); (2) 🔴 khối đó **KHÔNG trả `created_by`** ⇒ UI không thể hiện «người tạo»; (3) 🔴 **UI = 0 dòng** — không có modal nào. **ĐÃ LÀM**: bổ sung `c.created_by AS createdBy, c.created_at AS createdAt` vào payload + khai `systemNotifications: Row[]` trong `AppData` (**BẮT BUỘC**, ⛔ không `?` — bài học TASK-069: thiếu khoá ⇒ `[]` âm thầm) + khối chuẩn hoá luôn trả mảng + `SystemNotificationModal` dùng `BaseModal` hiện **3 trường** §14 (nội dung cuộn + bọc dòng §23/§24) + hiện **tuần tự từng thông báo**, đóng thì **không mất** thông báo còn lại (state `dismissedSystemNotices`). ⛔ **CỐ Ý CHƯA LÀM** (thuộc task sau, ⛔ không dựng nút chết / không tự gắn cờ trạng thái chưa có action): «Không nhắc lại hôm nay» = **P13-02** (`mark_notification_snooze`) · «Đánh dấu đã đọc» = **P13-03** (`mark_notification_read`). Gate `tsc` 0 · `regression` 69/69 · test hợp đồng `p13-01-system-notification-modal` **6/6** · `mvn package` EXIT=0 · build `VNTECH-FP-284EAD9939F04FF2` (485 file · head `0210`) · 3 HTTP 200 · marker `system-notification-modal`=1 / `system-notice-meta`=1 · đối chứng âm = 0 |
| MT2-P13-02 | Nút **“Không nhắc lại hôm nay”** + lưu theo **userID + notificationID** (§14) | UI + API (P3-03) | **DONE** — §14: «Phải lưu trạng thái theo **userID + notificationID** ⇒ ⛔ **không đánh dấu đọc global cho tất cả user**». **AUDIT HỢP ĐỒNG BACKEND (đọc từ mã, không đoán)**: `mark_notification_snooze` **ĐÃ CÓ** (`SystemController:1274-1277` → `snooze(cu.id(), payload)`) · payload `{configId, snoozeUntil?}`, thiếu `configId` ⇒ **400** · `NotificationStoreAdapter:177-181` ghi `notification_user_states` theo **`WHERE config_id=? AND user_id=?`** ⇒ đúng *userID + notificationID* · `RbacService:45` đưa action vào `PUBLIC_ACTIONS` ⇒ mọi user đã đăng nhập tự dùng được (⚠️ `ActionRbacRegistry:59` là `List.of()` = mặc định 403 nhưng `PUBLIC_ACTIONS` kiểm **trước**). **ĐÃ LÀM**: thêm nút «Không nhắc lại hôm nay» vào modal thông báo, ⛔ **KHÔNG** gửi `userId` (backend lấy `cu.id()` từ phiên ⇒ không thể tạm ẩn thay người khác / không đánh dấu global) · ⛔ **KHÔNG** gửi `snoozeUntil` để giữ **luật sẵn có +24h** (⛔ không tự đặt luật nghiệp vụ mới) · khoá nút khi đang lưu + hiện lỗi nếu API lỗi. ✅ **SỬA LỖI CỦA CHÍNH MÌNH TỪ P13-01**: `ModalFooter` có nút primary kiểu **submit** nhưng đặt ngoài `<form>` ⇒ nút «Đóng» là **NÚT CHẾT**; đã bọc modal trong `<form>` + nút submit thật. Gate `tsc` 0 · `regression` 69/69 · test hợp đồng `p13-02-snooze-notification` **7/7** (có chặn ⛔ payload không mang `userId`) · build `VNTECH-FP-2DF24630A2261D24` (487 file · head `0211`) · 3 HTTP 200 · marker `mark_notification_snooze`=1 · đối chứng âm = 0 |
| MT2-P13-03 | **Read status**: đánh dấu đã đọc / tất cả — **theo user** (§14.1) | UI + API (P3-03) | **DONE** — §14.1: «Mỗi notification có **"Đánh dấu đã đọc"** + **"Đánh dấu tất cả đã đọc"**. Trạng thái **theo user**.» 🔴 **ROOT CAUSE THẬT Ở TẦNG DỮ LIỆU** (P1-03 làm backend nhưng bỏ sót 1 nhánh): `notificationsForUser` đọc bằng `LEFT JOIN notification_user_states … WHERE s.read_at IS NULL` ⇒ thông báo **chưa từng có state row** (chưa snooze/chưa đọc) **vẫn hiện**; mà `markAllRead` bản cũ **CHỈ UPDATE** dòng đã tồn tại ⇒ bấm xong **thông báo đó vẫn hiện lại** ⇒ nút không thực sự làm hết ý nghĩa (fake completion). ✅ **ĐÃ SỬA TẠI NGUỒN**: `markAllRead` = **UPDATE + INSERT** các dòng còn thiếu, dùng **ĐÚNG bộ lọc** của `notificationsForUser` (`active=1` · `channel='web'` · `send_at<=now` · `end_at>now`) + `NOT EXISTS` (⛔ không tạo trùng) và trả `updated + inserted`. **UI**: thêm 2 nút §14.1 vào modal thông báo; ⛔ **KHÔNG** gửi `userId` ở cả 2 (`mark_notification_all_read` **không nhận tham số**; backend `markRead(cu.id(), …)` / `markAllRead(cu.id())` ⇒ trạng thái **theo user**, ⛔ không global) · khoá nút khi đang lưu + hiện lỗi API. Gate `tsc` 0 · `regression` 69/69 · test hợp đồng `p13-03-read-status` **6/6** · `mvn package` EXIT=0 · build `VNTECH-FP-34F5ADA82DC70B9C` (489 file · head `0212`) · 3 HTTP 200 · marker `mark_notification_read`=1 / `mark_notification_all_read`=1 |
| MT2-P13-04 | **Audit event thực tế** của hệ thống + **đề xuất** danh mục event (§15) — ⛔ không tạo event không có nguồn dữ liệu | tài liệu | **DONE** — §15: «① audit các module hiện có ② xác định **event thực tế** ③ **đề xuất thêm** event phù hợp ④ ⛔ **không tạo event không có nguồn dữ liệu**». **ĐÃ AUDIT (chỉ đọc mã, không code)**: (1) hạ tầng §15.1 **ĐÃ ĐỦ** — `NotificationManagementUseCase.dispatch(eventKey)` = cửa DUY NHẤT (pipeline rule→resolver→web/email→log, test xanh P3-01) nhưng **chưa module nào gọi dispatch** trong mã production (chỉ test gọi) ⇒ việc P13-05 = **nối dispatch vào điểm nghiệp vụ đã có**, ⛔ không dựng engine thứ hai; (2) phát hiện **kênh email NGẮT HAI KHÚC**: Java chỉ xếp hàng `email_outbox` (`status='queued'`) còn worker gửi SMTP thật (`scripts/email-dispatcher.mjs:150-180` — claim · STARTTLS/AUTH · retry `attempt_count<3` · tự phát event `overdue` qua `queueOverdueReminders` :132-147 có chống lặp `reminder_sent_at`) **chưa được port sang Java** (grep `@Scheduled` = duy nhất `SlaComplianceWorker`) ⇒ port worker = khoản việc thật của P13-05; (3) **DANH MỤC**: ✅ **13 event CÓ nguồn** (Approval 5: tới lượt duyệt/được duyệt/bị từ chối/bị trả lại/SLA quá hạn — điểm gắn `decideApproval` + JS đã có khuôn email `approvalEmailStatement` `system-route.mjs:570-593` · Work 4: giao việc [W1 đã có sẵn `task_notifications`+email `task_assigned`]/quá hạn/hoàn thành/bị thay đổi · Procurement 4: PO liên quan [rejectPo đã notify in-app `decidePo` :266-277]/giao hàng [JS :1591,1654 `delivery_waiting_bch/completed/partial`]/GRN/thiếu vật tư [`closed_shortage` PurchaseStoreAdapter:294-343] · Project 1: được thêm vào dự án [`user_project_scopes.joined_at` V14] · Warehouse 4: nhập/xuất/cấp phát/hoàn trả/tồn kho thấp [`materials.min_stock` + balance]) · ⛔ **3 dạng CHƯA có nguồn KHÔNG tạo**: «SLA sắp hết» (chưa có ngưỡng/job) · «sắp đến hạn» Work (chưa có job/ngưỡng) · «project milestone» (grep = 0, không có bảng) · trùng lặp gộp: PJ2=PJ1 · PJ3=W1 · PR1=A1 · PR2=A2. 📄 Deliverable: `docs/agent-progress/MT2-P13-04-EVENT-AUDIT.md` (đề xuất 4 đợt triển khai P13-05 + REUSE cho P13-06) |
| MT2-P13-05 | **Email engine** theo event: Approval · Work · Procurement · Project · Warehouse (§15) | service + test | **DONE** — triển khai worker `EmailOutboxDispatchWorker` (JavaMailSender · claim `status='sending'` có điều kiện · retry tối đa 3 lần · `next_attempt_at` · `last_error` · stale sending recovery); nối `NotificationManagementUseCase.dispatch` vào các điểm chuyển trạng thái có nguồn thật: Approval returned/stage completed/finalized, Procurement PO approved/rejected + delivery waiting BCH/completed/partial, Work task completed/status changed/reassigned. ⛔ Không tạo event không có nguồn; rule vẫn nằm trong `notification_configs`. **GATE**: worker test **3/3 xanh** · `mvn -q -pl web -am -DskipTests compile` **0** · `NotificationCenterTest` **xanh**; full Java suite **56 test / 3 failure CÓ SẴN** (`ProductionRoleCounterProofTest`, không do thay đổi notification) · chưa chạy gd-cycle/LIVE trong lượt này. |
| MT2-P13-06 | **Notification Log + Delivery Status** (§15.1) | bảng + API | **DONE** — thêm action `notification_log` (admin-only) tổng hợp log web từ `notification_user_states` và log email từ `email_outbox`, trả `source` + `status` + timestamps; ⛔ không tạo bảng trùng, REUSE schema V26/baseline. Test `NotificationCenterTest` **4/4 xanh** · compile Java EXIT 0. |

---

# PHASE 14 — QA & NGHIỆM THU (§25, §26, §51, §52) · 5 task
| ID | Task | Deliverable | Trạng thái |
|---|---|---|---|
| MT2-P14-01 | Test **UI**: rendering · responsive · modal · tab · table · search/sort/filter · **không overlap/overflow** | biên bản | **DONE** — chạy browser probe cutover UI: đăng nhập admin + dashboard thật qua proxy :9000 ✅; visual self-test **17 màn × 4 kích thước = 68 ảnh, nhiễu nền 0 px** ✅; focused UI contract **38/38 xanh**; frontend `tsc` 0 · regression **69/69** · workflow ✅; sửa 2 điểm thật: cleanup Edge profile không còn làm đỏ sau khi UI thành công; cập nhật assertion P13-01 theo modal P13-02/P13-03. Không phát hiện overlap/overflow trong phạm vi probe. |
| MT2-P14-02 | Test **API**: success · validation · permission · error | biên bản | **DONE** — live API probe: success `save_payment_plan` HTTP 200 với project thật; validation thiếu `projectId` trả **400**; RBAC action probe **20/20 đúng**, **0 lọt quyền**, **0 khóa nhầm**; `notification_log` admin trả dữ liệu; tài khoản probe được dọn. Đã sửa probe để tính cả module capability + project write scope (tránh báo khóa nhầm giả). |
| MT2-P14-03 | Test **DB**: migration · quan hệ · toàn vẹn dữ liệu | biên bản | **DONE** — sau khi `MT2-P14-03b` vá xong, **cả 3 cổng DB đều XANH, đo lại trong phiên này**: ① `node tools/probe-java-sql-schema.mjs` ⇒ **«không thấy tham chiếu bảng/cột sai trong SQL tĩnh»** (127 bảng · 1615 cột · 108 tệp Java; **8 mismatch license = 0**) ✅ ② `node tools/probe-schema-drift.mjs` ⇒ **«tệp migration TÁI LẬP ĐƯỢC DB đang chạy (0 lệch)»** ✅ ③ `node tools/p2-reference-integrity.mjs` ⇒ **«15/15 cặp quan hệ không có dòng mồ côi»** ✅ · identity gate **0 lỗi** (giữ nguyên từ lượt trước). Chi tiết nguyên nhân gốc + đường sửa: [`MT2-P14-03-LICENSE-PARITY-AUDIT.md`](docs/agent-progress/MT2-P14-03-LICENSE-PARITY-AUDIT.md). |
| **MT2-P14-03b** | *(mới 23/09)* **Vá parity luồng LICENSE** theo hợp đồng JS hiện hành (⛔ không đổi lược đồ) | Java port + use case + test H2 + probe SQL 0 mismatch | **DONE** — đã làm ĐỦ 5 bước của đường sửa: ① lớp MỚI `application/trust/LicenseEnvelopeVerifier.java` = **port 1-1** của `lib/trust/{canonical-json,license-schema,license-verifier}.mjs` (canonical JSON sắp khoá đệ quy + validate schema + **xác minh Ed25519** bằng JDK `java.security`, ⛔ không thêm dependency) ② port `SystemSettingsStore` đổi sang hợp đồng THẬT (`readTrustIdentity()` · `installLicenseFoundation(Map)` · `recordTrustAudit(...)` · `requestLicenseTransfer(licenseId, source, destination, reason, …)`) ③ adapter ghi **ĐÚNG 18 cột** `vntech_license_installations` + 12 cột `vntech_license_transfer_requests` (`status='requested'`) + `vntech_trust_audit` — ⛔ **KHÔNG** còn `license_key/company_name/edition/activated_by/activated_at/created_at/to_company_name`, ⛔ **KHÔNG** còn `UPDATE … status='transfer_requested'` (JS không làm) ④ use case đọc `licenseEnvelope` (chuỗi JSON do **tầng web parse** qua `JSON_MAPPER` — giữ đúng câu lỗi JS «Nội dung license không phải JSON hợp lệ.») ⇒ xác minh ⇒ hợp lệ ghi + `LICENSE_VERIFIED`; ⛔ không hợp lệ thì ghi `LICENSE_REJECTED` + 400 «License không hợp lệ: …» ⑤ validate `reason` bắt buộc + `destinationMachineFingerprint` **64 hex**. **BẰNG CHỨNG**: test MỚI [`LicenseFoundationParityTest.java`](java-backend/web/src/test/java/com/vntech/erp/web/controller/LicenseFoundationParityTest.java) **4/4 XANH** (canonical JSON ⇄ JS · chữ ký hợp lệ ⇒ 200 + hàng license + audit VERIFIED · chữ ký sai ⇒ 400 + ⛔ KHÔNG ghi hàng + audit REJECTED · chuyển license: thiếu lý do/fingerprint sai ⇒ 400, hợp lệ ⇒ 200 + `status='requested'` + audit TRANSFER_REQUESTED) · **full Java `mvn test` = 61/61 · 0 Failures · 0 Errors · BUILD SUCCESS** (baseline cũ từng 3 Failures ở `ProductionRoleCounterProofTest` — nay cũng xanh) · `node tools/probe-java-sql-schema.mjs` ⇒ **8 mismatch = 0** ✅. **🎓 BÀI HỌC ĐO ĐƯỢC**: H2 trả **NHÃN CỘT VIẾT HOA** (`TENANTID`) còn MySQL giữ camelCase ⇒ đọc map bằng khoá camelCase làm MỌI giá trị rỗng (triệu chứng: «keyId không thuộc Trust Root» + «Unable to decode key») ⇒ đã vá bằng tra khoá **không phân biệt hoa/thường**. |
| MT2-P14-04 | Test **Workflow + RBAC**: state · transition · history · quyền theo chức vụ/phòng ban/dự án | biên bản | **DONE** — live workflow probe UI/backend đạt: 4 quy trình, 4 bước mặc định, mọi bước có người duyệt, modal single/any_of/all_of + tìm ứng viên, tạo/đọc/xóa workflow, validation sai và chặn xóa mặc định; workflow+RBAC regression **16/16**, role parity **0 điểm**, scope parity **67/67**, live action RBAC **20/20 đúng · 0 lọt · 0 khóa nhầm**. Đã sửa 2 probe parser để nhận delegated method/indentation, không sửa nghiệp vụ. |
| MT2-P14-05 | **Regression toàn bộ** (69/69 + 42 test Java) + **final audit §52** + báo cáo §53 | báo cáo cuối | ✅ **DONE (26/09/2026)** — Đã đủ **3 điều kiện** ở `MT2_FINAL_REPORT.md` §9. **Regression đo lại 26/09**: frontend contracts **579 = 578 PASS · 0 FAIL · 1 skip** · regression **69/69** · `tsc` **0** · `lint` **0 error** (205 warning) · **Java 134 test / 0 lỗi** (domain 19 · application 38 · infrastructure 13 · web 64) · `verify:master-baseline` **ĐẠT** · `verify:css-baseline` **ĐẠT** (3654 `!important`) · `probe-css-budget` **ĐẠT** (3893/3918) · `verify:fingerprint` **ĐẠT `VNTECH-FP-38A7A45C22EA6DC3`** (519 tệp) · build + `BUILT ARTIFACT VALIDATION` **ĐẠT** · **`test:release-static` ĐẠT CẢ 3 TẦNG** (manifest **7611 files** · migrations `0000..0224`) · **LIVE 3/3 cổng HTTP 200** (18081/8787/9000) · probe mới `probe-approval-horizontal` **ĐẠT** (dải ngang `o---o`) · ảnh chuẩn visual **đã ghi lại 68 ảnh**. **§52 audit 9 nhóm + §53 report** ở `MT2_FINAL_REPORT.md` §1/§1b/§5/§6/§9. ⛔ **Còn 2 mục KHAI BÁO trung thực, không chặn chốt** (§6.7 BLK-03 residual: 2 action tổ đội registry rỗng ⇒ CHT 403 — user đã chốt «Đóng», ⛔ không tự đổi quyền; §6.8 xung đột §4.3 «bước chưa tới» với yêu cầu mới — cần user chốt). ⛔ **NO COMMIT · NO PUSH**. |

---

## PHỤ LỤC A — PHẦN **SKIPPED** THEO YÊU CẦU (§38 — không coi là bug)
```text
MT2-P7-05  Logic đánh giá tiến độ dự án          — chưa có mô tả nghiệp vụ (CHT + TP Dự án quyết)
MT2-P11-05 So sánh/Đối chiếu BOQ · Soát trùng Alias · Chất lượng danh mục
MODULE     MEP · Đối tác · Tài chính-Kế toán · Thi công/Sản lượng/Thu hồi vốn
```

**SỔ ĐĂNG KÝ ĐỦ 9 MIỀN CỦA MT2 §2** (⚠️ MT2 §2 ghi 9 miền; ⛔ **KHÔNG tạo task** cho miền không có dòng — đúng §38):

| # | Miền §2 | Có dòng task? | Bằng chứng ĐO LẠI 23/09/2026 (⛔ không kế thừa trạng thái cũ) |
|---|---|---|---|
| ① | MEP | ⛔ không | không có dòng task nào — đúng §38 |
| ② | **Đối tác (§6.5)** | ⛔ không | ⚠️ **trước đây `MT2-P8-05` mượn lý do này ⇒ SAI PHẠM VI, đã ĐÍNH CHÍNH thành DONE** (deliverable của P8-05 là §6.3 Nhà cung cấp); tính năng **Đối tác** vẫn tạm bỏ qua |
| ③ | Tài chính–Kế toán (§9) | ⛔ không | không có dòng task nào — đúng §38 |
| ④ | Phần thi công trong Quản lý dự án | ⛔ không | không có dòng task nào — đúng §38 |
| ⑤ | Sản lượng | ⛔ không | không có dòng task nào — đúng §38 |
| ⑥ | Thu hồi vốn | ⛔ không | không có dòng task nào — đúng §38 |
| ⑦ | So sánh/Đối chiếu BOQ | ✅ `MT2-P11-05` | grep `so sánh BOQ`/`đối chiếu BOQ` trong `app/**` + `lib/**` ⇒ **0 mã liên quan** ⇒ giữ SKIPPED |
| ⑧ | Soát trùng Alias & chất lượng danh mục | ✅ `MT2-P11-05` | grep `soát trùng`/`trùng alias`/`chất lượng danh mục` ⇒ **0 mã liên quan** ⇒ giữ SKIPPED |
| ⑨ | **Logic đánh giá tiến độ dự án** | ✅ `MT2-P7-05` | `app/screens/ProjectDetailTabs.tsx:17` ghi **«% TIẾN ĐỘ DỰ ÁN CỐ Ý ĐỂ TRỐNG»** (nguồn dữ liệu = nhật ký thi công, nghiệp vụ chưa chốt) + dòng 63 «⛔ KHÔNG tự tạo logic đánh giá tiến độ (＝ P7-05 SKIPPED, §38)» ⇒ giữ SKIPPED |

## PHỤ LỤC B — BLOCKED CẦN USER QUYẾT
```text
BLK-01  MT2 §3.2 “phó giám đốc trở lên” nhưng danh mục KHÔNG có cấp `pho_giam_doc`
        ⇒ (A) thêm cấp mới  (B) quy ước “từ `giam_doc` trở lên”          → chặn MT2-P4-01
BLK-02  2 user có `system_level_code` TRỐNG ⇒ gán cấp nào                → chặn MT2-P4-01
```

## PHỤ LỤC C — QUY TẮC CHẤM TRẠNG THÁI TASK
```text
DONE        = Implementation + Validation + Testing + Diff Review + Documentation + TODO Update (§8)
IN_PROGRESS = đã code nhưng chưa test  ≠ DONE  (§7)
BLOCKED     = cần quyết định không tự giải quyết được
SKIPPED     = MT2 yêu cầu bỏ qua (phải ghi reason — §37)
⛔ KHÔNG đánh DONE chỉ vì: component render · page mở được · button hiện · API 200 · DB có cột chưa dùng
   · workflow UI hiện nhưng backend không enforce (§44)
```

## PHỤ LỤC D — NHẬT KÝ TIẾN ĐỘ (cập nhật mỗi task)
| Ngày | Task | Kết quả | Bằng chứng |
|---|---|---|---|
| 21/09/2026 | MT2-000 | Audit 3 đợt, GAP analysis | `docs/dsh/AUDIT_MT2_GAP.md` |
| 21/09/2026 | MT2-001 | Tài liệu persistent | `MASTER_TASK_2.md` · `GOAL_MASTER_TASK_2.md` · `MT2_EXECUTION_STATE.md` |
| 21/09/2026 | **MT2-P1-01 + P1-02** | **Migration V25 áp dụng thành công** | Flyway log: *now at version **v25*** · cột `approvals.overdue_reason` + `users.signature_url` |
| 21/09/2026 | MT2-P1-03 | Vá `schema-h2.sql` | *(cần xác nhận bằng test H2)* |
| 22/09/2026 | **MT2-P8-01 … P8-12** | **PHASE 8 = 11/11 DONE** (+ `P8-05` SKIPPED §6.5/§38) | log `docs/agent-progress/MT2-PHASE-6-AUDIT.md` §P8-01…§P8-12 (mỗi mục: mã + gate + LIVE + marker bundle) |
| 22/09/2026 | **MT2-P9-01 … P9-06** | **PHASE 9 (phần §7.1–§7.6) DONE** | log §P9-01…§P9-06 · `P9-02`+`P9-04④`+`P9-05` có **USER CHỐT OPTION A** |
| 22/09/2026 | **MT2-P9-07 + P9-08 + P9-09** | **DONE** — cột 2 tab «Cấp phát/Hoàn trả» + tài liệu ⛔ không tự nghĩ workflow | `app/screens/AllocateReturn.tsx:38-60` · bundle `allocate-return-screen`/`allocate-return-tab`=1 · thiếu nguồn ⇒ "—" (§14) |
| 22/09/2026 | **MT2-P10-01** | **DONE** — Tổ đội: danh sách + **filter theo dự án** (§8) | `TeamDirectory.tsx`: `filters` `ListToolbar` + lọc tại nguồn `teams.project_id` · gate `tsc` 0 · `regression` 69/69 · LIVE marker `team-project-filter`=1 · FP-805A0E84E1BAE705 |
| 22/09/2026 | 🔴 **PHÁT HIỆN RBAC (có sẵn, KHÔNG do P10-01)** | 3 action tổ đội để `List.of()` trong `ActionRbacRegistry` ⇒ **403 cho mọi non-admin** trong khi use-case lại đòi `commander` ⇒ test hợp đồng `tm04` ĐỎ (1/30) | `ActionRbacRegistry.java:80,111,243` vs `OpsTaskManagementUseCase.java:369` · `tests/tm04-team-crud.test.mjs:64` · ⛔ **cần USER CHỐT** module nào (`site_command` hay `teams`) |
| 22/09/2026 | **MT2-P10-02** | **DONE** — §10.1 Hồ sơ nhân sự: ⛔ bỏ filter dự án (AUDIT: `HrScreen` **không có** ⇒ 0 dòng) + modal **3 tab** nguyên văn | `app/page.tsx` `USER_PROFILE_TABS` + `UserProfilePanel` (tab0 = Thông tin user **đầu** + Thao tác + Đơn từ; tab1 = Thông tin cá nhân; tab2 = Dự án) · gate `tsc` 0 · `regression` 69/69 · LIVE marker `user-profile-tabs`=2 / `data-user-profile-tab`=1 · FP-BEE41F8A0FF6FBDE · 3 HTTP 200 |
| 22/09/2026 | **MT2-P10-03** | **DONE** — §10.2 Bảo hiểm & chế độ: ⛔ **không** filter dự án — **AUDIT-VERIFIED, 0 dòng mã** | `app/screens/BenefitsScreen.tsx` (toàn file 27 dòng): prop `project` chỉ ở destructuring `:16` và ⛔ không dùng; ⛔ 0 `ListToolbar`/⛔ 0 `select` dự án · `benefitRecords` chỉ dùng ở `BenefitsScreen:17` + `UserProfilePanel:2729` (lọc theo `userId`) · ⛔ **KHÔNG rebuild** (không có thay đổi source) |
| 22/09/2026 | **MT2-P10-04** | **DONE** — §10.3 Công văn: modal **Tạo/Sửa công văn** + **upload nhiều ảnh** (`AttachmentPanel` REUSE) + **nút Sửa** mỗi dòng (dùng upsert `corrId` có sẵn ở `HrManagementUseCase:101-107`) | `app/screens/CorrespondenceScreen.tsx` (viết lại toàn màn) · cột **Ngày tạo** từ `createdAt` có sẵn · ⛔ 0 API mới · ⛔ 0 migration · gate `tsc` 0 · `regression` 69/69 · LIVE marker `corr-create`/`corr-edit`/`corr-form`/`corr-attachments`=1 · FP-EEBA0ED21D5111EA |
